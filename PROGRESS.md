# Progress

[2026-08-19] Context meter reads occupancy instead of lifetime spend. `aggregateTokens` summed every turn's input and output, but each turn resends the whole conversation, so the sum counted the history once per turn: a two-turn task rendered "216.9k / 200k", painted the bar red at 100%, and raised the "Compact now" banner while the model still had most of its window free. `ConversationTokens` and `TaskTokens` gained `contextUsed` (the latest turn's input plus its reply, which is what the model is actually holding) and the meter reads that; `total` stays as the lifetime figure a cost view wants. Older conversations have no `contextUsed`, so the meter falls back to `total` rather than showing nothing. Unit tests pin the difference, including the case that used to read over 100%.

[2026-08-18] Status bar now shows a pill for a connected integration that runs its own server. The mechanism is a `statusProbe` trait on the catalog entry (`src/lib/integrations/status-probe.ts`), not a per-vendor branch: an entry names the `.cabinet.env` key holding its URL, a cheap liveness path, an optional readiness field, and an optional second path plus dotted field for a detail worth showing. lilbee is the first and only consumer (`/api/health` + `chat_ready`, model name from `/api/models` `chat.active`), so its pill reads "lilbee . Qwen3.6-35B-A3B-UD-Q5_K_M" and turns amber while the model cold-loads. `GET /api/integrations/status` does the fetching so the bearer token never reaches the browser, caches for 5s across clients, and returns nothing on cloud, where a server-side fetch of a tenant-supplied URL would be a request aimed at whatever the container can reach. A failing probe holds the last state for a 45s window rather than for a count of failures: servers that serialize requests stall a health check behind whatever they are streaming, so one timeout means "busy" as often as "down", but a count is only a duration if probes arrive on a cadence, and they do not (two clients out of phase burn a count twice as fast, and a tab hidden for an hour returns with a count of zero and would hold an hour-old green pill). Memory is keyed by id plus resolved URL, so repointing an integration at another server cannot carry the old one's model name over, and a detail endpoint that answers with no model clears the label instead of keeping a stale one. Readiness needs an explicit yes; only an absent field counts as ready, for servers too old to report it. The pill state reads by shape as well as colour, carries the bar's focus ring, announces changes through an `aria-live` region, hides its label under 820px, and clicks through to that integration's settings. Unit tests cover the pure probe logic including the version-suffix, stale-memory and unloaded-model cases; catalog invariants tie every probe's env keys to its own entry's `serverEnv` and pin id uniqueness. 448/448 unit, tsc + eslint clean, and every state verified in the running app against a server speaking lilbee's response shapes.

[2026-07-14] E2E suite: replaced the static fake agent CLI with a scripted, recording one and grew the suite from 1 tracer-bullet test to 16 covering the agentic use cases. The old fake (`test/support/fake-agent-cli.ts`) was a /bin/sh script that ignored stdin/argv and printf'd the same canned stream on every invocation — so no test could assert what Cabinet *asked* the CLI for, vary a reply per turn, fail, hang, or write a file. It is now a Node program that (a) consumes one **step** per invocation (`stdout`/`stderr`/`exitCode`/`delayMs`/`files`/`hang`), reprogrammable mid-test via `program()`/`reset()`, and (b) **records** every invocation's argv, stdin, cwd, and a spawn-time snapshot of any directory passed in argv (`dirs`/`filesIn()` — needed because the skills mount is a per-session tmpdir Cabinet deletes when the run ends, so asserting on it afterwards cannot distinguish "cleaned up" from "never mounted"). Harness (`test/support/harness.ts`) now exposes `agent(name)`, `read()`, `logs()`, and a `files` boot option that seeds extra personas/jobs/skills into the temp state root without forking the fixture. New `test/support/cabinet-api.ts` wraps the conversation/job/action routes. New coverage, all against the real app+daemon+adapter with no network: adapter print-mode contract (argv flags, prompt-on-stdin, cwd clamped inside DATA_DIR); session resume (`--resume` carries the id captured from turn 1, prompt stays short); the 409 `busy` guard; the cabinet-block retry firing **exactly once**; the session-expired → replay fallback; error classification (`auth_expired`, `rate_limited`) from stderr; Stop killing a hung CLI; an agent-written file landing in the KB and being recorded as an artifact; actions parked as pending and dispatching **nothing** until approved, then spawning a real `trigger:"agent"` child; scheduled-job runs via the same route the daemon's cron tick calls; and skills mounted via `--plugin-dir`/`--add-dir` (plus the negative case). Emitting a well-formed ```cabinet block from the fake also removed the old spec's `.first()` race — the tracer bullet now pins **one** agent turn instead of tolerating two. 16/16 e2e pass in parallel; 410/410 unit; eslint clean. FINDING (not fixed, no product caller hits it): cabinet-scoped skills at `<cabinet>/.agents/skills` are only scanned when the run carries a `cabinetPath` — `POST /api/agents/conversations` without one silently mounts no skills, while the finished conversation still shows `cabinetPath: "."`. All three production callers (manual route, job, heartbeat) do pass it.

[2026-07-14] Prepared v0.5.3 to supersede the unpublished v0.5.1 and v0.5.2 candidates. v0.5.1 failed npm authorization; v0.5.2 was immediately restored to draft and cancelled when a bundle uploader unexpectedly made it public. The recovery keeps every generic and Electron uploader in draft mode until final approval. The release includes all prior product work plus the subsequently merged Slack setup UX and Google Ads integration. npm publishing consumes the granular `NPM_TOKEN` GitHub Actions secret for both packages while retaining provenance. Windows signing remains optional; native installed-app smoke testing remains mandatory.

[2026-07-11] Provider onboarding overhaul — "max UI, less terminal" setup for agent CLIs, driven from the status-bar popup + a home empty-state banner. New reusable `ProviderSetupDialog` (`src/components/settings/provider-setup-dialog.tsx`, mounted globally in app-shell, opened via app-store `openProviderSetup(id)`/`providerSetupId`) orchestrates Install → Log in → Verify per provider; also surfaced as a "Set up / Log in" button on each not-ready Settings → Providers card. **(1) Install for me**: new streaming endpoint `POST /api/agents/providers/[id]/install` runs the provider's install command server-side (reuses verify's `/bin/sh -c` + `withAdapterRuntimeEnv`), streams stdout/stderr as SSE (echoes the exact command first), re-probes availability at the end, `cancel()` kills the child on disconnect. Command picked by `installCommandFor()` (`src/lib/agents/provider-setup.ts`, unit-tested) behind a package-manager allowlist. **(2) Claude connect seamless on desktop**: un-gated the `claude setup-token` flow (removed `CABINET_CLOUD` 404 in `server/cabinet-daemon.ts` `/auth/claude/*` + the proxy route). Other OAuth CLIs (Codex/Cursor/Copilot/Gemini) log in via an embedded `WebTerminal` running `<cli> login` (added client-side `initialInput` prop). API-key providers (grok → `XAI_API_KEY`) via inline field → PUT `/api/agents/config/cabinet-env`. **(4)** `ProvidersEmptyBanner` on home when zero providers ready. **(5)** sole-ready provider promoted to default via full read-modify-write PUT. Status-bar rows open the dialog (removed dead `PROVIDER_INSTALL_URLS`). i18n: `settings:providerSetup.*` + `home:providersEmpty.*` (en only). **NOTE: the running Cabinet app hard-resets its own git working tree (Sync/auto-stash), which silently wiped this feature's uncommitted tracked-file edits mid-development (reflog: repeated `reset: moving to HEAD`) — re-applied and committed on branch `feat/provider-onboarding` so it survives.** tsc + eslint clean; unit test green; backend curl-verified (install 404 path + claude-login/status 200 on desktop).

[2026-07-11] Merged PR #189 (external contributor, fix/pi-model-id-heal-inside): "fix(pi): read available models correctly from `pi --list-models`". Pi prints a whitespace-columned table, but the old parser read each whole line as a single model id — so picking a row saved the entire table row (`tfm  glm/glm-5.2  128K  …`) as the model id → "Unknown provider", and rows whose model column had no `/` were silently dropped. Fix adds `modelIdFromLine` (split on 2+ spaces, drop header + stat columns, rebuild `<provider>/<model>`) shared by both `parsePiModels` and a new `normalizePiModelId` that repairs already-corrupted saved ids on read (no migration); consumption sites in pi.ts + pi-local.ts route through it. `provider-settings.ts` untouched. Reviewed for malice (no network/exfil/eval/new deps, all 4 files are Pi model-parsing + tests) — clean. Ran the suite locally: 10/10 pass. Squash-merged with --admin (branch protection required checks).

[2026-06-20] Channel "<agent> is typing…" indicator + why it's client-side. The reply path runs a real ~30s agent run, so the right UX is a typing indicator during the wait. The pre-existing design drove it from a server-side respondingAgents Map (channels route writes, events SSE reads) — but it never actually surfaced: Next 16/Turbopack isolate route handlers so the two routes don't share in-memory state (verified: a shared lib module AND globalThis both failed; even a file-backed store that correctly held the entry mid-run wasn't reflected by the events route). So the indicator is now CLIENT-SIDE optimistic: when you @mention an agent, the panel shows "<name> is typing…" instantly and clears it when that agent's reply lands (or after a 3-min setTimeout safety). Reliable, immediate, no server round-trip. Also: scoped the panel's @mention/persona fetch to the active room (cabinetPath) so room agents resolve; relabeled "is thinking" → "is typing". Kept the file-backed responding-state as a (currently-Next-limited) cross-user supplement, merged into the indicator via a deduped union. tsc + eslint clean. NOTE: visual confirmation pending — the chrome MCP dropped mid-session, so this round is verified by tsc/eslint + logic review, not a screenshot.

[2026-06-20] Fixed @mention agent replies in channels (they silently never responded). Root cause was a stale execution path in runQuickResponse (fires when a human @mentions an agent in a channel): (1) it POSTed the daemon `{id, args:["-p", prompt]}` but `/sessions` reads `{id, prompt, providerId, cwd}` and ignores `args`, so the prompt never reached the agent; (2) even patched, the daemon ran Claude as an interactive PTY/TUI that never exits → hit the 120s timeout → killed, and the captured "output" was terminal escape codes. Reworked runQuickResponse to run through startConversationRun — the same structured conversation-runner heartbeats use: clean text output, real completion tracking, runs in the agent's own room (cwd = cabinetPath). The agent wraps its final answer in <reply></reply> (extracted in onComplete) so the channel shows clean prose, not the run's thinking/tool dumps. Added a fallback message on timeout/empty so a failed run no longer produces silence. Verified end-to-end: @editor replies in ~30s with a clean, accurate answer ("Hila's Home is the top-level workspace room…"). tsc + eslint clean.

[2026-06-20] Channels are now per-room (per-cabinet) and the on-disk dir is `.channels` (was the global `.slack`). channels-manager resolves `<cabinet>/.agents/.channels` via resolveCabinetDir(cabinetPath); every fn takes an optional cabinetPath. initChannelsDir lazily migrates a legacy `.slack` dir to `.channels` per cabinet on first access (fs.rename, preserves history, race-safe). Threaded cabinetPath through: the route (GET `?cabinetPath`, POST body), the panel (new cabinetPath prop + `&cabinetPath=` on every fetch; channel-list/meta re-fetch on room switch), tabs-layout (new ChannelsMount reads useAgentsContext().cabinetPath), and the heartbeat WRITER (all 6 postMessage sites + runQuickResponse now post to the agent's own room, and runQuickResponse reads persona/memory/recent-messages scoped to it). Verified via API: hilas-home shows fresh defaults (data/hilas-home/.agents/.channels created); root "." migrated data/.agents/.slack -> .channels and serves the 25-channel history. Known lim: the events-route `channel_activity` SSE still reads the root board (one app-global stream), so non-home rooms refresh via the 10s poll, not SSE. tsc 0, eslint clean.

[2026-06-20] Channels list now sorts most-recently-active first. Extended the per-channel meta fetch (channels-panel.tsx) to also capture each channel's last-message timestamp (count + lastTs), and render the pills via a `sortedChannels` useMemo ordered by lastTs desc (ISO sorts lexically; empty/no-message channels sink to the bottom). Verified in-browser: order is #platform (Jun 20) → #leadership/#support-macros/#leadership-meeting/#help-center-updates (Jun 19) → Jun 18 batch → #general (Jun 17) → older. tsc 0, eslint clean.

[2026-06-20] De-slacked the internal team-chat ("Channels") + 2 UX fixes. Renamed the misnamed-as-Slack internal chat everywhere it's not really Slack: slack-panel.tsx -> agents/v2/channels-panel.tsx (ChannelsPanel), slack-manager.ts -> channels-manager.ts, /api/agents/slack -> /api/agents/channels, SlackMessage -> ChannelMessage, SSE slack_activity -> channel_activity (events route + app-shell forward), heartbeat SLACK token -> CHANNEL (parser still accepts legacy SLACK). Kept genuinely-Slack things (mcp-catalog connector, notification-service slack_webhook) and the on-disk `.slack` dir (hidden detail, avoids a data migration; commented). Deleted the orphaned dead UI that held the rest of the slack refs: mission-control.tsx, agent-detail-panel.tsx, pulse-strip.tsx (unmounted, no importers). UX: (1) date separators — day-divider rows ("Today"/"Yesterday"/"Wed, Jun 3") at day boundaries since channels span days; (2) switch lag — the per-channel count fetch was 1 sequential request per channel on every switch; now parallel + keyed on the channel list only, plus messages are a derived per-channel cache (useMemo) so switching paints instantly. Real-time also fixed as a side effect: the panel now listens directly to app-shell's forwarded channel_activity/agent_responding (the old cabinet:slack-refresh bridge died with mission-control). Verified in-browser after a dev restart (Turbopack stale-cached the git mv renames): tab renders, #competition switch instant, dividers visible (THU JUN 11 / THU JUN 18), counts loaded. tsc 0 errors, eslint clean.

[2026-05-27] Moved the "Delete room" affordance out of the switcher dropdown and into the Edit dialog. Switcher entry renamed from "Customize this room…" to **Edit room** (locale `rooms:customizeRoom` value updated; key kept to avoid churn across 39 locales) and the modal heading changed from "Customize room" to "Edit room" (`rooms:editTitle`). The top-level "Delete room…" menu item is gone; `RoomEditDialog` gained `onRequestDelete?` + `canDelete?` props and renders a destructive "Delete room…" button at the bottom-left of the footer (trash icon, `text-destructive` hover, disabled with the `deleteLastRoomDisabledHint` tooltip when only one room remains). The switcher wires the prop: clicking Delete inside Edit closes the edit dialog and opens the slug-typed `RoomDeleteConfirm` — so the dangerous step is still one extra click behind the confirm, just no longer a peer of the switch list. Verified in-browser: dropdown shows {Cabinet Room, bla, Open in new window, Edit room, Add room…} (no top-level Delete), clicking Edit room opens "Edit room" dialog with the new Delete button in the footer, clicking it transitions cleanly into the existing confirm with the slug gate.

[2026-05-27] Implemented all three follow-ups from `docs/ROOMS_WORKSPACES_PRD.md` §10 (the rooms regressions reported the same day). **§10.1 Stale list.** Extended `useRoomsStore` (`src/stores/rooms-store.ts`) with debounced refetch on `window.focus` + `visibilitychange→visible`; added a `BroadcastChannel('cabinet-rooms')` pub/sub (`src/lib/cabinets/rooms-events.ts`) so multi-window/multi-tab mutations propagate instantly; the switcher now refetches `/api/rooms` (with `cache: "no-store"`) every time its dropdown opens. `resolveDefaultRoom` self-heals `data/.home/home.json` when `defaultRoom`/`lastActiveRoom` point at a dead slug — verified: the live `home.json` was rewritten from the dead `hilss-home` to `Cabinet` on first GET. **§10.2 Edit reliability.** `updateRoomMeta` now writes atomically (`.tmp-<pid>-<ts>` + rename); the edit dialog no longer self-closes on success (parent owns the close+invalidation), stays open with an inline error on PATCH failure, and shows a "Folder name on disk stays as X" hint distinguishing display name from slug; the switcher's `handleRoomSaved` busts `invalidateCabinetOverview(path)` and the `cabinet.name.<path>` localStorage cache, reloads the tree, and broadcasts so peer windows refresh. Verified end-to-end in-browser: rename → switcher trigger updated to "Cabinet Room (renamed)" immediately, disk manifest atomically updated; reverted via API and dropdown picked up the rename on next open. **§10.3 Delete room.** New `deleteRoom()` in `src/lib/cabinets/rooms.ts` and `DELETE /api/rooms` (accepts `?path=` or body `path`): moves the room dir to `data/.trash/<slug>-<ISO-ts>/` (soft delete, reversible by hand), repoints `home.json` if the deleted slug was `defaultRoom`/`lastActiveRoom`, and returns the trash path. Refuses: the home container (`.`), non-direct children of `DATA_DIR` (traversal), a path with no `.cabinet`, and the last remaining room — all four guards verified live (400 each). New `room-delete-confirm.tsx` is a GitHub-style confirmation: warning list, recovery note, slug-typed gate (button disabled until the user types the directory slug), inline error stays open on failure. New switcher entry "Delete room…" in the customize area (destructive styling, disabled when only one room remains). On success the switcher snaps to the new default room before the refresh lands so the user never renders a now-missing path; tree-store/editor-store selections cleared; overview cache + name localStorage busted; rooms-store + tree reloaded; broadcast emitted. Locale strings added under `rooms:*` in `src/i18n/locales/en.json` (en only; `scripts/i18n-translate.mjs` propagates to the other 38 locales later). tsc + eslint clean. End-to-end browser verification: switcher dropdown refetches on open (confirmed by mutating via curl while the dropdown was closed and seeing the new state on reopen), customize dialog opens with slug hint, save propagates immediately, delete confirm enforces slug typing, full delete→trash→restore round trip preserved a multi-MB room intact.

[2026-05-27] Consolidated the rooms docs into a single source of truth at `docs/ROOMS_WORKSPACES_PRD.md` (the former `docs/ROOMS_V3_TRUE_ISOLATION_PLAN.md` is now a one-line stub redirect). Added a "Known gaps & follow-ups (2026-05-27)" section to the PRD covering three regressions reported against the shipped switcher: (1) the room list goes stale relative to disk because `useRoomsStore.load()` early-returns once `loaded === true` and the switcher only force-refreshes after in-app Add/Edit dialogs — observed showing `Cabinet` + `bla` while disk also held `temp` (real room) and `hilss-home` (folder without `.cabinet` but pointed to by `home.json` `defaultRoom`); fix is refresh-on-open + visibility/focus refetch + cross-window `rooms:invalidated` (Electron IPC + `BroadcastChannel`) + heal `home.json` when `defaultRoom` is dead. (2) Room-name edits don't reliably reflect everywhere — `RoomEditDialog` writes manifest `name` (not the slug, on purpose) but the drawer/header subscribe via tree-store/app-store paths that hold cached names, so the visible name lags until reload; also no atomic write and the dialog can hide PATCH failures. (3) No delete-room affordance — users drop to `rm -rf`, leaving dangling refs in `home.json` and the search DB; design specifies a `DELETE /api/rooms` that moves the dir to `data/.trash/<slug>-<timestamp>/`, closes the DB handle, repoints `home.json`, emits `rooms:invalidated`, with a slug-typed confirm in the per-room Customize menu (not top-level) and guards against deleting the home or a room open in another window. Filed three TaskCreate entries (#1 stale list, #2 edit reliability, #3 delete-room). No code changes; docs + task list only.

[2026-05-22] Wrote `docs/DICTATION_PRD.md` — full PRD for offline voice dictation via whisper.cpp. Mic button on the shared composer (chat + task + replies) and the Tiptap `.md` editor; click-to-toggle with a right-click/long-press helper menu; first-use explainer popup + Settings → Dictation section. Engine is `smart-whisper` (whisper.cpp) run in an Electron `utilityProcess`, exposed over `window.CabinetDesktop.dictation.*`; renderer captures 16kHz mono PCM via AudioWorklet (no ffmpeg) and inserts text at the caret. On-device/free/private; one-time ~142MB model download to userData. NOT a wrapper around OS dictation (no API to start it). Phase 0 is a signing/entitlements de-risk spike before any UI (same hardened-runtime area as the v0.4.x DMG breakage). No code yet — doc only.

[2026-05-22] Moved agent-picking from onboarding to the agents page. (1) Onboarding "Build your team" step (`TeamBuildStep`) stripped to title + Back/Next only ("for now"); removed the department-column picker, drag-to-scroll machinery, phase animation, and all picker props (parent now passes just onBack/onNext). The mandatory-agent set still seeds the launch payload, so a default team is still created. Leaves a few unused onboarding helpers/parent locals as lint warnings (matches the pre-existing baseline; picker may return). (2) Agents page `/#/agents` "Add an agent" popup (the ACTIVE one is `src/components/agents/v2/new-agent-dialog.tsx`, not the legacy `agents-workspace.tsx`) restyled to match the onboarding picker: kept the search box, group results into vertical department sections of compact checkbox rows (checkbox + emoji + name + role), multi-select with a "(N selected)" title counter, already-on-team agents disabled with an "Added" badge, and a single "Add N agents" footer button that POSTs each `/api/agents/library/<slug>/add` (409 = success) then refreshes + closes (no longer navigates to a single agent). `agents-workspace-v2.tsx` now passes `existingSlugs` to the dialog. (3) Fixed the sidebar/tree "New Agent" button: it dispatches `cabinet:open-add-agent`, but the only listener lived in the legacy workspace (not rendered by V2) — added a listener in V2's `Dialogs` that opens the dialog. tsc + lint clean. NOTE: working tree also contains unrelated in-progress work by the user (tasks board, sidebar file dialogs, new API routes) — left untouched.

[2026-05-22] Removed the "Start from a template" path from the Build-your-team step. Deleted the template carousel, the import dialog, the full-screen registry browser overlay, and the post-import CabinetCreatedScreen success animation, plus the orphaned `TeamCarousel` and `CabinetCreatedScreen` components, the `PreMadeTeam` alias, the import/registry state + `handleImport` + the `/api/registry` fetch effect, and now-unused imports (RegistryBrowser, RegistryTemplate, TiltCard, STARTER_TEAMS, StarterTeam, Dialog*, House, XCircle, Users) and the `homeName` prop on TeamBuildStep. The step is now just the agent picker; relabeled "Or pick your agents" -> "Pick your agents" (en.json key `orPickAgents` -> `pickAgents`, also dropped dead `startFromTemplate`). Phase animation simplified to a single fade-in. tsc + eslint clean for the touched symbols. NOTE: i18n for the new `pickAgents` key (and the earlier provider heading/subtitle/moreModels) is still English-only; run scripts/i18n-translate.mjs to push to 39 locales once the English copy is signed off.

[2026-05-21] Onboarding provider step polish (round 2). Dropped the harsh accent border on the selected provider row (now always `WEB.borderLight`); selection is communicated by the warm background + a soft `${WEB.accent}14` shadow ring, with a slightly stronger `${WEB.accent}1F` ring when a row's guide is open. Clicking a ready row while a guide is open now also swaps the guide to that row, so users can browse guides by clicking through the list. Guide chevron switched from `ChevronDown` (rotated 180 on expand) to `ChevronRight` with `rtl:rotate-180`, since the guide opens to the end side and the down-arrow no longer matched. The chevron + label color shifts to accent when expanded. Restyled the "More AIs" toggle as a quiet uppercase label matching "COMING SOON" (no rounded background). Moved the "Coming soon" providers (Anthropic API / OpenAI API / Google AI API / Plugin SDK) INSIDE the "More AIs" reveal, rendered as full-width rows with reduced opacity so they share the same visual rhythm as the rest of the list. Re-check providers is now a quiet text link with a small RefreshCw icon at the bottom of the list, instead of a rounded pill.

[2026-05-21] Onboarding provider step rework. Renamed heading "Agent Provider" -> "Connect your AI" and subtitle "Cabinet needs an AI CLI to power your agents." -> "Pick an AI assistant to power your agents." (English in en.json only; re-run scripts/i18n-translate.mjs to push to 39 locales). Replaced the 4-column card grid with a single-column row list inside a max-w-3xl container. Names + versions no longer truncate. Added a two-tier model: tier 1 (gemini-cli, claude-code, opencode, codex-cli) always visible; tier 2 (cursor-cli, copilot-cli, grok-cli, pi, generic) hidden behind a "More AIs" toggle. Clicking Guide on a row collapses the list to ~42% width on md+ and slides a guide column in from the end side (right in LTR, left in RTL) using a new onboarding-guide-slide-in keyframe driven by --guide-slide-from. Below md the list hides and the guide takes full width. Cursor not-detected bug is NOT addressed by this PR; separate investigation needed.

[2026-05-21] Welcome-screen blueprint: room labels are now i18n'd. `HomeBlueprintBackground` switched its hardcoded `OFFICE/STUDY/LAB/LIBRARY/KITCHEN/FAMILY/DINING/STUDIO` to `t("onboarding:blueprint.rooms.<id>")` with `text-transform: uppercase` so the blueprint look stays for Latin scripts and degrades to natural case for CJK/RTL. Added the 8 keys to `en.json` and ran `scripts/i18n-translate.mjs --all`; Gemini translated those plus any other missing strings across all 39 locales (one locale, Hausa, ended up with 2 unrelated keys as English fallback after 4 retries — both pre-existing strings, not the new room labels). Note: a couple of locales have soft ambiguities (Spanish "Estudio" covers both study + studio; Hebrew "לימוד" reads as "learning" rather than "study room"). Re-run the script per-locale to refine, or hand-edit.

[2026-05-20] Onboarding intro screen trim: shortened all three dictionary definitions in `IntroStep` (cupboard/politics/software), reworded the politics one to "Senior advisors consulting on government policy." to prime the AI-agents metaphor, and compressed the staggered reveal from ~4.2s to ~1.6s (per-element fade 0.6s → 0.4s). Switched the definition body to a proper dictionary serif (registered `--font-serif` in the Tailwind v4 theme, backed by Cardo which the project already loads for the RTL logo). Chips ("politics" / "software"), pronunciation, and the italic example sentences ("a filing cabinet" etc.) stay mono. Added Key Rule #17 in `docs/CLAUDE.md`: no em-dashes in user-facing copy.

[2026-05-19] Documented the update-vs-release mechanism diagnosis in `docs/deployment-packaging-versioning.md` (new "Known Gaps" section + corrected the Electron-updates note). Findings: in-app update dialog isn't gated by `installKind` so `electron-macos` users get a dead source-managed modal; `hasPersistentUpdateState` (driven by the native updater's shared `update-status.json`, incl. `failed` from incomplete macOS signing) force-reopens it; "Later" dismissal is lost because it's in origin-scoped localStorage and the runtime port isn't stable. Release side: `release.yml` publishes the manifest to a `draft` release and the Electron build is a separate manual workflow, so `cabinet-release.json` can advertise a version with no installable build. Listed the UX + CI fixes needed; no code changes yet.

[2026-05-07] Expanded `docs/PROVIDER-CLI.md` §13 "Adding a new provider" from a 5-step bullet to a complete touch-list — added the previously undocumented edits (`legacy-ids.ts`, `status-bar.tsx` PROVIDER_INSTALL_URLS, test files) and an explicit "do not enumerate providers in components" note. Surfaces that read `/api/agents/providers` (Settings, composer picker, onboarding grid, providers-demo) are called out as automatic.

[2026-04-28] **Skills**: browse skills.sh, attach per agent, or `@`-mention per task. Bundles are mounted into Claude via `--plugin-dir` so they register as proper plugin skills (not just `--add-dir` files); continuation turns re-merge the mount so mid-conversation `@`-mentions take effect on the next reply. **API Keys**: a `.cabinet.env` file at the project root, edited from a preset-driven UI (OpenAI, Anthropic, GitHub, Google AI, plus Custom). Values masked as `••••<last4>`, file written `0600`, gitignored. Spawn-time merge into `withAdapterRuntimeEnv` and the PTY manager forwards keys to every CLI, so skills like `imagegen` read `OPENAI_API_KEY` without a restart. Cabinet backups gained two opt-in toggles for the same files, both defaulting off.

[2026-04-25] Pinned Node 22 as the dev runtime via `.nvmrc`. Silences the `EBADENGINE` warning from transitive `chevrotain@12` (pulled in by `mermaid → langium`) which declares `engines: ">=22"`. README "Requirements" updated to recommend Node 22 LTS with `nvm use`.

[2026-04-25] Removed unused `@emoji-mart/react` dependency from `package.json`. The package was never imported — the emoji picker uses the vanilla `emoji-mart` `Picker` mounted via `useEffect` (`src/components/editor/emoji-picker.tsx`). This also fixes a fresh `npm install` ERESOLVE error since `@emoji-mart/react@1.1.1` only declared peer support up to React 18 while the repo is on React 19.

[2026-04-25] Documented the new install/update/uninstall flow in the root `README.md` (new "Install, update, uninstall" section) and `cabinetai/README.md` (expanded uninstall section with the `remove` alias, `--yes` flag, and per-platform telemetry paths removed by `--all`).

[2026-04-25] `cabinetai uninstall` now prints a summary of what will be deleted and asks for confirmation, with a `remove` alias and a `-y/--yes` flag to skip the prompt. `--all` additionally removes the platform-specific telemetry directory (`~/Library/Application Support/cabinet-telemetry` on macOS, `%APPDATA%\cabinet-telemetry` on Windows, `$XDG_CONFIG_HOME/cabinet` on Linux). New `cabinetai/src/lib/prompt.ts` confirm helper; telemetry path resolution mirrored in `cabinetai/src/lib/paths.ts`.

[2026-04-25] Added Daily Review folder (`data/Daily Review/index.md`) and a daily-review scheduled job (`data/.jobs/daily-review.yaml`, cron `0 9 * * *`) that writes a YYYY-MM-DD entry covering yesterday's recap and today's plan.

[2026-04-25] Replaced the external Cabinet Cloud waitlist link in onboarding with an inline opt-in email form. Adds a small browser-side waitlist client (`src/lib/telemetry/waitlist-client.ts`) that records anonymous view/start events alongside the explicit email submit, all keyed by a per-tab visit id.

[2026-04-25] Tightened TELEMETRY.md: opt-out instructions now appear in the first paragraph, and the `install_id` / `session_id` descriptions are more precise about cross-machine and cross-process behavior.

[2026-04-25] Extended anonymous telemetry with per-event payload-key enforcement, shared session id across daemon + Next.js, and additional events (`provider.verified`, `cabinet.switched`, `template.installed`, `theme.changed`, `task.created`, `task.completed`, and onboarding sub-steps). TELEMETRY.md updated.

[2026-04-25] Added anonymous telemetry client module under `src/lib/telemetry/` with opt-out via `CABINET_TELEMETRY_DISABLED=1` env var and a Settings toggle. New `/api/telemetry` + `/api/telemetry/settings` routes. Emissions wired in the daemon lifecycle (`cabinet-daemon.ts`), onboarding completion, agent-run start/finish/fail (`conversation-runner.ts`), and top-level error handlers. Added `TELEMETRY.md` documenting what's collected and how to disable; README links to it under a new Privacy section.

[2026-04-24] Portfolio Manager weekly health check (Fund II, April 24): appended new section to `data/archive/data-old/vc-os/portfolio/index.md`. April metrics book is now locked with no revisions since April 17 — aggregate ARR $13.05M (+1.3% MoM), no metric moved >15%, no new `news.md` entries this week. Open risk flags (NeuralFlow 2nd flat month, DevForge runway 18 mo flat, DataStream Series A ARR gap) remain open into May.

[2026-04-24] Route task launches + notification clicks to the conversation panel (`type: "task"` → `TaskConversationPage`) instead of the calling agent panel (`type: "agent"` → `AgentsWorkspace`/`AgentDetailV2`). Fixed in `notification-toasts.tsx` (drops the now-redundant `cabinet:open-conversation` event), `home-screen.tsx` (both "now" submit and `StartWorkDialog` handoff), `cabinet-view.tsx` (`CabinetTaskComposer`'s `onNavigate`), and `agents-workspace.tsx` (`StartWorkDialog` handoff). Conversation id doubles as task id, so navigation only needs `taskId` + `cabinetPath`.

[2026-04-24] Added Songs/13-all-night-laundromat.md — original americana/slowcore song set in a 3 a.m. laundromat, full structure (verses, pre-chorus, chorus, bridge, outro).

[2026-04-24] Perf round 2 — deeper caching + SSE consolidation + code-splitting. New `src/lib/cache/ttl-cache.ts` (generic TTL + single-flight dedupe). Wrapped four server hotspots: `readCabinetOverview` (3s TTL), `buildTree` (5s), `listAllPersonas` (5s), `discoverCabinetPaths` (10s). Result: `/api/tree` 1623ms → 8ms warm, `/api/cabinets/overview` 1067ms → 4ms warm. `listPersonas` also parallelized (Promise.all over candidates). `/api/agents/events` SSE tick: Slack channels now fetched in parallel (was 5 sequential `getMessages` calls), duplicate per-channel fetch removed, goal reads parallelized. Consolidated the dual `/api/agents/events` SSE subscription — `app-shell.tsx` now runs the single EventSource and re-dispatches `cabinet:agents/<event>` CustomEvents; `mission-control.tsx` listens to those window events instead of opening its own stream. `recent-tasks.tsx` conversation-events handler now debounced at 200ms. Code-split eight section components via `next/dynamic` in `app-shell.tsx` (AgentsWorkspace, AgentDetailV2, TasksBoard, TaskConversationPage, SettingsPage, CabinetView, RegistryBrowser, OnboardingWizard). EmojiPicker lazy-loaded through `next/dynamic` in `slash-commands.tsx` (defers `@emoji-mart/data`). Replaced `lowlight` `common` preset with a 13-language curated set in `extensions.ts`. Status-bar health poll now visibility-aware. LCP 1596 → 1515 ms (dev); prod bundle win should be bigger given the section dynamic imports.

[2026-04-24] Editor agent is now always-on. Baked the full getting-started knowledge (file types, directory conventions, `.repo.yaml` / `.cabinet-meta` / `CABINET_DATA_DIR` semantics, dispatch syntax, user-facing UI surfaces) directly into `src/lib/agents/library/editor/persona.md` so the library fallback ships with Cabinet and does not depend on `data/getting-started/` being present. Made `"editor"` mandatory in the Blank room (`src/lib/onboarding/rooms.ts`) alongside the other four room types, so every room floor now includes it; the wizard UI and `mergeMandatoryAgentSlugs` pick this up automatically. Silent-fail behavior at the status-bar composer is preserved — when the cabinet-scoped persona is missing, the enriched library template resolves via `SOURCE_AGENT_LIBRARY_DIR`, so the "persona not found" degraded-prompt branch in `conversation-runner.ts` is effectively unreachable for the editor slug.

[2026-04-24] Perf pass driven by Chrome DevTools trace. Three fixes: (1) `applyTheme()` now loads only the active theme's Google Font families instead of 30+ at once (LCP 3686ms → 1596ms). (2) New `fetchCabinetOverviewClient` dedupes in-flight requests and caches for 3s; six callers (tree-view, home-screen, ai-panel, use-board-data, agents-workspace, cabinet-view) migrated; tree-view's 5s poll now pauses when tab is hidden — initial overview calls dropped 5 → 2. (3) All eight provider `healthCheck` callsites migrated from `execSync` to new `execCli` spawn-based helper so Promise.all actually parallelizes; `/api/agents/providers` gained a 15s in-memory cache + in-flight dedupe (2200ms cold → ~5ms warm).

[2026-04-24] DATA slide (scene 0): viewer now only appears when user clicks Next, not auto-animated. TourBody tracks viewerRevealed state; first Next click on data-0 reveals the viewer, second click advances to data-1. Also widened SlideTasks sidebar (260-320px → 360-420px) and increased command text from 11px to 13px for legibility.

[2026-04-24] DATA slide: cabinet shell now animates in first (0ms), then text (350-800ms), then file rows (1000-1290ms), then viewer (1650ms). Updated thailand-video caption to "View all your files in one place." Removed the "Work / Board deck.pptx" scene from the DATA slide; deleted PresentationViewer component and Presentation icon import.

[2026-04-24] Halved DATA slide animation duration. Text column now completes by ~750ms (was ~1450ms), sidebar rows by ~1140ms (was ~2270ms), viewer at 1200ms (was 2700ms). Reduced individual fade durations from 0.4–0.5s to 0.25–0.35s and tightened all delay offsets proportionally.

[2026-04-24] DATA slide cleanup: removed mouse cursor travel animation (only the click ripple remains, delay reduced to 1400ms); reverted copy column to original fade-up animations (no typewriter, no grid-collapse state); moved Night market.mp4 scene to position 0 (removed the Phuket sunset.jpg scene); removed PhotoViewer and unused typewriter code. Also fixed blank DATA slide caused by React StrictMode double-invocation: `introRan.current` guard was set to true on the first effect run, the cleanup cleared the interval, and the second run bailed out — so typing never started. Removed the guard; the effect now resets all three state vars at the top of each run so StrictMode's second invocation starts cleanly from zero. Also moved the setTimeout handles (t1, t2) to outer-scope `let` variables so the cleanup function can clear them correctly.

[2026-04-24] Added typewriter-then-slide-right intro to the DATA slide. On first mount, the copy column (badge + h2 + p1 + p2) now types in character-by-character at 6ms/char while the grid is collapsed to "0px 0px 1fr" so column 3 fills the full slide width and the text appears centered via mx-auto. The accent color on "single source", the @ glyph, and the "same files" bold all render correctly during typing. After ~2s the typing completes, then a 200ms pause triggers the CSS grid transition (column-gap and grid-template-columns both animated at 460ms ease) that reveals columns 1 and 2 — the text naturally slides right as the left columns expand. Sidebar rows and the viewer panel use animationPlayState:"paused" while hidden and flip to "running" when showSidebar becomes true (simultaneously with the grid expansion), so the staggered row fade-ins and cursor animation all play relative to when the sidebar first appears rather than component mount.

[2026-04-24] Rebuilt the Data slide's left column as an auto-rotating showcase of five knowledge-base scenes to demonstrate the breadth of file types Cabinet supports — the old single static tree of file icons didn't convey that web apps, linked repos, office docs, and media types are all first-class. New `SCENES` array in `slide-data.tsx` cycles every 3800ms through: (1) **Thailand Trip** — itinerary markdown, photos, video, spreadsheet budget, audio notes, flight PDF; (2) **Tax 2026** — a live Calculator web app embedded inside the folder alongside income sheet, receipts PDF, deductions note, W-2, CPA notes docx; (3) **Health** — vitamins CSV, supplements markdown, dosage xlsx, lab results PDF, progress chart image; (4) **Work** — Q2 report PDF, client contract PDF, board deck pptx, revenue xlsx, policy docx, all-hands notes; (5) **cabinet-repo** — README, package.json, src folder, schema.ts, .repo.yaml — a linked Git repo as a first-class tree node. Each scene has a one-line caption below the sidebar that reads as the "why" of that file arrangement (e.g. "Tax 2026 — a live calculator web app, embedded right in your cabinet"). Scene dots under the caption show position in the rotation. Adopted the vivid icon palette from the `Supported File Types` table in `data/getting-started/index.md`: a constant `ICON` object maps gray/green/red/violet/pink/cyan/amber/blue/orange to Tailwind-500 hex values so markdown is gray, CSVs/xlsx/apps are green, PDFs are red, videos cyan, audio amber, code violet, docx blue, pptx orange, Git branches orange, images pink — each file type announces its shape at a glance instead of everything reading as a uniform mocha blur. Rotation logic uses a local `intervalId` captured in the `useEffect` closure (previous attempt leaked through a window-scoped handle and got re-fired across HMR remounts, causing the carousel to advance 3× faster than intended). Tree root row and child rows each animate in with staggered `cabinet-tour-fade-up`; the whole tree container is keyed on `scene.id` so React remounts it on each swap and the row-by-row stagger replays cleanly per scene. The `@Market Research` floating chip from the previous Data slide was removed — the rotating scenes + captions carry the "all kinds of content" story more effectively than a one-off mention demo. Left column also got a bit wider (`minmax(280px, 340px)`) to give the tree room for longer filenames like `Chiang Mai temple.jpg`.

[2026-04-24] Redesigned the tour's intro slide around a staged expansion animation and restored the sidebar's yellow-amber accent on the Cabinet chrome. Removed the "WELCOME" chip and the "All ▾" badge from `slide-intro.tsx` — the copy now opens directly with "Meet your Cabinet." + "Your AI team. Your knowledge base. One place." and the Cabinet header pill no longer carries a trailing depth-chip it never actually needed. The intro cabinet now starts narrow (92px wide) with opacity 0, fades in as a tight near-square, then expands horizontally to the tour's 300px width over 680ms via a CSS `transition: width` with a `cubic-bezier(0.22, 1, 0.36, 1)` (subtle overshoot ease-out). After the expansion settles, the "Cabinet" title text fades in inside the header (new `titleDelay` prop on `MockupSidebar` drives the delayed `cabinet-tour-fade-up` animation on the title span), then the DATA / AGENTS / TASKS tab buttons pop in one by one staggered 180ms apart (the existing `tabsPopIn` got a new `tabsPopInDelay` prop so the intro can retime the baseline away from its default 900ms). Timing constants live at the top of `slide-intro.tsx` so the whole sequence reads like a storyboard (`COPY_H1_DELAY=80`, `COPY_SUB_DELAY=240`, `SHELL_APPEAR_DELAY=520`, `EXPAND_TRIGGER_DELAY=820`, `TITLE_REVEAL_DELAY=1500`, `TABS_START_DELAY=1620`). Restored the warm yellow accent on the Archive glyph and the active drawer pull-handle by extending `palette.ts` with `iconAmber: #FBBF24` + `iconAmberSoft: rgba(251,191,36,0.6)` — the rest of the palette (copy, borders, backgrounds, accents on text) stays mocha-brown so the tour still reads as a continuation of the onboarding wizard, but the Cabinet chrome itself carries the sidebar's native yellow flash the way the real app does. Verified live: width timeline samples 92px → 135px → 205px → 249px → 298px → 300px across ~680ms starting ~1s after mount; narrow shell visible during the pause before expansion; title + tabs populate in the scripted order. The view-transition morph into slide 1 still lands cleanly because the intro's final width (300px) equals slide 1's left-column width.

[2026-04-24] Retinted the entire onboarding tour to the warm mocha-brown palette from the onboarding wizard, replacing Tailwind's golden `amber-*` classes (which read yellow) with the wizard's hex palette so the two surfaces feel like one continuous experience. New `src/components/onboarding/tour/palette.ts` exports `TOUR_PALETTE` mirroring the wizard's `WEB` palette: paper `#FAF6F1`, paperWarm `#F3EDE4`, bgCard `#FFFFFF`, text `#3B2F2F`, textSecondary `#6B5B4F`, textTertiary `#A89888`, accent `#8B5E3C` (mocha), accentWarm `#7A4F30`, accentBg `#F5E6D3`, border `#E8DDD0`, borderLight `#F0E8DD`, borderDark `#D4C4B0`. Every tour file (`tour-modal.tsx`, `mockup-sidebar.tsx`, `slide-intro.tsx`, `slide-data.tsx`, `slide-agents.tsx`, `slide-tasks.tsx`) now drives colors through inline `style={{ color: P.x, background: P.y }}` instead of theme tokens or Tailwind amber classes so the tour looks identical regardless of the app's light/dark theme — matching the wizard's behavior of forcing its own aesthetic. The modal backdrop swapped from `bg-background/95` + amber/violet radial gradients to `P.paper` at 94% opacity with two soft mocha radial washes. The Back/Skip buttons became `bgCard` pills with `border`, the Next button is the dark `text` color on `paper`, and the finish CTA is a solid `accent` pill with a warm mocha glow. The MockupSidebar drops the Tailwind amber classes on the Archive glyph, drawer-pull handles, and chevron — all now render in the palette's `accent` + `textTertiary`; the header pill is `paperWarm` with a `border` ring, the tab rail is `rgba(243,237,228,0.7)` with a `borderLight` ring, and the active tab uses `bgCard` with a subtle mocha shadow. Slide-side chrome got the same pass: section chips (`01 · DATA` etc.) went from amber-500/10 → `accentBg` with `borderDark`; agent-card callouts (1/2/3 badges) now use `accentBg` + `accent` text; the Research Analyst avatar gradient went from `amber-400 → orange-500` to `accent → accentWarm`; emerald live dots swapped to muted sage `#5A9E7B / #4A8E6B` so they read quieter on cream. File-icon colors in the Data tree moved to muted sidebar-friendly hues (`#5A7FB5` blue, `#C26B6B` red, `#5A9E7B` green, etc.) rather than pure Tailwind saturation. Verified visually in Chrome: intro, Data, Agents, and Tasks slides all render on warm cream paper with mocha accents — the tour now feels like a natural extension of the onboarding wizard instead of a parallel surface with a different color story.

[2026-04-24] Reskinned the tour's Cabinet mockup to match the real sidebar exactly and aligned all tour typography with the onboarding wizard's treatment. `src/components/onboarding/tour/mockup-sidebar.tsx` was rebuilt with the sidebar's two-container structure (the old implementation was a single `rounded-2xl` card which no longer matched the real UI): container 1 is the `rounded-lg bg-muted/60 ring-1 ring-border/60` header rail with an 18px amber `Archive` glyph + `text-sm font-medium text-muted-foreground` title + a right-side badge slot rendering a chevron-suffixed string (defaults to "+1", intro slide uses "All"); container 2 is the drawer-tab rail, inset by `mx-[9px]` so the header reads as a wider crown, `rounded-b-lg border bg-muted/40 p-1 pt-2` with three grid cells. Each tab now renders with the sidebar's exact treatment: a 2px x 16px amber-or-muted pull handle pinned at `top-1 left-1/2`, an 18px icon (swapped `Kanban` → `SquareKanban` to match the real tree-view import), and an 8px uppercase label at `tracking-[0.1em]`; active state is a 1px lift + background card + subtle `ring-1 ring-border/70` shadow. The tree/body area moved to a sibling below the two containers (not nested inside one card), mirroring where the tree lives in `tree-view.tsx`. Added a `hideTabs` prop so the Cabinet header can appear alone before tabs populate. Typography across every slide now uses the onboarding wizard's `font-logo italic tracking-tight` treatment (Instrument Serif) for headlines, with the key noun in the amber accent color: "Meet your **Cabinet**.", "Your **single source** of truth.", "Your **AI team**.", "Ready to **start**?" Body copy on the right column switched from the default sans-serif to `font-body-serif` (Source Serif 4) at `leading-relaxed` — matches `WelcomeBackStep` and the wizard's home screen. The intro-slide card width tightened from `w-[440px]` → `w-[300px]` to sit closer to the ~280px real-sidebar width so the view-transition morph into slide 1 lands with minimal size change. Verified in Chrome: intro card's two containers read as the real sidebar's Cabinet chrome (header pill + drawer rail with pull handles), Next morphs into slide 1 with DATA active + tree populated, slides 2 and 3 carry the same typography treatment through.

[2026-04-24] Prepended a "Meet your Cabinet" intro slide to the onboarding tour so the first thing users see is the Cabinet itself. New `src/components/onboarding/tour/slide-intro.tsx` renders a centered layout: "WELCOME" chip → "Meet your Cabinet." (5xl headline) → "Your AI team. Your knowledge base. One place." subtitle → an empty Cabinet card (440px wide) that fades in, whose DATA / AGENTS / TASKS tabs then pop in one-by-one with staggered delays (900/1080/1260ms). Extended `src/components/onboarding/tour/mockup-sidebar.tsx` with four new props: `hideBody` (skips the body `<div>` so the card sits short on the intro slide), `tabsPopIn` (applies `cabinet-tour-pop-in` keyframe per-tab with per-index `animationDelay`), `title` (defaults to "Jane's Cabinet" for the tour proper; intro slide overrides to the generic "Cabinet"), and `viewTransitionName` (threads into an inline `style={{ viewTransitionName }}` on the card root). Made `activeTab` accept `null` so the intro can render with no tab highlighted. Updated `tour-modal.tsx` to insert SlideIntro at index 0 (so the tour is now 4 slides; progress dots reflect this automatically) and wrap every navigation path (`next`, `back`, `goTo` for dot clicks) in a `transition()` helper that calls `document.startViewTransition(() => flushSync(update))` when available and falls back to a synchronous `setIndex` otherwise. The shared `viewTransitionName="cabinet-card"` on the MockupSidebar in slide-intro / slide-data / slide-agents / slide-tasks gives the browser an identity to morph the card between its centered (intro) and left-column (tour) layouts — the tabs, header, and border all animate in place across the slide switch without needing a bespoke tween. Verified in Chrome via CDP: slide 0 renders with populating tabs, Next morphs the card to the left and reveals the tree + right-side copy, Back morphs back to the centered intro state.

[2026-04-24] Added the "Meet your Cabinet" onboarding tour — a full-screen 3-slide takeover (Data → Agents → Tasks) that auto-opens once after the existing wizard finishes and is replayable from a new "Tour" button in the status bar (inserted left of the Discord "Chat" link). New directory `src/components/onboarding/tour/`: `tour-modal.tsx` (modal shell with Back/Next/Skip, progress dots, arrow-key + Esc navigation, and a final "Write your first task" CTA that prefills the task composer with `"Run 10 tasks, each writing a new song, save to @Songs/"`), `mockup-sidebar.tsx` (stylized sidebar with DATA/AGENTS/TASKS tabs that each slide reuses), `slide-data.tsx` (tree populates row-by-row with mixed icons md/pdf/png/xlsx/pptx/app/repo/Google, icons pulse in sequence, then a floating `@Market Research` chip foreshadows mentions), `slide-agents.tsx` (agent list fades in, a Research Analyst card lifts forward with a live heartbeat dot and three numbered callouts for Persona/Heartbeat/Jobs revealing sequentially), `slide-tasks.tsx` (composer types `Run 10 tasks, each writing a song, save to @Songs/` character-by-character, then 10 task cards fan out in a 2-col grid with streaming preview bars and per-card heartbeat dots), and `use-tour.ts` (persistence via `cabinet.tour-done` localStorage key + `cabinet:show-tour` event bridge + a `requestShowTour()` helper). Wired into `src/components/layout/app-shell.tsx` (renders `<TourModal>` alongside the other global overlays; `useTour(showWizard === false)` auto-opens once after the wizard signals done; `handleLaunchTourTask` switches the section to Tasks and dispatches `cabinet:open-create-task` with `{ detail: { initialPrompt } }` after 100ms so the board's listener is mounted) and `src/components/layout/status-bar.tsx` (new Sparkles "Tour" button with amber styling to match the other footer pills). Extended `src/components/tasks/board/tasks-board.tsx` to read `initialPrompt` from the event's `CustomEvent.detail` and forward it to `<StartWorkDialog>` (which already accepted an `initialPrompt` prop), resetting it on dialog close. Added tour-scoped keyframes to `src/app/globals.css` (`cabinet-tour-fade-up`, `-fade-in`, `-pop-in`, `-icon-pulse`, `-mention-float`, `-agent-lift`, `-callout-in`, `-typing`, `-caret-blink`, `-heartbeat-dot`, `-stream-bar`) plus a `prefers-reduced-motion` guard on the `.cabinet-tour-animated` class and a `.cabinet-tour-no-scrollbar` utility. No Framer Motion dep added — all animation is pure CSS + Tailwind staggered `animation-delay` values.

[2026-04-24] Thinned the sidebar tree scrollbar from 8px → 6px by tightening the ScrollArea utility overrides in `src/components/sidebar/tree-view.tsx`: `w-2 → w-1.5` on the bar and `pl-1 → pl-0.5` on the inner padding. Thumb remains visually centered and stays on the shared base-ui `ScrollArea` primitive.

[2026-04-24] Unified navbar height across the Data, Agents, and Tasks routes (`#/cabinet/./data/.`, `#/cabinet/./agents`, `#/tasks`) to a consistent 49px with matching styling (`border-b border-border/70 bg-background/95 px-4 py-2.5 sm:px-6`). Updated `src/components/layout/viewer-toolbar.tsx` (replaced the translucent `bg-background/80 backdrop-blur-sm` + `py-2` styling with the shared `/70 /95 py-2.5 sm:px-6` look), `src/components/tasks/board/tasks-board.tsx` (dropped to `py-2.5 sm:px-6`, added `flex-wrap`, and forced `h-7` on the `NewWorkButton` wrapper), and `src/components/tasks/board/view-toggle.tsx` (added `h-7` on the segmented-control wrapper). Normalized all global header-action icon buttons from `h-8 w-8` → `h-7 w-7` so the ViewerToolbar matches the 28px content height budget: `src/components/layout/header-actions.tsx` (Terminal + AI toggles), `src/components/layout/theme-picker.tsx` (theme button), `src/components/layout/header.tsx` (Download dropdown trigger), and `src/components/editor/version-history.tsx` (history toggle). Before: Data/Cabinet=53px, Agents=49px, Tasks=55.5px. After: all 49px.

[2026-04-24] Simplified the onboarding provider step — removed the `RuntimeSelectionBanner` summary, the "Default model" grid, and the "Reasoning effort" grid from `src/components/onboarding/onboarding-wizard.tsx` (step 5, `STEP_PROVIDER`). The three UI blocks were overwhelming for first-run users who just need to pick an AI CLI; model + effort are still seeded automatically when a provider is picked (auto-select path uses `p.models[0]` + `getSuggestedProviderEffort`, the tile click does the same), so the saved `defaultProvider`/`defaultModel`/`defaultEffort` preferences continue to be set sensibly. Users can refine model/effort later via Settings → Providers. Also cleaned up the now-unused symbols: dropped the `RuntimeSelectionBanner` import, narrowed `runtime-options` import to just `getSuggestedProviderEffort`, and removed `activeProvider` / `activeModel` / `activeModels` / `activeEffortLevels` derivations. Pre-existing lint warnings + one unrelated `react/no-unescaped-entities` error on line 2507 left untouched.

[2026-04-24] Restarted the onboarding flow on the local dev workspace by renaming the four files that mark setup-complete to `.bak-20260424-112229` copies so they stay recoverable: `data/.agents/.config/workspace.json`, `data/.agents/.config/company.json`, `data/.agents/.config/onboarding-complete.json`, and `data/.agents/.config.json`. `/api/agents/config` now returns `{exists:false}`, so `src/components/layout/app-shell.tsx` (lines 98–186) will render the `OnboardingWizard` again on next page load — the localStorage key `cabinet.wizard-done` also needs to be cleared in DevTools to avoid a brief flash of the main UI before the config check self-corrects. No code changes; purely a data/config reset.

[2026-04-24] Reworked the `WhenChip` layout across every composer — the previous `header` slot approach pushed the placeholder down by ~50px, leaving the textarea starting visually in the middle of the card (chip on top, placeholder below it with a big gap). Replaced with an absolute-positioned overlay. Added new `topRightOverlay?: React.ReactNode` prop to `src/components/composer/composer-input.tsx`: renders the content at `absolute right-3 top-3 z-10` inside the card, and when present automatically adds `pr-28` (112px) to the textarea so wrapped text can't collide with the overlay. Flipped five callsites from the `header`-slot pattern to `topRightOverlay`: `src/components/home/home-screen.tsx`, `src/components/cabinets/cabinet-task-composer.tsx`, `src/components/tasks/conversation/task-composer-panel.tsx`, `src/components/agents/agents-workspace.tsx` (renderSettingsComposerPanel), and `src/components/agents/agent-detail-v2.tsx`. The chip now floats in the corner without stealing vertical space; the placeholder sits at the top-left where it belongs. Start Work modal (`start-work-dialog.tsx`) untouched — it puts the chip in the `DialogHeader` alongside the dialog title, which is a different concern. Agents workspace quick-send popup also untouched — its chip lives inline in the agent-card row next to the close button.

[2026-04-24] Consolidated the agent detail page composer onto the shared `ComposerInput`. The inline custom composer in `src/components/agents/agent-detail-v2.tsx` (raw textarea + hand-rolled Send button + its own focus-state tracking) had two small visual features the shared component didn't: agent-tinted focus ring, and a denser text layout (14px / `pt-3 pb-1` / `leading-relaxed`). Rather than keep a parallel implementation, extended `src/components/composer/composer-input.tsx` with two props: (1) `focusTint?: { borderColor: string; ringColor: string }` — when set, the card runs `onFocus`/`onBlur` (React synthetic events bubble, so textarea focus lands on the outer div), tracks `cardFocused` state, and applies inline `borderColor` + 3px `boxShadow` ring in the supplied colors while focus is inside the card; (2) `textareaClassName?: string` — appended via `cn`/twMerge so callers can override padding / text-size / line-height conflicts. Rewrote the `Composer` function inside `agent-detail-v2.tsx` as a thin persona-aware wrapper: calls `useComposer`, renders `<ComposerInput>` with `focusTint={{ borderColor: palette.text, ringColor: palette.bg }}` + `textareaClassName="pt-3 pb-1 text-[14px] leading-relaxed"` + the WhenChip header slot + a TaskRuntimePicker in `actionsStart`; the suggested-prompts chip row stays as a sibling below and now calls `composer.setInput(s)` instead of a local setValue. Side-effect UX change: the page now submits on plain Enter (the `useComposer` default) and uses Cmd+Enter for newlines — matches every other composer in the app. Dropped the inline "⌘↵ to send" hint (the shared composer's "⌘↵ newline" hint now carries that role).

[2026-04-24] Added `WhenChip` to the agent detail page composer ("Ask X something…" on `/#/cabinet/./agents/<slug>`) — this composer was missed in the earlier sweep because it's a custom inline component in `src/components/agents/agent-detail-v2.tsx` (not built on the shared `ComposerInput`). Extended the inner `Composer` function with an optional `onScheduleHandoff?: (mode, message) => void` prop; when provided, a new header row renders inside the rounded-2xl composer card (`flex items-center justify-end px-3 pt-3`) with the chip anchored top-right — same pattern as every other composer in the app. Parent `AgentDetailV2` handles the dialog side: new `handoffOpen` / `handoffMode` / `handoffPrompt` state, a single-agent `CabinetAgentSummary[]` built from the current `persona` (the page is scoped to one agent, so no `/api/agents/personas` fetch needed), and a `<StartWorkDialog>` at the tail of the component seeded with the captured draft + mode. `onStarted` closes the dialog and calls `refresh()` to pick up the newly-created routine/heartbeat in the page's Schedule section. Separately, the standalone top-right "Schedule" button (the existing Schedule calendar view) is untouched — it's a different affordance for browsing the agent's schedule, not for creating a scheduled variant of a new prompt.

[2026-04-24] Made the post-import sidebar refresh reliable. `fetchTree` in `src/lib/api/client.ts` now sets `cache: "no-store"` so the browser can't serve a stale `/api/tree` response right after an upload mutation (without this the newly-imported file could take a user-driven refresh to appear). Also restructured `useFileImport.importFilesList` so `loadTree()` runs in its own guarded block even when a file mid-batch fails — successfully-uploaded files still show up in the sidebar, and the error alert fires after the refresh. Verified end-to-end: upload → GET /api/tree returns the new directory + dedup-suffixed child on the first call.

[2026-04-24] Extended `WhenChip` to the task-conversation reply composer — previously excluded as "not a kickoff surface", but turning a mid-conversation draft into a recurring routine or heartbeat variant is a real workflow. Added optional `onScheduleHandoff?: (mode, message) => void` prop to `src/components/tasks/conversation/task-composer-panel.tsx`; when present, the panel renders WhenChip in the composer card's `header` slot (top-right, same `flex items-center justify-end px-3 pt-3` pattern as the other composers). Panel stays dumb — it just forwards the picked mode + current draft string. `src/components/tasks/conversation/task-conversation-page.tsx` owns the dialog side: new `openScheduleHandoff` callback captures `mode` + `message`, lazy-fetches `/api/agents/personas` once (reusing the same `AgentListItem → CabinetAgentSummary` adapter shape used in `agents-workspace.tsx`), then opens a single `<StartWorkDialog>` seeded with the captured prompt, `handoffMode`, and the task's current `agentSlug` + `cabinetPath`. Both `TaskComposerPanel` instances on the page (terminal mode + standard mode) pass the callback, so WhenChip lights up regardless of runtime. Right-side AI panel (`ai-panel.tsx`) still intentionally excluded — that composer is page-context chat, not task scheduling.

[2026-04-24] Added sidebar file-import so users can bring a file into the KB without opening the editor. New `ContextMenuItem` "Import File…" in `src/components/sidebar/tree-node.tsx` sits between "Load Knowledge" and "Create Cabinet Here"; clicking it spawns a transient `<input type="file" multiple>` via the new `useFileImport` hook (`src/components/sidebar/use-file-import.ts`). The hook POSTs each file to the existing `/api/upload/[...path]/` endpoint, then calls `loadTree()` + `expandPath(target)` so the file appears under the right node immediately. Target directory is resolved per node: containers (`directory`, `cabinet`, `app`, `website`) import into themselves, leaf files (`pdf`, `csv`, standalone `.md`, etc.) import into their parent. Also extended the existing `onDragOver`/`onDrop` handlers to detect OS drag-drop (`dataTransfer.types.includes("Files")`) — drop effect switches to `copy`, the zone is forced to `"into"` so the primary highlight ring shows, and files are routed through the same `importFilesList` path. Internal drag-reorder behavior is unchanged (`text/plain` payload branch still runs when no files are present). Added an `autoCommit(\`\${virtualPath}/\${filename}\`, "Add")` call in `src/app/api/upload/[...path]/route.ts` (which previously skipped the auto-commit path entirely), so imports get versioned alongside page edits.

[2026-04-24] Wired `WhenChip` ("Run now / On a schedule / Heartbeat") into the two remaining task-kickoff composers in `src/components/agents/agents-workspace.tsx`: (#5) the agent settings composer (`renderSettingsComposerPanel`) and (#6) the quick-send popup modal. Both routes fire `createConversation` just like Home / Cabinet / Start Work, so scheduling is a legitimate option there. #5 gets the chip via the composer card's `header` slot (`flex items-center justify-end px-3 pt-3`) with the target agent captured from `agentSlug` at render time. #6 inlines the chip in the existing agent-card header row, left of the close button — captures the `quickSendAgent` slug. Added imports (`StartWorkDialog`, `WhenChip`, `StartWorkMode`) and three state atoms (`handoffOpen`, `handoffMode`, `handoffAgentSlug`). Reused the existing `cabinetAgentsView` (the `AgentListItem[] → CabinetAgentSummary[]` adapter already in the file) so the dialog gets the shape it expects. Rendered a single `<StartWorkDialog>` at the tail of the component, seeded with the current composer input + captured agent slug; `onStarted` resets the composer, closes the quick-send popup, navigates to the new conversation via `buildAgentSection`. Task-conversation reply composer and right-side AI panel intentionally left untouched — those aren't kickoff surfaces.

[2026-04-24] Kept the tasks board header trash button always visible instead of hiding it when the current filter (agent, Manual/Jobs/Heartbeat, etc.) yields zero tasks — the header was jumping width whenever the user flipped between trigger chips. In `src/components/tasks/board/tasks-board.tsx` the `{filteredTasks.length > 0 && …}` guard around the Trash2 button is gone; the button now always renders and branches on click: if `filteredTasks.length === 0` it opens a `ConfirmPopover` with an informational message (title `No <scope> to delete` plus a body explaining which filter produced the empty state — `triggerFilter` and `agentFilter` are composed into "the heartbeat filter", "the selected agent filter", etc.), otherwise it falls through to the existing destructive bulk-delete confirm. Empty-state variant shows a muted icon (`text-muted-foreground/40`) with neutral hover (`hover:bg-muted/60 hover:text-muted-foreground/70`) instead of the destructive red hover, so it doesn't look like it's about to delete everything. `title`/`aria-label` swap to "Nothing to delete in this view" / "No tasks in this view" in that state. Added `infoOnly?: boolean` to `PendingConfirm` in `src/components/tasks/board/confirm-popover.tsx` — when true the cancel button is hidden so the popover reads as a single-button "OK"-style info dialog; the info popup uses `confirmLabel: "Got it"` with a no-op `onConfirm`. Imported `cn` from `@/lib/utils` in `tasks-board.tsx` for the conditional button classes.

[2026-04-24] Renamed `src/components/tasks/board-v2/` → `src/components/tasks/board/` and `tasks-board-v2.tsx` → `tasks-board.tsx` + component `TasksBoardV2` → `TasksBoard`. The `v2` suffix was historical (from a previous rewrite that fully replaced the old board) — there was never a parallel v1, so the name was just misleading noise that suggested duplication. Moved via `git mv` so history is preserved. Updated every import site: `src/app/tasks/page.tsx`, `src/components/layout/app-shell.tsx` (both import + JSX tag), `src/components/tasks/conversation/task-conversation-page.tsx` (board-actions), `src/components/agents/agents-workspace.tsx` (schedule-view), `src/components/cabinets/activity-feed.tsx` (agent-pill + status-icon), `src/components/cabinets/next-up-runs.tsx` (agent-pill). Replaced the `[board-v2]` console.error prefix with `[board]` across `tasks-board.tsx`, `detail-panel.tsx`, `row-actions.tsx`, `use-board-data.ts`, `kanban-view.tsx`, `use-drag-handler.ts`. Updated the "board-v2" prose mentions inside `schedule-dialogs.tsx` doc comments, `src/lib/agents/runtime-overrides.ts`, and `src/lib/agents/terminal-mode-cache.ts` to "tasks board". Also trimmed the outdated "Phase 1 ships read-only" docstring above `TasksBoard` — it's long since been a full DnD-enabled board. `npx tsc --noEmit` clean. Historical PROGRESS entries still mention `board-v2` by name — left untouched since they're a log of when that path existed.

[2026-04-24] Swept the `WhenChip` ("Run now / On a schedule / Heartbeat") to the top-right corner in every composer that hosts it. Inventoried all seven composer surfaces: #1 Home "I want to create…" (`src/components/home/home-screen.tsx`), #2 Cabinet task composer (`src/components/cabinets/cabinet-task-composer.tsx`), #3 Start Work modal (`src/components/composer/start-work-dialog.tsx`), #4 Task-conversation reply (`src/components/tasks/conversation/task-composer-panel.tsx`), #5 Agent settings composer + #6 Quick-send popup (both in `src/components/agents/agents-workspace.tsx`), #7 right-side AI panel (`src/components/ai-panel/ai-panel.tsx`). Only #1/#2/#3 surface the WhenChip; the other four don't expose scheduling and were left alone. Changes: (1) home-screen: moved WhenChip from `actionsStart` into the composer `header` slot (`flex items-center justify-end px-3 pt-3`); TaskRuntimePicker stays in bottom-left. (2) cabinet-task-composer: already moved to `header` slot. (3) start-work-dialog: lifted WhenChip out of both `ComposerInput.actionsStart` AND the inline `HeartbeatModePanel` top row into the `DialogHeader` — wrapped the title + chip in `flex items-start justify-between gap-3` so the chip anchors top-right next to the title in all three modes (now/recurring/heartbeat). Dropped the `whenChip` prop from `HeartbeatModePanel` (single DialogHeader instance covers every mode); heartbeat panel's internal row collapses to just the AgentDropdown and only renders when there are agents.

[2026-04-24] Moved the `WhenChip` ("Run now / On a schedule / Heartbeat") out of the bottom-left `actionsStart` row in `src/components/cabinets/cabinet-task-composer.tsx` and into the composer card's `header` slot with right-alignment (`flex items-center justify-end px-3 pt-3`). Now renders in the top-right corner of the tasks composer container; `AgentPickerCompact` and `TaskRuntimePicker` stay in the bottom actions row.

[2026-04-24] Swept the rest of the codebase to use the unified `DepthDropdown`. Grepped for `CABINET_VISIBILITY_OPTIONS` / `setCabinetVisibilityMode` usages and replaced four ad-hoc depth selectors with `<DepthDropdown>`: (1) `agents-workspace.tsx` had a `<Select>` cluster with a "Depth" label — replaced with `<DepthDropdown>`; (2) same file had a second segmented button-pill group (`rounded-full border … rounded-full px-2 py-0.5 text-[10px]`) — replaced; (3) `cabinets/cabinet-view.tsx` had the identical segmented button-pill group — replaced; (4) `cabinets/agent-status-grid.tsx` had individual bordered pill buttons with a "Scope" label prefix — replaced with the dropdown (label dropped since the FolderTree icon + shortLabel conveys the same intent). Pruned now-unused imports: `CABINET_VISIBILITY_OPTIONS`, `CabinetVisibilityMode` cast, `Select*` cluster in `agents-workspace.tsx`; `CABINET_VISIBILITY_OPTIONS` + unused `cn` in `cabinet-view.tsx`; `CABINET_VISIBILITY_OPTIONS` in `agent-status-grid.tsx` (kept `cn` and `FolderTree` which are used elsewhere in the file). Every depth selector in the app now goes through the single `DepthDropdown` component.

[2026-04-24] Hide the FolderTree icon in the compact (sidebar) variant of `DepthDropdown`. The icon still shows in the normal (tasks nav bar) variant. Tiny conditional — `{!compact && <FolderTree … />}` — keeps the sidebar trigger as just `+1 ▾` while the tasks bar keeps the full icon+label+chevron for discoverability.

[2026-04-24] Unified the depth dropdown between the sidebar and the tasks nav bar. New shared component at `src/components/cabinets/depth-dropdown.tsx` that ships the demo's Variant B (ghost minimal, no border, hover-fill, `data-[popup-open]:bg-muted/60`) with the `FolderTree` lucide icon replacing the previous `Layers`. The component takes `mode`, `onChange`, and an optional `compact` prop (sidebar uses `compact`; tasks board uses normal). Extended `src/components/ui/dropdown-menu.tsx` to forward a `collisionAvoidance` prop to the base-ui Positioner, and the shared `DepthDropdown` passes `{ side: "none" }` so the menu never flips upward. In `src/components/sidebar/tree-view.tsx`: replaced the base-ui `<Select>` cluster (SelectTrigger / SelectContent / SelectGroup / SelectItem / SelectValue) with `<DepthDropdown compact />`; removed now-unused imports (`Select*`, `CABINET_VISIBILITY_OPTIONS`, `CabinetVisibilityMode`, `cabinetVisibilityModeLabel`). In `src/components/tasks/board-v2/tasks-board-v2.tsx`: removed the inline `DepthDropdown` function (previously Layers + bordered pill) and `CABINET_VISIBILITY_OPTIONS` / `Layers` / `Check` imports; the JSX callsite is unchanged since it already read as `<DepthDropdown mode={…} onChange={…} />`.

[2026-04-24] Added "Icon alternates" section to `/demo/depth-dropdown`. Ten lucide icon candidates rendered in an interactive Variant-B (ghost minimal) trigger so each can be clicked and felt at the real 3.5px size: `Layers` (current), `Layers3`, `FolderTree`, `ListTree`, `Network`, `Boxes`, `Workflow`, `Focus`, `ScanSearch`, `Telescope`. Each shows its import name + a one-line note on what it semantically implies (hierarchy vs. scope vs. theme-fit). Shared depth state so picking an option flows across all ten triggers.

[2026-04-23] Added `/demo/depth-dropdown` route for unifying the depth selector between the sidebar cabinet rail (currently a base-ui `Select` with tiny uppercase text) and the tasks nav bar (currently a custom `DepthDropdown` with Layers icon + bordered pill). `src/app/demo/depth-dropdown/page.tsx` renders five trigger variants (A bordered pill / B ghost minimal / C text chip / D soft capsule / E labeled) in both contexts side-by-side — each rendered inside a sidebar-width H-style cabinet rail mock and inside a tasks nav-bar mock (tone-accurate: ViewToggle pill, trigger chips, New Task button). All variants share the same `DepthMenuItems` list so only the trigger differs. Each card has its own shared `CabinetVisibilityMode` state so picking an option from either context reflects in both. Production-port note: all variants will pass `collisionAvoidance={{ side: "none" }}` to base-ui's positioner to disable the upward-flip behavior.

[2026-04-23] Shipped cabinet-header demo Variant H into the real sidebar. In `src/components/sidebar/tree-view.tsx`: wrapped the cabinet header + drawer-tab strip in a shared `<div className="px-2 pt-3">` column. Header rail became a single rounded pill (`flex items-center gap-2 rounded-lg bg-muted/60 px-2.5 py-1.5 ring-1 ring-border/60 hover:bg-muted/80`) with the Archive icon bumped to `h-[18px] w-[18px]` and the cabinet name re-typed as `text-sm font-medium text-muted-foreground` (muted, matches inactive tab labels — per H spec). The SelectTrigger's "ALL"-chip size dropped to `text-[9px]` with tone `text-muted-foreground/70`. Drawer-tab strip became `mx-[9px] rounded-b-lg border border-border/60 p-1 pt-2`, flush against the header. Each tab button gained (1) `pt-3 pb-2` + `relative`, (2) a `h-[2px] w-4` drawer-pull handle span at top-center (amber-400/50 when active, muted-foreground/30 otherwise), (3) icon bumped to `h-[18px]`, (4) label shrunk to `text-[8px] tracking-[0.1em]`. Hover-to-reveal `+` buttons still work. Split the original `{cabinetExpanded && (<>…</>)}` block so the tab strip lives inside the outer wrap and the agents/tasks/data drawer contents live in a second cabinetExpanded block after it. Demo page at `/demo/cabinet-header` kept for now.

[2026-04-23] Added Variant I "H + full-strength title" to the cabinet-header demo. Identical to H in every other respect (flush join, `rounded-b-lg` frame, `mx-[9px]` inset, soft amber 2px pulls), but the cabinet name text returns from `text-muted-foreground` to `text-foreground` so it's instantly legible — amber Archive icon stays as the accent, not the sole anchor. Purely additive to `src/app/demo/cabinet-header/page.tsx`.

[2026-04-23] Widened Variant H's drawer frame by 5px per side: `mx-[14px]` → `mx-[9px]`. Final inset is now 5px narrower per side than G (net 10px total narrower than the header).

[2026-04-23] Narrowed Variant H's drawer frame by 10px per side. `mx-1` (4px each) → `mx-[14px]`, so the bottom container is now 20px narrower than the header overall — exaggerates the crown/lip effect from the flush-join.

[2026-04-23] Revised Variant H to match user spec: header keeps G's full `rounded-lg` (not `rounded-t-lg`), drawer frame becomes `rounded-b-lg` (flat top, rounded bottom), and the `mt-1` gap is removed so the frame sits flush against the header. Top border of the frame is kept (no `border-t-0`) to match G's border exactly. All sizes otherwise identical to G.

[2026-04-23] Added Variant H "Seamless crown" to the cabinet-header demo. Based on G but the header and drawer frame touch flush — removed the `mt-1` gap, header went from `rounded-lg` to `rounded-t-lg` (flat bottom), and the drawer frame went from `rounded-lg` to `rounded-b-lg` with `border-t-0` (flat top, no duplicate border where the two pieces meet). Header stays `mx-1`-wider than the frame so the crown/lip geometry is preserved. Purely additive to `src/app/demo/cabinet-header/page.tsx`.

[2026-04-23] Tweaked Variant G's active handle color from `bg-foreground` to `bg-amber-400/50` — keeps the brass/cabinet vibe from E/F but at 50% opacity instead of the 80% used in E/F, so it reads softer. Handle height stays at `h-[2px]`, inactive handle unchanged.

[2026-04-23] Added Variant G "Crown dock, refined pulls" to the cabinet-header demo. Based on F but the drawer pull handles are thinner (`h-[2px]` instead of `h-[3px]`) and the active handle uses `bg-foreground` instead of `bg-amber-400/80` so the pull color tracks the active label color. Purely additive to `src/app/demo/cabinet-header/page.tsx`.

[2026-04-23] Added Variant F "Crown dock" to the cabinet-header demo. Based on E but the cabinet name text drops from `text-foreground` to `text-muted-foreground` (matching inactive tab labels, so the amber Archive icon becomes the primary anchor) and the drawer frame is inset `mx-1 mt-1` with its own full `rounded-lg` + border, so the title rail reads as a wider crown sitting over a narrower drawer shelf. Purely additive to `src/app/demo/cabinet-header/page.tsx`.

[2026-04-23] Tasks board header declutter + theme alignment. Replaced the 4-button depth segmented control (Own / +1 / +2 / All) with a single `DepthDropdown` — a compact `Layers`-iconed trigger showing the current short label, opening a menu of all four options with the full labels ("Own agents only", "Include direct children", etc.) and a check mark on the active one. Moved the standalone `{n} tasks` count onto the "All" trigger chip as a trailing number (`All 142`, or `All 12/142` when an agent filter narrows), removing the dedicated rightmost span. Switched all theme-less active states from `bg-foreground text-background` (near-black in the paper theme) to `bg-primary text-primary-foreground` (the theme's accent brown) so the board's active controls match the "+ New Task" CTA and the theme palette: `ViewToggle` active pill, the new depth dropdown's check glyph, `TriggerChip` "all" active, and the `FilterBar` "All agents" + active agent pill borders. Trigger tones (sky/emerald/pink on Manual/Jobs/Heartbeat) were left alone — those are intentional semantic colors. Touched: `src/components/tasks/board-v2/tasks-board-v2.tsx`, `src/components/tasks/board-v2/filter-bar.tsx`, `src/components/tasks/board-v2/view-toggle.tsx`.

[2026-04-23] Added Variant E "Drawer pulls" to the cabinet-header demo. Based on D (Labelled dock) but each tab button grows a 3px × 16px rounded handle bar at top-center — amber when the drawer is active, muted otherwise — to mimic a physical drawer pull. Labels shrink from `text-[9px]` to `text-[8px]` with `tracking-[0.1em]`, and the tab strip gets `pt-2` to give the handles breathing room. Purely additive to `src/app/demo/cabinet-header/page.tsx`.

[2026-04-23] Sidebar footer is now tab-aware. The drawer state (`Data / Agents / Tasks`) was lifted from local state in `tree-view.tsx` into `app-store.ts` as `sidebarDrawer` (persisted under `cabinet.sidebar.drawer`), and the sidebar footer in `sidebar.tsx` now renders a different primary action set depending on which drawer is open: **Data** → `New Page` + `New Cabinet`, **Agents** → `Add Agent`, **Tasks** → `New Task`. `Settings` stays pinned on the right as a constant anchor across all three. The Agents and Tasks buttons reuse the existing custom events (`cabinet:open-add-agent`, `cabinet:open-create-task`) that the workspace + board already listen for, so clicking them routes to the right view and opens the same dialog the drawer's hover-`+` already does. For responsiveness, `new-page-dialog.tsx` and `new-cabinet-dialog.tsx` trigger buttons lost their `whitespace-nowrap` and gained `min-w-0` + a `truncate` span on the label, so labels gracefully shrink on narrow sidebars (220 px min) instead of overflowing.

[2026-04-23] Added `/demo/cabinet-header` route for previewing cabinet header + drawer-tab variants. `src/app/demo/cabinet-header/page.tsx` renders five options side-by-side (Baseline, A — Cabinet plaque, B — Drawer face, C — Heading + shelf, D — Labelled dock) in 280px sidebar-sized panels with the real `bg-sidebar` background, each with clickable Data/Agents/Tasks tabs so the active state is interactive. Also includes a "Tasks icon alternates" row showing `SquareKanban` (current) vs `ListChecks` vs `ClipboardList`. No changes to the live sidebar yet — this is a decision-making surface.

[2026-04-23] Removed legacy `/jobs` page and pruned the sidebar footer. The bottom icon row in `src/components/sidebar/sidebar.tsx` used to carry Home / Jobs / Settings; the first two were redundant (Home is already the "cabinet" wordmark in the header, and the Jobs button routed to the pre-agents JobsManager at `#/cabinet/./jobs`, which is no longer part of the system) so the footer is now just Settings. Ripped out the supporting route plumbing as well: deleted `src/components/jobs/jobs-manager.tsx` and the whole `src/components/jobs/` folder, removed the `JobsManager` import + `section.type === "jobs"` branch in `app-shell.tsx`, dropped `"jobs"` from `SectionType` in `app-store.ts`, stripped the `jobs` hash build/parse branches (including the bare `/#/jobs` alias) in `use-hash-route.ts`, and removed the `jobs` case in `return-to-chip.tsx`. Cabinet-view's "jobs" count pill and tasks-board schedule view still reference the scheduled-jobs domain concept (per-agent cron jobs) — that's a separate surface and was intentionally left alone.

[2026-04-23] Onboarding lands on Kanban with clean filters. `usePersistentState` in `tasks-board-v2.tsx` already defaulted to `"kanban"` (good), but first-time users who went through onboarding in a prior build could carry in stale `cabinet.tasks.v2.view = "list"` or `cabinet.tasks.v2.trigger = "heartbeat"` and end up on an unfamiliar view with filters active (the original UX-audit #51 foot-gun). `handleWizardComplete` in `app-shell.tsx` now writes `"kanban"` to `cabinet.tasks.v2.view`, `"all"` to `cabinet.tasks.v2.trigger`, and removes `cabinet.tasks.v2.agent` so a freshly onboarded user always lands on the Kanban board with no filters — no stale state can leak through.

[2026-04-23] Tasks board skeleton loader. Replaced the bare `Loader2` spinner on the Tasks board with view-aware skeletons so the page feels responsive from the first paint instead of looking "stuck at 0 of 0 tasks" for the 1–2 s while `/api/agents/conversations` loads (this is what the user was seeing when they reported `/#/cabinet/./tasks` "doesn't show"). New `src/components/tasks/board-v2/board-skeletons.tsx` exports `KanbanSkeleton` (5 lane columns with 2–3 placeholder cards each, matching the real lane titles), `ListSkeleton` (10 varied-width rows with alternating background stripes), and `ScheduleSkeleton` (6 agent rows with timeline segment bars), plus a `BoardSkeleton` dispatcher that renders the right one based on `view`. Each placeholder is a `bg-muted/60 animate-pulse` bar; `aria-busy="true"` + `aria-label="Loading tasks"` on the container for screen readers.

[2026-04-23] Drawer animation polish. (1) Reordered the drawer tabs from Agents · Tasks · Data to **Data · Agents · Tasks** so the most-used surface leads. (2) Tasks drawer rows now fade-in + slide-in-from-top with the same 22 ms stagger the Agents drawer uses; capped at 12 rows so long task lists settle quickly. (3) Data drawer tree now has a flowing waterfall stagger — `TreeNode` gained an optional `animationDelayMs` prop that the parent threads into children with `+30 ms` per descent level and `+14 ms` per sibling (capped at 360 ms). Top-level tree rows get `index * 22 ms`, so nested files inherit and extend the cascade. Result: when the Data drawer opens, every visible row (regardless of depth — already-expanded subtrees included) ripples in from top to bottom like a drawer being pulled out. Touched: `src/components/sidebar/tree-view.tsx`, `src/components/sidebar/tree-node.tsx`, `src/components/sidebar/recent-tasks.tsx`.

[2026-04-23] Sidebar drawer redesign — replaced the three-section vertical accordion (AGENTS / TASKS / DATA) with a horizontal "physical cabinet drawers" tab row. The sidebar now shows a 3-column tab strip in a pill container; exactly one drawer is open at a time, matching the metaphor the user asked for (each drawer = one mini portal). Active tab lifts slightly with a soft shadow + ring; inactive tabs are muted and flat. Clicking a drawer tab (a) opens that drawer inside the sidebar AND (b) routes main to the corresponding section: Agents → agents workspace, Tasks → tasks board, Data → home/page. Drawer content animates with `fade-in slide-in-from-top-1 duration-200 ease-out`; agent rows stagger 20 ms apart for a drawer-pull feel. `activeDrawer` is auto-synced with `section.type` (so hash-nav or Cmd+K jumps flip the sidebar too) and persisted under `cabinet.sidebar.drawer`. Per-drawer Plus action surfaces on hover over the active tab (add-agent / add-task / add-page) so the icons from the old per-section headers aren't lost. The Data drawer preserves the KB context menu (Add Sub Page / Load Knowledge / Copy Full Path / Open in Finder) via right-click on the drawer content. Removed the short-lived TopNav I shipped earlier today (its role is now owned by the drawers); moved Home + Jobs icon buttons into the sidebar footer next to the existing Settings gear and New Page / New Cabinet pair. Touched: `src/components/sidebar/tree-view.tsx`, `src/components/sidebar/sidebar.tsx`, `src/components/layout/app-shell.tsx` (TopNav removed), deleted `src/components/layout/top-nav.tsx`.

[2026-04-23] Added `data/architecture.md` — a KB page with four Mermaid diagrams documenting Cabinet's architecture: high-level components (Next.js app / daemon / data / AI providers), AI task/conversation sequence (structured adapter vs terminal/PTY), storage model (/data layout + API routes), and the runtime split between the Next.js app (4000) and cabinet-daemon (4100). Includes a summary of the key architectural rules the diagrams encode.

[2026-04-23] Pre-Release final tranche — cleared 5 more P0s (#2, #3, #7, #14, #16) plus partial #10. (1) **#3 hash-route aliases** — `parseHash()` in `use-hash-route.ts` now recognizes bare `/#/agents`, `/#/agents/<slug>`, `/#/tasks`, `/#/tasks/<id>`, `/#/jobs` and resolves them to the root cabinet, so every shared link of those shapes renders the right view instead of silently redirecting to Home. (2) **#7 task detail timeout** — `task-conversation-page.tsx` gains an 8 s connect watchdog: if the initial `fetchTask(taskId)` hasn't resolved, the status pill flips `connecting → unreachable`, the heading drops the literal "Loading…" in favor of the prompt first line or a real message, and a **Retry** button appears that increments a `retryNonce` and re-runs the fetch. Error path now also flips the pill to "error" with the same Retry button. (3) **#2 tasks board 0-tasks recovery** — header count now uses `filteredTasks.length` whenever *any* filter is active (was only for agent filter, hiding the trigger-filter-hides-everything case). Added an amber "N tasks hidden by filters · Clear filters" banner in the board body when `tasks.length > 0 && filteredTasks.length === 0`, so a stale `cabinet.tasks.v2.trigger` value in localStorage can no longer silently wipe the board. (4) **#14 API dedup** — new `src/lib/api/dedup-fetch.ts`: in-flight-request deduper (plus optional short-TTL cache) that collapses identical concurrent GETs to one network request. Wired at the hot first-paint call sites: `use-provider-icons.ts`, `app-store.loadProviders`, `app-shell.tsx` config fetch, `cabinet-view.tsx` config fetch, `mission-control.tsx` config fetch, `status-bar.tsx` (both health endpoints + providers/status), `data-dir-cache.ts` health fetch, `use-cabinet-update.ts` system/update fetch. The audit's `providers ×3 / overview ×5 / health ×3` burst now collapses to one request each. (5) **#16 top-nav IA** — new `src/components/layout/top-nav.tsx`: a 40 px header row mounted above `<main>` in AppShell with Home / Agents / Tasks / Jobs / Settings tabs (icon + label, or icon-only below `sm`), aria-selected wired to the current section, click routes via `setSection()` using the current cabinet scope. Includes a mobile-only hamburger at the left that toggles `sidebarCollapsed` so the existing mobile slide-in sidebar has a visible entry point (partial #10). (6) **#10 mobile MVP** — partial: hamburger in TopNav + responsive label hiding at `<sm`. Status-bar horizontal-overflow polish and mobile editor toolbar deferred. (7) **#13 fonts** — still blocked on the ongoing theme-initializer WIP; no edits to that file. Touched: `use-hash-route.ts`, `task-conversation-page.tsx`, `tasks-board-v2.tsx`, new `api/dedup-fetch.ts`, `use-provider-icons.ts`, `app-store.ts`, `app-shell.tsx`, `cabinet-view.tsx`, `mission-control.tsx`, `status-bar.tsx`, `data-dir-cache.ts`, `use-cabinet-update.ts`, new `layout/top-nav.tsx`.

[2026-04-23] Added `data/experiments/` section with five interactive physics simulations, each a self-contained embedded app (index.html + `.app` marker) using vanilla JS + Canvas 2D: **double-slit** (photon-by-photon buildup of Fraunhofer fringes with wavelength/slit-width/separation controls), **pendulum-wave** (N tuned harmonic oscillators producing phase-locked patterns over an adjustable cycle period), **projectile-motion** (ballistics with angle/velocity/gravity/height/drag plus trajectory trails and drag-free comparison), **wave-interference** (two coherent point sources with draggable positions, ripple and time-averaged-intensity modes, adjustable phase/wavelength/damping), and **brachistochrone** (race between line/arc/cycloid/parabola paths solving ∫ds/v with cycloid winning via calculus-of-variations fit). Also wrote `experiments/index.md` indexing the section.

[2026-04-23] Audited all 112 avatars under `public/agent-avatars/` and fixed three classes of bugs. (1) Six avatars had `fill="undefined"` from a generator bug and failed to render — gave each a sensible background (`avatar-86/93/94/98/107/108`; `94` also had undefined face/skin fills). (2) Fifteen avatars with the long-hair tombstone path (`M34 70 … Z`) had hair completely covering the face (03, 12, 19, 21, 30, 53, 59, 65, 67, 69, 70, 71, 96, 99, 105) — rewrote the hair path to use `fill-rule="evenodd"` with an inner circle subpath (r=20 @ 60,54) so hair now frames the face instead of covering it. Harry-Potter-style glasses on avatar-19 are now visible, Che's beret on 21 sits above a visible face, Gandalf's beard on 65 shows, etc. (3) Three avatars with bun/bun-buns used the thin crescent fringe (`M40 50 Q60 34 80 50 Q80 44 60 38 Q40 44 40 50 Z`) as their only hair, which rendered as a ribbon across the forehead (04, 17, 74) — replaced with a fuller dome fringe path (`M38 52 Q38 28 60 26 Q82 28 82 52 Q78 42 60 42 Q42 42 38 52 Z`) that reaches above the top of the face circle and covers the forehead. Verified all 112 via Chrome headless render.

[2026-04-23] Pre-Release batch B — cleared 4 more P0s from the tracker (#8, #11, #17, #18). (1) **#18 alert/confirm** — new toast helper at `src/lib/ui/toast.ts` (`showError / showSuccess / showInfo / showToast`) dispatching the existing `cabinet:toast` event picked up by `SystemToasts`, plus a promise-based `confirmDialog({ title, message, confirmText, destructive })` at `src/lib/ui/confirm.ts` backed by a new `ConfirmDialogHost` component mounted in AppShell (base-UI `Dialog`, destructive variant, dismiss-on-backdrop, autofocus on confirm). Migrated 10 `alert()` + 4 `window.confirm()` call sites across `settings-page.tsx`, `agent-detail-v2.tsx`, `version-history.tsx`, `task-conversation-page.tsx`, `edit-user-avatar-dialog.tsx`, `edit-agent-identity-dialog.tsx`, and `onboarding-wizard.tsx`. No more browser-native dialogs breaking focus management. (2) **#8 agent Recent work fragments** — `normalizeSingleArtifactCandidate()` in `conversation-store.ts` now requires every artifact-path candidate to either contain a directory separator or end in a known file extension, AND to have no whitespace in the path-head segment. Prose fragments like `"line per file you created or updated. Do not list multiple files on a single"` that used to render as fake filenames in the agent "Recent work" surface are now dropped. (3) **#11 editor toolbar aria-labels** — confirmed N/A by inspection: every `ToolButton` in `editor-toolbar.tsx` already wires `aria-label={label}` and `title={label}` from its props, and every call site passes a label string. No fix needed. (4) **#17 empty default cabinet** — confirmed N/A: the 70+ CV and song files the audit flagged at DATA root were archived to `data-old-5/` on 2026-04-22; current `/data/` only auto-seeds `getting-started/` from resources on first run. Tracker status flipped for all four in `data/getting-started/PRE-RELEASE-ISSUES/index.md`.

[2026-04-23] Pre-Release batch A — cleared 7 of the remaining 18 P0s in one pass. (1) **#5 greeting** now reads the real user from `/api/user/profile` (was reading `/api/agents/config`'s company name, hence the literal "Good afternoon, there."); fallback drops the comma instead of placeholder-ing "there", and the duplicate subhead "What are we working on today?" is gone so the composer placeholder "I want to create…" owns the prompt. (2) **#4 last-route** — `useHashRoute` now marks each tab with a `cabinet.tab-visited` sessionStorage flag; fresh tabs land on home regardless of what was in `cabinet.last-route`, while in-tab reloads and back/forward still restore the previous route. (3) **#6 carousel** — each cloned RegistryCarousel card is wrapped in a div with `aria-hidden` + `inert` so screen readers announce each template once, not twice. (4) **#9 status bar** — removed the Chat / Contribute / ★-stars marketing trio (plus the full star-fetch, star-animation, and StarExplosion machinery: ~80 lines) from `status-bar.tsx`; GitHub and Discord will resurface inside About in the P1 settings pass. (5) **#15 Next DevTools overlay** — `next.config.ts` now sets `devIndicators: false`, fixing the floating button that was sitting on top of the sidebar "New Page" action. (6) **#19 mission-control backdrops** — both `<div onClick>` modal backdrops (`mission-control.tsx:713` + `:773`) are now `<button>` elements with `aria-label="Close dialog"`, so Escape/Enter close the NL agent dialog and the Start-All confirm dialog just like the visible actions. (7) **#12 live-region shout** — verified clear by inspection: zero `role="alert"` or `aria-live` usages anywhere in `src/`, so there's nothing to fix. Tracker status in `data/getting-started/PRE-RELEASE-ISSUES/index.md` flipped for all seven.

[2026-04-23] Audited all 112 avatars under `public/agent-avatars/` and fixed three classes of bugs. (1) Six avatars had `fill="undefined"` from a generator bug and failed to render — gave each a sensible background (`avatar-86/93/94/98/107/108`; `94` also had undefined face/skin fills). (2) Fifteen avatars with the long-hair tombstone path (`M34 70 … Z`) had hair completely covering the face (03, 12, 19, 21, 30, 53, 59, 65, 67, 69, 70, 71, 96, 99, 105) — rewrote the hair path to use `fill-rule="evenodd"` with an inner circle subpath (r=20 @ 60,54) so hair now frames the face instead of covering it. Harry-Potter-style glasses on avatar-19 are now visible, Che's beret on 21 sits above visible face, Gandalf's beard shows, etc. (3) Three avatars with bun/bun-buns used the thin crescent fringe (`M40 50 Q60 34 80 50 Q80 44 60 38 Q40 44 40 50 Z`) as their only hair, which rendered as a ribbon across the forehead (04, 17, 74) — replaced with a fuller dome fringe path (`M38 52 Q38 28 60 26 Q82 28 82 52 Q78 42 60 42 Q42 42 38 52 Z`) that reaches above the top of the face circle and covers the forehead. Verified all 112 via Chrome headless render.

[2026-04-23] Shipped Issue #1 of the Pre-Release tracker: world-class search palette. New daemon module under `server/search/` builds a `flexsearch` Document index over `/data/**/*.md` on daemon boot (plaintext via `src/lib/markdown/to-plaintext.ts` + gray-matter frontmatter), serves ranked results via `GET /daemon/search?q=&scope=&limit=`, and incrementally re-indexes on `chokidar` add/change/unlink (debounced 150 ms) — broadcasts `search:indexed` on the existing WS event bus. `src/app/api/search/route.ts` now proxies to the daemon and degrades to a structured 503 with a "start the daemon" hint instead of spinning forever (UX audit #110). Replaced the old `SearchDialog` with a VS Code-style 2-pane `SearchPalette` (`src/components/search/search-palette.tsx`): scope tabs (All / Pages / Agents / Tasks), match-count chips, grouped results by kind, right-pane snippets with line numbers and `<mark>`-highlighted matches, recent-queries / recent-pages empty state, and a one-click "Ask the cabinet" fallback (via the existing headless agent endpoint) on zero results. New `src/hooks/use-global-hotkeys.ts` is now the single document-level listener: `Cmd+K` opens the palette from anywhere (including inside the Tiptap editor — link shortcut rebound to `Cmd+E`), `/` opens it when focus is idle (not inside an input, contenteditable, ProseMirror, or xterm), and the prior `Cmd+S / Cmd+\` / Cmd+Shift+A / Cmd+M / Cmd+Shift+.` bindings are consolidated here. Also shipped the new `data/getting-started/PRE-RELEASE-ISSUES/` page tracking all 19 P0 findings from the UX audit. Verified live: `?q=dumbledore` returns results in 1 ms against the in-memory index, watcher picks up file adds and unlinks within ~1 s. Deleted `src/components/search/search-dialog.tsx` and `src/components/shortcuts/keyboard-shortcuts.tsx` (fully superseded).

[2026-04-23] New Task split button on the v2 tasks board now follows the selected theme — swapped the neutral `bg-foreground` / `text-background` pair for `bg-primary` / `text-primary-foreground`, plus a faint `ring-primary/20` so the button reads as the theme's signature CTA. Touches `NewWorkButton` in `tasks-board-v2.tsx`. No logic change; the button still opens `StartWorkDialog` in "now" mode and the caret still offers Task / Routine.

[2026-04-23] Extended the unified composer to the two inline surfaces that weren't routed through the tasks board: the Home screen greeting composer (`src/components/home/home-screen.tsx`) and the cabinet-page task composer (`src/components/cabinets/cabinet-task-composer.tsx`). Both now show a **When** chip next to the runtime picker; picking "Run now" keeps today's inline submit behavior, and picking "On a schedule" or "Heartbeat" opens `StartWorkDialog` in the chosen mode with the typed prompt and selected agent already seeded (no keystrokes lost in the handoff). Extended `StartWorkDialog` with `initialPrompt` / `initialAgentSlug` props and exported `WhenChip` so consumers can drop it into any `actionsStart` slot. HomeScreen was also updated to store the richer `CabinetAgentSummary[]` (it needs `cabinetPath` + heartbeat fields for the dialog) instead of narrowing to `AgentListItem`. The in-place routine/heartbeat edit dialogs on the cabinet page still open from their existing row actions — untouched.

[2026-04-22] Archived current KB content to `data-old-5/` and created a fresh empty `data/` directory to validate the first-run / onboarding experience from a truly clean state.

[2026-04-22] Added pre-release UX audit as a KB page at `data/getting-started/CABINET-PRE-RELEASE-REPORT/index.md` with 225 numbered findings (P0–P3) and 21 screenshots under `./screenshots/`. Audit used Chrome DevTools MCP against `localhost:4000` at 1440×900 + 390×844, cross-referenced with a source-level scan. Captures the "minute-1 wow" blockers (broken hash routes for `/#/tasks` and `/#/agents`, duplicated template carousel, empty tasks board with live tasks present, stuck task-terminal "Loading… / connecting", 22-font first paint, duplicated API calls, Next.js DevTools button leaking into prod chrome) plus 20+ sections covering navigation, sidebar, editor, search, AI panel, settings, accessibility, performance, copy, error handling, and mobile responsiveness.

[2026-04-22] Unified Task / Routine / Heartbeat creation into a single progressive-disclosure composer. New `StartWorkDialog` (`src/components/composer/start-work-dialog.tsx`) opens in "Run now" (task) mode by default; a new **When** chip (icons: `Zap` / `Repeat` / `HeartPulse`) in the composer actions row pivots to "On a schedule" (grows dialog to `sm:max-w-3xl` and reveals a Routine details panel: name auto-slugs to id, cron picker, enabled toggle) or "Heartbeat" (hides prompt, shows persona explainer + schedule + active toggle, keeps typed prompt stashed for flip-back). Submit label + endpoint branch by mode: `Start` → `POST /api/agents/conversations`, `Create routine` → `POST /api/agents/{slug}/jobs`, `Save heartbeat` → `PUT /api/agents/personas/{slug}`. Extracted shared `RoutineFields` / `HeartbeatFields` into `src/components/composer/scheduling-fields.tsx` so the in-place edit dialogs can reuse them later without visual drift. Wired into tasks board (`tasks-board-v2.tsx`); deleted the now-obsolete `new-task-dialog.tsx`. Legacy `NewRoutineDialog` / `HeartbeatDialog` stay for edit-in-place flows — only the create path is unified.

[2026-04-22] Dead-code cleanup ahead of unified composer work. Deleted `src/app/tasks-v2/` (915-line demo route with no inbound links; app shell links to `/tasks`, which has been the only v2 board since "v1 parity was reached" per the file comment). Deleted `src/app/tasks/new/` (standalone legacy new-task page using `createTaskRequest`, unreachable from UI — modern flow uses `cabinet:open-create-task` → `NewTaskDialog`). Dropped the now-unused `createTaskRequest` export from `src/lib/agents/task-client.ts` (rest of file stays — used by `task-conversation-page.tsx`). Fixed a stale `/tasks-v2 demo` reference in `src/lib/themes.ts`. Verified with `tsc --noEmit`, `next lint`, and `next build` — all clean, no broken imports.

[2026-04-22] Added `data/have-fun/cv-angelina-johnson.md` — in-character CV for Angelina Johnson (Weasley), Head Coach of the Holyhead Harpies, written in the same literary voice/structure as the existing Lavender/Ron/Seamus entries. Covers her Gryffindor Chaser/Captain years, DA membership, eleven-year Harpies playing career (1999–2010), coaching ladder (Reserves → Assistant → Head Coach 2018), marriage to George Weasley, and kids Fred II and Roxanne. Indexed in `data/have-fun/index.md`. Order: 93.

[2026-04-22] Added `data/have-fun/cv-seamus-finnigan.md` — in-character CV for Seamus Finnigan, written in the same voice/structure as the existing Ron Weasley entry (warm, self-aware, plain-spoken). Post-war career framed as founder of "Finnigan Spell-Safety & Site Clearance" in Co. Kerry — leans into the canonical "things blow up around Seamus" gag as a professional specialty (controlled demolition, spell-stress testing, post-conflict site reconstruction). Also indexed him and the previously-unlisted Ron Weasley entry in `data/have-fun/index.md`. Order: 91.

[2026-04-22] Added have-fun/cv-ron-weasley.md — in-character CV for Ronald Bilius Weasley (co-owner of Weasleys' Wizard Wheezes, former Auror), written to match the existing CV series voice and structure. Order 90.

[2026-04-22] Follow-up fixes for the manual terminal persistence flow, found via end-to-end testing with 4 providers (claude/codex/opencode/gemini). Two leaks were silently force-finalizing manual Claude sessions after ~10s: (1) the daemon's `/session/:id/output` poll-completion shortcut called `completeClaudeSession` on idle detection — now guarded with `active.trigger !== "manual"`; (2) `maybeResolveCompletedConversation` in `conversation-store.ts` flipped `meta.status` to `completed` on any detail-fetch that saw `transcriptShowsCompletedRun` return true — now short-circuits when `meta.trigger === "manual"` and `meta.status === "running"`. Also tuned the awaiting-input oscillation: Claude's idle prompt continuously re-renders, which was firing `scheduleClaudeBusyFlip` within 400ms and flickering the chip; busy-flip now requires 2s sustained AND ≥120 bytes of new output since the snapshot (strictly longer than the 1.2s idle-flip window). Verified: manual Claude stays live with `status=running, awaitingInput=true` stable, daemon `exited=false` throughout idle, graceful Done closes to `completed` in ~4s. One-shot providers (codex/opencode/gemini) naturally exit after their prompt completes (unchanged — they don't have an idle prompt to stay at).

[2026-04-22] Manual terminal-mode tasks now stay alive on idle instead of force-exiting ~1.2s after Claude's TUI settles at its `>` prompt. When the CLI goes idle on a manual run the daemon flips `meta.awaitingInput = true` (status chip turns amber "awaiting input") and keeps the PTY alive; when new output streams in after the user types into the xterm, it flips back to false. Scheduled jobs / heartbeats / agent-triggered runs keep the classic auto-exit so they don't accumulate unclosed sessions. New graceful close path: a green "Done" button appears in the terminal-mode header for manual running/awaiting tasks, wired to a new `action: "close"` on `PATCH /api/agents/conversations/[id]` → `POST /session/:id/close` → `pty.write("/exit")` + 2s SIGTERM fallback. The natural PTY exit then finalizes as `status: "completed"`, distinct from Stop (SIGTERM + failed). Users can also type `/exit` or Ctrl+D directly in the xterm — that always worked, we just stopped force-exiting on top of it. Threaded `trigger` through `PtySession` / `SpawnPtyInput` / `createSession`; both WS-connect and `POST /sessions` daemon paths now read `meta.trigger` before spawn. `finalizeConversation` clears `meta.awaitingInput` on terminal statuses so exited sessions don't stay "awaiting-input" in the UI. Touched: `server/pty/types.ts`, `server/pty/manager.ts`, `server/pty/claude-lifecycle.ts`, `server/cabinet-daemon.ts`, `src/lib/agents/daemon-client.ts`, `src/lib/agents/conversation-store.ts`, `src/app/api/agents/conversations/[id]/route.ts`, `src/components/tasks/board-v2/board-actions.ts`, `src/components/tasks/conversation/task-conversation-page.tsx`.

[2026-04-22] Overhauled the post-exit UX for terminal-mode tasks. (1) Terminal-mode tasks now auto-switch from Terminal to Details tab the moment the PTY exits — the raw xterm replay of a claude-code TUI session was unreadable (hundreds of duplicate "thinking with xhigh effort" spinner frames once ANSI positioning was stripped). (2) When the user does click back to Terminal after exit, they get a new `TerminalExitedView` (`src/components/terminal/terminal-exited-view.tsx`) with a clean status header (exit cleanly / failed / errorKind+Hint banner), a run-length-compressed transcript tail (spinner redraws collapse to one line × N), and a "Show raw replay" escape hatch that mounts the classic `WebTerminal`. (3) Fixed upstream transcript corruption: `stripAnsi` / `stripAnsiText` now catch DEC-style single-char ESC sequences (`\x1B7`, `\x1B8`, `\x1Bc`, `\x1B=`, etc.) that were leaking into `meta.summary` as literal "78─────" garbage, plus charset-designate two-byte sequences. Also changed `completeClaudeSession` and the poll-based `/session/:id/output` finalize path to feed `finalizeConversation` a distilled one-liner instead of the full TUI-noise transcript, and taught `finalizeConversation` to preserve stream-extracted `meta.summary`/`artifactPaths` when the finalize-time parse comes back empty (was clobbering clean values with prompt-echo fragments). Touched: `src/components/tasks/conversation/task-conversation-page.tsx`, `src/components/terminal/terminal-exited-view.tsx` (new), `src/lib/agents/conversation-store.ts`, `server/pty/ansi.ts`, `server/pty/claude-lifecycle.ts`, `server/cabinet-daemon.ts`.

[2026-04-22] Career-ops pattern-analyst heartbeat — Day 5 of pipeline stall. Tracker.csv MD5 changed (`28a4bfec…` → `a55e48c1…`) for the first time in 72h, but decomposition shows **all 4 diffs are Pipeline Conductor hygiene** (Datadog nudge note, ElevenLabs → Blocked-Stale, Linear #18 → Queued-Eval, Cohere → Closed-Stale). Zero changes on the 3 experiment-target rows (Anthropic/Elena, HF/Sophie, n8n/Max); none of the 3 forcing-function drafts sent. User was heavily Cabinet-active this morning (~10 cv-tailor runs for Harry Potter CVs, multiple heartbeat reviews, Pipeline Conductor run) — strengthens Scenario C2 (Cabinet-active, career-ops-outbound-silent) evidence. Declined pattern-analysis-2026-04-22.md (6th consecutive decline — same Apr 20 false-precision rationale). Updated `analytics/experiment-forcing-function-2026-04-21.md` with a T+12h observation section that refines the verdict protocol: a tracker hash change is NOT sufficient to fire Scenario A/B — must decompose into user-outbound-authored diffs vs agent-hygiene diffs. Named the sharper failure-mode label: **"outbound-initiation latency"** — not capacity or avoidance, specifically *authoring/sending to humans external to Cabinet*. Apr 23 EOD verdict fires as planned; C2 remains the leading scenario.

[2026-04-22] Added `data/have-fun/cv-peeves.md` — in-character CV for Peeves the Poltergeist, written in a chaotic rhyme-and-prose voice distinct from the measured Ministry/Hogwarts tone of most other entries in the library. Includes sections on the Pringle Affair (1876) and the Battle of Hogwarts (1998). Linked into `data/have-fun/index.md` alphabetically between Oliver Wood and Percy Weasley.

[2026-04-22] Added `data/have-fun/cv-mafalda-hopkirk.md` — in-character satirical CV for Mafalda Hopkirk of the Improper Use of Magic Office, matching the established voice-driven format (see cv-griphook.md). Chosen as a not-yet-covered Harry Potter figure; order: 88.

[2026-04-22] Quieted "xterm.js: Parsing error: {}" console spam in `src/components/terminal/web-terminal.tsx`. xterm's VT parser calls `logService.error(...)` at `error` level whenever it recovers from an unknown CSI/DCS escape state — which some CLIs (codex, claude-code) emit routinely during status redraws. The error is non-fatal (parser returns to idle and keeps going) but clutters DevTools. Set `logLevel: "off"` on the Terminal constructor. Also hardened `writeToTerminal` / `ws.onmessage` to drop zero-length chunks and ignore unexpected WebSocket payload types (Blob etc.) — defensive against future sources of the same log.

[2026-04-22] Color-coded vocal parts in `data/marketing/drafts/original-cinematic-pop-ballad.md` ("Midnight at the County Line") as a Lana Del Rey / Taylor Swift duet. Used inline HTML `<span style="color:...">` tags: deep red `#C71F37` for Lana (cinematic/imagery lines), warm gold `#D4A84B` for Taylor (narrative/confessional lines), purple `#9B59B6` for shared harmony (title refrain + hooks). Added a legend line at the top of the page.

[2026-04-22] Added `data/have-fun/cv-cho-chang.md` — satirical Harry Potter CV for Cho Chang, written in her voice as a Grade III Mind Healer at St Mungo's (Fourth Floor, bereavement specialism) applying for a senior clinical post. Matches the measured, period-formal tone of the existing library and slots into `have-fun/index.md` alphabetically between Charlie Weasley and Cornelius Fudge. Generated by the cv-tailor job.

[2026-04-22] Fixed stale-content flash when opening a markdown artifact from a conversation transcript. Root cause: `loadPage()` in `src/stores/editor-store.ts` only wrote state asynchronously (after `fetchPage` resolved), while `setSection` + `focusPath` in `openArtifactPath` fired synchronously — so `KBEditor` re-rendered with the previous file's content for the duration of the fetch. Fix: `editor-store.ts` now synchronously clears `content` and sets a new `isLoading` flag when navigating to a different path, plus a race guard (`get().currentPath !== path → return`) on each post-fetch `set` so a slow earlier fetch can't clobber a newer navigation. `src/components/editor/editor.tsx` now tracks a `renderedPath` local state (flipped inside the existing content-setter effect) and paints a blurred-background + spinner overlay over the Tiptap area whenever `isLoading || renderedPath !== currentPath` — covering both the fetch window and the async `markdownToHtml` → Tiptap sync window. Wrapped the scroll container in a `relative` parent so the overlay sits in the viewport instead of the scrollable content. Same-path reopens stay instant via the existing `kb-page-cache` fast path.

[2026-04-22] Sped up terminal-task first paint per `data/PRDs/terminal-task-first-paint.md`. `src/components/terminal/web-terminal.tsx` now kicks off `/api/daemon/auth` + the PTY WebSocket in parallel with the xterm dynamic imports and buffers incoming bytes until the terminal finishes constructing (flushed FIFO on mount). Added `webpackPrefetch: true` magic comments on the five xterm imports so idle browsers prefetch them. `src/components/tasks/conversation/task-conversation-page.tsx` now reads an `earlyIsTerminal` hint from a new client-side cache (`src/lib/agents/terminal-mode-cache.ts`, populated by every consumer of `conversationMetaToTaskMeta`) and mounts the xterm shell before the full task detail fetch resolves. Browser-verified: WS+bytes arrive within ~1.6s on warm dev nav and buffer safely for 60s+ while xterm compiles on cold nav, flushing instantly when the terminal is ready. Non-terminal (structured) task view is unchanged.

[2026-04-22] Editor agent heartbeat: wrote `data/content-creator/content-calendar/reviews/2026-04-23-video-setting-up-your-stack.md` with P0/P1/P2 review notes for Thursday's tutorial video (the strategist's flagged blocker — slot now unblocked pending creator action). Also rewrote `data/content-creator/brand-voice/index.md` from a 1-line placeholder into a working v0 guide (tone ladder, banned words, examples, pre-publish checklist, 5 items flagged for creator sign-off), and sent a handoff message back to content-strategist at `.agents/.messages/content-strategist/2026-04-22T10-25-00Z_from_editor.md`.

[2026-04-22] Added `data/have-fun/cv-cedric-diggory.md` — CV for Cedric Diggory written as his own pre-third-task draft in spring 1995 (Hufflepuff prefect, Quidditch captain, Hogwarts Champion). Follows the established satirical in-character CV format in `have-fun/` alongside Luna, Snape, McGonagall, and ~75 others.

[2026-04-22] Added "Conversation started" toast for background runs. Scheduled jobs, heartbeats, and agent-spawned conversations now surface a blue `STARTED` toast (with the job name / "Heartbeat" / "Spawned by another agent" subtitle) the moment they kick off, so work happening in the background becomes visible instead of appearing only on completion 10 minutes later. Manual composer runs stay silent because the transcript is already in the foreground. Implementation reuses the existing notification queue + SSE drain: `createConversation()` in `src/lib/agents/conversation-store.ts` enqueues a `status: "running"` row (gated by new `shouldEnqueueConversationStart` helper in `conversation-notification-utils.ts`), `src/app/api/agents/events/route.ts` partitions the drained queue by status and emits `conversation_started` alongside the existing `conversation_completed`, `app-shell.tsx` rebroadcasts as `cabinet:conversation-started`, and `NotificationToasts` gains a sky-blue `Play`-icon variant plus a softer single-tone sound (C5, 120ms, 8% gain) so heartbeats don't jingle every cycle. Click behavior unchanged — opens the live transcript. Test coverage in `test/conversation-notification-utils.test.ts`.

[2026-04-22] Added `data/PRDs/terminal-task-first-paint.md` — PRD for fixing the 4–16 second delay before PTY output renders in terminal-mode tasks. Chrome DevTools timing pins the dominant cause to `WebTerminal` awaiting 5 serial dynamic xterm imports (2.6s warm / 8.5s cold) before opening the daemon WebSocket; secondary cause is `TaskConversationPage` gating render on the full `fetchTask` which is queued behind ~20 parallel app-shell API calls. PRD proposes (A) connect WS in parallel with imports + buffer-and-flush, (B) optimistic terminal-shell mount from the already-fetched conversations list.

[2026-04-22] Added `data/have-fun/cv-fred-weasley.md` — satirical Harry Potter CV in Fred Weasley's voice to complete the twin pair (cv-george-weasley already existed). Set in early 1998 pre-Battle-of-Hogwarts; matches the tone and structure of the existing cv-*.md entries under have-fun/.

[2026-04-22] Added `data/have-fun/duet-lana-britney-3am-in-vegas.md` — a quieter, ballad-style alternative to the existing "Neon & Velvet" Lana Del Rey × Britney Spears duet. Same two voices, hotel-bar piano, intimate trade-offs instead of a pop flip.

[2026-04-22] Fixed "Conversation not found" 404 when deleting tasks (e.g. a heartbeat in the "Your Turn" column) whose `meta.json` carried a stale `cabinetPath` from an older migration (e.g. `jobs/career-ops` while the file actually lives under `data/career-ops/...`). In `src/lib/agents/conversation-store.ts`, `resolveConversationCabinetPath` now falls back to cabinet discovery when the passed `cabinetPath` doesn't match disk, and `readConversationMeta` always returns the resolved on-disk `cabinetPath` instead of the stale in-file value — so subsequent DELETE/PATCH calls and `writeConversationMeta` target the real storage location and self-heal the meta file on next write.

[2026-04-22] Deduped the cabinet overview agents list by slug, nearest-cabinet wins. When +1/+2/All visibility pulled in a sub-cabinet agent whose slug collided with one the current cabinet already owns (e.g. two `editor` personas), the UI surfaced both in the sidebar, the "Meet the team" grid, the org chart, and every agent-picker dropdown. Product call from @hilash: one row per role, not one per cabinet-that-defines-it. Fix lives in `src/lib/cabinets/overview.ts` so all consumers stay consistent: sort by `cabinetDepth` asc, then keep the first occurrence per slug. If the user wants to see a sub-cabinet's own editor, they navigate into that cabinet. The earlier client-side key fix in `agents-workspace.tsx` stays as defensive code in case another surface ever bypasses the overview dedup.

[2026-04-22] Fixed duplicate React key warning on `/cabinet/./agents`. `agents-workspace.tsx` had five places (`key={agent.slug}`) inside `agents.map(...)`, but when the +2 visibility mode surfaces both a cabinet-owned `editor` and an inherited `editor` from a sub-cabinet, two items share the slug and collide. Switched all five to `agent.scopedId ?? (agent.cabinetPath ? \`${agent.cabinetPath}::${agent.slug}\` : agent.slug)`, matching the existing pattern at line 2090. Verified via DevTools — the two "Encountered two children with the same key, `editor`" errors are gone.

[2026-04-22] Fixed a runaway request storm on app load. `NewCabinetDialog` is rendered once per tree node inside `tree-node.tsx`, and its `NewCabinetOverlay` was mounted unconditionally — so `useAgentPicker()` fired `GET /api/agents/library` on every tree node (observed ~250+ in-flight requests per page load with 65 cabinets × StrictMode double-mount). Fix gates the overlay on `open` in both controlled and uncontrolled branches of `src/components/sidebar/new-cabinet-dialog.tsx`, so the fetch only runs when the user actually opens the dialog. Verified via Chrome DevTools: requests dropped from 283 (252 pending `/api/agents/library`) to 26 with zero library fetches on idle.

[2026-04-22] Added have-fun/cv-hannah-abbott.md — satirical CV for Hannah Abbott (Hufflepuff; DA; landlady of the Leaky Cauldron; later Hannah Longbottom), matching the voice and structure of the existing Harry Potter CV series.

[2026-04-22] Added satirical CV for Dean Thomas at have-fun/cv-dean-thomas.md — a quieter, artist's-voice entry for the character-CV library. Written as a working illustrator and Wizengamot courtroom artist out of a Bloomsbury studio, formerly an Auror trainee who left the programme. Covers Gryffindor years (1991–1998), the 1997–98 gap year on the run with Ted Tonks, Dirk Cresswell, and the goblins Griphook and Gornuk, the ambush and capture at Malfoy Manor, Battle of Hogwarts, Auror training 1998–2000, and the post-1998 illustration career (Goshawk textbook plates, *Quibbler* political cartoons, *Toffee the Hippogriff* children's books, Muggle-side Evening Standard work under the initial *D. Thomas*). Includes a note on discovering his father was a murdered wizard, West Ham as an interest, and references from Kingsley, Seamus, Luna, Parvati, and Hestia Spavin. Slotted into have-fun/index.md between Cuthbert Binns and Dobby.

[2026-04-22] Removed stale claims from the docs. Port defaults everywhere were still saying `:3000` / `:3001` even though `runtime-config.ts` has defaulted to `:4000` / `:4100` (with `CABINET_APP_PORT` / `CABINET_DAEMON_PORT` overrides and auto-bump-on-busy) for a while — fixed `CLAUDE.md` (commands + debug:chrome paragraph), `README.md` (commands + onboarding URL), `PRD.md` (two-process block), `TASKS_CONVERSATIONS_PRD.md` (two sequence/architecture diagrams + glossary), `deployment-packaging-versioning.md` quick-start, `data/getting-started/architecture.mermaid` (Next + Daemon subgraph headers), and the `scripts/launch-chrome-debug.sh` default URL argument. Dropped the defensive "Do not assume the terminal is being removed" rule #9 from `CLAUDE.md` (fossil from a past debate) and rewrote it as the positive assertion "Terminal is a first-class runtime." Fixed rule #3 under "AI Editing Behavior" that still called terminal-mode adapters "experimental escape hatches" — terminal mode has been a first-class composer toggle since commits `a767892`/`e922c63` (documented in `PROVIDER-CLI.md` line 308). Updated `PROVIDER-CLI.md` migration entry #5 and `TASKS_CONVERSATIONS_PRD.md` §11 table footer to match: the `*_legacy` suffix is historical naming, the adapters are user-selectable first-class runtimes. Added the missing `task-store` to the `CLAUDE.md` Zustand list and replaced the "Claude Code and Codex CLI" AI line with the actual 8-provider set.

[2026-04-22] Docs follow-up for the PTY extraction. `README.md` architecture tree now lists `server/pty/` alongside `server/cabinet-daemon.ts`. `PRD.md` daemon-composition block replaces the old "Terminal Server ← existing PTY/WebSocket" line with a `PTY module` entry pointing at `server/pty/`, plus structured adapters, scheduler, event bus, and the actual HTTP/WS endpoint list. All remaining mentions of `terminal-server.ts` / `dev:terminal` across top-level docs (CLAUDE.md, README.md, PRD.md, PROVIDER-CLI.md, TASKS_CONVERSATIONS_PRD.md) were re-swept and left as historical changelog entries only.

[2026-04-22] Killed the standalone terminal server and extracted the PTY layer out of the daemon. Deleted `server/terminal-server.ts` (it was a strict subset of the daemon with zero client coupling and no auth — a security footgun waiting to happen). Pulled the PTY-specific helpers out of `server/cabinet-daemon.ts` (2029 → 1570 LOC) into a new `server/pty/` module: `ansi.ts` (`stripAnsi`, now shared between PTY and structured finalizers), `types.ts` (`BaseSession`, `PtySession`, `CompletedOutputEntry`), `claude-lifecycle.ts` (`claudePromptReady`, `submitInitialPrompt` with the 600ms paste-window guard, `completeClaudeSession`, `maybeAutoExitClaudeSession`, `consumeStructuredOutput`/`flushStructuredOutput`, `distillPtyOutput`, `scheduleStreamCabinetExtraction`), and `manager.ts` (the `createPtyManager` factory exposing `spawn` + `writeInput`; deps-injected so it stays decoupled from conversation-store and event-bus callbacks). The daemon keeps the unified `sessions` map (PTY + structured) and delegates PTY spawn via `ptyManager.spawn`, stdin injection via `ptyManager.writeInput`. Swapped `scripts/dev-daemon.mjs` to spawn tsx in `watch --clear-screen=false` mode so PTY-module edits hot-restart the daemon (verified: touching `server/pty/manager.ts` increments the "Cabinet Daemon running" line in the log). Removed `dev:terminal` from `package.json`, purged `server/terminal-server.ts` from `scripts/prepare-electron-package.mjs` (replaced with `server/pty`), updated `CLAUDE.md` (rule #8 + commands table + architecture layout), and refreshed `data/getting-started/architecture.mermaid` to show the PTY module as a sub-graph inside the daemon instead of a dashed-alt node.

[2026-04-22] Content Strategist added `data/content-creator/content-calendar/daily-queue-2026-04-22.md` — daily queue note covering today (Wed social thread already published) and tomorrow (Thu video still in editor review, flagged as the only at-risk slot in the 2-day window). Also called out Friday blog post draft risk and the static-calendar limitation.

[2026-04-22] Fixed three gaps in multi-task dispatch inheritance surfaced by a live Harry Potter fan-out test. (1) `resolveDispatchRuntime` (`src/lib/agents/action-dispatcher.ts`) now pushes the parent conversation's provider + adapterType + model + effort to spawned sub-tasks by default, falling back to the target persona's declaration only when the parent has none. Action-authored `providerId`/`adapterType`/`model`/`effort` on a `LAUNCH_TASK` still win. Effort is portable across providers via a new `providerSupportsEffort()` helper in `provider-registry.ts`. `LaunchTaskAction`/`ScheduleJobAction`/`ScheduleTaskAction` in `src/types/actions.ts` gained optional `providerId`/`adapterType`. `action-parser.ts` picks up the new fields from JSON + inline forms; `action-validator.ts` adds `provider_unavailable` (hard) and `cross_provider_push` (soft) warnings; the dispatcher system prompt in `conversation-runner.ts` now explains inheritance so the LLM stops clobbering user intent. (2) Codex error surfacing: `codex-stream.ts` now handles in-stream `{"type":"error"}` and `{"type":"turn.failed"}` events and writes the unwrapped inner message to `CodexStreamAccumulator.errorMessage`; `codex-local.ts` prefers that over filtered stderr and adds a `classifyCodexModelUnavailable` matcher. `cabinet-daemon.ts` now passes `result.errorMessage` to `classifyError` when stderr is empty. A new `ConversationErrorKind` variant `model_unavailable` lights up the UI with a "pick a different model or switch to API-key auth" hint instead of the previous empty-transcript "unknown" shrug. (3) Codex model catalog refresh: `codex-cli.ts` gained `gpt-5.4` (the only model that works on ChatGPT-plan Codex accounts) and annotated `gpt-5.2-codex`, `gpt-5.1-codex-max`, `o3`, `o4-mini`, `gpt-4.1*` with `requires: "api_key"`; `ProviderModel.requires` is exposed through `/api/agents/providers` and rendered as an "API key" badge in `task-runtime-picker.tsx`. Verified end-to-end: re-ran the parent → 5-child fan-out with `claude-code/opus/medium` and all 5 children now inherit that runtime (previously every child fell back to `codex-cli` with empty adapterConfig); fired an unsupported-model run and confirmed `errorKind: model_unavailable` + the real codex reason make it to `meta.errorHint`. New unit tests: `action-dispatcher.test.ts` (6 cases covering same-provider inheritance, parent-provider push, action override, effort portability, effort-not-supported drop, persona fallback); `codex-local.test.ts` gains a `{type:error}` fixture that asserts the inner-message unwrap + classifier match.

[2026-04-22] Agent color now applies even when an avatar image is set. `AgentAvatar` tints the image-wrapper background with the agent's color (explicit hex via `tintFromHex`, or slug-derived via `getAgentColor`) instead of `bg-muted`, giving a colored halo through rounded corners / transparent PNGs. `AgentPill` likewise paints the pill background with the agent tint when an avatar image is present, so board pills carry the identity color whether the agent uses an icon or a photo.

[2026-04-22] Added satirical CV for Dudley Dursley at have-fun/cv-dudley-dursley.md — a Muggle angle for the character-CV library written in the voice of a 46-year-old regional operations manager at a Berkshire fitness chain, boxing coach, and parent governor. Covers Smeltings (1991–1998), the gap from 1997–98 when the Dursleys were moved into Order protection, career progression from reception desk to regional role, and a closing note about the dementor attack in 1995 and his cousin, handled obliquely and without naming locations. Slotted into have-fun/index.md between Draco Malfoy and Filius Flitwick.

[2026-04-22] Added satirical CV for Quirinus Quirrell at have-fun/cv-quirinus-quirrell.md — a Ravenclaw alum's understated self-presentation with steadily accumulating tells: references that "never leave," first-person-plural replies, a "complimentary turban" acquired on sabbatical in Albania, layered familiarity with the Stone's protective enchantments, and a fondness for close evening collaboration. Covers pre-career tutoring, Muggle Studies (1988–1990), the Albanian sabbatical (1990–1991), and the 1991–1992 DADA post. Slotted into have-fun/index.md between Poppy Pomfrey and Regulus Black.

[2026-04-22] Added `data/getting-started/architecture.mermaid` — a whole-app architecture diagram covering the browser (React/Tiptap/xterm/Zustand), Next.js API routes on :3000, the unified Cabinet Daemon on :3001 (PTY sessions, structured adapters, scheduler, event bus, WebSocket), the standalone legacy terminal server, external CLIs (Claude/Codex/Cursor/OpenCode), the `/data` filesystem layout with conversations/tasks/linked repos/symlinks, and the git auto-commit path.

[2026-04-22] Added satirical CV for Vernon Dursley at have-fun/cv-vernon-dursley.md — in the voice of the 54-year-old Director of Grunnings Drill Manufacturers' Southern Division, preparing a refreshed CV at the Managing Director's request. Covers twenty-seven years at Grunnings (assistant → representative → senior manager → director), management of a 48-person division across four counties, the Surrey County Council contract win, Salesman-of-the-Year trophies, plus Smeltings, golf, the Residents' Association, and the front garden at Number Four. Includes two "notes" — on The Family (an oblique acknowledgement of Harry as "a dependant of my wife's") and on Perfect Normality (a housemaster-prompted self-description that sprawls despite itself). Slotted into have-fun/index.md between Sybill Trelawney and Viktor Krum.

[2026-04-22] Added satirical CV for Ginny Potter (née Weasley) at have-fun/cv-ginny-potter.md — in the voice of the former Holyhead Harpies Chaser and captain turned senior Quidditch correspondent at the Daily Prophet. Covers her eight seasons at Holyhead (2000–2008, three as captain, 2006–2007 League title), 102 England caps, shoulder-injury retirement at 27, the move to the Prophet sports desk and her focus on the junior game, her books (Holyhead: A Club and Its Women, 2012; Reading the Bludger, 2015), her three children with Harry, and her standing editorial carve-out on family and former-club coverage. Includes a Note on the Name (why she publishes as Potter) and an honest single-line acknowledgment of being present at Hogwarts on 2 May 1998. Slotted into have-fun/index.md between George Weasley and Gilderoy Lockhart.

[2026-04-22] Added satirical CV for Stan Shunpike at have-fun/cv-stan-shunpike.md — in the voice of the Knight Bus conductor turned Operations Supervisor for the South Coast Route, writing the CV for his 2026 Route Manager's certificate renewal. Covers his 1992 induction under Ernie Prang, thirteen years as conductor, the 2006 Azkaban detention and 2007 Imperius period (both openly disclosed with a closing note), the return to driving, promotion to senior conductor in 2009 and supervisor in 2019, incident-reporting reforms, and the ongoing dispatch-yard archive project. Slotted into have-fun/index.md between Sirius Black and Sybill Trelawney.

[2026-04-22] Added satirical CV for Kreacher at have-fun/cv-kreacher.md — written in Kreacher's curmudgeonly third-person voice, covering his century-plus service to the House of Black (Walburga/Orion, Sirius, Regulus), the 1979 sea cave expedition and the sixteen-year keeping of Regulus's instruction, the hostile handoff to Harry in 1996, the locket-horcrux retrieval from Mundungus, the turning point when Harry gave him Regulus's fake locket, leading the Hogwarts elves into the Battle of Hogwarts, and his present duties at a reformed Grimmauld Place. Includes notes on Regulus and on the Master. Slotted into have-fun/index.md between Kingsley Shacklebolt and Lee Jordan.

[2026-04-22] Added satirical CV for Gellert Grindelwald at have-fun/cv-gellert-grindelwald.md — written from the topmost cell of Nurmengard in his ninety-third year, at a biographer's request. Covers the Durmstrang expulsion, the summer of 1899 at Godric's Hollow, the travelling years, the taking of the Elder Wand from Gregorovitch in 1912, the quarter-century leadership of the Movement for the Common Good, the 1945 duel with Dumbledore, and the fifty-year imprisonment. Includes notes on the Summer of 1899, the Wand, and the Question of Remorse. Slotted into have-fun/index.md between Garrick Ollivander and George Weasley.

[2026-04-22] Added satirical CV for Oliver Wood at have-fun/cv-oliver-wood.md — in the voice of the former Gryffindor Keeper turned Puddlemere United head coach. Covers his reserve years, nine seasons in the first team, three England caps, the transition to Keeping coach in 2005 and head coach in 2012, three League titles and two League Cups as manager, the "seventeen-point" Keeper monograph, and a note on the years he spent playing professional Quidditch during the second war. Slotted into have-fun/index.md between Olympe Maxime and Percy Weasley.

[2026-04-22] DevOps review written to engineering/devops/system-review-2026-04-22.md covering CI/CD, infrastructure, monitoring, security, and backup/DR. Flagged missing PR/main CI workflow (tests exist but never run), unbounded conversation log growth (183 sessions / 7.2 MB), no containerization, optional KB_PASSWORD on a self-hostable product, and no backup story for `/data`. Prioritized action list at the bottom.

[2026-04-22] Added satirical CV for Lee Jordan at have-fun/cv-lee-jordan.md — Hackney broadcaster's register, covers the Gryffindor commentary years with Professor McGonagall in the box, the Weasleys' Wizard Wheezes on-air testing stint, the wartime *Potterwatch* broadcasts as "River," and his current afternoon slot on the Wizarding Wireless Network. Includes a Note on the Dead for Fred Weasley and a standing disclosure about the retained WWW shareholding. Slotted into have-fun/index.md between Kingsley Shacklebolt and Ludo Bagman.

[2026-04-22] Added satirical CV for Sir Nicholas de Mimsy-Porpington (Nearly Headless Nick) at have-fun/cv-nearly-headless-nick.md — courtly, late-medieval register, framed as the 462nd application to the Headless Hunt. Includes a long Note on the Manner of My Passing and a Note on the Headless Hunt's membership criterion. Also slotted Nearly Headless Nick and the previously-missing Newt Scamander entry into have-fun/index.md in alphabetical position.

[2026-04-22] Fixed Claude Code terminal-mode tasks sitting at the prompt waiting for a manual Enter. `submitInitialPrompt` (and the `/session/:id/input` continue path) wrote `\r` only 150 ms after the prompt text, inside Claude's TUI paste-detection window — so the Enter was absorbed into the `[Pasted text #N +X lines]` block instead of submitting. Bumped the delay to 600 ms so the paste window closes before Enter arrives.

[2026-04-22] Toned down the board-v2 "Delete all" button to an icon-only ghost: muted foreground by default, destructive tint on hover, no border or red wash. Still sits next to the task count, still opens the same `ConfirmPopover` with the full count + warning, so the commitment moment stays intentional while the resting state no longer shouts.

[2026-04-22] Added satirical CV for Narcissa Malfoy (née Black) at have-fun/cv-narcissa-malfoy.md — private-sphere register, covers the Malfoy/Black household and genealogical stewardship, the wartime occupation of the Manor, and glances at the clearing-at-Hogwarts moment without rehearsing it. Also added Lucius Malfoy and Narcissa Malfoy entries to have-fun/index.md in alphabetical position.

[2026-04-22] Added satirical CV for Lucius Malfoy at have-fun/cv-lucius-malfoy.md — aristocratic register, euphemistic about the Death Eater years, pointed about the Wiltshire estate and post-tribunal public record. Matches the tone established by cv-remus-lupin.md and the other Harry Potter CVs in have-fun/.

[2026-04-21] Fixed three gaps in multi-task dispatch inheritance surfaced by a live Harry Potter fan-out test. (1) `resolveDispatchRuntime` (`src/lib/agents/action-dispatcher.ts`) now pushes the parent conversation's provider + adapterType + model + effort to spawned sub-tasks by default, falling back to the target persona's declaration only when the parent has none. Action-authored `providerId`/`adapterType`/`model`/`effort` on a `LAUNCH_TASK` still win. Effort is portable across providers via a new `providerSupportsEffort()` helper in `provider-registry.ts`. `LaunchTaskAction`/`ScheduleJobAction`/`ScheduleTaskAction` in `src/types/actions.ts` gained optional `providerId`/`adapterType`. `action-parser.ts` picks up the new fields from JSON + inline forms; `action-validator.ts` adds `provider_unavailable` (hard) and `cross_provider_push` (soft) warnings; the dispatcher system prompt in `conversation-runner.ts` now explains inheritance so the LLM stops clobbering user intent. (2) Codex error surfacing: `codex-stream.ts` now handles in-stream `{"type":"error"}` and `{"type":"turn.failed"}` events and writes the unwrapped inner message to `CodexStreamAccumulator.errorMessage`; `codex-local.ts` prefers that over filtered stderr and adds a `classifyCodexModelUnavailable` matcher. `cabinet-daemon.ts` now passes `result.errorMessage` to `classifyError` when stderr is empty. A new `ConversationErrorKind` variant `model_unavailable` lights up the UI with a "pick a different model or switch to API-key auth" hint instead of the previous empty-transcript "unknown" shrug. (3) Codex model catalog refresh: `codex-cli.ts` gained `gpt-5.4` (the only model that works on ChatGPT-plan Codex accounts) and annotated `gpt-5.2-codex`, `gpt-5.1-codex-max`, `o3`, `o4-mini`, `gpt-4.1*` with `requires: "api_key"`; `ProviderModel.requires` is exposed through `/api/agents/providers` and rendered as an "API key" badge in `task-runtime-picker.tsx`. Verified end-to-end: re-ran the parent → 5-child fan-out with `claude-code/opus/medium` and all 5 children now inherit that runtime (previously every child fell back to `codex-cli` with empty adapterConfig); fired an unsupported-model run and confirmed `errorKind: model_unavailable` + the real codex reason make it to `meta.errorHint`. New unit tests: `action-dispatcher.test.ts` (6 cases covering same-provider inheritance, parent-provider push, action override, effort portability, effort-not-supported drop, persona fallback); `codex-local.test.ts` gains a `{type:error}` fixture that asserts the inner-message unwrap + classifier match.

[2026-04-21] Added a "Delete all" bulk action to the board-v2 header (next to the task count). The button is destructive-styled, only renders when `filteredTasks.length > 0`, and routes through the existing `ConfirmPopover` with an explicit count + "can't be undone" warning. The confirmation body mentions when filters are narrowing the scope so the user sees whether they're about to delete the filtered view or everything. On confirm, each conversation is deleted in parallel via `deleteConversation`; if the currently-open detail panel was one of the deleted, `selectedId` is cleared and the selection set is emptied before `refresh()`.

[2026-04-21] Added steampunk-pirates Snape vignette at have-fun/steampunk-pirates/snape.md — reimagines Severus Snape as captain aboard the Hogwarts zeppelin-academy, with a black-lacquered steam-rifle, bandolier of volatile reagents, and ambiguous loyalties between Admiralty, Dumbledore, and his own cipher ledger.

[2026-04-21] Added steampunk-pirates Dumbledore vignette at have-fun/steampunk-pirates/dumbledore.md — reimagines Albus Dumbledore as captain of the HMS Hogwarts zeppelin-academy, with brass-rimmed spectacles, ornate phoenix-engraved steam-rifle, and a twinkling wisdom edged with quiet barometric menace.

[2026-04-21] Kanban board clarity pass on board-v2. (1) `deriveStatus` in `conversation-to-task-view.ts` now returns `"awaiting-input"` whenever `meta.pendingActions` has unresolved items, even if the run already set `doneAt` — parent tasks with a proposed sub-task awaiting approval now land in the "Your turn" lane instead of "Just Finished". (2) Renamed the "Needs attention" lane to **"Your turn"** with hint "Agent asked a question or needs approval"; other lane hints rewritten for the conversation → lane mapping. (3) Running lane auto-collapses when empty and auto-expands when a run arrives; effect only fires on the empty ↔ non-empty transition so a manual toggle in between still sticks. Default `collapsedLanes` now includes `running`. (4) Task card layout flipped per mockup: status icon + task title on top row (with right-padding reserved for RowActions), agent pill + optional provider/model + timestamp on the bottom row. Removed the now-unused `showModelRow` branch. Status-icon `ask` tooltip updated to "Your turn — reply or approve".

[2026-04-21] Added satirical CV for Moaning Myrtle (Myrtle Elizabeth Warren) to have-fun/ (cv-moaning-myrtle.md, order 54) — in-character posthumous CV from the second-floor girls' lavatory: 54 years of bathroom haunting, plumbing-based locomotion, Chamber of Secrets witness, prefects' bathroom consultant to Harry Potter, and the 1996 emotional-companion role to a certain sixth-year Slytherin. Keeps Myrtle's self-pity, melodrama, and late-life honesty. Indexed in have-fun/index.md between Minerva McGonagall and Mundungus Fletcher.

[2026-04-21] Added satirical CV for Dobby the house-elf to have-fun/ (cv-dobby.md, order 53), extending the Harry Potter CV series with the free elf — Malfoy Manor staff (former), Hogwarts kitchens (current), kept in Dobby's signature third-person voice throughout. Includes a posthumous entry for the Malfoy Manor rescue, a note on what freedom actually means for house-elves, and a reference list anchored by Harry Potter.

[2026-04-21] Added satirical CV for Molly Weasley to have-fun/ (cv-molly-weasley.md, order 52), extending the Harry Potter CV series with the Burrow matriarch — homemaker of nine, Order of the Phoenix (both intakes), Gryffindor, and the occasional duellist whose single most-cited professional outcome was the defence of her daughter at the Battle of Hogwarts. Tone matches Luna Lovegood and Arthur Weasley, with Molly's characteristic warmth, directness on loss, and one line of steel.

[2026-04-21] Added satirical CV for Newt Scamander to have-fun/ (cv-newt-scamander.md), extending the Harry Potter CV series with the magizoologist, author of Fantastic Beasts and Where to Find Them, sometime operative for Albus Dumbledore through the Grindelwald conflict, and Hufflepuff alum (expulsion noted, by his own request). Tone matches the existing Arthur Weasley / Neville Longbottom entries.

[2026-04-21] Added satirical CV for Neville Longbottom to have-fun/ (cv-neville-longbottom.md, order 46), extending the Harry Potter CV series with the current Hogwarts Professor of Herbology and Head of Gryffindor — former Auror, DA resistance leader under the Carrow administration, and, for one specific morning on 2 May 1998, bearer of the Sword of Gryffindor. Tone kept in line with his grandmother Augusta's existing entry.

[2026-04-21] First-turn blank-gap fix on new conversations. `readTurn1` in `conversation-store.ts` now synthesizes a pending agent turn 1 with `content: "Working on it…"` when `meta.status === "running"` and the transcript is empty, matching what continue turns already do via `continueConversationRun`. Error paths (failed/cancelled/completed-with-empty-output) still return `null` so real failures aren't masked. `waitForConversationCompletion` in `conversation-runner.ts` now polls the daemon immediately (first poll at t=0 instead of after a 700 ms sleep) and uses a 250 ms interval for the first 5 s before backing off to the existing 700 ms. Together these cut ~500–700 ms off the perceived blank gap between "prompt sent" and "agent responding" on a cold start, and replace the silent window with a visible "Working on it…" placeholder.

[2026-04-21] Conversation side panels (board-v2 `DetailPanel` and global `TaskDetailPanel`) can now go fullscreen via a new Maximize2/Minimize2 toggle in the panel header. The panel swaps from its 420–460 px side-panel positioning to `fixed inset-0 z-50` when enlarged; clicking the button again (or pressing Escape on the board panel) shrinks it back. State lives in `useAppStore.taskPanelFullscreen` and auto-resets on section changes and when the panel is closed/opened, so navigating away never leaves a stale fullscreen hanging around. Board context (selected card, scroll) is preserved under the overlay, unlike the existing ↗ route-based escape hatch.

[2026-04-21] Simpler tooltip on the **Can dispatch** / **No dispatch** pill in the agent detail header. Plain-English phrasing ("Can hand off work to teammates. You approve each handoff before it runs.") plus a 5-way fan-out example ("Try: 'Launch 5 research tasks — one per top competitor — each on its own task.'") that mirrors how users actually phrase these prompts, and a trailing "Click to turn off" line in the on-state. No jargon like LAUNCH_TASK.

[2026-04-21] Tasks board agents filter now honors the cabinet-visibility toggle. `use-board-data.ts` was sending `visibilityMode=` to `/api/cabinets/overview`, but the route reads `visibility=` — so the toggle was a silent no-op and the board was pinned to the active cabinet's own agents no matter what. Changed the overview call's query param to `visibility`. Left the conversations call alone: `/api/agents/conversations` actually does read `visibilityMode`, so the two endpoints remain inconsistent with each other but both are now correctly addressed.

[2026-04-21] Fix task composer agent dropdown selection: switched `DropdownMenuItem` from `onSelect` to `onClick`. Base-ui's `Menu.Item` doesn't wire `onSelect` to clicks, so agent picks were silently dropped; matches the pattern already used in `reassign-menu.tsx` and `cabinet-task-composer.tsx`.

[2026-04-21] Task composer agent dropdown (`new-task-dialog.tsx`) now renders the real `AgentAvatar` shared component in both the trigger button and each menu item, replacing the raw emoji span. Shows uploaded avatar images when set, and falls back to the tinted icon when none — matching how the sidebar and reassign menu already render agents.

[2026-04-21] Extracted `ConversationApprovalPanel` (`src/components/agents/conversation-approval-panel.tsx`) as the single shared entrypoint for the dispatch-approval UI. Wraps `PendingActionsPanel` with the null/empty guard and an `onApproved` callback, accepts any meta shape with `id`, `cabinetPath`, `pendingActions`, `dispatchedActions` (both `ConversationMeta` and `TaskMeta` satisfy it). All three conversation views now mount it — `conversation-result-view.tsx`, `conversation-live-view.tsx`, and `task-conversation-page.tsx` — and each carries a sibling-view comment above the render so a future change here automatically propagates (or at minimum leaves a breadcrumb). The mistake this fixes: I added the raw panel to two of the three views and missed the task-viewer page because its render site lives in a different component tree.

[2026-04-21] Added `data/experiments/inclined-plane/` as a pure HTML/CSS/JS Galileo inclined-plane simulator. The app includes an `.app` marker, adjustable angle and friction controls, an animated ball on the ramp, live time/velocity/acceleration/position readouts, and a canvas position-vs-time graph that updates during each run.

[2026-04-21] Added `data/experiments/millikan-oil-drop/` as a pure HTML/CSS/JS interactive Millikan oil drop simulator. The app includes a `.app` marker, voltage-adjustable capacitor field, animated charged oil droplets with gravity/electric-force balance, selectable drops, force bars, balance voltage controls, and a measured-charge readout that infers `q = mg/E` and quantizes it to nearest multiples of elementary charge `e`.

[2026-04-21] Added `have-fun/cv-sirius-black.md` — in-character CV for Sirius Black (Order of the Phoenix, Animagus, Marauder, godfather). Addresses the twelve-year Azkaban gap directly on the first page and keeps the understated British voice shared across the Harry Potter CV library. Indexed in `have-fun/index.md`.

[2026-04-21] User profile feature: the "You" label + empty User icon in conversation turn blocks are replaced by the real user's name + avatar. New `user.json` under `.agents/.config/` stores name, displayName, role, avatar, color, iconKey; auto-seeds from `workspace.json` on first read so existing installs get a usable profile with zero friction. New `/api/user/profile` (GET/PUT) and `/api/user/avatar` (GET/POST/DELETE) mirror the agent avatar API via a shared `src/lib/avatars/avatar-io.ts` helper (validation, write, clear). New `UserAvatar` primitive + `useUserProfile` hook (global cache + optimistic mutations) + `EditUserAvatarDialog` (preset grid + upload + remove) are wired into the turn block — clicking the avatar opens the focused editor. Settings grows a new "Profile" tab (first in the list) with personal fields (name, displayName, role, avatar, color, fallback icon) and workspace fields (workspace name, description, team size) that writes both user.json and workspace.json in one save.

[2026-04-21] Added `have-fun/cv-charlie-weasley.md` — in-character CV for Charlie Weasley (Lead Keeper, Romanian Dragon Sanctuary; former Gryffindor Quidditch captain; Order of the Phoenix). Extends the Harry Potter CV library in the house style.

[2026-04-21] Added a satirical CV for Professor Cuthbert Binns to `data/have-fun/cv-cuthbert-binns.md` and linked it from `data/have-fun/index.md`. Continues the Harry Potter character CV library in the project's in-voice format.

[2026-04-21] Conversation turn blocks now show the actual agent's avatar + color instead of a generic sparkles circle with the label "Agent". `TurnBlock` accepts an optional `agent` prop (`AgentAvatarInput & { name? }`); `TaskConversationPage` fetches the persona from `/api/agents/personas/:slug` once per task and passes it down. Assistant turns render `<AgentAvatar size="md" shape="circle" />` (portrait when the agent has one, tinted icon otherwise) and use `displayName || name` as the header label. User turns keep the neutral `User`-icon circle; the sparkles fallback stays for demo mode.

[2026-04-21] Four follow-up fixes to the agent-to-agent dispatch flow so the approval panel actually works end-to-end in the task viewer. (1) Prompt: `buildCabinetEpilogueInstructions` is now async and injects the real teammate roster (slug — name — role) from `listPersonas(cabinetPath)` so agents don't hallucinate slugs; picks a generalist fallback (preference: `editor` → slug/role match of editor|generalist|assistant|copywriter|writer → any teammate) and tells the agent to route there rather than refusing when no specialist fits. Updated all five call sites to pass `cabinetPath` + `selfSlug`. (2) Finalization: removed the `if (!meta.actionsProposedAt)` guard in `finalizeConversation` that was dropping every proposal emitted on turn 2+ (when the user redirected mid-run). Now re-parses every finalize and dedupes against pending+dispatched by fingerprint. (3) Task viewer panel: `TaskMeta` carries `pendingActions`/`dispatchedActions`; `conversationMetaToTaskMeta` copies them; `task-conversation-page.tsx` renders `PendingActionsPanel` between the last turn and the composer with an `onRefresh` that re-runs `fetchTask`. Before, the panel only existed in `/#/agents/...` routes — the `/#/cabinet/./tasks/...` route (which is where the "Open in task viewer" button lands) had no panel at all, so proposed actions were invisible. (4) Session view: wired `onRefresh` through `conversation-session-view.tsx` (via a `refetchRef` + `refreshDetail` callback) to both `ConversationResultView` and `ConversationLiveView`, so approve/reject clicks actually update the panel. Verified end-to-end via Chrome DevTools: approving 2 rows spawned 2 child conversations with `trigger: "agent"` + `parentTaskId` linkage, and the panel re-rendered to show "18 pending · 2 dispatched".

[2026-04-21] Added `have-fun/cv-arabella-figg.md` — in-character satirical CV for Arabella Figg (Squib watcher of Privet Drive, Kneazle breeder, Order of the Phoenix member), order: 38, matching the established CV style in the have-fun corpus.

[2026-04-21] Fixed the pending-actions panel so it doesn't vanish mid-run and so approve/reject actually updates the UI. Two changes to `conversation-session-view.tsx`: (1) exposed a `refreshDetail` callback backed by a `refetchRef` so both child views can re-fetch `ConversationDetail` on demand; (2) pass `onRefresh` to both `ConversationLiveView` and `ConversationResultView`. Added the `PendingActionsPanel` render to `conversation-live-view.tsx` so proposals stay visible during follow-up turns (when status flips back to `running`). Before: result view had no `onRefresh` prop wired up (so approve clicks worked server-side but the panel kept showing stale rows), and live view never rendered the panel at all (so the 10 pending tasks "vanished" the moment the user typed a follow-up).

[2026-04-21] Unified every agent glyph in the app behind a single `AgentAvatar` primitive (`src/components/agents/agent-avatar.tsx`) that accepts `shape` ("square" | "circle"), `size` ("xs" | "sm" | "md" | "lg"), and resolves an avatar image when the agent has one (preset or custom upload) before falling back to the tinted icon. Replaced hand-rolled icon+color spans in the unified task composer dropdown, the v2 task board (AgentPill, filter-bar chips, reassign menu), heartbeat and new-routine dialog headers, and the agents workspace grid/dropdowns. `AgentPill` and the filter-bar chip now switch to a neutral chip with a portrait glyph when an avatar image exists so the image isn't fighting a tinted background. Extended `AgentListItem` and `NewRoutineDialogAgent` with optional identity fields (`displayName`, `iconKey`, `color`, `avatar`, `avatarExt`) so the type carries what the API already returned. Deleted `agent-identity.tsx`; `AgentIdentity` is now `AgentAvatar`.

[2026-04-21] Taught the dispatch prompt to pick a generalist fallback instead of refusing. When `canDispatch` is on, `buildCabinetEpilogueInstructions` now picks a fallback agent from the roster (preference: `editor` → first teammate whose slug/role matches editor|generalist|assistant|copywriter|writer → any teammate) and tells the agent: "If no teammate is a clear specialist fit, dispatch to the generalist `<slug>` rather than refusing. Mention in your reply that you routed to the generalist so the human can reassign if they want." This stops the CEO from pausing with an ask_user when there's no dedicated agent — it routes to editor/copywriter and tells you, so you can reassign in one click instead of restarting the turn.

[2026-04-21] Fixed a second dispatch bug where actions emitted on follow-up turns were silently dropped. `finalizeConversation` was gating the whole proposal path on `if (!meta.actionsProposedAt)` — an idempotency flag set after turn 1 — so when the user redirected mid-run ("send to copywriter") and the CEO emitted a fresh `cabinet-actions` block on resume, the parser never looked at that turn's output. Removed the guard; now every finalize re-parses and dedupes new proposals against `meta.pendingActions` + `meta.dispatchedActions` by fingerprint. Exported `fingerprint` from `action-parser.ts` for the dedupe check.

[2026-04-21] Fixed dispatch hallucination: `buildCabinetEpilogueInstructions` now fetches `listPersonas(cabinetPath)` and injects the real teammate roster (slug — name — role) into the prompt whenever `canDispatch` is on, plus a one-line rule "Only dispatch to slugs listed above. Do not invent slugs." Made the helper async and updated all five call sites (manual, editor, scheduled-job, resume, replay) to pass `cabinetPath` + `selfSlug`. Root cause of the 10-task Disney run that stuck: CEO proposed `cv-tailor` / `CV-tailor` but the root cabinet has an empty `cv-tailor/` dir (no `persona.md`); the persona only exists in `career-ops/.agents/cv-tailor/`, so validation correctly flagged all 10 as `unknown_agent` hard blocks. With the roster now in the prompt, agents will only dispatch to real teammates.

[2026-04-21] Consolidated the stacked chrome above every viewer into a single unified toolbar row. Built a shared `ViewerToolbar` component (`src/components/layout/viewer-toolbar.tsx`) that embeds the new `ReturnToChip` (pops the app-store `returnTo` stack), inline `ViewerBreadcrumb`, optional badge/sublabel, a children slot for viewer-specific actions, and the global `HeaderActions`. Refactored every file viewer to use it — SourceViewer, PdfViewer, CsvViewer, ImageViewer, MediaViewer, MermaidViewer, WebsiteViewer (with a special `leading` slot for the Exit-app button in fullscreen mode where the breadcrumb is hidden), FileFallbackViewer, OfficeChrome (docx/xlsx/pptx), and the markdown editor Header. Deleted the separate `ReturnToBanner` row and the app-shell-level `ViewerBreadcrumb` injection. Screenshot case: JSON file viewer collapsed from 3 stacked rows (Back-to-task + breadcrumb + SourceViewer title/actions) to a single row containing all of them, and the fullscreen app viewer from 2 rows to 1.

[2026-04-21] Added satirical CV for Griselda Marchbanks to have-fun/ (cv-griselda-marchbanks.md, order 33), extending the Harry Potter CV series with the retired Chief Examiner's exacting, long-view voice — sixty-three years of OWL/NEWT examining (including Dumbledore's papers), the 1995 Wizengamot resignation, and a standing defence of the examination as a fairness instrument. Index updated.

[2026-04-21] Added satirical CV for Amelia Susan Bones to have-fun/ (cv-amelia-bones.md, order 34), extending the Harry Potter CV series with the Head of Magical Law Enforcement's crisp, procedurally-rigorous voice — monocle, Wizengamot procedure, Auror pedigree, and a standing memorandum on the independence of law enforcement from the Minister's office.

[2026-04-21] Added satirical CV for Rolanda Hooch to have-fun/ (cv-rolanda-hooch.md, order 32), extending the Harry Potter CV series with the Flying Instructor's brisk, rulebook-held-in-the-head voice — including her Holyhead Harriers reserve-Beater years, IQA Grade I refereeing certification, and her standing view that school fixtures warrant a higher standard than the senior game. Index updated to list Hooch alongside Pomfrey, Ollivander, and the rest.

[2026-04-21] Added satirical CV for Poppy Pomfrey to have-fun/ (cv-poppy-pomfrey.md, order 31), extending the Harry Potter CV series with the Hogwarts Matron's clipped, patient-first, discretion-bound voice — including her St Mungo's training, the Petrified-patient case series, and her non-negotiable line on surrendering case notes to the Ministry.

[2026-04-21] Added satirical CV for Garrick Ollivander to have-fun/ (cv-garrick-ollivander.md, order 30), extending the Harry Potter CV series with the wandmaker's measured, craft-proud voice — including a restrained note on the 1996–1998 off-premises consultation and the Elder Wand question he would prefer not to be asked again.

[2026-04-21] Conversation-panel artifact buttons now render type-aware icons and colors (AppWindow/emerald for `.app`, Table/green for CSV, FileType/red for PDF, Globe/blue for websites, Code/violet for source, Image/Video/Audio, docx/xlsx/pptx, etc.) by looking up each path via `/api/kb/pages/meta` with a path-inferred fallback — reusing the same `pageTypeIcon`/`pageTypeColor` helpers the sidebar uses. Extracted `usePageMeta` into `src/hooks/use-page-meta.ts` and deduped it out of `artifacts-list.tsx`, upgraded both `conversation-result-view.tsx` and `conversation-live-view.tsx`. Extended `artifactPathToTreePath` to also strip `/index.html`, so an artifact pointing at an app's entry HTML resolves to the app directory's tree node and `app-shell` renders the full-screen `<WebsiteViewer>`. Reworked `openArtifactPath` to normalize the path and call `focusPath` (which expands ancestors, selects, and bumps `focusTick`) so the sidebar now scrolls the target row into view and blinks it on click, and skips redundant `loadPage` for app/website node types. Added a new `ViewerBreadcrumb` component rendered in `app-shell.tsx` above `renderContent()` whenever a KB page is open — clickable Home + path segments that each call `focusPath` + `loadPage`; hidden in full-screen `.app` mode to preserve the immersive experience.

[2026-04-21] Removed the `General` pseudo-agent and made Editor the default agent everywhere. Deleted the hardcoded `GENERAL_AGENT` constant and `general-agent-view.tsx`, dropped the four `=== "general"` branches in `conversation-runner.ts` so every slug flows through `readPersona(...)` uniformly, added a `normalizeAgentSlug` helper (aliases legacy `"general"` → `"editor"`), defaulted composer submit targets and task/schedule/tree fallbacks to `"editor"`, removed the "General is manual-only" settings panel, and broadened Editor's persona with `canDispatch: true` plus Q&A / repo / dispatch framing so it can fill General's role.

[2026-04-21] Added satirical CV for Pomona Sprout to have-fun/ (cv-pomona-sprout.md, order 29), extending the Harry Potter CV series with the Herbology Mistress and Hufflepuff Head's earthy, practical, unsentimentally-proud voice — including the Mandrake programme of 1992–93 and her disposition of the greenhouses at the Battle of Hogwarts.

[2026-04-21] Added `data/have-fun/cv-arthur-weasley.md` — a satirical in-character CV for Arthur Weasley (founder of the Misuse of Muggle Artefacts Office 1981–1998, Head of the post-war Office for the Detection and Confiscation of Counterfeit Defensive Spells and Protective Objects, Order of the Phoenix member in both intakes, Order of Merlin First Class 1998), extending the Harry Potter CV collection.

[2026-04-21] Added `data/have-fun/cv-bill-weasley.md` — a satirical in-character CV for William "Bill" Weasley (Senior Curse-Breaker at Gringotts, Cairo posting 1989–1995, Order of the Phoenix member, survivor of the Greyback encounter at the Battle of the Astronomy Tower), extending the Harry Potter CV collection.

[2026-04-21] Added `data/have-fun/cv-nymphadora-tonks.md` — a satirical in-character CV for Nymphadora "Tonks" Tonks (Hufflepuff Auror, Metamorphmagus, Order of the Phoenix member), written in her own voice and extending the Harry Potter CV collection.

[2026-04-21] Added `data/have-fun/cv-kingsley-shacklebolt.md` — a satirical in-character CV for Kingsley Shacklebolt (Auror → close-protection for the Muggle Prime Minister → acting Head of the Auror Office → Minister for Magic after the Battle of Hogwarts), extending the Harry Potter CV collection.

[2026-04-21] Added `data/have-fun/cv-cornelius-fudge.md` — a satirical in-character CV for Cornelius Oswald Fudge (junior Ministry clerk → Minister for Magic 1990–1996 → elder statesman and "senior policy consultant" after a mutually agreed handover to Scrimgeour), extending the Harry Potter CV collection.

[2026-04-21] Added `data/have-fun/cv-ludo-bagman.md` — a satirical in-character CV for Ludovic "Ludo" Bagman (Wimbourne Wasps Beater → Head of Magical Games and Sports → Triwizard adjudicator → independent gentleman of leisure dodging goblin creditors), extending the Harry Potter CV collection.

[2026-04-20] Added `data/have-fun/cv-rita-skeeter.md` — a satirical in-character CV for Rita Skeeter (Daily Prophet special correspondent, bestselling unauthorised biographer, unregistered Animagus), extending the Harry Potter CV collection.

[2026-04-20] Added `data/have-fun/cv-peter-pettigrew.md` — a satirical in-character CV for Peter "Wormtail" Pettigrew (Marauder → Order member → double-agent → rat-form family pet → servant of Voldemort), extending the Harry Potter CV collection.

[2026-04-20] Extended `have-fun/still-alive/` from 17s excerpt to 35s. `generate_midi.py` now appends a composed EXTENSION (beats 34-70) to the canonical SCORE: reprise variation with A5 peak, ascending build to another A5, return to the opening G5-F#5-E5 theme, and a four-beat D-major cadence chord (D3-A3-D4-F#4-A4-D5). Melody/bass threshold bumped to G#4 so F#4 and G4 stay in the arpeggio voice. Output: 183 notes (75 melody / 108 bass), 35.0s duration.

[2026-04-20] Rewired `data/have-fun/still-alive/` to use the canonical SCORE from `career-ops/tools/portal_still_alive.py` (the intro piano excerpt the user actually wanted — earlier transcription was the vocal line). `generate_midi.py` now imports SCORE directly so both renderers stay in sync; emits polyphonic MIDI (120 BPM, 87 notes across 22 chord onsets) splitting melody ≥F4 onto channel 0 and bass below onto channel 1. Web app updated to show the highest-pitched active note in the huge display (melody vs. bass is auto-detected) and colors the piano-roll lanes by role — orange for melody, blue for bass.

[2026-04-20] Added `data/have-fun/still-alive/` — Portal "Still Alive" (Jonathan Coulton) as a generated MIDI file plus a fullscreen web app (`.app` marker). Python script (`generate_midi.py`, pure stdlib) emits `still-alive.mid` (3 tracks: tempo/melody/bass, 112 BPM, D major) and `still-alive.json` (117-note timeline). `index.html` renders each active note in 28vw letters with sharp/octave styling, a scrolling piano-roll canvas with playhead, Web Audio sine+triangle synth, play/pause/restart, and Space/R keyboard shortcuts.

[2026-04-20] Added satirical CV for Severus Snape to `data/have-fun/cv-severus-snape.md`, extending the Harry Potter CV collection (adds Potions Master / Head of Slytherin / double-agent angle; follows the Moody-style in-character format).

[2026-04-20] Added satirical CV for Alastor "Mad-Eye" Moody to `data/have-fun/cv-alastor-moody.md`, extending the Harry Potter CV collection (now also covers Auror/Order-of-the-Phoenix angle alongside the Hogwarts-staff majority).

[2026-04-20] Added satirical CV for Xenophilius Lovegood to `data/have-fun/cv-xenophilius-lovegood.md`, extending the Harry Potter CV collection (previously covered Lockhart, Umbridge, Trelawney, Filch, Hagrid, Lupin, Slughorn, Fletcher).

[2026-04-20] **PendingActionsPanel: one-click Approve all / Reject all / Deselect all.** Previously fan-outs of 500+ tasks required two clicks (Select all → Approve). The header now surfaces always-visible bulk buttons: **Approve all (N)** dispatches every approvable row in one click (skips hard-blocked), **Reject all (N)** clears every pending row without dispatching, and the select-all toggle flips between **Select all** and **Deselect all**. Previous Approve / Reject buttons now render as **Approve selected (N) / Reject selected** only when rows are checked — supporting partial-approval flows without cluttering the default case.

[2026-04-20] **Agent-to-agent task injection with human approval.** Agents can now propose work for other agents via provider-agnostic text markers parsed from the transcript at finalization. Three action types: `LAUNCH_TASK` (immediate), `SCHEDULE_JOB` (recurring cron), `SCHEDULE_TASK` (one-shot at ISO datetime — implemented as a `oneShot: true` JobConfig that the daemon auto-disables after first fire). Both inline one-liners inside the existing ```cabinet block and a new ```cabinet-actions JSON block are supported (JSON authoritative, deduped by `type:agent:title:prompt`). Proposals are *not* auto-dispatched — they land in `meta.pendingActions` on the parent ConversationMeta and surface in a new `PendingActionsPanel` inside `ConversationResultView` for the human to bulk approve / reject. Supports 1000+ rows (cap-visible at 100, "show more" to expand). New per-persona `canDispatch` flag (defaults `true` for `type: "lead"`, `false` otherwise) gates both the system-prompt instructions (appended in `buildCabinetEpilogueInstructions`) and the dispatcher; exposed as a "Can assign tasks to other team members" toggle in the agent detail overflow menu. Validator computes soft warnings (self-dispatch, cycle risk, depth ≥ 3, inactive target, budget low) and hard blocks (unknown agent, persona cannot dispatch, invalid cron/ISO) — hard blocks disable the approve checkbox. New API: `POST /api/agents/conversations/:id/actions {approve, reject}` dispatches via in-process `startConversationRun` / `saveAgentJob` → `reloadDaemonSchedules`; writes audit into `meta.dispatchedActions` with deep links to spawned conversations/jobs; publishes `task.updated` SSE. New files: `src/types/actions.ts`, `src/lib/agents/action-parser.ts` / `action-validator.ts` / `action-dispatcher.ts`, `src/components/agents/pending-actions-panel.tsx`, `src/app/api/agents/conversations/[id]/actions/route.ts`, plus a full PRD at `data/PRDs/agent-task-injection.md`. Extended `ConversationTrigger` / `TaskTrigger` with `"agent"`, `ConversationMeta` with lineage (`parentTaskId`, `triggeringAgent`, `spawnDepth`) + `pendingActions` + `dispatchedActions`, `JobConfig` with `oneShot` / `runAfter` / `ownerTaskId`. `transcript-parser.ts` regex accepts the new markers and emits a new `actions` block type; `conversation-content-viewer` renders it. `npm run lint` + `npm run build` clean.

[2026-04-20] **Consolidated every heartbeat-editor dialog into shared `HeartbeatDialog`.** Complementary to yesterday's `NewRoutineDialog` consolidation. New `src/components/agents/heartbeat-dialog.tsx` owns its own state (cron + active), manages the PUT to `/api/agents/personas/<slug>` for save and `action: "run"` for run-now, and routes to the agent detail page via an `Edit agent` button in the footer. Title shows the agent's colored avatar + `HeartPulse` icon + "for <Agent> · <role>". Description explains the mental model: "A heartbeat wakes this agent on its own rhythm. Each time it fires, the agent checks in and decides what to work on — driven by its persona instructions. Set how often it should wake up here; to change what it does each time, open the agent page." Cron picker label reinforces: "This is how often the heartbeat wakes up." Active checkbox explains: "when off, the heartbeat won't fire automatically." Accepts an optional `missedRun` prop for the amber "did not execute" banner. Three call sites migrated: `agents-workspace.tsx` (three live callsites — team-schedule `onHeartbeatClick`, Heartbeats list row click, fullscreen-schedule `onHeartbeatClick` — now all drive one unified `heartbeatDialog: { agent, initialHeartbeat, initialActive } | null` state; dead `orgChartHeartbeatDialog` JSX + old `runOrgChartHeartbeat` / `saveOrgChartHeartbeat` handlers now unreachable but left as dead code so I don't break the mode-settings fallback render), `cabinet-view.tsx` (`heartbeatDialog` state reshaped; `runDialogHeartbeat` / `saveDialogHeartbeat` deleted; leftover `Dialog` primitive imports + `SchedulePicker` + `MissedRunBanner` + `Zap` / `Save` / `HeartPulse` / `AlertTriangle` imports cleaned up), `board-v2/schedule-dialogs.tsx` (`ScheduleHeartbeatDialog` is now a thin wrapper forwarding `state` + `onClose` + `onRefresh` into `<HeartbeatDialog>` so existing board-v2 callers keep compiling). Also fixed pre-existing `TRIGGER_LABELS` / `TASK_CARD_TRIGGER_STYLES` to include the new `"agent"` `ConversationTrigger` key that landed in a separate commit. Net: another ~270 LOC removed from the three callers, one new 190 LOC shared component. `tsc --noEmit` clean on my files; `HTTP 200`.

[2026-04-20] Added `data/have-fun/cv-argus-filch.md` — a satirical caretaker's CV for Argus Filch (Squib, Hogwarts, long-serving), matching the voice and structure of the existing Hogwarts-staff CV series (Umbridge, Lockhart, Slughorn, Hagrid, Trelawney, Lupin).

[2026-04-20] Added `data/have-fun/cv-remus-lupin.md` — a satirical Defence Against the Dark Arts CV for Remus J. Lupin (Order of the Phoenix, werewolf) as output of the `create-cv` scheduled job.

[2026-04-20] **Consolidated every job-editing dialog into the shared `NewRoutineDialog`.** Replaced three separate dialogs with one: (1) `agents-workspace.tsx`'s big `newJobDialog` + `libraryDialog` + smaller `orgChartJobDialog` (~540 LOC removed combined), (2) `cabinet-view.tsx`'s `jobDialog` (~100 LOC removed, including its old `runDialogJob` / `saveDialogJob` handlers), and (3) `board-v2/schedule-dialogs.tsx`'s `ScheduleJobDialog` (~150 LOC removed inside the component body) — that file now exports a thin wrapper that forwards `state` + `onClose` + `onRefresh` into `<NewRoutineDialog>` so existing `board-v2` callsites keep compiling. All three pages now use one unified `routineDialog: { agent, existingJob?, missedRun? } | null` state per page, with a single `<NewRoutineDialog>` render driven by it. Added `missedRun?: { scheduledAt: string }` prop to `NewRoutineDialog` so the cabinet-view "run did not execute" banner continues to work when wired up. Relaxed `existingJob` to accept `Partial<JobConfig>` so callers with lightweight job summaries (e.g. `CabinetJobSummary` on the schedule calendar) can open the editor without reconstructing full JobConfigs — the dialog merges with sensible defaults. `ScheduleJobDialog`'s `onStateChange` prop is now ignored (kept for source-compat) because the inner dialog owns the draft. Dropped leftover imports (`Clock3` in cabinet-view, `SchedulePicker` in agent-detail-v2). `tsc --noEmit` clean across all four files; `HTTP 200`.

[2026-04-20] Added `data/have-fun/cv-horace-slughorn.md` — a satirical CV for Horace E. F. Slughorn (Order of Merlin, First Class; Slug Club convenor) as output of the `create-cv` scheduled job.

[2026-04-20] Added `data/have-fun/cv-rubeus-hagrid.md` — a satirical Hogwarts-groundskeeper CV for Rubeus Hagrid (Keeper of Keys, Care of Magical Creatures professor) as output of the `create-cv` scheduled job.

[2026-04-20] Added `data/have-fun/cv-gilderoy-lockhart.md` — a satirical CV for Gilderoy Lockhart (Order of Merlin, Third Class) with post-Memory-Charm St Mungo's context, as output of the `create-cv` scheduled job.

[2026-04-20] Added `data/have-fun/cv-dolores-umbridge.md` — a satirical Ministry-of-Magic-style CV for Dolores Umbridge as output of the `create-cv` scheduled job.

[2026-04-20] **Extracted `NewRoutineDialog` component; agent detail page's "Add routine" now opens the same dialog.** Pulled the full-featured new-routine UI out of `agents-workspace.tsx` into a new `src/components/agents/new-routine-dialog.tsx` so it can be reused. The shared component owns its own draft state, POSTs for new jobs and PUTs for edits (with `?cabinetPath=` so it works for agents outside the root), exposes `onSaved` / `onDeleted` callbacks for parent refresh, reuses `TaskRuntimePicker` for provider/model/effort, and renders the colored `AgentAvatar` in the title. Providers + default come from `useAppStore` so there's no prop-drilling. Wired into `agent-detail-v2.tsx`: `ScheduleSection`'s inline add-form (Name/Schedule/Prompt inputs) deleted in favor of a simple `Add routine` button that calls `onAddRoutine`; parent tracks `routineDialogOpen` + `routineEditJob` and renders `<NewRoutineDialog>` at the root. Each job row in the schedule list is now click-to-edit — wrapping the label+cron in a button that sets `routineEditJob` and opens the dialog in edit mode. `addJob` callback removed (the dialog now handles the POST itself). `tsc --noEmit` clean; `HTTP 200` on `/`.

[2026-04-20] **Fix ARTIFACT: lines with comma-separated paths being treated as a single file.** Root cause (two parts): (1) instructions in `conversation-runner.ts` and `heartbeat.ts` were ambiguous — didn't say "one ARTIFACT: line per file", so the model sometimes emitted `ARTIFACT: a.md, b.md`; (2) `normalizeArtifactPath` in `conversation-store.ts` lazy-matched `(.+?\.[A-Za-z0-9]+)(?:\s|$)`, which swallowed the whole comma-list into one path because the next token also ended in `.md`. The transcript UI (`transcript-parser.ts`) likewise rendered one badge for the whole line. Fix: (a) tightened the prompt in both runner + heartbeat; (b) replaced `normalizeArtifactPath` with exported `normalizeArtifactPaths` that splits on `,`/`;`/multi-space before normalizing each token, used by both `parseCabinetBlock` branches; (c) added the same split in `transcript-parser.ts` so the cabinet code block renders one ARTIFACT badge per file. Added two regression tests to `conversation-store-turns.test.ts` covering meta-level parsing and the split helper itself.

[2026-04-20] **New-routine dialog redesigned — agent avatar in title, bigger fonts, shared `TaskRuntimePicker` replaces Provider+Runtime selects.** Title row now shows the target agent's colored `AgentAvatar` + "New routine" / "Edit routine" at `text-[22px] font-semibold` with a subtitle "for <Agent name> · <role>" at `text-[13px]` underneath. Description rewritten in plain English: "A routine is a prompt this agent runs on a schedule. Write the prompt once, pick when it should run, and let the agent take it from there." Bumped label font to `text-[11px] font-semibold` (from `text-[10px]`), input text to `text-[14px]` (from `text-[13px]`), textarea to `text-[14px] leading-6`. Prompt height reduced to `55vh` so the right column breathes. The two side-by-side Provider + Runtime `<select>`s are replaced by a single `<TaskRuntimePicker>` — the same cross-app model picker the tasks composer uses — so provider + model + effort selection is consistent with the rest of the app. Mapping: `jobDraft.provider`/`jobDraft.adapterType` ↔ `providerId`/`adapterType`, and `jobDraft.adapterConfig.{model, effort}` ↔ `value.model`/`value.effort`. The picker's trigger is styled `h-10 w-full rounded-lg bg-muted/60` to match the other inputs. Save button label changed from "Create job" to "Create routine". Run-now / Delete action buttons bumped to `h-9 text-[12px]`. `tsc --noEmit` clean; `HTTP 200` on `/`.

[2026-04-20] **Agent detail: fix avatar editor revert after save.** Root cause: `saveFields` called `refresh()` on success, which runs 4 parallel fetches and calls `setPersona(serverData)` — this raced with and silently overwrote the optimistic update. Fix: `saveFields` now only calls `refresh()` if the PUT returns non-OK (error path revert). Upload and delete paths use a new `applyOptimistic` callback (pure `setPersona` patch, no fetch) instead of the old `onRefresh`, so the avatar state is never overwritten on success.

[2026-04-20] **Agents page: real fix — base-ui Menu.Item uses `onClick`, not `onSelect`.** Previous rAF-based defer was a red herring. The actual bug: `@base-ui/react/menu`'s `MenuItem` type has no `onSelect` prop; React was forwarding it to the rendered `<div>` where it means "text-selection event" (useless for clicks), so every agent-pick in the Add-routine / Add-scheduled-job / Configure-heartbeat dropdowns was a silent no-op. Swapped all three `onSelect={() => …}` callsites in `agents-workspace.tsx` to `onClick={…}`. Kept the `requestAnimationFrame` defer inside `openNewRoutineFor` — now correctly useful because base-ui closes the menu on click, so the dialog needs to wait one frame for focus-lock to settle before opening. Also back-fixed the same bug I introduced earlier in the cabinet-page `AgentPickerCompact` (`cabinet-task-composer.tsx` line 212). `tsc --noEmit` clean on my files (unrelated `claude-transcript-view` import failure is separate in-progress work).

[2026-04-20] **Agents page: Add routine actually opens the dialog; contextual CTAs on Routines + Heartbeats.** Two fixes on top of yesterday's rebuild. (1) Big "Add routine" button was firing state updates but the `newJobDialog` never appeared — base-ui `DropdownMenu` focus-lock was cancelling the dialog open in the same tick. Extracted a shared `openNewRoutineFor(agent)` helper that wraps the state updates (`setSettingsTarget` → drives `settingsAgentSlug`, `setSelectedJobId("__new__")`, `setJobDraft(blankJobDraft(...))`, `setNewJobDialogOpen(true)`) in `requestAnimationFrame` so the dropdown finishes closing before the dialog mounts — reliable open. (2) Added contextual CTAs inside each sub-section. Routines header now has a secondary "Add scheduled job" DropdownMenu (agent picker → same `openNewRoutineFor`) on its right side; Heartbeats header has a "Configure heartbeat" DropdownMenu that routes to the agent's detail page (since heartbeats are edited there), showing the current cron under each agent's name. Empty-state strings updated to point at these buttons. Same handler powers both the big header CTA and the in-section CTAs, so there's one source of truth for "new routine". `tsc --noEmit` clean; `HTTP 200` on `/`.

[2026-04-20] **Agents page: friendlier beginner copy, bigger titles, Add-routine now opens the full Add-job dialog, fixed schedule overflow.** Three coupled changes on `/#/cabinet/./agents`. (1) Copy + typography overhaul for beginners — H1 bumped from `text-[22px]` to `text-[36px]/44px` with the reframing "This is your AI team."; followed by two paragraphs in `text-[17px]` and `text-[15px]` that explain the mental model (agents work for you around the clock; they wake up, check in, and ship work) and orient the user to what they can do on this page. Section headings "Meet the team" and "Routines & heartbeats" jumped to `text-[24px]/28px font-semibold`. The Routines section gained a paragraph explaining that routines are scheduled jobs ("every weekday at 9am, summarize yesterday's activity") plus a separate explainer that the heartbeat is "the living brain of an agent" — one per agent, self-directed, no prompt needed. Jobs and heartbeats sub-lists got `text-[16px]` inline titles with short explanatory paragraphs and friendlier empty states ("No routines yet. Use Add routine above to create your first one."). (2) Hoisted the `newJobDialog` + `libraryDialog` JSX (272 lines) out of the now-unreachable settings branch to the root so they render unconditionally. Rewired the big "Add routine" button — it's now `h-12 rounded-xl text-[15px] font-semibold` with a `size-4.5` clock — to set `settingsTarget`/`settingsAgentCabinetPath` + `selectedJobId="__new__"` + `blankJobDraft(...)` and then `setNewJobDialogOpen(true)`. Existing `saveJob` detects new vs edit via `selectedJobId === "__new__"` and POSTs or PUTs accordingly. The simpler `orgChartJobDialog` still handles quick-edits from individual schedule rows. (3) Fixed calendar overflow in the inline team-schedule embed: the wrapper is now `flex h-[460px] min-h-0 flex-col overflow-hidden rounded-xl border bg-card` so `ScheduleView`'s internal `min-h-0 flex-1` actually gets a bounded axis — calendar scrolls within its frame instead of spilling out. `tsc --noEmit` clean; `HTTP 200` on `/`.

[2026-04-20] **New-task composer overhaul.** (1) Providers now cached globally in `app-store` (`loadProviders` called once by `AppShell`) so `TaskRuntimePicker` reads from the store instead of fetching on every mount — no more slow model-button appearance. (2) `use-composer` now handles `Cmd+Enter` (metaKey) as a manual newline insertion in addition to `Shift+Enter`. (3) `ComposerInput` always shows a `⌘↵ newline` hint inside the action row; removed the external `showKeyHint` block. (4) `NewTaskDialog` now has a dedicated agent-selector dropdown button (emoji + name, `ChevronDown`) alongside `TaskRuntimePicker` in `actionsStart`; agent is tracked as separate state so @mentions in text remain for pages only; `mentionDropdownPlacement` set to `"below"` so the @ dropdown doesn't clip above the dialog.

[2026-04-20] **Agent detail: click avatar to edit icon, color, or avatar.** In `agent-detail-v2.tsx`, wrapped the `AgentIdentity` in `Hero` with a clickable button that shows a `Pencil` overlay on hover. Clicking opens `AvatarEditorPopover` — a floating panel with two tabs: "Icon & Color" (8-column icon grid from `ICON_PICKER_KEYS` + 12 color swatches + custom color input) and "Avatar" (6-column preset grid from `AVATAR_PRESETS` + upload custom image + remove). Icon/color selection calls `saveField` (existing PUT persona route). Custom avatar upload POSTs to `/api/agents/personas/[slug]/avatar`. Remove calls DELETE on that route then calls `refresh()`. Both catalog imports (`ICON_CATALOG`, `ICON_PICKER_KEYS`, `AVATAR_PRESETS`) added to the detail file. No new API routes needed.

[2026-04-20] Fixed conversation-completion toasts + chimes going silent for structured-adapter runs. Root cause was cross-process: `notificationQueue` in `conversation-store.ts` is an in-memory array, and since `cabinet-daemon` runs in its own Node process, whenever the daemon won the `finalizeConversation` race (nearly always for structured adapters) the push landed in the daemon's queue, while Next.js's SSE tick kept draining an empty queue. Fix: exported `enqueueConversationNotification(notification)` from `conversation-store.ts` with identity-based dedupe (`cabinetPath::id::status` via `buildConversationNotificationIdentity`) and called it unconditionally from `waitForConversationCompletion` in `conversation-runner.ts` once the poller sees a terminal status — next to the existing Next.js-side `publishConversationEvent("task.updated")` belt-and-braces call. Same comment pattern as the task.updated fix (2026-04-18) so the intent is obvious on inspection. Dedupe means it's safe even if a future refactor accidentally calls `finalizeConversation` on the Next.js side too. Timeout branch at the bottom of the poller still pushes through `finalizeConversation`'s normal queue so nothing changes there.

[2026-04-20] **Agents workspace Schedules: big "Add routine" CTA + embedded team schedule board with fullscreen.** The Schedules section grows a top-right, prominent 10px-tall `Add routine` button with a `Clock3` icon (plus `ChevronDown`) — it's a `DropdownMenu` that lists every agent in the team; picking one opens the existing job editor (`orgChartJobDialog`) pre-seeded with `blankJobDraft(slug, defaultProvider)` and a new `newRoutineIsNew` flag. `saveOrgChartJob` now branches on `newRoutineIsNew || !draft.id`: for new routines it POSTs `/api/agents/<slug>/jobs` (matching the detail-page `addJob` handler) and splices the created job into `agentJobsMap`; for edits it keeps the existing PUT path. Cancel/close resets the flag. Below the intro copy, the Schedules section now embeds `<ScheduleView>` (the same board-v2 wrapper used by the Tasks > Schedule view) at a fixed 460px height, with a `Maximize2` button in its header that pops it into a `94vh × 96vw` Dialog with a `Minimize2` toggle. Both inline and fullscreen schedule views wire `onJobClick` to the edit dialog (resolving the full `JobConfig` by id from `agentJobsMap`), `onHeartbeatClick` to `orgChartHeartbeatDialog`, and `onConversationClick` to the existing conversation-mode switch. Added imports: `ScheduleView`, `DropdownMenu` primitives, `ChevronDown`, `Maximize2`, `Minimize2`. `tsc --noEmit` clean on this file; `HTTP 200` on `/`.

[2026-04-20] **V2 Kanban: per-lane collapse, persisted.** Previously only the Archive column could collapse into the narrow 48px strip; now every lane can. Replaced the single `archiveOpen: boolean` with a `collapsedLanes: Set<LaneKey>` persisted to `localStorage["cabinet.tasks.v2.collapsedLanes"]` as a CSV of lane keys (default: `"archive"` so the prior default is preserved). `LaneHeader` now renders a `ChevronLeft` collapse button on the right edge of every open lane; the narrow-column render is genericized to use each lane's icon + label + count instead of hardcoding Archive. Dropped the now-dead `onToggle` prop from the expanded header (chevron was previously only used to re-collapse Archive; regular expand/collapse now lives in the dedicated button). No other lane behavior changes — Inbox still shows the `+ New task` CTA, Running still shows Kill/Restart All, Needs attention still shows Restart All for failed, bulk DnD paths unchanged. Reuses existing `usePersistentState`. `tsc --noEmit` clean for touched file.

[2026-04-20] **Tasks board: depth + trigger filters moved to header row.** Removed the two-row FilterBar (depth segmented + trigger chips in row 1, agent pills in row 2) and replaced it with a single-row FilterBar that only shows agent pills (returns null when no agents). The visibility depth segmented control (Own/+1/+2/All) and trigger chips (All/Manual/Jobs/Heartbeat) are now in the main header row on the right side, before the task count and selection pill. `TriggerChip` exported from `filter-bar.tsx`; `CABINET_VISIBILITY_OPTIONS`, `cn`, `Bot`, `Clock3`, `HeartPulse` added to `tasks-board-v2.tsx`. Layout: left side has Tasks title + ViewToggle + DensityToggle; right side has depth segmented | separator | trigger chips | [selection pill if active] | task count.

[2026-04-20] **Task conversation panel: auto-scroll to bottom on open + new turns.** Added `chatScrollRef` attached to the chat `overflow-y-auto` container in `task-conversation-page.tsx`. A `useEffect` keyed on `task?.turns.length` scrolls to `scrollHeight` whenever turns are added (including on initial panel open when the task first loads).

[2026-04-20] **V2 Tasks board: remove invisible People rail, replace with discoverable in-card + bulk Reassign.** The right-hand rail's only job was drag-to-reassign — a gesture most users never discovered because the rail stays collapsed at 48px with no labels at rest. Replaced with a direct, keyboardable menu. **Deleted**: `src/components/tasks/board-v2/people-rail.tsx`, `AGENT_DROP_PREFIX` + `agentDropId()` in `dnd-keys.ts`, the `AGENT_DROP_PREFIX` branch (and now-unused `reassignConversation` import + `agentsBySlug` arg) in `use-drag-handler.ts`. **New**: `reassign-menu.tsx` — a shared `<DropdownMenu>` that takes a caller-supplied trigger + `agents`, `currentSlug`, `onSelect`. Paused agents show a "paused" flag and are still selectable; the task's current owner is marked with a check + disabled. Reuses `getAgentColor`, `tintFromHex`, `resolveAgentIcon`. **row-actions.tsx** now imports `ArrowRightLeft` and mounts `ReassignMenu` between Restart and Delete — visible on hover on every card and list row. Click fires `reassignConversation(id, toSlug, cabinetPath)` then `onRefresh`. **task-card.tsx + list-view.tsx + kanban-view.tsx** each grew an `agents?: CabinetAgentSummary[]` prop, threaded from `tasks-board-v2.tsx` (`overview?.agents ?? []`). **Header "N selected" pill** grows a "Reassign" button when `selection.size > 0`; click opens the same menu, fans out via `Promise.all` skipping already-owned tasks, then clears selection and refreshes. **Removed** the `<PeopleRail>` mount from `tasks-board-v2.tsx`. Reclaims ~48px of board width. Drag-to-archive, drag-to-reorder, multi-select drag, and all other DnD paths unchanged. Scope intentionally skips undo toast for in-card reassign (drag path had one; not restoring for v1). Out-of-scope follow-ups: keyboard shortcut `R`, reassign in detail panel, agent-pill-as-dropdown on cards.

[2026-04-20] **Fix: `AgentDetailV2` stuck on "Loading…" for agents outside the root cabinet.** Opening `/#/cabinet/career-ops/agents/application-assistant` hung indefinitely. Root cause: `AgentDetailV2`'s `refresh()` called `/api/agents/personas/<slug>` without a `cabinetPath` query param, so `readPersona(slug, undefined)` fell back to the root cabinet and 404'd; the render then gated on `loading || !persona` and never moved past the spinner. Added a `cabinetPath?: string` prop to the component, threaded it into the persona/jobs/conversations/tasks fetches (`?cabinetPath=<path>`), and added it to the `refresh` `useCallback` dep array. `app-shell.tsx` now passes `section.cabinetPath ?? "."` when routing `type === "agent"` into the detail. Verified via `curl /api/agents/personas/application-assistant?cabinetPath=career-ops` returning 200 + 3.3 KB of persona data. `tsc --noEmit` clean; `HTTP 200` on `/`.

[2026-04-20] **Agents workspace: re-scoped to "your AI team" — agents grid + schedules view + explanatory copy; tasks sidebar kept.** Previous iteration replaced the center with `ActivityFeed`, but that duplicated what the right panel already shows and lost the ability to view+configure schedules. User pushback was clear: the agents page exists to (a) view the team, (b) view and edit all jobs/heartbeats, (c) explain what these things are, with the conversations sidebar still on the right. New default view is a scrollable column (`max-w-5xl`) with three blocks. (1) Intro paragraph — "Your AI team" H1 + short body copy describing jobs vs heartbeats and pointing at the sidebar for live activity. (2) Agents grid — responsive 1/2/3-col card grid, each card opens the detail page on click; card shows a colored `AgentAvatar` (slug-based tint + lucide icon via shared `getAgentColor`/`resolveAgentIcon`), name + role, active/paused dot, and pill-chips for heartbeat/jobs/department. (3) Schedules section — two tables wrapped in `divide-y rounded-xl border bg-card`: **Jobs** (flat list across all agents, derived via new `allJobs` `useMemo` over the existing `agentJobsMap`) and **Heartbeats** (filtered `agents.filter(a => !!a.heartbeat)`). Each row shows icon + name + owner + human cron via `cronToHuman`, with an Enabled/Paused pill on the right. Clicking a job row opens the existing `setOrgChartJobDialog` (reuses the cron editor) with the full `JobConfig` draft; clicking a heartbeat row opens `setOrgChartHeartbeatDialog`. The header row keeps counts + scope pills + Org chart button + Add agent + refresh. `refreshAgents()` now also captures `jobs` and `children` from the `/api/cabinets/overview` response into new `cabinetJobs` + `cabinetChildren` state, so `OrgChartModal` finally gets real data for jobs and child-cabinet pills. `ActivityFeed` import removed from this page (cabinet page still uses it, so the component itself stays). `AgentAvatar` helper is local to this file. `tsc --noEmit` clean; `HTTP 200` on `/`; lint warnings unchanged from baseline.

[2026-04-20] **V2 Tasks board: restoration phase (R1–R5) — row actions, bulk actions, inline composer, schedule dialogs, sidebar sort.** Ships everything called out in `TASK_BOARD_RESTORE_PRD.md`. **R1** — `src/components/sidebar/recent-tasks.tsx` fetches 30 conversations then re-sorts locally by `lastActivityAt ?? completedAt ?? startedAt` DESC before slicing to 6, so an actively-streaming conversation outranks a freshly-created idle one. **R2** — new `row-actions.tsx` with hover-revealed Stop / Restart / Delete whose visibility flips on `task.status` (running → Stop+Delete, awaiting-input → all three, failed/done/idle → Restart+Delete, archived → Delete). Wired into `TaskCard` (absolute overlay outside the button tree) and list rows. Added `deleteConversation()` to `board-actions.ts`. **R3** — Running lane header now shows Kill + Restart bulk buttons when non-empty; Needs-attention lane shows Restart when `failedCount > 0`. Parallelized `Promise.all` with per-item error isolation. **R4** — new `new-task-dialog.tsx` using `ComposerInput` + `TaskRuntimePicker`. Default agent = first active; `@-mention` to reassign; submit POSTs `createConversation` and routes to the new task via `setSelectedId`. Wired to Inbox `+`, empty-state card, and the long-orphaned `cabinet:open-create-task` window event. **R5** — new `schedule-dialogs.tsx` with `ScheduleJobDialog` (SchedulePicker + prompt + Enabled + Run/Save) and `ScheduleHeartbeatDialog` (SchedulePicker + Active + Run/Save). `ScheduleView` now threads `onJobClick` + `onHeartbeatClick` to both the calendar (via `onEventClick` sourceType branching) and the list. `tsc --noEmit` clean.

[2026-04-20] Researcher heartbeat produced `career-ops/evaluations/research-mistral.md` — 6-axis report for Mistral (pipeline ID #2, Screen stage). Filled the biggest in-flight pipeline research gap; flagged VERIFY items (funding round specifics, current model lineup, remote policy) and red flags (Paris on-site risk against candidate's non-negotiables, frontier-lab burnout, HF offer as real alternative). Recommended next-call probes for remote policy and reporting line.

[2026-04-20] Content-marketer heartbeat stood up `/marketing/` surface: `index.md` with voice/audience notes, first blog draft `blog/stop-pasting-context/index.md`, plus X (7) and LinkedIn (3) social drafts for 2026-04-20. Moves goals to posts 1/10 and social 10/20.

[2026-04-20] **Agents workspace: org-chart center swapped for `ActivityFeed`; settings + persona/job editing now route to the detail page.** Redesign of `/#/cabinet/./agents` (`AgentsWorkspace`) on the same principles as the cabinet page. Default mode now renders a tight one-row header (cabinet name + agent/active/dept count pills + scope pills + "Org chart" + "Add agent" + refresh) above `<ActivityFeed/>` from the cabinet page, in a centered `max-w-5xl` column. Old brown-palette dept-grouped org-chart visualization is gone from the center; `<OrgChartModal/>` opens fullscreen from the header button (same component the cabinet page uses). `openAgentSettings()` now navigates to `/#/agent/<slug>` via `setSection(buildAgentSection(slug, cabinetPath))` instead of opening the inline `mode === "settings"` editor — every settings call site (right-panel header, conversation top bar, org-chart entries) reroutes for free. Right conversations panel kept verbatim per user request — still resizable, still has trigger/status filter chips, still drives `mode === "conversation"`. The browse-agent-library + custom-agent-creation dialogs were lifted out of the (now-unreachable) settings render branch and re-anchored at the root JSX next to `OrgChartModal`, so the new "Add agent" button in the header opens them from any mode. Added an optional `agentSlug` prop on `ActivityFeed` (plus `onAgentPillClick` and `onClearAgentFilter`) — the workspace passes `activeAgentSlug` so clicking an agent pill in any feed row narrows the list to that agent and surfaces a "Clear filter" chip in the feed header; the existing right panel reads the same `activeAgentSlug` so both lists move together. New `cabinetAgentsView` `useMemo` adapts the workspace's `AgentListItem[]` to `CabinetAgentSummary[]` (defaults for missing `taskCount`, `cabinetDepth`, `inherited`, etc.) so the shared cabinet-page components type-check. Settings render branch and the org-chart job/heartbeat popups remain in the file as dead code — unreachable (`openAgentSettings` no longer flips mode, org-chart cards aren't rendered) but harmless; ~1.3k LOC of cleanup deferred to a follow-up to keep the diff focused. `tsc --noEmit` clean; `HTTP 200` on `/`; lint warnings unchanged from baseline.

[2026-04-20] **V2 Tasks board: depth + trigger filters, Inbox +, flat list view, lane rename, horizontal-scroll fix.** Five coupled fixes after user feedback that the v2 board was missing the filter chips + depth selector + add-task CTA that the Agents workspace already had, and the kanban wasn't scrolling horizontally. (1) Rewrote `filter-bar.tsx` — now two rows: Row 1 is a segmented visibility-depth control (Own / +1 / +2 / All, using `CABINET_VISIBILITY_OPTIONS` shortLabels) plus trigger chips (All / Manual (Bot) / Jobs (Clock3) / Heartbeat (HeartPulse)) tinted sky/emerald/pink when active; Row 2 keeps the agent pills. Exported `TriggerFilter` type + `triggerFromFilter()` helper. (2) Renamed the "Needs Reply" lane to "Needs attention" in `kanban-view.tsx` (internal `LaneKey` key unchanged). (3) Added an Inbox "+" button in the lane header and a dashed "New task" card in the empty-state; both call `handleAddTask` which does `setSection({type:"cabinet", cabinetPath})` so the user lands in the cabinet page's composer. (4) Fixed horizontal scroll — kanban container now `w-full min-w-0 overflow-x-auto overflow-y-hidden`, `<main>` got `min-w-0` so the 5-lane row actually scrolls instead of overflowing the shell. (5) Rewrote `list-view.tsx` as a flat scrolling list mirroring the Agents workspace style — status dot + agent pill + title + trigger badge pill (colored ring) + relative time; no lane grouping. Sort: running first, then newest by lastActivity/started. `tasks-board-v2.tsx` now owns `visibilityMode` state (seeded from the prop / app-store per-cabinet map), `triggerFilter` state (localStorage-backed via `usePersistentState`), and computes both a `filteredByLane` for Kanban and a `flatList` for List. Changing visibility via the segmented control also writes back to `app-store.cabinetVisibilityModes` so sidebar + board stay in sync. `app-shell.tsx` reads the cabinet's stored visibility on tasks-board mount so drilling into a cabinet where +2 was selected persists across navigation. `tsc --noEmit` clean.

[2026-04-20] **Back banner from v2 board DetailPanel now restores the task, not just the board.** `TaskConversationPage` now accepts an optional `returnContext?: SelectedSection` prop that's threaded to `TurnBlock` and `ArtifactsList`, and finally to `KbArtifactRow` / the artifact rows in ArtifactsList. When set, it overrides the default "capture current section" behavior. Wired up at both compact-embed mount points: v2 `DetailPanel` (`src/components/tasks/board-v2/detail-panel.tsx`) and the legacy `TaskDetailPanel` (`src/components/tasks/task-detail-panel.tsx`) — both now pass `{type:"task", taskId, cabinetPath}`. Fixes the UX where clicking an artifact from the board's right-side panel set `returnTo = {type:"tasks"}`, so Back took the user to the board with no task reselected (DetailPanel's `selectedId` is local state, not a section field). Now Back lands in the full-panel task view for that exact task. Standalone mounts (app-shell `section="task"`, `/tasks/[id]` route) don't pass `returnContext` and fall back to reading the current section, which is already `{type:"task"}` in those cases. `tsc --noEmit` clean.

[2026-04-20] **Artifact clicks: scroll + blink sidebar, infer viewer from extension, add Back-to-task banner.** Three tightly-coupled UX fixes for the task-viewer artifact rows (KbArtifactRow in `turn-block.tsx` + ArtifactsList in `artifacts-list.tsx`): (1) added `focusPath(path)` to tree-store — expands every ancestor, selects the leaf, and bumps a new `focusTick` counter; `TreeNode` watches `isSelected && focusTick` via `useEffect` and calls `scrollIntoView({behavior:"smooth", block:"center"})` plus a 1.4s `cabinet-tree-blink` keyframe (new in `globals.css`, violet 0.28-opacity wash + ring that fades in/out twice). (2) Extended `inferredType` in `app-shell.tsx` for paths not in the sidebar tree — now also covers code (`.py`/`.ts`/`.tsx`/`.js`/`.jsx`/`.rb`/`.go`/`.rs`/`.java`/`.kt`/`.swift`/`.c`/`.cpp`/`.cs`/`.php`/`.sh`/`.json`/`.yaml`/`.sql`/etc.), image, video, audio, mermaid. Fixes the bug where clicking a `.py` artifact from a conversation rendered it through `KBEditor` as markdown; it now routes to `SourceViewer` with syntax highlighting. (3) Added `returnTo: SelectedSection | null`, `pushSection(next, from)`, `popReturnTo()` to app-store. Artifact-row clicks call `pushSection({type:"page", cabinetPath}, currentSection)` so the page viewer remembers where we came from. A new `ReturnToBanner` rendered at the top of `<main>` shows a "← Back to task/tasks/agent/…" pill when `returnTo` is set; clicking it calls `popReturnTo` to restore the prior section. Any normal navigation via `setSection` clears `returnTo` so the banner never lingers. `tsc --noEmit` clean.

[2026-04-20] **Pending-turn indicator in the task conversation panel.** While an agent turn is running (`turn.pending === true` on a non-user turn) the task viewer now renders a small italic "thinking" indicator — a rotating verb from a pool of 25 (Thinking, Pondering, Cogitating, Jizzling, Weaving, Grokking, Sizzling, Wrangling, ...) next to three bouncing dots (`animate-bounce` with staggered `[animation-delay:-0.3s|-0.15s|0s]` + `[animation-duration:1s]`), plus an `Ns` elapsed counter that only appears after 2s so quick turns don't flash it. Verb rotates every 2.4s with a small random stride so it doesn't feel locked in a round-robin. Lives in a new `PendingIndicator` component at the bottom of `TurnBlock`; the turn body now hides content entirely when it's empty (so a pending turn with no streamed output yet is the indicator alone). Applies everywhere `TaskConversationPage` mounts — v2 board DetailPanel and standalone `/tasks/[id]`. Terminal-mode tasks aren't affected since they render `WebTerminal` instead of turn blocks. `tsc --noEmit` clean; no new lint errors on the touched file.

[2026-04-20] **Stop cabinet meta-blocks leaking into KB file bodies.** Agents were occasionally writing the epilogue ` ```cabinet SUMMARY: ... ARTIFACT: ... ``` ` block into the content of `.md` files they saved (e.g., `data/have-fun/harry-potter-8.md`), instead of keeping it as a transcript-only meta-annotation. Three-part fix: (1) tightened `buildCabinetEpilogueInstructions()` in `src/lib/agents/conversation-runner.ts` — now explicitly says the block goes at the end of the chat response (text sent back to the user), NOT inside any file content, and adds a "Never write a ` ```cabinet ``` ` block inside the body of any .md file you save" rule. (2) Added `sanitizeArtifactCabinetBlocks()` in `src/lib/agents/conversation-store.ts` — regex-strips a trailing `\n*```cabinet\b[\s\S]*?\n```\s*$` fence from any `.md` artifact before it's exposed to the UI. Path-traversal guarded (`path.resolve` must stay inside `DATA_DIR`). Wired into `finalizeConversation` (first-turn, parallel with `writeConversationMeta`/`replaceConversationArtifacts`) and both continue-turn paths (`appendAgentTurn`, `updateAgentTurn`) guarded by `!pending && turn.artifacts?.length`. (3) Cleaned `data/have-fun/harry-potter-8.md` — removed the trailing cabinet block the copywriter agent had written into the story body. `tsc --noEmit` clean.

[2026-04-20] Added Python prime-sieve script to `have-fun/primes-under-100.md` and a runnable `have-fun/primes_under_100.py` — Sieve of Eratosthenes, verified output matches the 25 primes already listed on the page.

[2026-04-20] Added `have-fun/harry-potter-8.md` — a short, fully fictional summary for a made-up Harry Potter 8 ("The Hollow Crown"), written by the Copywriter agent.

[2026-04-19] **Skills track shipped end-to-end.** Backend: catalog at `~/.cabinet/skills/<slug>/SKILL.md`, `_shared/skills-injection.ts` (`readSkillCatalog` + `syncSkillsToTmpdir`), persona frontmatter `skills: []`, runner injects `skillsDir` + slug list into `adapterConfig`, Claude adapter wires `--add-dir`. UI-1 Agent detail editable multiselect (toggleable violet pills, orphan-slug detection in amber). UI-2 Settings "Skills" tab with "Coming soon" pill + disabled read-only catalog preview. UI-3 Violet Sparkles chip in task header (fullscreen terminal + regular) showing N skills attached with full list on hover. UI-4 Editable multiselect widget saves via PUT `/api/agents/personas/:slug { skills }`. Also shipped the remaining §12 provider items this session: model+effort passthrough for all 8 providers on `/api/agents/headless` (Gemini `-m`, Cursor/Grok/Copilot `--model`, OpenCode `--model`+`--variant`, Pi `--model`+`--thinking`); `_shared/cli-args.ts` deduped `readStringConfig`+`readEffortConfig` across all 8 adapters; `isAgentProviderSelectable()` centralizes the `type==="cli"` UX gate; terminal-mode fullscreen viewer polish (T16-T23 — `WebTerminal` swap, top-strip PTY chip with pulsing ring, Stop button, Continue via stdin-inject if alive / respawn otherwise, legacy-adapter continuation fix); daemon `POST /session/:id/input` + client helpers for stdin injection; PTY output distillation to a 1-line summary on exit. PROVIDER-CLI §12 consolidated into grouped buckets by what each item is blocked on; remaining open: #2b (skills for other 6 providers), #4 (full directory split), #5 (agent-live-panel adapter awareness), #9 (effort policy product call), #11 (licensed glyph artwork).

[2026-04-19] **Task viewer transcript rendering: match the /agents transcript view.** `TurnBlock` in `src/components/tasks/conversation/turn-block.tsx` now renders agent turns via `ConversationContentViewer` instead of the Markdown prose component. User turns still use `<Markdown>` (they're chat prompts), but agent output — which for PTY/heartbeat runs is raw terminal text with embedded `SUMMARY`/`ARTIFACT`/`DECISION` CABINET blocks — goes through `parseTranscript`, which preserves newlines (`whitespace-pre-wrap` + `font-mono`), recognizes diff/code fences, and renders CABINET fields as colored labeled cards. Fixes the regression where `markdownToHtml` was collapsing newlines into spaces and dropping the "result section" clarity the old `/agents` view had. Applies everywhere `TaskConversationPage` mounts (v2 board DetailPanel + standalone `/tasks/[id]`). `tsc --noEmit` clean.

[2026-04-19] Added `data/have-fun/primes-under-100.md` listing all 25 primes under 100, grouped by decade with a summary table.

[2026-04-19] **Cabinet page cleanup: drop awkward one-word description; mirror activity-feed list style on Next-up runs; child cabinets become pill chips.** (1) The cabinet manifest's `description` field seeded as "Develop" was rendering as a short, orphaned paragraph under the composer — dropped the `{cabinetDescription ? <p>…</p> : null}` block and the now-unused `cabinetDescription` binding. The cabinet name already sits in the header row, so the description paragraph was redundant at best and typo-looking at worst. (2) `next-up-runs.tsx` now wraps its rows in the same `divide-y rounded-xl border bg-card` card-list container as `ActivityFeed`, and each row reuses the two-tier layout: top sub-row = source icon (heartbeat/clock) + event label (left) + `in 1h` time (right); bottom sub-row = `<AgentPill size="sm" />` when `event.agentRef` is present. The source icon kept the pink/emerald tint that distinguished it before. (3) Child cabinets switched from a vertical list with `ArrowUpRight` affordance to `flex flex-wrap gap-1.5` pill chips — each chip is `rounded-full border bg-muted/30 px-2.5 py-1` with a `FolderTree` lucide icon + truncated `max-w-[160px]` name; clicking still routes via `openCabinet(child.path)`. Removed the `ArrowUpRight` import from `cabinet-view.tsx`. `tsc --noEmit` clean; `HTTP 200` on `/`; no new lint output on the touched files.

[2026-04-19] Added `data/have-fun/harry-potter-poems/index.md` as a landing page for the HP poetry collection — links to existing Bellatrix and Voldemort pieces and lists candidates still to write (Snape, Luna, Dobby, Minerva, Neville).

[2026-04-19] Added `data/plants/` folder with index and 5 Hebrew pages on Israeli plants (זית, חרוב, אלון התבור, רימון, תמר) — each with description, climate, and 1/2-year sapling prices.

[2026-04-19] **Cabinet `ActivityFeed` row: model pill bigger and shows the model name.** Provider glyph grew from `size-2.5` in a `size-4` frame to `size-4` in a `size-5` frame, and the glyph + model name sit together in one `rounded-md border bg-muted/30` pill on the right side of the top row. Model name reads from `conv.adapterConfig?.model` (checked for `typeof string`) and renders in a `font-mono text-[10.5px]` span with `max-w-[140px] truncate` so long ids like `gpt-5.1-codex-max` don't blow out the column. Tooltip on the pill shows `providerName · modelName`. Row layout is now: [status] title / summary on the left, time over the model pill over tokens on the right, agent pill on a second line. `tsc --noEmit` clean; `HTTP 200` on `/`.

[2026-04-19] **Cabinet `ActivityFeed` row reflowed — agent pill + status moved to a bottom meta row so variable pill widths stop jittering the title column.** Screenshot review showed the pills ("Pattern Analyst" vs "CTO" vs "general") were pushing each title's x-position around, making the list hard to scan, and four cream/beige pills in a row blended into visual mush. Each row is now a column instead of a single flex row: the top sub-row holds `title` + 2-line `summary` on the left and the right meta cluster (`providerGlyph + time`, then `4.7k tok` below) on the right; the bottom sub-row holds `<AgentPill size="sm" />` + `<StatusIcon />` side by side. Pill width variance now lives on its own line where it doesn't affect anything else. Title column starts at a predictable x. No component API changes; `tsc --noEmit` clean, `HTTP 200` on `/`.

[2026-04-19] **Cabinet `ActivityFeed` row gets agent color pill, board-v2 status icon, and right-side model + time + tokens cluster.** Iteration on yesterday's task-list-style rewrite. Each row now reuses three components from elsewhere in the codebase rather than re-rendering its own status chip: (1) the agent's colored pill via `<AgentPill agent={…} slug={conv.agentSlug} size="sm" />` from `tasks/board-v2/agent-pill.tsx` (so the rounded chip with the agent's tint + Lucide icon + name matches the v2 board and the cabinet sidebar exactly); (2) the single-color Lucide status indicator via `<StatusIcon state={…} />` from `tasks/board-v2/status-icon.tsx` instead of the previous filled "Done / Failed / Idle" chip — `ConversationMeta.status` → `TaskStatus` (via existing `deriveStatus`) → `CardState` through a small `TASK_STATUS_TO_CARD_STATE` table inline; (3) the provider/model glyph via `<ProviderGlyph icon={…} asset={…} />` looked up from `useProviderIcons()` (one fetch, module-level cache, hooked at the top of `ActivityFeed`). Right side of each row stacks the model glyph + relative time on the top line, then the tokens (`4.7k tok`) on a second muted line — both right-aligned, tabular-nums. The center column keeps the bold title + 2-line clamped summary. Re-added the `agents: CabinetAgentSummary[]` prop on `ActivityFeedProps` (removed in the previous commit) and updated the cabinet view to pass `overview?.agents || []` so `AgentPill` can resolve the per-conversation agent's `displayName`, `iconKey`, and `color`. Old `STATUS_META`, `StatusBadge`, and `runtimeLabel` helpers gone — replaced by component reuse. `npx tsc --noEmit` clean; `HTTP 200` on `/`; no new lint output on the touched files.

[2026-04-19] **Cabinet `ActivityFeed` rebuilt with the legacy task-list row layout — colored status pill, title + relative time, summary, runtime + tokens.** Replaced the dense single-row `avatar + title + agent-name/summary inline + trigger badge + time` layout with the three-line layout from the deleted `task-list.tsx` (commit 8cb95b2): a colored `StatusBadge` chip (Idle/Running/Awaiting input/Done/Failed/Archived — `bg-{tone}/15` + matching text + tiny lucide icon) on the left, then a stacked content column with bold title + light grey relative time, a 2-line clamped summary, and a small `model · provider · 4.7k tok` meta row. The 6-state `STATUS_META` table was ported back into `activity-feed.tsx`; status is derived from each `ConversationMeta` via the existing `deriveStatus()` helper in `lib/agents/conversation-to-task-view.ts` (`archivedAt` → archived, `doneAt` → done, `status==="running"` → running, `awaitingInput` → awaiting-input, `status==="failed"` → failed, else idle) so the cabinet feed and the v2 board read from the same source of truth. List itself is now a card: `divide-y divide-border/60 rounded-xl border border-border/70 bg-card`, with `px-4 py-3.5` per row and `hover:bg-muted/40`. Dropped the agent avatar circle and the trigger badge from the row to match the cleaner reference layout — the user can still see manual/job/heartbeat distinctions on the v2 task board. Cleaned up the now-unused `agents` prop on `ActivityFeedProps` and updated the single caller in `cabinet-view.tsx`. `npx tsc --noEmit` clean; `HTTP 200` on `/`; no new lint output on the touched files.

[2026-04-19] **Cabinet page: dropped the right People rail; agent picker dropdown now uses themed thin scrollbar.** The right-side `CabinetPeopleRail` stopped earning its keep once the composer got the compact agent picker on the action row — clicking an agent there primes the composer the same way the rail did, and team presence is already covered by the agent count pill in the header + the fullscreen `OrgChartModal` one button click away. Removed the `<CabinetPeopleRail/>` element + import from `cabinet-view.tsx`, deleted `cabinet-people-rail.tsx` (no other importers — verified via grep), and collapsed the now-redundant outer flex wrapper in the view (was `flex` + nested `flex flex-col`; now a single `flex flex-col overflow-hidden`). Modals (`OrgChartModal`, job/heartbeat dialogs) render through portals so their JSX position is irrelevant. `primeTaskComposer` stays — still wired to `OrgChartModal`'s `onAgentSend` so clicking an agent in the org chart still primes the composer. Page width unchanged (`max-w-6xl` centered); the reclaimed 48–224px becomes outer whitespace. Separately, the new `AgentPickerCompact` dropdown picked up the existing `.scrollbar-thin` utility from `globals.css` (line 230, already used in `onboarding-wizard.tsx`) — `scrollbar-width: thin`, 8px webkit width, thumb tint via `color-mix(in oklch, var(--muted-foreground) …%, transparent)` so it follows the active theme automatically (light, dark, custom). One-line className change on `DropdownMenuContent`. `npx tsc --noEmit` clean; `HTTP 200` on `/`; lint warnings unchanged from last commit.

[2026-04-19] **Cabinet composer compacted; mention dropdown now flips below the textarea on this page.** Two follow-up polish items on the cabinet page composer. (1) Collapsed the agent picker out of the bottom `footer` row and into the same `actionsStart` row as the model picker, so model + agent now sit on a single compact line. The agent picker stopped being a `Select` (label-only) and is now a `DropdownMenu` whose trigger and items render the same colored circle + Lucide icon used in `CabinetPeopleRail` and the sidebar — shared via `tintFromHex` / `getAgentColor` from `cron-compute` and `resolveAgentIcon` from `icon-catalog`. New small `AgentPickerCompact` and `AgentAvatar` components live in the same file. The `selectedAgentMeta` line below the row is gone — the trigger already shows the agent name, the dropdown items show role + cabinet. (2) `MentionDropdown` got a `placement?: "above" | "below"` prop (default `"above"` to preserve every other composer surface — chat panel, AI panel, task composer, etc.); the cabinet composer now passes `mentionDropdownPlacement="below"` so typing `@` near the top of the page renders the suggestions downward instead of getting clipped under the page header. Plumbed through `ComposerInput`. `npx tsc --noEmit` clean; `HTTP 200` on `/`. Remaining lint warnings on the file (`Cannot create components during render` for `AgentAvatar`, `setState in useEffect`) match the existing project-wide patterns and are not new regressions.

[2026-04-19] **Cabinet Page v2 promoted to default; v1 removed.** Flipped the `?cabinet=v2` flag in `src/components/layout/app-shell.tsx` — `section.type === "cabinet"` now always renders the new page; lazy-init flag state + import of `CabinetViewV2` are gone. Moved `cabinet-view-v2.tsx`, `cabinet-people-rail.tsx`, `next-up-runs.tsx`, `org-chart-modal.tsx` from `src/components/cabinets/v2/` up to `src/components/cabinets/` (folder removed); `cabinet-view-v2.tsx` was renamed `cabinet-view.tsx`, replacing the legacy file (~1220 LOC of stat-strip + brown org-chart + full schedule cockpit + embedded `KBEditor`). Renamed exported component `CabinetViewV2` → `CabinetView`; dropped the "v2" badge in the header. Deleted `src/components/cabinets/interactive-stat-strip.tsx` (only v1 used it). Kept `schedule-calendar.tsx` and `schedule-list.tsx` — both are still consumed by `agent-detail-v2.tsx` and `tasks/board-v2/schedule-view.tsx`. `npx tsc --noEmit` clean; `HTTP 200` on `/`.

[2026-04-19] **Cabinet Page v2 shipped behind `?cabinet=v2`** — first pass at the redesigned cabinet dashboard, written as a side-by-side rebuild rather than an in-place mutation so v1 remains the default until we've dogfooded the new layout. Hero is the composer (full-width, top of page); under it sits a two-column row with `ActivityFeed` on the left (~2/3) and a new compact `NextUpRuns` list on the right (~1/3) that derives the next 8 events over a 7-day horizon from `getScheduleEvents()` — clicking a row opens the same job/heartbeat edit dialog as v1. Right side of the page now mounts a static `CabinetPeopleRail` (board-v2 style: avatar by default, hover expands to 56) — no DnD; click an agent and it primes the composer with that agent assigned. Header collapsed into a single tight row: cabinet name + v2 badge + agent/job/heartbeat count pills + scope pills (Own / +1 / +2 / All) + an "Org chart" button + scheduler controls + version history + header actions. The org-chart graph is too good to drop, so it lives behind the header button: extracted the existing `CompactOrgChart` into `src/components/cabinets/compact-org-chart.tsx` (renamed the `children` prop to `childCabinets` to escape React's reserved name) and wrapped it in `OrgChartModal` (`Dialog`, `h-[92vh] w-[96vw]`). v1 `cabinet-view.tsx` was updated to import the shared chart instead of re-defining it. Removed from v2: the embedded `KBEditor` at the bottom, the full schedule calendar/list section, and the `InteractiveStatStrip` band — the cabinet's own markdown stays reachable via the sidebar tree, and counts moved into the header pills. Flag is read once at mount via `useState` lazy initializer in `app-shell.tsx` (matches the `?board=v2` pattern from the tasks board rollout). PRD lives at `data/ideas/cabinet-page-v2-prd.md`. `npx tsc --noEmit` clean; `HTTP 200` against `/?cabinet=v2`. The `Cannot create components during render` lint on `cabinet-people-rail.tsx` line 60 is the same pattern the existing `board-v2/people-rail.tsx` ships with — accepted for now. Not yet verified in a real browser session.

[2026-04-19] **Phases 5 + 6 of Task Board v2 shipped** — keyboard DnD + flip default. Phase 5: added `KeyboardSensor` alongside `PointerSensor` in `tasks-board-v2.tsx` with `sortableKeyboardCoordinates` from `@dnd-kit/sortable` so Tab moves focus between cards, Space picks up, arrow keys move between cards/lanes, Space drops, Esc cancels. Sortable card wrapper in `kanban-view.tsx` gained a visible `focus-visible:ring-2 focus-visible:ring-foreground/70 focus-visible:ring-offset-2` so tabbing through the board shows the keyboard focus clearly. @dnd-kit's baked-in `aria-describedby` ("To pick up a draggable item, press the space bar...") already appears on every sortable card. Phase 6: flipped the default — `/tasks` now renders `<TasksBoardV2 standalone />` by default; `?board=v1` is kept as a short-lived escape hatch that renders the legacy `<TaskList>` with a "Back to new board" link in its header, and the heading reads "Tasks (legacy)". Replaced the `<TasksBoard>` (v1 board) import + render in `src/components/layout/app-shell.tsx` with `<TasksBoardV2>` so in-app section routing (sidebar → Tasks) also lands on the new board. `npx tsc --noEmit` clean; `/tasks` and `/tasks?board=v1` both serve HTTP 200. The legacy `src/components/tasks/tasks-board.tsx` (~1800 LOC) is now orphaned — no imports from anywhere in `src/` — and will be deleted in a follow-up commit once v2 has been exercised against real cron-driven jobs and heartbeats for a cycle. `board.yaml` on-disk state was already unused by v2 (lanes derive from `ConversationMeta` status + `archivedAt`); any legacy file-level metadata (priority, column) is simply ignored. Phase 1-6 of the PRD are now complete. Outstanding polish items (deferred, tracked in PRD §14): card density toggle, heartbeat-card collapsing, muted-tasks list, batch multi-select drag, `boardOrder` reorder persistence, legacy board deletion.

[2026-04-19] **Phase 4 of Task Board v2 shipped** — People rail drop targets for agent handoff. `/api/agents/conversations/[id]` PATCH now accepts `{ agentSlug }` and rewrites the conversation's owner (trim/validate-shape only, no agent-exists check — the existing store-level write tolerates unknown slugs and the UI only offers valid ones anyway). `board-actions.ts` gained `reassignConversation(id, toAgent, cabinetPath)`. New shared `dnd-keys.ts` centralizes the three drop-id prefixes (`lane:`, `card:`, `agent:`) + helper constructors (`laneDropId`, `cardDropId`, `agentDropId`) — both the Kanban lanes and the People rail register droppables under one shared `DndContext`. Big refactor to hoist DnD state: `kanban-view.tsx` now ONLY renders the droppable lanes + sortable cards (no DndContext of its own, no dnd handler callbacks — props trimmed to just `byLane / agentsBySlug / selectedId / now / onSelect`); `tasks-board-v2.tsx` owns the `<DndContext sensors={[PointerSensor({distance:6})]}>` that wraps both the Kanban grid and the People rail, holds `dragTaskId` state for the `<DragOverlay>` preview (rotated TaskCard with shadow-2xl), and sources its drag-end handler from a new `use-drag-handler.ts` hook. The hook is a pure function of `(byLane, agentsBySlug, onUndoQueued, onConfirmRequested, onRefresh)` returning a memoized `DragEndEvent` callback — moves all the existing Phase 2 (archive/restore) + Phase 3 (stop / restart confirm) branches out of the view component, and adds a Phase-4 AGENT_DROP_PREFIX branch at the top: if `over.id` starts with `agent:`, resolve target slug, reject self-drops, call `reassignConversation`, refresh, and queue an undo toast (`Reassigned "X" to Y` → undo reassigns back). `people-rail.tsx` each agent row is now a `<DroppableAgent>` with `useDroppable({ id: agentDropId(agent.slug) })` — active over-state shows a `ring-2` in the agent's own color (via `tint.text` inline style). Preserved: paused agents still accept drops (PRD §7.2 — pausing only disables cron, manual handoff always works), just the small Pause badge remains on the avatar. `npx tsc --noEmit` clean; `HTTP 200` from the route. Phase 4 complete per PRD §12. Next: Phase 5 keyboard DnD polish (Space-to-pick, arrow navigation, Esc cancel — half-free from @dnd-kit already via the aria descriptions), Phase 6 flip the default + delete legacy board.

[2026-04-19] **Phase 3 of Task Board v2 shipped** — destructive drops with inline confirm. `board-actions.ts` gained `stopConversation(id, cabinetPath)` (PATCH `{action:"stop"}`) and `restartConversation(id, cabinetPath)` (PATCH `{action:"restart"}`), both wrapping endpoints that already existed in `/api/agents/conversations/[id]/route.ts`. New `confirm-popover.tsx` renders a small modal-feeling card (backdrop blur, docks near bottom on mobile, center on ≥sm) with title/body/Confirm/Cancel — destructive Confirm button goes red. Mounted once in `tasks-board-v2.tsx` controlled by `pendingConfirm: PendingConfirm | null`. `kanban-view.tsx` extended `handleDragEnd` with two destructive branches: (1) source=`running` && target≠`running` → Stop confirm (body copy switches to "Stop & archive" if the target is Archive so the user knows the combined effect; on Confirm runs `stopConversation` then, if archiveAfter, `archiveConversation`; then refresh + undo toast whose `undo` calls `restoreConversation` + `restartConversation` to recover the run); (2) source=`archive` && target=`running` → Restart confirm (spawns a fresh run from the archived conversation's original prompt via `restartConversation`; runs `restoreConversation` first so the original doesn't stay archived). Esc closes the popover; clicking the backdrop dismisses. Type-check clean. Fully Phase 3 per PRD §12 — Phase 4 (People rail drop targets for agent handoff) is next.

[2026-04-19] **Phase 2 of Task Board v2 shipped** — drag-and-drop archive/restore with a 5s undo toast. Added `@dnd-kit/core` + `@dnd-kit/sortable` + `@dnd-kit/utilities` (via `--legacy-peer-deps` due to an `@emoji-mart/react` react-peer version range mismatch — not new, same issue blocks any unrelated install). `src/app/api/agents/conversations/[id]/route.ts` gained a `boardOrder?: number` field in `PatchBody` and a `typeof body.boardOrder === "number" && Number.isFinite(body.boardOrder)` guard in the updates pass; the existing `body.archived` / `body.archivedAt` field-level updates cover archive + restore without needing a new `action:` type. `src/components/tasks/board-v2/board-actions.ts` (new) wraps PATCH calls in `archiveConversation(id, cabinetPath)`, `restoreConversation(id, cabinetPath)`, and `setConversationBoardOrder(id, boardOrder, cabinetPath)`. `src/components/tasks/board-v2/undo-toast.tsx` (new) is a fixed-position `bottom-left` toast with a 5s countdown progress bar, Undo button, and X close; takes a `PendingUndo` record `{id, message, undo()}` and calls `onDismiss` when the countdown ends OR the user clicks Undo/X. `src/components/tasks/board-v2/kanban-view.tsx` rewrite: wrapped in `<DndContext sensors={[PointerSensor({distance:6})]}>`, each lane is a `<DroppableLane>` (uses `useDroppable` with id `lane:{key}`), each card is a `<SortableTaskCard>` (uses `useSortable` with id `card:{taskId}`), per-lane `<SortableContext strategy={verticalListSortingStrategy}>`. On `handleDragEnd`, resolve `sourceLane` by scanning `byLane`, resolve `targetLane` from the over id (lane prefix or card's lane), then dispatch: (a) source≠archive && target=archive → `archiveConversation` + refresh + queue undo (`restore`); (b) source=archive && target≠archive → `restoreConversation` + refresh + queue undo (`archive`); (c) everything else = no-op (Phase 2 scope). Drop target lanes show a `ring-2 ring-foreground/40` outline via `isOver`. `DragOverlay` renders a rotated card preview (`rotate-[-2deg] shadow-2xl`) while `dropAnimation={null}`. `tasks-board-v2.tsx` holds `pendingUndo: PendingUndo | null` state and passes `onUndoQueued={setPendingUndo}` + `onRefresh={refresh}` (new from hook) into `KanbanView`; `<UndoToast>` mounted at bottom-left of the board container. A11y for free: @dnd-kit's aria descriptions ("press space bar to pick up…") now appear on every sortable card. Tested end-to-end via CDP `evaluate_script` — the PATCH flow archives/restores as expected (`archivedAt` toggles between set and cleared; `200 OK`); the synthetic `drag` tool doesn't trip @dnd-kit's 6px distance activation constraint, so UI drag verification needs a real pointer. No DnD on List or Schedule views yet (Phase 2 is Kanban-only per PRD §12). Reorder-within-lane is a no-op in Phase 2 even though @dnd-kit rearranges visually on drop — persistent `boardOrder` write comes in a follow-up. Destructive drops (Stop live run, Restart from archive, Archive a running task) remain disabled until Phase 3 which adds inline confirm popovers. `npx tsc --noEmit` clean.

[2026-04-19] **Phase 1 commits 3 + 4 of Task Board v2 shipped** — Schedule view + List view + Kanban/List/Schedule view toggle. `src/components/tasks/board-v2/use-board-data.ts` now also returns `conversations` (raw `ConversationMeta[]`) and `jobs` (`CabinetJobSummary[]` from overview) alongside `tasks`, since Schedule's `ScheduleCalendar` + `ScheduleList` primitives want the raw shape, not the mapped `TaskMeta`. `view-toggle.tsx` (new) is a 3-button segmented control — `KanbanSquare` / `LayoutList` / `CalendarRange` icons, copied visual treatment from the existing `tasks-board.tsx:1526-1535` schedule sub-toggle. `list-view.tsx` (new) renders flat rows grouped by lane (sections `INBOX · NEEDS REPLY · RUNNING · JUST FINISHED · ARCHIVE` with lane-icon + count headers), each row is a local `TaskRow` with `StatusIcon` + `AgentPill` + truncated title + right-aligned relative time — skips empty sections. `schedule-view.tsx` (new) is a thin v2 wrapper around the existing `ScheduleCalendar` + `ScheduleList` from `src/components/cabinets/` (reused, not extracted — the `tasks-board.tsx` schedule pane has 430 LOC of entangled dialog/density/filter state that wasn't worth refactoring for Phase 1): minimal control bar with Calendar/List sub-toggle, Day/Week/Month mode buttons, Prev/Today/Next nav, and a date label computed with the same `calendarLabel` logic as v1. Event clicks route to `onConversationClick` which opens the DetailPanel via the shared `selectedId` state; manual-conversation dedup map is keyed `${agentSlug}|${conversationId}` to satisfy the `scheduledConversations?: Map<string, ConversationMeta>` prop. Dropped filter dropdowns / density slider / visible-hours / fullscreen from v1 — those come back as proper filter chips in a later commit. `tasks-board-v2.tsx` gained `view: "kanban" | "list" | "schedule"` state and branches the `<main>` on it; the People rail + DetailPanel stay mounted across all three views so the selection persists when switching. Verified at `http://localhost:4000/tasks?board=v2`: Schedule view renders the real week-grid with recurring **heartbeats** pinned across every weekday (DevOps Engineer 8 AM, Copywriter + Editor 10 AM, CEO/Content Marketer/CTO 9 AM) plus the 10+ past manual conversations of this cabinet laid out at their real timestamps (`Write a Html app... · CTO · 3:35 PM`, `Add page about hermiony · CEO · 1:51 PM`, etc.); List view shows the same 17 tasks grouped into 3 lane-sections (6 Needs Reply red-alerts, 1 Just Finished CTO green-check, 10 Archive muted idle). Fixed two bugs along the way: (1) a `child_process` import leaked from `@/lib/agents/adapters` barrel into the client bundle via `task-card.tsx`'s PTY badge — solved by creating `src/lib/agents/adapters/legacy-ids.ts`, a client-safe mirror with just `LEGACY_ADAPTER_TYPES` + `isLegacyAdapterType`, kept in sync with `registry.ts`'s `LEGACY_ADAPTER_BY_PROVIDER_ID`; (2) `ScheduleCalendar`'s `scheduledConversations` prop expects a `Map<string, ConversationMeta>`, was initially passed an array — built the dedup map in `schedule-view.tsx`'s `useMemo`. `npx tsc --noEmit` clean. Phase 1 nearly complete — only Cabinet + Agent filter chips left.

[2026-04-19] **Phase 1 commits 1 + 2 of Task Board v2 shipped** (per `TASK_BOARD_PRD.md`). Commit 1: additive `boardOrder?: number` field on `ConversationMeta` (`src/types/conversations.ts`) + `TaskMeta` (`src/types/tasks.ts`, also added `archivedAt?: string` which was missing from TaskMeta but present on ConversationMeta), passed through `conversationMetaToTaskMeta` in `src/lib/agents/conversation-to-task-view.ts`. Default 0/undefined; no migration needed, no readers yet (Phase 2 DnD will use it). Commit 2: new read-only board tree under `src/components/tasks/board-v2/` behind a `?board=v2` query flag on `/tasks`. Files created: `lane-rules.ts` (pure `deriveLane(task, now)` implementing PRD §6 — archived > running > needs (ask/failed) > done (fresh ≤1h) > inbox (no activity) > archive, plus `laneSort` comparator per lane), `status-icon.tsx` (fixed-color status icons per the "no breathing dot" rule — Loader2 sky-500 spinning, MessageCircleQuestion amber, AlertCircle red, CheckCircle2 emerald, ArrowDownToLine violet, Circle muted — plus `deriveCardState(task, lane)` to pick the right one), `agent-pill.tsx` (color + Lucide icon + display name via `resolveAgentIcon` + `tintFromHex`/`getAgentColor`; tolerant of missing agent — falls back to deterministic hash-color on `slug`), `use-board-data.ts` (fetches `/api/cabinets/overview` + `/api/agents/conversations` in parallel, subscribes to global `/api/agents/conversations/events` SSE with 200ms debounce on refresh, window focus refetch, 60s `now` tick bucketed to keep `byLane` memo stable; returns `{overview, tasks, byLane, agentsBySlug, loading, refreshing, now, refresh}`), `task-card.tsx` (card with agent pill + status icon + title + hover-reveal relative time), `people-rail.tsx` (48→224px on `group-hover`, display-only in Phase 1 — Phase 4 wires drops; pause badge on `!agent.active`), `detail-panel.tsx` (slide-out 460px aside with custom agent-pill + title + close-X header, embeds `<TaskConversationPage taskId variant="compact" />` so Chat/Artifacts/Diff/Logs tabs + composer + SSE stream come for free — no extraction needed), `kanban-view.tsx` (5-lane grid with collapsible Archive rail + LaneHeader), `tasks-board-v2.tsx` (entry with optional `standalone` prop for full-screen mode), and `index.ts` barrel. `/tasks/page.tsx` branches on `searchParams.board === "v2"` to render `<TasksBoardV2 standalone cabinetPath={params.cabinet ?? "."} />` instead of the legacy `<TaskList>`; added a "Try v2 board" link in the v1 header. Verified at `http://localhost:4000/tasks?board=v2` against the user's real cabinet — 17 tasks loaded, 6 in Needs Reply (all failed adapters with red AlertCircle icons + CEO/Editor/general pills in their real persona colors), 1 Just Finished in emerald (CTO fractal app), 10 in Archive (collapsed to 48px vertical rail by default). Clicking a card opens the DetailPanel with the real TaskConversationPage inside — real turns, real composer, real SSE-driven live updates, runtime model displayed. Zero console errors, `npx tsc --noEmit` clean. Old v1 board at `/tasks` (no flag) untouched. Remaining Phase 1 work: Schedule view (reuse `ScheduleCalendar` + heartbeats/jobs from `tasks-board.tsx:1466-1900`), List view, 3-button view toggle, Cabinet/Agent filter chips.

[2026-04-19] Added a **Kanban / List / Schedule** view switcher to the `/tasks-v2` demo (`src/app/tasks-v2/page.tsx`). New `ViewMode = "kanban" | "list" | "schedule"` state drives a 3-button segmented `ViewToggle` in the header (copied from the existing schedule calendar-vs-list toggle pattern in `src/components/tasks/tasks-board.tsx:1526-1535`): `KanbanSquare`, `LayoutList`, `CalendarRange` Lucide icons, same `bg-foreground text-background` active treatment. **Kanban** is the existing 5-lane board, unchanged. **List** renders a flat rows view — each task lane becomes a bordered section (`INBOX · NEEDS REPLY · RUNNING · JUST FINISHED · ARCHIVE`) with its lane icon and count in the section header; rows carry the same status icon + agent pill + optional "from {agent}" handoff label + truncated title + runs count + right-aligned relative time via a new shared `TaskRow` component. **Schedule** groups the same tasks by time bucket (`Last 5 minutes · Last hour · Today · Earlier`), sorted newest-first within each bucket, with sticky section headers (`sticky top-0 bg-background/95 backdrop-blur`) so the bucket label stays pinned while scrolling; empty buckets are filtered out. All three views share the same `selectedId` state, so clicking a row in List or Schedule opens the same slide-out `DetailPanel` as the kanban. People rail stays visible across all three views. Verified in Chrome via CDP — zero console errors, screenshots at `/tmp/tasks-v2-06-list.png` (list view with section-grouped rows), `/tmp/tasks-v2-07-schedule.png` (schedule view with 3 time buckets — 3 tasks in "Last 5 minutes", 7 in "Last hour", 2 in "Earlier"), and the earlier kanban shots. Same pattern as existing board's `boardView` state (board ↔ schedule) but lifted to the top as a peer toggle instead of a deep-link back button.

[2026-04-19] Swapped the `/tasks-v2` demo's colored pulsing status dot for **status-specific icons with fixed status colors** (user feedback: "a breathing dot in different colors is confusing"). New `STATUS_STYLE` map in `src/app/tasks-v2/page.tsx` pairs each state with a Lucide icon + Tailwind color class, all at `strokeWidth={2.25}`: `running` → `Loader2` sky-500 spinning at `animate-spin [animation-duration:1.6s]`, `ask` → `MessageCircleQuestion` amber-500, `failed` → `AlertCircle` red-500, `just-done` → `CheckCircle2` emerald-500, `handoff` → `ArrowDownToLine` violet-500 (new — was outlined circle), `idle` → `Circle` muted-foreground/50. `StatusDot` component renamed to `StatusIcon` with a `size: "sm" | "md"` prop (size-3.5 cards, size-4 detail panel header), no longer takes `agentColor` — agent color lives on the pill only, status color is fixed. Updated all three call sites: `TaskCard` header, `DetailPanel` header (now `size="md"`), and the nested conversation runs list. Verified in Chrome via CDP: running cards now show a crisp sky-blue spinner (motion conveys liveness without color conflict), Needs Reply column has amber question-mark icons + one red alert for the failed RCA task, Just Finished shows emerald check-circles, Inbox handoff card gets a violet download-into-tray icon. Zero console errors; screenshots at `/tmp/tasks-v2-04-icons.png` (board) and `/tmp/tasks-v2-05-detail-icons.png` (detail panel with mixed-status runs). PRD's §5 card-anatomy bullet should be updated from "status dot" → "status icon" when we sync the spec.

[2026-04-19] Removed `SectionMode = "ops" | "cabinet"` from `src/stores/app-store.ts` and every call site. Root is already a cabinet (`ROOT_CABINET_PATH = "."`) and `CabinetVisibilityMode` already controls breadth, so the two-coordinate scope model was redundant. Now `cabinetPath` is the only scope discriminator across scoped sections (`page | agents | agent | tasks | task | jobs | cabinet`), defaulting to `ROOT_CABINET_PATH` when callers don't specify. Hash URLs collapse to a single shape: `#/cabinet/{path}/...` for all scoped views, plus `#/home`, `#/settings`, and `#/page/{pagePath}` as existing shortcuts. Deleted the `#/ops/*` branch entirely — old bookmarks in that shape now fall back to `#/home` (accepted clean break; this is a new project). Dropped the `workspaceMode` prop from `AgentsWorkspace`, `TasksBoard`, and `JobsManager`, along with the `resolvedWorkspaceMode` fallback logic at each site; all three now derive their behavior from `cabinetPath` vs `ROOT_CABINET_PATH`. Touched: `stores/app-store.ts`, `hooks/use-hash-route.ts`, `components/sidebar/{tree-view,tree-node,new-cabinet-dialog,recent-tasks}.tsx`, `components/agents/agents-workspace.tsx`, `components/tasks/{tasks-board,task-detail-panel}.tsx`, `components/jobs/jobs-manager.tsx`, `components/cabinets/cabinet-view.tsx`, `components/layout/{app-shell,notification-toasts}.tsx`, `components/home/home-screen.tsx`, `components/shortcuts/keyboard-shortcuts.tsx`. `npx tsc --noEmit` passes; lint has no new errors beyond pre-existing ones. PRD archived at `.claude/plans/yes-write-prd-first-fluffy-prism.md`.

[2026-04-19] Scaffolded a static demo at `/tasks-v2` (`src/app/tasks-v2/page.tsx`, single-file client component) to preview the redesigned Task Board from `TASK_BOARD_PRD.md` before implementing Phase 1 for real. Five hardcoded agents (CTO indigo / PM emerald / Growth amber / QA rose-paused / Editor violet) with Lucide icons, 12 mock tasks spread across the 5 fixed lanes (`Inbox 2 · Needs Reply 3 · Running 3 · Just Finished 2 · Archive 2 collapsed`). Renders the full card anatomy: agent pill via `tintFromHex` (color + icon + name), status dot (pulsing agent-color ring for running via `animate-ping`, amber for ask, red for failed, emerald for just-done, outlined for handoff, grey for idle), 2-line title clamp, and a hover-reveal meta row showing relative time + run count + a contextual action pill (Start / Resume / Retry). Archive column collapses to a 48px vertical rail with rotated "ARCHIVE · N" label; click expands. People rail on the right (48→224px on `group-hover`) with avatar-only collapsed / full agent cards on expand (Ready/Paused status + pause badge on QA avatar). Clicking a card slides out a 440px right-docked detail panel showing the agent pill, relative time, title, action buttons (Stop for running or Start new run for idle, Archive, Reassign), a Conversation runs list (3 runs for the Q2 board deck card with mixed running/done dots), and a Send composer at the bottom. Verified in headless Chrome via CDP at `http://localhost:4000/tasks-v2` — HTTP 200, zero console errors, three screenshots captured showing initial state, card-click opening the detail panel, and Archive expanded with People rail hovered. Dev server reuses the existing one at port 4000 via `scripts/dev-next.mjs`. No DnD wired yet — interactions (Start/Stop/Archive/Reassign, drag-to-handoff) are visual affordances only; Phase 1 of the rollout wires read-only v2 first, DnD in Phase 2.

[2026-04-19] Collapsed the sidebar agent-list into a single render path in `src/components/sidebar/tree-view.tsx`. "General" is now always shown at top, Editor is pinned first, and the status dot uniformly reflects `runningCount > 0` (running right now) instead of `active` (enabled). Per-agent click handler still chooses `cabinet` vs `ops` routing mode based on whether the agent has a `cabinetPath`, so URL hashes keep working; only the duplicated rendering branches were removed. Context-menu "Edit agent" is available for any cabinet-scoped agent regardless of the active view.

[2026-04-19] Wrote `TASK_BOARD_PRD.md` at the repo root — a focused design spec for redesigning `/tasks` into an attention-first kanban of AI conversations. Five fixed columns (`Inbox → Needs Reply → Running → Just Finished → Archive (collapsed)`) derived from conversation state instead of `board.yaml`, one card per task carrying an agent pill (color + icon + name) and a status dot matching the sidebar legend, and a slide-out right-side detail panel for nested conversation runs. DnD is the control surface: drops trigger real actions via a full drop matrix (Inbox→Running = Start, Running→Archive while live = Stop+Archive with inline confirm, Archive→Running = Restart, etc.), non-destructive drops fire with a 5s Undo toast and destructive ones show an inline confirm popover. Cross-agent handoff happens by dropping a card onto a right-side **People rail** (48→220px on hover) of agent avatars — card lands in recipient's Inbox, never auto-starts, paused agents accept drops with just a badge. Spec covers card anatomy, full drop matrix, live-SSE reconciliation rules (don't yank hovered cards), keyboard DnD (Space-to-pick / arrows / Space-to-drop / Esc), data model (new `archivedAt` + `boardOrder` fields on `AgentTask`, `board.yaml` deprecation strategy with a one-time `mtime → archivedAt` migration), API additions (`archive` / `restore` / `reorder` / `reassign` task actions + `task.reordered` SSE event), a 6-phase rollout behind a `?board=v2` flag, success metrics (p50 start-time from handoff, zero accidental live-run kills), and 5 deferred open questions (density toggle, heartbeat-card collapsing, filter set, muted tasks, batch actions). Cross-references `data/getting-started/index.md` §Agents & Tasks at a Glance, `AGENT_PAGE_PRD.md` §5.3b for the Inbox wait-don't-execute invariant, and `recent-tasks.tsx` for the just-finished 1h rule to reuse.

[2026-04-19] Enriched the sidebar **task status dots** in `src/components/sidebar/recent-tasks.tsx`: running tasks now pulse in the owning agent's own color (resolved from the overview `persona.color` via `tintFromHex`, with a deterministic `getAgentColor(slug)` fallback), so you can tell at a glance *which* agent is working. Needs-reply stays amber, failed stays red, and a new derived "just finished" state flips idle/done tasks to emerald for 1 hour after `lastActivityAt` (a local `setInterval` ticks the component every minute so fresh-green fades back to muted without waiting for an SSE event). Tooltips now spell out the state in plain English (*"Write Voldemort poems — running"*, *"PR babysitter — just finished"*). Tree-view passes its existing agents fetch (`/api/cabinets/overview`) down so no extra network call is introduced. Documented the full legend in a new **Agents & Tasks at a Glance** section inside `data/getting-started/index.md` (between Content & Features and Keyboard Shortcuts): a dot-color/meaning table, the three-state agent chip, the color-discipline rule (agent color appears only on avatar + chip dot + composer focus ring + sidebar running dots), and a short note on the Inbox wait-don't-execute convention.

[2026-04-19] Added the agent page's **Inbox** section (`src/components/agents/agent-detail-v2.tsx`) — an `AgentTask` queue rendered between the composer and Conversations when the agent has pending/in-progress assignments. Fetches from `GET /api/agents/tasks?agent={slug}` (alongside the existing persona / conversations / jobs parallel fetch) and reads `AgentTask[]` per `src/types/agents.ts`. Hidden entirely when no tasks are pending — no "Inbox (0)" clutter. Row anatomy: status icon (Inbox muted for pending, spinner for in_progress), title + priority chip (P1 red, P2 amber, P3 neutral, P4-P5 fainter), single-line description preview, meta line `from {fromName || fromAgent} · {relative createdAt} · Running → (if linked)`. Hover reveals a **Start** button on pending rows without a linked conversation; clicking Start POSTs `/api/agents/conversations` with `userMessage = "${title}\n\n${description}"` + `mentionedPaths = kbRefs`, then POSTs `/api/agents/tasks {action:"update", status:"in_progress", linkedConversationId, startedAt}` per the existing endpoint contract, and routes to the new conversation. Click a row body: if `linkedConversationId` is set we open that run; else fall through to Start. Design decision codified in `AGENT_PAGE_PRD.md §5.3b`: tasks **wait in inbox** until explicit Start — prevents `@CTO` mention-bombing from burning tokens and keeps the "humans define intent" invariant. Doc updated (section added to layout, Inbox contract written, future `@mention → AgentTask` bridge described as a thin client-side POST to `/api/agents/tasks` on mention resolution, landing in the recipient's inbox with `priority: 3` by default).

[2026-04-19] Agent page v2 — Phase B wave 1 shipped (per `AGENT_PAGE_PRD.md` §10.2). **Inline Add Routine form**: `ScheduleSection` in `agent-detail-v2.tsx` now owns add-form state (name input, `SchedulePicker`, prompt textarea, Cancel / Create routine buttons with spinner); clicking `+ Add routine` expands an inline `<li>` card below the jobs list instead of alerting. Submit POSTs to `/api/agents/{slug}/jobs` and refreshes. **⋯ menu wired** via shadcn `DropdownMenu` (`Download` + `Trash2` icons): *Export persona* opens `/api/agents/personas/{slug}/export` in a new tab so the browser handles the file download; *Delete agent* uses `window.confirm` with the agent display name in the prompt, calls `DELETE /api/agents/personas/{slug}`, and navigates back on success (or alerts on failure). Delete item is styled red (`text-red-500 focus:text-red-500 focus:bg-red-500/10`) to telegraph destructiveness. **See all → wired** to the tasks board: added `agentScopedId` seed to `tasks-board.tsx:882` so `TasksBoard` reads `useAppStore.getState().section.agentScopedId` on mount and preselects the filter chip (falls back to `"all"` when absent); v2's `ConversationsSection` exposes an `onSeeAll` prop which `app-shell.tsx` wires to `setSection({ type: "tasks", agentScopedId: "${cabinetPath}::agent::${slug}", cabinetPath, mode })`, landing the user on a tasks board already filtered to this agent.

[2026-04-19] Sidebar cleanups in `src/components/sidebar/tree-view.tsx`: Tasks section is now collapsible via its own `tasksExpanded` state + rotating chevron (matching Agents / Data), and the per-agent subtitle (`agent.role`) is removed from the Agents list so rows show only the agent name + status dot. Subtitle wrapper span was flattened into a single truncating span; `showSubtitle` logic dropped.

[2026-04-19] Shipped agent page v2 as the canonical agent surface. `src/components/agents/agent-detail-v2.tsx` replaces the v1 `agent-detail.tsx` (deleted) and is now wired from `app-shell.tsx:262` when `section.type === "agent" && section.slug`, with `onBack` + `onOpenConversation` callbacks routing via `useAppStore.setSection`. The PRD is at [`AGENT_PAGE_PRD.md`](./AGENT_PAGE_PRD.md); the v1 spec (`AGENT_PAGE_SPEC.md`) is archived with a superseded banner. V2 is a single scroll column, 840-px max-width profile: top bar (Back · Schedule toggle · Active/Stopped · ⋯), 64-px identity row with three-state chip (Working pulsing / Ready static / Paused muted), sticky Tiptap-ready composer with agent-color focus ring + slug-tailored suggested prompts, then inline sections — **Conversations** (max 7, six-state row rendering Working / Needs reply amber-highlighted / Done / Failed / Cancelled dimmed / Closed auto-computed at 7d of inactivity, each row surfacing trigger icon + relative time + duration), **Recent work** (max 5 deduped artifact paths aggregated from `conversation.artifactPaths`, each opens `#/page/{path}`), **Schedule** (heartbeat row + jobs with on/off toggle + Play, `+ Add routine`, `Manage →` routes into the full `ScheduleCalendar` with Day/Week/Month picker filtered to this agent), **Details** (compact 6-col always-editable field grid — display name, role, department, type, tags, workspace, provider read-only — blur or Enter commits per-field), and a full-width **Tiptap persona editor** at the bottom (no section chrome) using the same `editorExtensions` as KB pages, driven by `markdownToHtml` on load and `htmlToMarkdown` on save with ⌘↵ + Esc shortcuts scoped to the editor DOM. The agent's `color` appears in exactly three places (avatar, status chip dot, composer focus ring) and nowhere else — color discipline codified in the PRD. `/agent-preview/[slug]` standalone route kept as a headless/embed entry with a "DEMO · AGENT PAGE V2" banner.

[2026-04-18] Rewrote the Schedule page's List view as a divided single-line list instead of a 3-column grid of cards. Each row now shows, on one line: a small type-icon (Clock3 for job, HeartPulse for heartbeat, MessageSquare for manual), the agent emoji, the title in bold, and — when present — the conversation's summary as muted secondary text after a middle-dot. Right side carries agent name + schedule/timestamp (hidden below md) and the status chip (ON/OFF for scheduled, COMPLETED/FAILED/RUNNING for manual). `ListItem` got a new `summary?: string` field populated from `ConversationMeta.summary` (deduped against the title). Outer container is now `divide-y divide-border/60 rounded-lg border` — no per-card shadow or border color. Dropped the unused `getAgentColor` import.

[2026-04-18] Unified the agent page's Recent activity rail on top of the new conversation system (per `TASKS_CONVERSATIONS_PRD.md` §1.1 invariant: one store, one `ConversationMeta` for manual/job/heartbeat). `AgentDetail` now fetches `/api/agents/conversations?agent=${slug}&limit=50` in parallel with the persona; `ChatTab` was re-typed from `history: HeartbeatRecord[]` → `conversations: ConversationMeta[]`. Each rail card carries a trigger-aware icon (`MessageSquare`/`Briefcase`/`Sparkles`), a status badge driven by `ConversationStatus` (`running`→spinner, `completed`→green check, `failed`→red X), the title (falls back to `summary` → "Untitled"), the trigger label, relative time from `lastActivityAt || startedAt`, and a computed duration (`completedAt - startedAt` when finished). Clicking a recent card routes through a new `openConversation` callback that calls `setSection({ type: "task", taskId, mode, cabinetPath })` — the same entry point `tasks-board.tsx` uses, so the rail opens the full in-shell `TaskConversationPage`. Pre-existing `HeartbeatRecord`-based rail with its unused summary regex is gone. Hero stats strip and HistoryTab still read `HeartbeatRecord[]` for now — flagged as a follow-up migration so legacy heartbeat history keeps rendering until the store-to-conversation cutover is ready.

[2026-04-18] Redesigned the individual agent page (`src/components/agents/agent-detail.tsx`) with a bold color-wash hero and a chat-first layout. The hero gradients from the agent's `color` (via `tintFromHex`) → transparent, pairs a glowing 80px `AgentIdentity` with a display-size name, status dot, and Run/Pause actions, and carries a stats strip below (runs this week, avg duration, last seen, cron summary via `cronToHuman`) — all computed client-side from the existing `HeartbeatRecord[]` history. Replaced the vertical left-rail tabs with a horizontal pill nav (Chat · Definition · Automations · History) and swapped the default tab from `definition` → `chat`. New `ChatTab` is a split layout: an empty-state composer on the left that starts a `WebTerminal` live session (same provider/adapter plumbing as the old `SessionsTab`), and a 260-px "Recent" rail on the right showing the 6 most recent heartbeat records with a "Show all →" that routes to the History tab. Renamed `SessionsTab` → `HistoryTab` (unchanged behavior; kept the full-width explorer) and renamed `JobsTab` → `AutomationsTab`, wrapping its return in a new Heartbeat section at the top (SchedulePicker bound to `persona.heartbeat`) above the existing Scheduled Jobs list — the heartbeat field was correspondingly removed from `DefinitionTab` (Workspace now spans the grid). Deleted the orphaned `HeartbeatField` component. Jobs runs aren't yet merged into the Recent rail (still only sessions/heartbeats) pending a check on whether job executions write separate records or flow through `HeartbeatRecord`.

[2026-04-18] Unified the schedule page: renamed "Jobs & Heartbeats" → "Schedule" (button + page title), added past manual conversations as first-class calendar pills alongside jobs/heartbeats, and added a trigger chip row (All / Tasks / Jobs / Heartbeat) above the calendar + list that shares state with the board-view chips so the filter persists when you switch. Types: `ScheduleEvent.sourceType` now includes `"manual"` with `conversationId` + `conversationStatus` passthrough. New `getManualScheduleEvents(conversations, agents, rangeStart, rangeEnd)` in `cron-compute.ts` synthesizes past-only `ScheduleEvent` rows from `ConversationMeta[]` with `trigger === "manual"` — future slots stay job/heartbeat-only since manual runs aren't cron-driven. `ScheduleCalendar` accepts `manualConversations?` and merges them into the events array; `isEventMissed` short-circuits `"manual"` (manual runs actually happened, never "missed"). `ScheduleList` renders manual entries with a MessageSquare icon + status chip (COMPLETED / FAILED / RUNNING) instead of ON/OFF cron toggle. Tasks-board wires `scheduleFiltered{Agents,Jobs,Manual}` from the trigger chip: `manual` → empty agents/jobs, only manual pills; `job` → only jobs; `heartbeat` → only heartbeat-bearing agents; `all` → everything. `handleScheduleEventClick` branches on `sourceType === "manual"` and routes to the conversation viewer via `openConversation`. Click-through verified live: Bellatrix manual pill → `#/cabinet/./tasks/...-copywriter-manual`. Calendar + List both render correctly; chip filters flip the visible data instantly.

[2026-04-18] Added `data/have-fun/bellatrix.md` — a seven-part poem sequence for Bellatrix Lestrange (Toujours Pur, The Devotion, Longbottom, Azkaban, The Duel, Molly, Epitaph), stylistically matching the existing `have-fun/voldemort/` sequence.

[2026-04-18] Updated `TASKS_CONVERSATIONS_PRD.md` to reflect the shipped wave: G7/G8/G9/G10/G11/G12/G13/G15/G17 flipped to 🟢 with evidence pointing at the live-run verification; added §14.5 "Cross-process event bus (Next.js ↔ daemon)" as a hard contract with new entries G22 (first-turn live SSE) and G23 (sidebar tree refresh on nested agent writes), both 🟢; added invariant #11 to §1.1 codifying that UX-relevant events must be emitted from (or polled by) the Next.js process since the daemon's event bus never reaches SSE subscribers; added §13 smoke tests S11 (live first-turn stream) and S12 (tree refresh on nested writes); rewrote §14.6 implementation order into Completed / Remaining lists.

[2026-04-18] Fixed two live bugs reported against the task/conversation flow: (1) first-turn chat didn't update until F5, and (2) sidebar tree stayed stale after agents wrote KB files anywhere deeper than the data root. Root cause of (1): the daemon runs in its own Node process, so its in-process `publishConversationEvent` calls never reached Next.js SSE subscribers, and the first-turn path only flowed through the daemon (no turn.appended/turn.updated events). Fix: emit `task.updated` from the Next.js side — on `createConversation`, on terminal completion in `waitForConversationCompletion` (announcing whichever process actually finalized), and throttled (500ms) during `appendConversationTranscript`; also dropped the 3s poll in `waitForConversationCompletion` to 700ms so live transcript diffs drive a stream of `task.updated {streaming:true}` events during the run. Root cause of (2): the `/api/agents/events` SSE's tree-change detection shallow-diffed `fs.stat(DATA_DIR)` mtime + top-level entry count, so nested writes like `data/have-fun/voldemort/index.md` never tripped it. Fix: when `drainConversationNotifications` returns any completed conversation, emit a `tree_changed` with `reason:"conversation_completed"` and refresh `lastDataVersion` so the AppShell's existing `tree_changed` listener calls `loadTree()`. Verified live against fresh `dev:all`: a new first-turn conversation produced 3 SSE events on `/events` (two `streaming:true` at 5.6s/6.3s + terminal `status:completed` at 6.3s; previously zero); the task page rendered the user turn instantly and the agent response streamed in; sidebar DATA section showed `Have fun → Voldemort` and newly-created `stream-test` without any reload.

[2026-04-18] E2E verification of the G7/G8/G9/G10/G13/G15/G17 wave against a fresh `npm run dev:all` daemon on :4100 + Next.js on :4000. Found and fixed a real bug discovered during testing: the daemon's `finalizeSessionConversation` (server/cabinet-daemon.ts) finalizes the conversation before `waitForConversationCompletion` polls, so the Next.js-side finalize path never wrote tokens to `meta.tokens`. Fixed by threading `adapterUsage` + `adapterErrorKind/Hint/RetryAfterSec` directly into the daemon's own `finalizeConversation` call. Also added `classifyError` invocation to the daemon's spawn-time catch branch so `ENOENT` (CLI not installed) surfaces as `cli_not_found` instead of `unknown`. Verified live: claude-code two-turn conversation produced `meta.tokens={input:6,output:39,cache:32929,total:45}` and `session.json` with `resumeId + codecBlob + displayId: "Claude · 37c9e295"`; continue turn's `lastResumeAttempt.result = "resumed"` confirms G8 resume path actually used the rehydrated codec blob; grok-cli test produced `meta.errorKind="cli_not_found"` + UI "cli not found" chip + remediation banner + Logs tab Classified-error section; runtime switch from Opus→Haiku mid-conversation wrote `adapterConfig={model:"claude-haiku-4-5",effort:"low"}` to meta, kept `resume=resumed` on same adapter, and Haiku answered "Tokyo"; Logs tab shows 12 sequentially-numbered events (seq=1..12) confirming G17 seq persistence; session bar renders "alive · Claude · 37c9e295 · claude_local" using the codec displayId. All 7 gaps work end-to-end against real providers.

[2026-04-18] Implementation of `TASKS_CONVERSATIONS_PRD.md` gaps G7, G8, G9, G10, G13, G15, G17 — the full highest-leverage wave from §14.5. Types: extended `ConversationMeta` with `errorKind/errorHint/errorRetryAfterSec/lastResumeAttempt`, added `ConversationErrorKind` + `ConversationErrorClassification` + `ConversationResumeAttempt`, extended `SessionHandle` with `codecBlob + displayId`. Adapter contract: `AgentExecutionAdapter.classifyError` now optional on every adapter. New shared `src/lib/agents/adapters/error-classification.ts` covers CLI-not-found / auth / rate-limit / session-expired / context-exceeded / timeout / transport / unknown; each of the 8 `*-local.ts` adapters now implements `classifyError` via the shared classifier. Added `sessionCodec` to `claude-local` (resumeId) + `codex-local` (threadId); cursor/opencode/pi already had codecs. All adapters now return `sessionParams` alongside `sessionId`. Daemon (`server/cabinet-daemon.ts`): `StructuredSession` carries `adapterSessionParams + stderrBuffer + adapterErrorKind/Hint/RetryAfterSec`; `createStructuredSession` passes `ctx.sessionParams`, buffers stderr, calls `adapter.classifyError` on failure, and writes `session.json.codecBlob` via `sessionCodec.serialize` on success. `/session/:id/output` response now includes `adapterSessionParams` + `adapterErrorKind/Hint/RetryAfterSec`. `POST /sessions` accepts `adapterSessionParams`. `daemon-client.ts`: `pollDaemonSessionUntilDone` + `getDaemonSessionOutput` return the full shape. Runner (`conversation-runner.ts`): `waitForConversationCompletion` now threads `adapterUsage` → `meta.tokens` via extended `finalizeConversation` (G7); `continueConversationRun` accepts `providerId/adapterType/model/effort` per-turn override, rehydrates `session.codecBlob` via `sessionCodec.deserialize` into `ctx.sessionParams` before execute, serializes `result.sessionParams` back to `codecBlob` after success (G8), calls `adapter.classifyError` on any failed run and writes `meta.errorKind/errorHint/errorRetryAfterSec + lastResumeAttempt` (G9+G10), switches resume → replay mode when runtime changes mid-conversation. `finalizeConversation` in `conversation-store.ts` accepts `tokens + errorKind/Hint/RetryAfterSec`. `appendEventLog` now assigns monotonic per-conversation `seq` (in-memory counter, cold-started from existing line count) and returns it; publishConversationEvent threads `seq` into the bus event. New `readEventLog(id, {fromSeq})` helper. SSE route (`conversations/[id]/events`) now honors `Last-Event-ID` header + `?lastEventId=` query, replays missed events from `events.log` before subscribing to the live bus, and emits `id: <seq>` on every event (G17). `POST /[id]/continue` accepts `providerId/adapterType/model/effort` and returns canonical `{ok:false, error, errorKind}` envelope on failures. UI: `TaskComposerPanel` fully rewritten to import the shared `ComposerInput + useComposer + TaskRuntimePicker` primitives, lazy-loads the tree as mentionable items, accepts `initialRuntime` + `onSend({text, mentionedPaths, runtime})`, snaps back to conversation runtime after send (G15). `task-client.postTurn` accepts `{mentionedPaths, runtime}` and POSTs them. `TaskConversationPage` handler refactored for the new payload; header shows errorKind chip + hint when present. `tasks-board.tsx::ConversationRow` + `task-detail-panel.tsx` render `<ProviderGlyph>` using new `useProviderIcon` hook (module-level cache, one fetch across the app) and an errorKind chip when set (G13). `TaskMeta` + `conversationMetaToTaskMeta` carry the new fields through. `LogsPanel` adds a "Classified error" section + "Session" section showing codec-derived display id. Verified: `tsc --noEmit` clean, `tsx --test` 22/22 pass across conversation-store-turns + adapter + registry suites, Chrome DevTools smoke test on running server confirms runtime picker renders with provider tabs/model rows/effort radios, `POST /continue` with `{providerId,adapterType,model,effort}` returns 202, SSE route accepts `lastEventId` and emits `text/event-stream`, provider glyph renders on task cards.

[2026-04-18] Full restructure of `TASKS_CONVERSATIONS_PRD.md` as a contract-shaped spec: organized around shared primitives (Part A Contracts, Part B Surfaces, Part C Operations, Part D Proof, Part E Reference). New §2 Shared-Code Contract enumerates the seven shared primitives with correct file paths (`composer/composer-input`, `useComposer`, `conversation-client`, `conversation-runner`, `conversation-store`, adapter registry, `TaskConversationPage`); new §5 API Contract with full payload shapes + canonical error envelope; new §7 Adapter Contract including proposed `classifyError` + `ConversationErrorKind` taxonomy and full session codec rules; new §8.2 surface×shared-code reuse matrix; new §13 verification matrix (11 surfaces × 8 providers = 88 cells) with seven per-cell acceptance criteria + ten secondary smoke tests; Gap Register (§14) folded inline as G1–G21 with status + unblocks; Appendix A adds a step-by-step adapter-authoring cookbook for a hypothetical `foo-cli`. Diagrams, data model, and original content preserved.

[2026-04-18] New `TASKS_CONVERSATIONS_PRD.md` at repo root: canonical spec for the v2 tasks/conversations continuity layer + the 8-provider CLI runtime. Enumerates all 11 prompt entry points, includes Mermaid sequence/component/state diagrams for prompt→daemon→adapter→persistence→SSE→UI, full provider + adapter matrix, per-turn runtime switching, and an audit appendix with 🟢/🟡/🔴 status per integration point (file:line evidence) plus a suggested implementation order.

[2026-04-18] Registry imports now cache the full GitHub tree response in-process (10 min TTL) so multiple back-to-back imports don't burn through the 60/hr unauthenticated API budget. On 403/429 the error message now surfaces the rate limit clearly and mentions `GITHUB_TOKEN` as the fix (5000/hr when set). Documented the optional token in `.env.example`.

[2026-04-18] Added a one-time "Cabinet is under breaking changes" warning dialog. Shown on first mount if the user hasn't acknowledged it — stored under localStorage key `cabinet.breaking-changes-warning-ack:v1`. Mounted in `AppShell` after the onboarding wizard resolves, so new users see it right after setup, and existing dev-branch users see it once on next load. Clearing the key re-triggers it.

[2026-04-18] Moved the `getting-started/` seed template from `/data` into repo-committed `/resources/getting-started/` so it survives data-dir resets. `seedGettingStartedDir` now checks `PROJECT_ROOT/resources/getting-started/` first and falls back to the legacy `/data/getting-started/` locations for existing installs. New onboarding runs automatically copy the seed into the user's data dir as part of `scaffoldCabinet`.

[2026-04-18] Guarded the RegistryBrowser import dialog against double-fire. The Import button lacked a busy-state `disabled` guard, so a fast second click fired a parallel POST — the first one created the directory, the second hit the "Directory already exists" 409. Added `disabled={importing}` on both buttons, a re-entry guard at the top of `handleImport`, and a spinner on the Import button while the request is in flight.

[2026-04-18] Post-import "cabinet created" screen now shows a `Home › Room` breadcrumb above the tree so the user can see where the new cabinet lives (e.g., `🏠 Maya's Home › 💼 The Office`). Uses Lucide House + the room's configured icon. `homeName` and `roomType` are threaded from `TeamBuildStep` down into `CabinetCreatedScreen`.

[2026-04-18] Tightened line-height on the post-import "cabinet created" tree diagram. `leading-relaxed` (1.625) left visible vertical gaps between box-drawing `│` connectors; switched to `lineHeight: 1.25` so the tree renders as a continuous structure.

[2026-04-18] Fixed broken registry import during onboarding. The `RegistryBrowser` detail view was hardcoded to `window.location.reload()` on successful import, which in the onboarding overlay restarted the wizard from scratch (workspace.json isn't written until launch) and left the first import stranded — the retry then hit "Directory already exists". Added an optional `onImported(template, name)` callback to `RegistryBrowser` / `DetailView`; when provided it replaces the reload. Onboarding now hands that callback through and jumps straight to the success screen. Also hardened the import path: `downloadRegistryTemplate` and the `/api/registry/import` route now rm the partial `targetDir` on any mid-flight failure so retries aren't blocked by leftover dirs.

[2026-04-18] Cards with vertical scroll now show a subtle visible scrollbar instead of hiding it. New `.scrollbar-thin` utility in `globals.css` (8px wide, themed via `color-mix(--muted-foreground)`, transparent track, padding-clipped thumb so it reads as a soft pill). Replaced `scrollbar-hide` + inline `scrollbarWidth: none` on the onboarding agent picker grid with `scrollbar-thin`, and applied to the Launch step's two halves (workspace summary + agent chat preview). Horizontal tab/toolbar strips (`scrollbar-none`) keep their hidden scrollbars — those are intentionally chrome-free.

[2026-04-18] Onboarding welcome blueprint now wraps the popup like a courtyard. Restructured `home-blueprint-background.tsx` from a 3-top/2-bottom room grid into a 3×3 layout with a central PATIO (340-660, 180-440 in viewBox units) where the welcome card lands. 8 rooms surround it (OFFICE / STUDY / LAB across the top, LIBRARY / KITCHEN flanking the patio, FAMILY / DINING / STUDIO across the bottom). Each adjacent interior room has a doorway arc opening onto the patio (top from STUDY, bottom from DINING, sides from LIBRARY and KITCHEN). Patio decoration: a dashed garden-path rect inset 18px, four corner plants (circle + stem), and a tiny "PATIO" label tucked at the top edge above the popup. Room furniture moved to outer walls so it never crowds the card. Decorative dots repositioned out of the patio area.

[2026-04-18] Compressed onboarding from 10 → 9 steps. Dropped the Home-name field (auto-derived as `${name}'s Home` at submit time since the rooms sidebar isn't built yet). Merged "Pick a room" and "About you" into a single step: 4 room cards at top, cabinet-name + description + (conditional) team-size in a card below, all labels adapting to the active room card. Renumbered step indices: welcome-home=1, room-setup=2, team=3, provider=4, community=5-7, launch=8. Room-name input is gone; each room just takes its type label ("The Office", "Study", "Research Lab", "Family Room"). Agent picker categories are now room-aware — irrelevant B2B departments collapse under "From the Office" in non-office rooms instead of flooding the picker.

[2026-04-18] Onboarding reframed around Home > Rooms > Cabinet with four room types (Office / Study / Research Lab / Family Room), each with its own mandatory lead + editor-equivalent, agent suggestions, keyword matcher, and starter-team gallery. Added two new steps ahead of the old wizard: "Welcome home" (names the Home) and "Pick a room" (four-card picker with a ghost "+" previewing the future rooms sidebar). The old "Welcome / About you" step now reads all its labels (`workspaceLabel`, `descriptionLabel`, `askTeamSize`, placeholders) from `src/lib/onboarding/rooms.ts::ROOMS[roomType]`. Mandatory CEO+Editor became room-specific: Office keeps CEO+Editor; Study uses Assistant+Librarian; Lab uses Research Lead+Librarian; Family Room uses Home Manager+Planner — with 18 new persona.md files shipped in the library (assistant, librarian, writing-coach, inbox-triage, calendar-keeper, habit-tracker, tinkerer, research-lead, lit-reviewer, note-synthesizer, teaching-assistant, citation-keeper, home-manager, planner, meal-planner, grocery-buyer, kid-coordinator, budget-keeper). Config file became `workspace.json` v2 — `{home:{name},room:{id,type,name},cabinet:{name,description,size}}` — forward-compatible with multi-room. `company.json` is still written for backward compat, and `GET /api/agents/config` synthesizes the new shape from a legacy file when needed. Persona template substitution in the setup route now accepts both `{{company_*}}` (legacy) and `{{workspace_*}}` / `{{home_name}}` (new), so existing library personas keep working. Launch-step chat greeting + specialist replies + closing now vary by room type. `library-manager.ts` gained `getMandatoryAgentSlugs(roomType)` / room-aware `mergeMandatoryAgentSlugs`.

[2026-04-17] Editable agent identity — each agent in the sidebar can now have a custom display name ("Steve"), a picked Lucide icon, a picked hex color, and an illustrated avatar. Right-click an agent row → "Edit agent" opens a dialog with name input, a 40-icon curated grid (`src/lib/agents/icon-catalog.ts`), the shared 8-color palette (now moved out of `cron-compute.ts` into `src/lib/themes.ts` as `AGENT_PALETTE` + a new `tintFromHex` helper — single source of truth for palette) with freeform hex input, and a 12-preset avatar picker (`public/agent-avatars/*.svg`, generated by `scripts/generate-agent-avatars.mjs` as featureless shoulders-up silhouettes to dodge uncanny-valley and role stereotyping) plus PNG/JPG/SVG upload to `data/.agents/{slug}/avatar.{ext}` via a new `POST /api/agents/personas/[slug]/avatar`. All identity fields (`displayName`, `iconKey`, `color`, `avatar`, `avatarExt`) live in the persona.md frontmatter — no DB, no sidecar — and are all optional with hash-based fallback so existing agents keep their current look. New shared `<AgentIdentity>` component renders avatar-as-circle when set, else icon-in-tinted-square (same look as before). Wired into the sidebar cabinet-agent list and the agent-detail page header; emoji-based agents-workspace cards and composer chips not yet migrated — follow-up.

[2026-04-17] Copywriter: created a narrative story "The Wand and the Mind" about Harry and Hermione's magical confrontation at Hogwarts. The duel explores their philosophical conflict—raw power vs careful reasoning—and resolves their estrangement through honest confrontation. Story added to data/harry-vs-hermione.md with proper frontmatter (title, created, tags, order).

[2026-04-17] Added `SIDEBAR.md` at the repo root — living design doc for the left sidebar. Covers visual hierarchy (font sizes, hover treatment, indentation, scrollbar scoping, +button affordance), file layout, and the full move/reorder architecture (data model with frontmatter + `.cabinet-order.yaml` sidecar, integer-with-gap-of-100 indexing, `order-store` API, drop-zone hit-testing, rounded drag ghost, MoveTo dialog, PATCH endpoint shape, EXDEV fallback, `movingPaths`/spinner/toast feedback). Plus a "gotchas + future" section flagging the standalone-`.md` path wart, missing per-dir mutex, and possible follow-ups (multi-select, undo, autoscroll).

[2026-04-17] Move feedback + EXDEV fallback: drag-drop / Move-To / reorder now surface what's happening. Tree store tracks a `movingPaths` Set — affected rows get a `Loader2` spinner, dim to 60% opacity, and become non-interactive while the PATCH is in flight. Server-side `movePage` catches `EXDEV` from `fs.rename` (cross-device, e.g. linked external cabinet on a different mount) and falls back to `fs.cp` + `fs.rm`, so dropping into a sister cabinet that's actually a symlinked external folder no longer silently fails. Errors and confirmations now flow through a new generic `SystemToasts` component (`cabinet:toast` events with `{ kind: 'info'|'success'|'error', message }`); the move flow dispatches an info toast on success ("Moved to <parent>") and an error toast carrying the server's actual error message on failure (previously only `console.error`'d). Moved item is also auto-selected after success so the user sees where it landed.

[2026-04-17] Sidebar reorder + move-to: items in a directory can now be manually ordered. New `src/lib/storage/order-store.ts` writes integer `order` (gap-of-100 with midpoint inserts and a one-time renumber when gaps close) — to `index.md`/`*.md` frontmatter for markdown-backed nodes and to a per-directory `.cabinet-order.yaml` sidecar for non-md files (PDF, CSV, image, code, etc.). `tree-builder` reads the sidecar, hides it from listings, and the sort fallback for unordered legacy items is now `Number.POSITIVE_INFINITY` (alphabetical-after-ordered). `createPage` and `movePage` assign sane defaults; the PATCH endpoint accepts `{ toParent, prevName, nextName }` so cross-dir moves and same-dir reorders share one atomic call.

[2026-04-17] Drag-and-drop in `tree-node.tsx` got drop-zone hit-testing — top 25% / middle 50% / bottom 25% for containers (top/bottom = reorder, middle = nest into), and 50/50 split for leaf files (no nest). Insertion-line indicators render at the row top/bottom in primary color; the existing ring stays for the "into" state. Drag image is a cloned, rounded ghost (8px radius, popover bg, soft shadow) attached via `setDragImage` so the dragged row doesn't read as a rectangular browser screenshot. New `MoveToDialog` (Cmd+K-style fuzzy picker over directories/cabinets) is wired to a "Move to…" context-menu item and to a `Cmd+Shift+M` shortcut on the selected node.

[2026-04-17] Calendar past-event ↔ conversation linkage, with missed-run detection. New `scheduledAt?: string` field on `ConversationMeta`: written by the daemon at scheduler-launch time (rounded to the minute — matches `ScheduleEvent.time`), threaded through `startJobConversation` / `runHeartbeat` / `createConversation` and persisted in `meta.json`. UI builds a `Map<key, ConversationMeta>` where `key = agentSlug::sourceType::jobId::minuteIso`, fetched alongside the cabinet overview. Clicking a past pill/dot now does exact key lookup (opens the real conversation), falls back to legacy ±90s fuzzy match on `startedAt` for pre-stamp data, and otherwise treats the event as "did not run": renders muted/hollow + amber warn icon, and the edit dialog shows an amber banner listing possible causes (daemon off, schedule disabled, failed-to-start). Disabled events keep their dashed-hollow treatment so the three states are visually distinct.

[2026-04-17] Legal: refreshed Terms of Use (`data/legal/terms-of-use/index.md`) with effective date 2026-04-17 and added Cabinet-specific clauses covering agent/terminal execution, scheduled jobs, and linked repositories under §4 Acceptable Use.

[2026-04-17] Task runner wiring: new `src/lib/agents/task-runner.ts` (with sibling `task-heuristics.ts`, both with tests) is now invoked in the background after `createTask` and `appendTurn` so the adapter starts running without blocking the API response. Skippable via `skipAgentRun: true` in the request body. Claude local adapter gained session-resume support — when `ctx.sessionId` is present it appends `--resume <id>` instead of `--no-session-persistence`. Default task `runtime.contextWindow` now falls back to 200k.

[2026-04-17] Image Creator agent heartbeat: built a pre-script visual mockup of Slide 2 (labeled chat inventory) for the "Group Chat Shame Spiral" carousel brief, answering the Script Writer's open research question (inventory reads at 4 rows, not 5). Added visual-direction doc, linked it from the brief, and registered the carousel as "Mockup (pre-script)" in the carousels index.

[2026-04-17] Sidebar list-item hover background tuned: dropped to `hover:bg-foreground/[0.03]` — visible enough to register as a hover state but quiet enough not to compete with the colored agent chips and the selected-row treatment. Replaces the original near-invisible `hover:bg-accent/50`.

[2026-04-17] AGENTS / TASKS / DATA section headers in the sidebar now reveal a `+` button on hover (200 ms reveal delay, instant fade-out via `group-hover:delay-200` on opacity transition). Each routes to its section first, then dispatches an event to open the right dialog: `cabinet:open-add-agent` (existing), new `cabinet:open-create-task` (added listener in `tasks-board`), and `setKbSubPageOpen` for DATA. Tasks header restructured into a `group` row to host the action.

[2026-04-17] Stripped the heartbeat label and hover tooltip from sidebar agent rows. Each row is now just `[colored chip] Name • status-dot` — quieter, matches the user's preference for a leaner sidebar. Removed the now-unused `HeartPulse`, `cronToShortLabel`, and `Tooltip*` imports from `tree-view.tsx`.

[2026-04-17] Sidebar polish: bumped AGENTS section bottom-padding (`pb-px` → `pb-1.5`) so the first agent row gets a touch of breathing room, and shrank the sidebar scrollbar to 6px wide with a 2px right inset (scoped via Tailwind arbitrary selectors so other ScrollAreas in the app are untouched).

[2026-04-17] Sidebar agent rows now wear their schedule color: each agent's icon sits in a 20×20 rounded chip tinted with `getAgentColor(slug)` — same palette the schedule-list/calendar uses, so the sidebar visually matches the heartbeat timeline. Replaced the `0j · 0t` clutter with a single `HeartPulse` + heartbeat label; detailed counts moved to the row tooltip. Also dropped non-section sidebar item text from 13px → 12px and softened the color to `text-foreground/75` (hover restores full contrast) so AGENTS / TASKS / DATA labels read as the dominant landmarks.

[2026-04-17] Cabinet-mode agent rows collapsed to single line: icon + name + inline meta (`0j · 0t · 9am`) + status dot, ditching the two-line layout that doubled row height. Tightened the `pb-px` gap under section headers so the first child sits closer to its label. Long meta strings truncate gracefully and live in a `title` tooltip.

[2026-04-17] Sidebar tree hierarchy refined: dropped child indent from `pad(2)` (40px) to `pad(1)` (24px) so each level steps in by a single 16px tab instead of two; bumped section-header type from 10px → 11px and tightened tracking; unified icon sizes (sections + items now both 14px), removed leftover chevron spacers under section headers, and tightened row vertical padding to `py-1`. Children no longer dwarf their parents and the AGENTS / DATA hierarchies read as proper outline levels.

[2026-04-17] `RuntimeMatrixPicker` now sorts the tab strip so ready providers appear first and unready (Not ready) ones are pushed to the right. Stable order preserved within each group. Applies to both the task composer dropdown and the Settings default-runtime matrix so users see their installed + authenticated options up front instead of scrolling past grayed-out tabs.

[2026-04-17] Day view of the jobs & heartbeats calendar now always renders events as pills (no dot collapse) and expands vertically to fit them. Buckets are walked in time order; if a tall pill stack would overlap the next slot, the next bucket is shifted down and the column grows to match. Hour labels stay at their natural positions — pills carry their exact timestamps so the drift is acceptable. Week view keeps the dot-collapse behavior because side-by-side columns can't grow.

[2026-04-17] Jobs & heartbeats calendar: crowded 15-min slots (week) and crowded days (month) now collapse into a row of small agent-colored circles instead of silently truncating beyond a hard cap. Each circle has a hover tooltip showing agent · label · time · past/upcoming state. Sparse slots keep the pill look unchanged. Also split pill/dot click routing: past events load the matching conversation (`GET /api/agents/conversations?agent=…&trigger=job|heartbeat` → closest `startedAt` to event.time, jobs also filtered by `jobId`) and open `TaskDetailPanel` via `setTaskPanelConversation`; future events keep opening the existing edit dialog.

[2026-04-17] `RuntimeMatrixPicker` now disables provider tabs whose CLI isn't ready (not installed, not authenticated, or disabled in Settings). Disabled tabs render grayscale + 50% opacity, carry a "Not ready" chip next to the name, and expose a hover tooltip explaining which step blocks them (e.g. "Cursor CLI — Not installed on this machine. Follow the install guide below."). `base-ui` TabsTrigger's `disabled` prop prevents click + keyboard activation. If the currently stored selection points at an unready provider, the picker auto-focuses the first ready tab instead. Works in both surfaces: task composer (where only ready providers were already shown) and Settings default-runtime matrix (where all enabled providers are shown so users can see what's available but can only click ones that'll actually run).

[2026-04-17] Extracted `RuntimeSelectionBanner` (the colored "Selected Model" header from the task composer dropdown) out of `TaskRuntimePicker` into its own exported component. Now used in two places: (a) the composer dropdown, where it renders with an `App default / Select app default` reset button injected as the trailing slot; (b) Settings → Providers, where it sits above the matrix picker labeled "Default Model" and picks up the same effort-toned background color as the composer. No duplicated banner markup.

[2026-04-17] Settings → Providers now reuses the task composer's runtime picker UI for default provider/model/effort selection. Extracted the inline tabs + matrix body out of `TaskRuntimePicker` into a new exported `RuntimeMatrixPicker` component (same file, so supporting helpers `ProviderRuntimeMatrix` / `SelectionRadio` / `getEffortTone` stay co-located). `TaskRuntimePicker` now just wraps `RuntimeMatrixPicker` inside its dropdown — no duplicated Tabs/TabsTrigger/ProviderGlyph markup, no duplicated active-tab state. Settings replaces the three hand-rolled blocks (provider buttons + model grid + effort grid) with a single `<RuntimeMatrixPicker includeUnavailable />` that saves to `/api/agents/providers` on every selection. `includeUnavailable` filters on `enabled` only (rather than `available && authenticated`) so users can set a default for a provider they're about to install.

[2026-04-17] Added in-UI provider verification. New endpoint `POST /api/agents/providers/[id]/verify` spawns the provider's "Verify setup" command (with `ADAPTER_RUNTIME_PATH` so PATH matches real task runs), captures stdout/stderr/exitCode, and classifies the result as one of `pass | not_installed | auth_required | payment_required | quota_exceeded | other_error` via keyword heuristics ("command not found", "quota exceeded", "payment required", "not authenticated"/"401", etc). The Settings → Providers guide and the Onboarding wizard both got a "Run verify" button per provider that shows a colored status chip, highlights which step failed (step marker turns red with "!"), and exposes raw stdout/stderr on demand. Verified classification against Claude (pass), Codex (pass), Gemini (pass), OpenCode (pass), Cursor / Pi / Grok / Copilot (`not_installed` with hint "The CLI binary isn't on your PATH yet. Rerun the Install step").

[2026-04-17] Verified headless "Reply with exactly OK" across installed providers. Claude ✅ / Codex ✅ / OpenCode ✅ (after today's stdin fix). Gemini CLI 0.1.9 rejected `--output-format` as an unknown argument, so the guide's verify command was changed to `gemini -p 'Reply with exactly OK' --yolo` and the install step was pinned to `@google/gemini-cli@latest` with a note that Cabinet's Gemini adapter needs 0.14+ for stream-json output. Cursor / Pi / Grok / Copilot are not yet installed on this machine, so their verify steps are pending install.

[2026-04-17] Fixed OpenCode adapter "silent success" bug discovered when running `google/gemini-2.5-pro` without a configured provider API key: (1) prompt is now piped via stdin instead of passed as a trailing CLI arg (matches paperclip's proven invocation and avoids the first-run migration path swallowing the prompt); (2) added a stderr filter that suppresses `Performing one time database migration…` / `sqlite-migration:*` / `Database migration complete.` bootstrap noise; (3) when OpenCode exits 0 with zero assistant messages and no session id, Cabinet now synthesizes a non-zero exit with a pointed error ("…the most common cause is a missing provider API key for the requested model e.g. GOOGLE_GENERATIVE_AI_API_KEY for google/* models…"). Updated the opencode test script to consume stdin.

[2026-04-17] Added Grok CLI (`grok_local`) and GitHub Copilot CLI (`copilot_local`) as two new structured providers. Both use plain-stdout passthrough (no stream-json contract yet), expose model selection + extraArgs config, ship install guides with the headless "Verify setup" step, and auto-register alongside the existing six. Providers API now returns 8 CLI providers / 10 adapters total (including legacy). Corrected the OpenCode install command to `npm i -g opencode-ai` (the published package name) and deleted the superseded `AI-claude-editor.md` plan doc.

[2026-04-17] Added "Verify setup — Confirm headless mode works" install step to every provider (Claude, Codex, Cursor, OpenCode, Pi) matching the Gemini pattern, so users can test each CLI end-to-end with a one-shot `Reply with exactly OK` prompt before trusting it in Cabinet task runs.

[2026-04-16] Runtime picker tab strip now horizontally scrolls (hidden scrollbar) so 6 provider tabs fit in the 32rem dropdown without clipping. Renamed Cursor provider display from "Cursor Agent CLI" → "Cursor CLI" to match the Codex/Gemini pattern and keep tab widths consistent.

[2026-04-16] Settings provider guide now reads `provider.installSteps` from the API instead of a hardcoded PROVIDER_SETUP_STEPS map that only covered claude-code and codex-cli. Cursor, OpenCode, and Pi now show their install guides in Settings → Providers. Added `iconAsset` to `ProviderInfo` type for consumers that want to pick up the provider-declared glyph.

[2026-04-16] Multi-CLI provider integration: added Cursor Agent, OpenCode, and Pi as structured adapters using the paperclip pattern (stream-json parsing + session codec + unknown-session retry). Extended adapter types with `AdapterSessionCodec` / `listSkills` / `syncSkills` hooks and `AgentProvider.iconAsset`. Added an external adapter plugin loader (`~/.cabinet/adapter-plugins.json`) with built-in-fallback preservation, wired into the daemon on startup. Updated provider-glyph + API to surface `iconAsset`, shipped placeholder SVGs for the three new providers, added stream-parsing tests for each new adapter, updated the registry assertion to cover all 8 adapters, and documented the round's work + deferred Round B items in AI_PROVIDER_RUNTIME_PROGRESS.md.

[2026-04-16] Claude Code model labels now include version numbers in the runtime picker ("Claude Opus 4.7", "Claude Sonnet 4.6", "Claude Haiku 4.5"), with Opus listed first.

[2026-04-16] Runtime picker: fixed gap between tabs and model table by wrapping the TabsList in a flex container, eliminating the CSS inline-flex baseline descender space that was adding ~4px below the tab buttons. Inactive tabs now use bg-muted/60 so the active tab stands out clearly.

[2026-04-16] COO heartbeat for Text Your Mom: delivered mid-week operating review (Apr 14 week). Audited Tuesday Proof-of-Life — all three cabinets missed (app-dev, TikTok, Reddit). Identified TikTok image-creator produced two script-ready briefs today (first marketing output ever). Reddit remains dark with zero job runs. Created content-calendar/index.md for TikTok, appended COO review to company/operations, updated COO memory, and sent urgent messages to CEO, Reddit researcher, and DevOps agent.

[2026-04-16] Models picker: renamed "Task Model" → "Selected Model", collapsed model info into a single row (icon + name + Provider · Effort with effort-toned colors), header row transparent (no box), tabs row retains background styling.

[2026-04-16] Models picker: removed bottom margin (mb-1.5) from the selected model banner row.

[2026-04-16] Models picker: provider tab backgrounds set to bg-background (matching the table) using !important overrides to beat line-variant transparent base styles; removed all borders from tabs.

[2026-04-16] DevOps daily bug triage for Text Your Mom app-development cabinet. Updated bug-triage.csv with fix targets, DevOps risk framing, and CTO-confirmed root cause notes for all 5 bugs. RT-4 (reminder 2h late) flagged as P2 blocker with zero delivery telemetry; PC-3 (paywall dismiss) and OB-5 (nickname) confirmed for this sprint. Updated DevOps agent context memory.

[2026-04-16] Michael Burry heartbeat: upgraded NVDA bear thesis from watching (Apr 14, conviction 3) to active bear signal (conviction 4) — specific catalyst is Blackwell hyperscaler uptake data at Q1 FY2027 earnings ~late May 2026; 57 Buy / 0 Sell consensus, Google TPU v6 / Amazon Trainium 2 / Microsoft Maia already live in production, DCF 25% premium, 37% downside on estimate + multiple compression. Initiated META bear signal (conviction 3) focused on cash flow arithmetic: $115-135B capex committed yields ~negative FCF in Q1 2026; FCF/NI already 0.72x; Reality Labs $24B/year annualized under unchecked dual-class governance. Both signals appended to market-analysis/signals.csv with catalyst and timeframe per Burry methodology.

[2026-04-16] Benjamin Graham heartbeat: conducted NCAV and balance sheet quality screens on AAPL, NVDA, and META. AAPL: negative NCAV (-$138B), P/B 48x — neutral, conviction 2. NVDA: trading at ~36x estimated NCAV, P/E 56x, pure growth speculation — bearish, conviction 4. META: NCAV near zero, P/E 23x, DCF-based cheapness not Graham cheapness — neutral, conviction 2. All three signals appended to market-analysis/signals.csv.

[2026-04-16] Warren Buffett heartbeat: evaluated NVDA through the four filters. CUDA switching-cost moat acknowledged, Jensen Huang rated excellent capital allocator, but 40%+ hyperscaler concentration and 56x P/E with 25% DCF premium leaves no margin of safety — neutral signal, conviction 2. Signal appended to market-analysis/signals.csv.

[2026-04-16] Charlie Munger heartbeat: applied inversion analysis to NVDA and META. NVDA flagged bearish — lollapalooza consensus (57 Buy / 0 Sell), 25% DCF premium, hyperscaler silicon competition risk. META flagged neutral — PEG attractive but dual-class governance and $115-135B capex bet create unacceptable minority shareholder risk. Signals appended to market-analysis/signals.csv.

[2026-04-16] Models picker: replaced generic Lucide icons in ProviderGlyph with brand images — Claude AI symbol SVG for Claude Code (icon="sparkles") and ChatGPT logo PNG for Codex CLI (icon="bot").

[2026-04-16] Peter Lynch heartbeat: wrote three signals to market-analysis/signals.csv using corrected prices from fundamentals analyst (AAPL $260.48, NVDA $188.63, META $629.86). NVDA neutral (PEG 0.77 trailing / 1.6 forward, customer concentration disqualifier, fully discovered by Wall Street). META downgraded from bullish to neutral — corrected price re-rates PEG from 0.96 to 1.17, hold existing position but do not add, stalwart sell trigger at P/E 36x. AAPL downgraded to bearish — corrected price re-rates PEG to 3.8 (stalwart sell rule triggered at P/E 42x vs 16.5x threshold).

[2026-04-14] CLI: `cabinetai run` is now fully all-in-one — no `create` needed first. Extracted scaffold logic into `cabinetai/src/lib/scaffold.ts` and added `resolveOrBootstrapCabinetRoot()` which auto-creates the cabinet structure (`.cabinet`, `.agents/`, `.jobs/`, `.cabinet-state/`) in the current directory if none is found. `ensureApp()` then detects and installs the web app if needed. Updated Quick Start in README and CABINETAI.md to reflect the single-command flow.

[2026-04-14] CLI: all user-facing messages and README docs now show `npx cabinetai run` instead of bare `cabinetai run`. Users install via npx, so the bare command doesn't exist.

[2026-04-14] Fixed task completion detection stuck on "running". Two bugs: (1) after ANSI stripping the `❯` idle prompt merged onto the same line as `⏵⏵ bypass permissions on`, so the exact-match regex `/^[❯>]$/` never matched — loosened to `/^[❯>](?:\s|$)/`; (2) Claude Code's completion timing line uses many verbs beyond "Brewed" (Sautéed, Baked, Churned, Crunched, etc.) — `isClaudeIdleTailNoise` now matches any spinner-prefixed `Verb for [time]` pattern generically instead of hardcoding individual verbs.

[2026-04-14] Unified `cabinetai-plan.md` and `CABINETAI_DEPLOYMENT.md` into single `CABINETAI.md`. Synced all three package versions to 0.3.1 (app, create-cabinet, cabinetai). Published both npm packages with READMEs.

[2026-04-14] CLI: added `cabinetai uninstall` command. Default removes cached app versions from `~/.cabinet/app/`; `--all` removes the entire `~/.cabinet` directory. Cabinet data directories are never touched.

[2026-04-14] Registry API: added `?limit=N` query param (defaults to 10) so the onboarding carousel caps at 10 templates. The full registry browser passes `limit=100` to show all.

[2026-04-14] Fix sidebar labels: cabinet name in header, "Data" for content section. The top header now always prefers the .cabinet manifest name (e.g. "APPLE") over the index.md frontmatter title ("Knowledge Base"). Previously, clicking the cabinet overview caused activeCabinet to resolve to the root tree node whose frontmatter title was "Knowledge Base".

[2026-04-14] Onboarding wizard: removed directory picker from Step 7 (CLI already owns dir selection via CABINET_DATA_DIR), added .cabinet manifest detection at wizard start with a WelcomeBackStep for existing cabinets that pre-fills company name, and added "team of teams" framing subtitle to Step 2's TeamBuildStep title.

[2026-04-14] Added zoom/pan controls to Mermaid viewer: toolbar buttons for zoom in/out/reset with percentage display, Ctrl+scroll wheel zoom, and click-drag panning with grab cursor.

[2026-04-14] Fixed Mermaid viewer error handling: added `suppressErrorRendering` and `mermaid.parse()` pre-validation so syntax errors show a clean inline error message instead of mermaid injecting broken error SVGs into the DOM.

[2026-04-14] After importing a registry cabinet in onboarding Step 2, show a "Your cabinet has been created" success screen with an animated file tree (cabinet name, .agents/, .jobs/, counts) that reveals line-by-line, then a "Continue setup" button to proceed through the remaining onboarding steps instead of skipping them.

[2026-04-14] Onboarding Step 2: removed "Coming soon" blur from the team carousel, connected it to live registry templates from /api/registry, made cards clickable with an inline import dialog (POST /api/registry/import), and added a "Browse all" button that opens the full RegistryBrowser in a dialog.

[2026-04-14] Added ai-hedge-fund cabinet to data/ — a full multi-agent stock analysis system inspired by virattt/ai-hedge-fund. Includes 12 agents (Portfolio Manager, Risk Manager, 6 legendary investor personas: Buffett/Munger/Graham/Lynch/Burry/Wood, and 4 analyst agents: Fundamentals/Valuation/Sentiment/Technicals), 3 scheduled jobs, example signals.csv with live-format data for AAPL/NVDA/META, portfolio tracking, investor philosophy research pages, and risk management parameters.

[2026-04-14] Extracted scaffoldCabinet() to src/lib/storage/cabinet-scaffold.ts — unified duplicated cabinet bootstrap logic (dirs, .cabinet manifest, index.md) previously spread across onboarding/setup and cabinets/create API routes. Both routes now call the shared utility.

[2026-04-14] Fixed onboarding to comply with cabinet protocol: `POST /api/onboarding/setup` now creates the root `.cabinet` YAML manifest (schemaVersion, id, name, kind, version, description, entry), `index.md` entry point, and `.cabinet-state/` runtime directory — all three were previously missing from root cabinet initialization.

[2026-04-13] Fix job cards in ScheduleList not opening when agent lookup fails — removed agentRef guard from click handler, falls back to slug/name/emoji already on the item.

[2026-04-13] Fixed Warp Ventures OS cabinet protocol compliance: added .cabinet identity files (root + 3 child cabinets: deal-flow, portfolio, intelligence), .cabinet-state/.gitkeep in all 4 cabinets, 4 agent personas (.agents/managing-partner, analyst, portfolio-manager, deal-scout), description fields and correct ownerAgent/agentSlug assignments across all 9 job YAMLs, and quoted cron schedule strings.

[2026-04-13] Created "Warp Ventures OS" — a comprehensive VC operating system cabinet under data/vc-os. Includes 47 files across 9 modules: Intelligence Hub (daily X digest, 5 watchlist topics, live intelligence feed webapp), Events Calendar webapp, Deal Flow kanban webapp with 15 deals, Portfolio section with 5 companies each having metrics CSVs and news logs, Portfolio Dashboard webapp with Chart.js charts, Competitors section with Mermaid landscape diagram, Team profiles, LP management with commitments CSV, Finance section with IRR model/cap table/fees CSVs and Q1 report, and Programs (Fellowship + Accelerator) with cohort CSVs. Nine scheduled jobs across root/.jobs, portfolio/.jobs, intelligence/.jobs, and deal-flow/.jobs for daily briefs, portfolio health checks, deal pipeline reviews, board prep, LP updates, competitor intel, and market maps.

[2026-04-13] Moved AI edit pill to the status bar (bottom), centered via absolute positioning; shows for all KB content (MD, CSV, PDF, webapp, dirs) whenever section.type === "page". Header reverted to original simple layout.

[2026-04-13] Fix "Add cabinet data" creating pages at root instead of inside the active cabinet — button now opens the kbSubPage dialog (which uses dataRootPath) instead of the root NewPageDialog.

[2026-04-13] Fix "New Page" failing at root level — added POST handler to /api/pages/route.ts so creating a root page no longer hits a 405.

[2026-04-13] Task board inbox empty state now shows an "Add task" button instead of instructing users to click the header Add button.

[2026-04-13] Sidebar "New Page" and "New Cabinet" buttons now use text-xs, tighter gap/padding, and whitespace-nowrap to keep labels on a single line.

[2026-04-13] Constrain Jobs & heartbeats calendar to 600px height with a scrollbar. MonthView grid is now scrollable within a flex-1 overflow-y-auto wrapper; the section no longer grows to full content height.

[2026-04-13] Paper theme updated to exact warm parchment palette from runcabinet.com: background #FAF6F1, card #FFFFFF, sidebar #F3EDE4, primary/ring #8B5E3C, secondary #F5E6D3, muted #FAF2EA, foreground #3B2F2F, muted-foreground #A89888, border #E8DDD0. All values converted to OKLCh. Accent preview color updated to #8B5E3C.

[2026-04-13] Registry import: fix GitHub 403 rate-limit error on large templates (e.g. career-ops). Replaced recursive per-directory API calls with a single Git Trees API call (GET /git/trees/HEAD?recursive=1), then download files via raw.githubusercontent.com which has no API rate limit. Reduces GitHub API usage from O(directories) to 1 call per import.

[2026-04-13] Fullscreen "New Cabinet" dialog: replaced the two-step small dialog with a single fullscreen overlay (fixed inset-0 z-50, backdrop-blur-md) rendered via createPortal. All fields shown at once — cabinet name input, full agent grid picker, and "or import a pre-made team →" registry link at the bottom. AgentPicker got a layout="grid" prop so department columns wrap instead of horizontal-scroll in the fullscreen context. Fixed agents-not-appearing bug: LIBRARY_DIR in create/route.ts was pointing to the non-existent DATA_DIR/.agents/.library — corrected to PROJECT_ROOT/src/lib/agents/library where templates actually live.

[2026-04-13] Task board header cleanup: moved "Jobs & Heartbeats" schedule button to topmost right corner of the title row (flex justify-between), removed schedule toggle from filter row so it's back to original (agent filter + scope select + Refresh only). Fixed LayoutList not-defined runtime error by adding the import.

[2026-04-13] Registry browser redesign: rewrote registry-browser.tsx to faithfully match the cabinets-website design. Warm parchment palette (#FAF6F1 bg, #8B5E3C accent, #3B2F2F text) scoped to the component. List view has search + domain filter chips + list rows with stats. Detail view has warm header strip with stats, org chart (full port of cabinet-org-chart.tsx — VLine/HBranch connectors, department columns, agent/job nodes, child cabinet nodes, stats footer), agents grid, jobs list, readme prose, import CTA banner. Both scroll properly via overflow-y-auto + min-h-0 (no ScrollArea dependency).

[2026-04-13] Text Your Mom CEO heartbeat: executed marketing activation that was decided but not done earlier. Enabled all 6 marketing jobs across TikTok and Reddit cabinets (4 daily scans + 2 weekly digests). Updated team directory from 8/16 Active to 16/16 Active. Sent activation orders with specific deliverables to both marketing cabinet leads. Answered CFO's open data request on finance page (pricing $4.99/mo, burn ~$12K/mo, organic/paid split 60/40). Updated goals page with execution checkpoints for the week. Sent coordination messages to COO and CFO.

[2026-04-13] Registry browser: full cabinet registry browsing experience as a new "registry" section. Home screen has "Browse all" link next to the carousel heading. The browser has a search bar + filterable list of all 8 registry templates, and clicking one opens a detail view with header, stats, cabinet structure tree, agent cards grid, job list, readme, and two "Import Cabinet" CTAs (top bar + inline banner). Detail data fetched live from GitHub via new GET /api/registry/[slug] endpoint that parses .cabinet manifests, agents, jobs, and child cabinets. Import flow uses the same fullscreen overlay + page reload pattern.

[2026-04-13] Import UX polish: clicking Import now closes the dialog and shows a fullscreen blur overlay with spinner and progress text while downloading. On error, reopens the dialog with the error message. Added "Cabinet names can't be renamed later" warning under the name input.

[2026-04-13] Cabinet creation and registry import: Added "New Cabinet" button to sidebar (multi-step dialog with name input + agent picker), "Create Cabinet Here" right-click option in tree context menu, and replaced the "Coming soon" home screen carousel with clickable registry import cards. Created shared AgentPicker component and useAgentPicker hook extracted from onboarding wizard. New APIs: POST /api/cabinets/create (creates .cabinet + .agents/ + .jobs/ structure with selected agents from library), GET /api/registry (serves bundled manifest of 8 registry templates), POST /api/registry/import (downloads templates from GitHub hilash/cabinets repo). New files: agent-picker.tsx, use-agent-picker.ts, new-cabinet-dialog.tsx, registry-manifest.ts, github-fetch.ts, plus 3 API routes.

[2026-04-13] Pipeline Conductor first heartbeat: stood up 3 missing agent personas (conductor, evaluator, cv-tailor). Assessed pipeline state — Scanner has populated 50 roles across 14 companies (Anthropic, Stripe, Figma, Vercel, Linear, Supabase, Databricks, Airtable, Scale AI, Airbnb, dbt Labs, Brex, Resend, Clerk), all in "Discovered" status with zero evaluations. Identified critical blocker: master CV and proof points are still templates, blocking all Block B evaluations and downstream CV tailoring. Updated career-ops hub with accurate pipeline health metrics and agent roster.

[2026-04-13] Pattern Analyst heartbeat v2: produced end-of-day pattern analysis (pattern-analysis-2026-04-13-v2.md). Key finding: 0/7 recommendations from two prior reports have been executed. Funnel metrics stable (19 entries, 12 beyond Evaluated, scoring system validated). Escalated execution gap to Pipeline Conductor. Critical actions: n8n follow-up (4 days overdue), ElevenLabs apply (by Apr 15), failure/recovery STAR+R story (before Apr 16 panel). Two new discoveries assessed (Linear IC PM #18 — high priority, Cohere #19 — needs location verification).

[2026-04-13] Tasks board: clicking a conversation opens a right-side detail panel at the app-shell level (like the AI panel), not inside the board. Added `taskPanelConversation` to app-store, created `TaskDetailPanel` component rendered in the app shell layout. Running tasks show a live WebTerminal; completed/failed show ConversationResultView. X button closes the panel. The board stays fully visible underneath.

[2026-04-13] Replaced the Jobs & Heartbeats sidebar with a full-width schedule section featuring two views: (1) Calendar view (default) with day/week/month modes — a CSS Grid time grid showing jobs and heartbeats as color-coded pills positioned at their scheduled times, with current-time red line indicator, today highlighting, agent emoji markers, navigation arrows, and "Today" snap button. Month view shows a calendar grid with event badges per day cell. (2) List view — full-width responsive card grid. Both views open the same job/heartbeat edit dialogs on click. Extracted `computeNextCronRun` from persona-manager.ts into shared `cron-compute.ts` with `getScheduleEvents`, `getViewRange`, and `getAgentColor` helpers for client-side cron → event computation.

[2026-04-13] Added job and heartbeat edit dialogs to the cabinet main page SchedulesPanel. Clicking a job opens a dialog with schedule picker, prompt editor, enabled toggle, "Run now" button, and save — matching the agents workspace org chart popups. Clicking a heartbeat opens a similar dialog with schedule picker, active toggle, run, and save. Both dialogs use the same SchedulePicker component and API endpoints as the workspace.

[2026-04-13] Iterated on cabinet main page layout: moved title + description into the header bar (task-board style), put compact stats and scope depth pills in one row below (stats left, scope right), removed the Agent Status Grid section (org chart already covers agents), removed "Back to Cabinet" button. Updated cabinet task composer @ mentions to show both agents AND pages (was agents-only), with page chips and mentionedPaths sent to the API.

[2026-04-13] Redesigned the cabinet main page (CabinetView) as a mission control dashboard. Extracted 5 sub-components from the monolithic 1470-line file into separate modules (cabinet-utils.ts, cabinet-task-composer.tsx, cabinet-scheduler-controls.tsx, schedules-panel.tsx). Built 4 new components: InteractiveStatStrip (clickable metric cards with popover breakdowns), AgentStatusCard (live status cards with running/idle/paused indicators and glow animation), AgentStatusGrid (agent cards grid with integrated depth filter), and ActivityFeed (redesigned conversation list with pinned running items and emoji avatars). New layout order: stats strip, org chart (moved up as hero), composer, agent status cards, activity feed, schedules. Added cabinet-card-glow CSS animation for running agents.

[2026-04-13] Created `cabinetai` CLI package — a new npm package that serves as the primary runtime CLI for Cabinet. Architecture: app installs to `~/.cabinet/app/v{version}/` (auto-downloaded on first use), cabinets are lightweight data directories anywhere on disk. Commands: `create` (scaffold .cabinet + .agents/ + .jobs/ + index.md), `run` (ensure app installed, start Next.js + daemon pointing at cabinet dir), `doctor` (health checks), `update` (download newer app version), `import` (fetch templates from hilash/cabinets registry), `list` (discover cabinets in directory tree). Built with TypeScript + esbuild + Commander.js. Refactored `create-cabinet` to thin wrapper. Updated release pipeline (release.sh, release.yml, manifest generator, .gitignore).

[2026-04-13] Added depth dropdown to the sidebar header next to the "CABINET" label and to the agents workspace org chart navbar. Both compact Select dropdowns show Own/+1/+2/All options controlling which agents from child cabinets are visible. Reuses existing visibility infrastructure from app-store, works at both root and sub-cabinet levels, and syncs with the cabinet page depth pills.

[2026-04-13] Added right-click context menu to the Cabinet header in the sidebar. Shows: Rename (disabled with "coming soon" tooltip), Copy Relative Path (nested cabinets only), Copy Full Path, Open in Finder, and Delete (nested cabinets only, with confirmation). Root cabinet hides Rename-breaking and destructive options.

[2026-04-13] Pattern Analyst heartbeat: updated career-ops pattern analysis with recommendation adoption tracker (0/7 actioned — critical gap), pipeline update (17→19 entries, +2 discoveries, +1 location-failed), revised scorecard, and escalation tasks for Interview Strategist (failure/recovery story before Apr 16 panel) and CV Tailor (ElevenLabs application before Apr 15).

[2026-04-13] Composer hint bar: moved quick action chips below the composer on the home screen. Added grey hints below all composer cards — "use @ to mention" on the left and "Shift + Enter new line" on the right (responsive, hidden on small screens). Send button stays inside the card. Also added hints to cabinet-specific page composer (cabinet-view.tsx) and standardized its keyboard to Shift+Enter for newline.

[2026-04-13] Unified composer component: Created shared `useComposer` hook and `ComposerInput` component that replaces 4 duplicate input implementations (home screen, agent workspace panel + quick-send popup, AI panel editor chat, task board). All surfaces now support `@` mentions for both pages and agents in a single unified dropdown with grouped sections. The "Add Inbox Task" dialog was redesigned from a rigid form (title/description/priority fields) into a conversational composer. Extracted shared `flattenTree` and `makePageContextLabel` into `src/lib/tree-utils.ts`. Submit behavior is Enter to send, Shift+Enter for newline across all surfaces.

[2026-04-13] CEO operating review: surveyed all cabinets, confirmed Option A (activate marketing this week), answered CFO data questions (pricing $4.99/mo, burn ~$12K/mo, 60/40 organic/paid split), set April 26 check-in criteria, flagged COO/DevOps overlap and CEO brief/review overlap, introduced decision-deadline process fix for blockers. Updated company/operations and company/goals.

[2026-04-13] CEO weekly operating review (scheduled job): Full cross-cabinet review covering root + app-development + marketing/tiktok + marketing/reddit. Wins: DevOps sprint plan, CTO RT-4 ownership, CFO unit economics, COO financial risk tracking. Made Option A decision official for marketing activation with specific deadlines. Introduced Tuesday proof-of-life process fix. Saved to company/operations/index.md.

[2026-04-13] DevOps agent: created weekly sprint plan for week of April 14 at backlog/sprint-2026-04-14. Priorities: ship 4 small stories (OB-2, OB-5, OB-6, PC-3), start OB-1 and OB-3, run first release as a dry run of the pipeline. Updated release checklist with actionable items and staged rollout plan for the first release.

[2026-04-13] Removed legacy run-agent.sh script and its references from Electron packaging configs. The in-app agent system has superseded this manual bash loop approach.

[2026-04-13] Upgraded "What needs to get done?" create-task dialog: title is now larger (text-xl), random placeholder sentences rotate each open, DialogDescription removed, CEO agent pre-selected as default mention chip (persistent — typing doesn't clear it), "Start now" button added alongside "Add to inbox" (bypasses inbox, directly starts a conversation with the resolved agent), Cmd+Enter keyboard shortcut triggers Start now, keyboard hint bar shown beneath buttons. Extended ComposerInput with secondaryAction and onKeyDown interceptor props; extended useComposer with initialMentionedAgents that are pinned against auto-removal.

[2026-04-12] Cabinet view: moved visibility depth selector from a separate column to a subtle inline pill bar beneath stats (more grounded). Added Start All / Stop All / Restart All controls to the cabinet header bar, scoped to own-cabinet agents only (no sub-cabinets). Scheduler API now accepts optional cabinetPath to scope start-all/stop-all operations.

[2026-04-12] Task board header: moved selectors and refresh button to same row as filter chips (chips left, selectors right). Active chip color now matches its type (sky for Manual, emerald for Jobs, pink for Heartbeat) instead of generic primary.

[2026-04-12] Made BACK button icon smaller (2.5) and nudged it up to align with the center of the letter height.

[2026-04-12] Removed scope/visibility label text next to AGENTS in sidebar (e.g. "Cabinet · Include two cabinet levels") and added more spacing between CABINET header and AGENTS sub-item.

[2026-04-12] Sidebar cleanup: removed chevron from main cabinet header (always expanded, no collapse needed), made cabinet icon amber/yellow to match child cabinet icons, and toned down the BACK button (smaller text, lighter color, smaller icon).

[2026-04-12] Removed inline Job editor panel from the agent settings jobs view. The jobs list now fills the full width. Clicking a job opens the styled New Job popup (now context-aware: "Edit Job" title, Run + Delete in footer, "Save job" button when editing an existing job).

[2026-04-12] Redesigned "New Job" popup to match the "Edit Agent" dialog style exactly: two-column layout with the prompt textarea on the left (60vh tall, bg-muted/60 borderless), fields grid on the right (uppercase tracking labels, muted-fill inputs), and a proper footer with Starter Library ghost button on the left and Cancel + Create on the right.

[2026-04-12] Fixed search API recursing into embedded app/website directories. `collectPages` in `src/app/api/search/route.ts` now skips directories that have `index.html` but no `index.md`, preventing internal files like `about.md` inside a pipeline app from appearing in Cmd+K search results.

[2026-04-12] Created data/getting-started/ KB section with three pages: index (full file-type matrix + sidebar icon reference + keyboard shortcuts + features overview), apps-and-repos (embedded apps, full-screen .app mode, .repo.yaml spec), and symlinks-and-load-knowledge (Load Knowledge flow, .cabinet-meta, .repo.yaml, CABINET_DATA_DIR). Updated data/CLAUDE.md with a supported file types table covering all 13 types the tree-builder recognises. Updated data/index.md with a link to the new guide.

[2026-04-12] Cabinet page agents section: replaced individual bordered cards with a compact divider-based list. Agents are grouped by department (executive first, general last) with a muted section label row. Each row shows emoji, name, role, heartbeat pill, and active dot. The lead/CEO agent gets a small amber Crown icon inline with their name instead of a separate card.

[2026-04-12] GitHub stars counter animation in status bar: on first load, the star count animates from 0 to the real fetched value over 2 seconds using an ease-out cubic curve (requestAnimationFrame). When the counter reaches the final number, 8 gold ✦ particles burst outward in all 45° directions using CSS custom properties and a @keyframes animation. The explosion auto-hides after 900ms. Falls back to the static star count until real data arrives.

[2026-04-12] Added CabinetTaskComposer to cabinet homepage: a "What are we working on?" prompt box below the header with agent pills for all visible agents. Own-cabinet agents appear as a pill row; child-cabinet agents are grouped under their cabinet name as a labeled row below. Selecting a pill sets the target agent; Enter submits and navigates to the new conversation. Also updated buildManualConversationPrompt and the conversations POST route to accept cabinetPath so child-cabinet agent tasks run in the right cwd and store conversations in the correct cabinet.

[2026-04-12] Added RecentConversations panel to cabinet homepage: full-width card below the header showing the 20 most recent conversations across visible cabinets. Each row shows status icon (spinning/check/x), agent emoji, title, summary snippet, trigger pill (Manual/Job/Heartbeat), and relative timestamp. Running conversations show a pulsing indicator in the header. Clicking any row navigates directly to that conversation in the agent workspace. Auto-refreshes every 6 seconds.

[2026-04-12] Redesigned cabinet homepage to match the app design system: clean header with large title, description, stat pills (rounded-full bg-muted/primary tokens), and a segmented visibility scope control. Org chart uses proper rounded-xl border bg-card containers with CEO featured in a slightly elevated card, department labels as uppercase mono caps, and agent rows with emoji + role + heartbeat badge. Schedules panel follows the same card pattern with Clock/HeartPulse icon headers and rows with status badges. Removed gradient banner, icon box, kind tag, and parent name from back button.

[2026-04-12] Multi-cabinet conversation aggregation: when viewing a cabinet with "Include children" or "Include all descendants" visibility mode, the Agents Workspace now aggregates conversations from all visible cabinet directories. The conversations API accepts a `visibilityMode` query param and uses `readCabinetOverview` to discover descendant cabinet paths, then merges and sorts conversations from all of them. AgentsWorkspace passes the current visibility mode and re-fetches when it changes.

[2026-04-12] CEO agent first heartbeat for Text Your Mom example cabinet: created company/updates page with weekly priorities, added reality check to goals (50K MAU target requires marketing activation this week), added action-by-metric table to KPIs page, and linked updates section from root index. Three priorities set: ship P1 onboarding stories, activate paused marketing cabinets, and start investigating the critical reminder timing bug.

[2026-04-12] Cabinet UI interaction layer: clicking agents in the sidebar now opens AgentsWorkspace scoped to the cabinet (passes cabinetPath through section state → app-shell → AgentsWorkspace). Agent cards in the cabinet dashboard org chart are clickable. All agent API calls (persona GET, run, toggle, jobs) pass cabinetPath for cabinet-scoped resolution. JobsManager accepts cabinetPath prop.

[2026-04-11] Daemon recursive cabinet scheduling: the daemon now discovers all `.cabinet` files recursively under DATA_DIR and schedules heartbeats and jobs for every cabinet's agents. Schedule keys are cabinet-qualified (e.g., `example-text-your-mom/marketing/tiktok::heartbeat::trend-scout`) to prevent slug collisions across cabinets. Cabinet-level `.jobs/*.yaml` with `ownerAgent` are picked up alongside legacy agent-scoped jobs. The file watcher now monitors `**/.agents/*/persona.md`, `**/.jobs/*.yaml`, and `**/.cabinet` across all depths. API endpoints accept `cabinetPath` in request body so heartbeats and jobs execute in the correct cabinet scope with the right cwd.

[2026-04-11] Cleaned data directory: moved all old content (agents, jobs, missions, playbooks, chat, and content dirs) to `old-data/` at project root. Created root `.cabinet` manifest and `index.md` for the root cabinet. Renamed `data/.cabinet/` (runtime config dir) to `data/.cabinet-state/` to avoid conflict with `.cabinet` manifest file.

[2026-04-11] Onboarding provider step: redesigned to show only working providers as selectable radio cards with model selector. Users choose their default provider (Claude Code or Codex CLI) and pick a model (sonnet/opus/haiku or o3/o4-mini/gpt-4.1). Selection is saved to provider settings on launch. Non-working providers show setup guides in an expandable section.

[2026-04-11] Onboarding launch step: replaced right-side activity feed with animated agent chat preview. Agents now appear to talk to each other in a #general channel — CEO greets the team, delegates tasks to selected agents by name, and agents reply and coordinate. Messages appear one-by-one with typing indicators. Panel height reduced.

[2026-04-11] Onboarding wizard: added final "Start your Cabinet" step with summary card (company, agents, provider status) and data directory choice — "Start fresh here" uses the current dir, "Open existing cabinet" lets users pick a folder via native OS dialog. If a custom dir is chosen, it's saved via the data-dir API before launching.

[2026-04-11] Onboarding intro page: added staggered entrance animations. Elements fade in and slide up sequentially — card border appears first, then "cabinet" title, pronunciation/noun, each dictionary definition one by one, tagline lines, and finally the "Get started" button. Total sequence ~4.2s.

[2026-04-11] Onboarding wizard: limited agent selection to max 5 with CEO and Editor as mandatory (can't uncheck, show "Required" label). Unchecked agents dim and become unclickable at limit. Added counter display. Changed "How big is your team?" to a blurred "Pre-made multi-human multi-agent teams" section with "Coming soon" overlay.

[2026-04-11] Added show/hide hidden files setting in Appearance tab with checkbox and keyboard shortcut display (⌘⇧. / Ctrl+Shift+.). The toggle is persisted to localStorage and reloads the sidebar tree. Also registered the global keyboard shortcut matching macOS Finder behavior.

[2026-04-11] Added fallback viewer for unsupported file types. Files like .docx, .zip, .psd, .fig, .dmg etc. now appear in the sidebar (grayed out) and show a centered "Open in Finder" + "Download" view. Uses a whitelist approach — only common document, archive, and design file extensions are shown; everything else is silently skipped. Added `/api/system/reveal` endpoint for macOS Finder integration.

[2026-04-11] Added Storage tab to Settings with data directory picker. Users can view the current data dir path, browse for a new one, or type a path manually. The setting is persisted to `.cabinet-install.json` and read by `getManagedDataDir()` at startup (env var still takes priority). A restart banner shows when the path changes. Also updated the About tab to show the actual data dir path.

[2026-04-11] Added Mermaid diagram viewer for .mermaid and .mmd files. Renders diagrams with the mermaid library, supports source toggle, copy source, and SVG export. Follows the current Cabinet theme (dark/light). Shows error state with fallback to source view if rendering fails.

[2026-04-11] Updated documentation for direct symlinks: shortened Load Knowledge section in getting-started, updated apps-and-repos page, added new "Symlinks and Load Knowledge" guide page under getting-started, updated data/CLAUDE.md linked repos section, and added Link2 + new file type icons to the sidebar icons table.

[2026-04-11] Added source/code viewer, image viewer, and video/audio player as first-class file viewers. Code files (.js, .ts, .py, .json, .yaml, .sh, .sql, +25 more extensions) open in a dark-themed source viewer with line numbers, copy, download, wrap toggle, and raw view. Images (.png, .jpg, .gif, .webp, .svg) render centered on a dark background with download/open-in-tab. Video/audio (.mp4, .webm, .mp3, .wav) use native HTML5 players. Tree builder now classifies files by extension and shows type-specific sidebar icons. Added node_modules and other build dirs to the hidden entries filter.

[2026-04-11] Load Knowledge now creates direct symlinks (`data/my-project -> /external/path`) instead of wrapper directories with a `source` symlink inside. Metadata is stored as dotfiles (`.cabinet-meta`, `.repo.yaml`) in the target directory, while legacy `.cabinet.yaml` is still read for compatibility. Added `isLinked` flag to TreeNode for UI differentiation — linked dirs show a Link2 icon and "Unlink" instead of "Delete" in context menus. Updated linked-folder page fallback and symlink cleanup to support the new metadata file plus the legacy filename during transition.

[2026-04-11] Added "Copy Relative Path" and "Copy Full Path" options to sidebar context menus. TreeNode menu gets both options; Knowledge Base root menu gets "Copy Full Path". Full path is resolved via `/api/health` with a client-side cache.

[2026-04-11] Added expandable setup guides to the Settings > Providers tab. Each CLI provider now has a "Guide" button that reveals step-by-step installation instructions with numbered steps, terminal commands (with copy buttons), "Open terminal" button, and external links (e.g. Claude billing). Also added a "Re-check providers" button. Matches the onboarding wizard's setup guide UX.

[2026-04-11] Added agent provider health status to the status bar. The health indicator now shows amber "Degraded" when no agent providers are available. Clicking the status dot opens a popup showing App Server, Daemon, and Agent Providers sections with per-provider status (Ready / Not logged in / Not installed). Provider status is fetched once on mount and refreshed each time the popup opens, with 30s server-side caching to avoid excessive CLI spawning.

[2026-04-11] Added Codex CLI login verification to onboarding agent provider step. Health check now runs `codex login status` to detect authentication (e.g. "Logged in using ChatGPT") instead of assuming authenticated when the binary exists. Updated the Codex setup guide to use `npm i -g @openai/codex` and simplified steps to: install, login, verify.

[2026-04-11] Updated Discord invite link to new permanent invite (discord.gg/hJa5TRTbTH) across README, onboarding wizard, status bar, settings page, and agent job configs.

[2026-04-10] Redesigned onboarding step 1 from "Tell me about your project" to "Welcome to your Cabinet". Added name and role fields (role uses predefined pill buttons: CEO, Marketer, Engineer, Designer, Product, Other). Moved goals question to step 2. Step 1 now requires both name and company name to proceed.

[2026-04-10] Fixed duplicate-key crash when a standalone .md file and a same-named directory coexist (e.g. `harry-potter.md` + `harry-potter/`). Tree builder now skips the standalone file when a directory exists. Link-repo API now auto-promotes standalone .md pages to directories with index.md when loading knowledge into them. Added warning banner to Load Knowledge dialog when the target page already has sub-pages.

[2026-04-10] Removed the first-launch data directory dialog from Electron. Cabinet now silently seeds default content (getting-started, example-cabinet-carousel-factory, agent library) into the managed data dir on every launch. Also fixed the build script referencing a wrong directory name (`cabinet-example` → `example-cabinet-carousel-factory`) and added `index.md` to the seed content. Created a new "Setup and Deployment" guide page covering data directory locations, custom `CABINET_DATA_DIR`, and upgrade instructions. Rewrote all getting-started pages to remove Harry Potter references and use the Carousel Factory example instead.

[2026-04-10] Renamed "Add Symlink" to "Load Knowledge" across the UI. Redesigned the dialog: top section has folder picker and name (for everyone), collapsible "For Developers" section exposes remote URL and description fields with explanation about symlinks and .repo.yaml. API now auto-detects git repos — only creates .repo.yaml for actual repos, plain directories just get the symlink. Updated getting-started docs.

[2026-04-10] Updated server health indicator to track both servers independently — App Server (Next.js) and Daemon (agents, jobs, terminal). Shows green "Online" when both are up, amber "Degraded" when only the daemon is down, and red "Offline" when the app server is down. Popup shows per-server status with colored dots and explains which features are affected. Added `/api/health/daemon` proxy route and updated middleware to allow all health endpoints.

[2026-04-10] Made "Add Symlink" available at every level of the sidebar tree, not just the root Knowledge Base label. Added the option to tree-node.tsx context menu, added parentPath prop to LinkRepoDialog, and updated the link-repo API to support creating symlinked repos inside subdirectories.

[2026-04-10] Restored "Add Symlink" option to the Knowledge Base context menu. It was lost when the sidebar was restructured to nest KB under Cabinet (commit e011d02). Moved LinkRepoDialog and its state from sidebar.tsx into tree-view.tsx where the context menu lives.

[2026-04-10] Added all 7 sidebar icon types to the example workspace: Posts Editor (full-screen .app with carousel slide previews, placeholder images, prompts, and platform/status filters), Brand Kit (embedded website without .app — Globe icon), media-kit.pdf (PDF — FileType icon). Updated .gitignore to track the renamed example directory and agent library templates.

[2026-04-10] Replaced Harry Potter example workspace with "Cabinet Carousel Factory" — a TikTok/Instagram/LinkedIn carousel content factory for marketing Cabinet itself. Includes: index.md (HQ page with brand guide, pipeline, hook formulas, posting schedule), competitors.csv (15 KB competitors updated daily by cron), content-ideas.csv (carousel backlog), content-calendar full-screen HTML app (.app) with Cabinet website design language (warm parchment, serif display, terminal chrome), .repo.yaml linking to Cabinet repo. Created 4 new agent personas (Trend Scout, Script Writer, Image Creator, Post Optimizer) and 3 scheduled jobs (morning briefing, daily competitor scan, weekly digest). Deleted old HP-themed content and jobs.

[2026-04-10] Fixed onboarding wizard to show all 20 agent library templates during fresh start, grouped by department (Leadership, Marketing, Engineering, etc.). Previously only 2-4 agents were shown via hardcoded suggestions. Now fetches templates from /api/agents/library and uses keyword matching against company description to smart pre-check relevant agents.

[2026-04-10] Pinned domain tag and agent count to the bottom of each carousel card using flex-col with mt-auto, and set a fixed card height so the footer row aligns consistently across all cards.

[2026-04-10] Made the "cabinet" logo in the sidebar header clickable — clicking it now navigates to the home screen, matching the behavior of clicking the Cabinet section label.

[2026-04-10] Added infinite carousel of "Cabinets" at the bottom of the home screen — 50 pre-made zero-human team templates with name, description, agent count, and color-coded domain badges. Carousel auto-scrolls and pauses on hover.

[2026-04-10] Changed home screen prompt input from single-line input to textarea. Enter submits the conversation, Ctrl/Cmd+Enter inserts a new line. Added a keyboard hint (⌘ + ↵ new line) next to the send button.

[2026-04-10] Added home screen that appears when clicking "Cabinet" in the sidebar. Shows a time-based greeting with the company name, a text input for creating tasks, and quick action buttons. Submitting a prompt starts a conversation with the General agent via /api/agents/conversations and navigates directly to the conversation view. Added conversationId to SelectedSection so the agents workspace auto-selects and opens the new conversation. Default app route changed from agents to home.

[2026-04-10] Made Knowledge Base sidebar item editable. Added data/index.md as the root KB page, a root /api/pages route for parameterless access, and split the KB sidebar button so the chevron toggles expand/collapse while clicking the label opens the page in the editor.

[2026-04-10] Unified sidebar: Agents and Knowledge Base nested under collapsible "Cabinet" parent. All items now use identical TreeNode styles (13px text, gap-1.5, h-4 w-4 icons, depth-based paddingLeft indentation, same hover/active classes). KB tree nodes render at depth 2 so they align with agent child items.

[2026-04-10] Fix false "Update 0.2.6 available" shown when already on 0.2.6. Root cause: stale cabinet-release.json (0.2.4) was used as current version instead of package.json. Updated the manifest and made readBundledReleaseManifest always use package.json version as source of truth.

[2026-04-10] Added Connect section to the About settings tab with Discord link (recommended) and email (hi@runcabinet.com).

[2026-04-10] Added default White and Black themes (neutral, no accent color) to the appearance tab. Reduced blur on coming-soon overlays from 3px to 2px with higher opacity.

[2026-04-10] Notifications settings tab now shows a blurred preview with "Coming Soon" overlay, matching the integrations tab treatment.

[2026-04-10] Integrations settings tab now shows a blurred preview of the MCP servers and scheduling UI with a centered "Coming Soon" overlay card on top.

[2026-04-10] Moved About section from Providers tab into its own About tab in settings with correct version (0.2.6) and product info.

[2026-04-10] Settings page tabs now sync with the URL hash (e.g. #/settings/updates, #/settings/appearance). Browser back/forward navigates between tabs. Added min-h-0 + overflow-hidden to the ScrollArea so tab content is properly scrollable.

[2026-04-09] Fix pty.node macOS Gatekeeper warning: added xattr quarantine flag removal before ad-hoc codesigning of extracted native binaries in Electron main process.

[2026-04-09] Added `export const dynamic = "force-dynamic"` to all `/api/system/*` route handlers. Without this, Next.js could cache these routes during production builds, potentially serving stale update check results and triggering a false "update available" popup on fresh installs.

[2026-04-09] Added Apple Developer certificate import step to release workflow for proper codesigning and notarization in CI. Deduplicated getNvmNodeBin() in cabinet-daemon.ts to use the shared nvm-path.ts utility.

[2026-04-09] Cap prompt containers to max-h with vertical-only scrolling. Added "Open Transcript" button to the prompt section in conversation-result-view (matching the existing one in Artifacts). Also added anchor link on the full transcript page.

[2026-04-09] Apply markdown rendering to Prompt section on transcript page via ContentViewer. Extracted parsing logic into shared transcript-parser.ts so server components can pre-render text blocks as HTML (client hydration doesn't work on this standalone page). Both prompt and transcript text blocks now render with full prose markdown styling.

[2026-04-09] Improved transcript viewer: pre-processes embedded diff headers glued to text, detects cabinet metadata blocks (SUMMARY/CONTEXT/ARTIFACT inside fenced blocks), renders orphaned diff lines with proper green/red coloring, renders markdown links and inline code in text blocks, styles token count as a badge footer. Also added +N/-N addition/removal counts in diff file headers.

[2026-04-09] Rich transcript viewer: diff blocks show green/red for additions/removals with file headers, fenced code blocks get language labels, structured metadata lines (SUMMARY, CONTEXT, ARTIFACT, DECISION, LEARNING, GOAL_UPDATE, MESSAGE_TO) render as colored badges. Copy button added to transcript section.

[2026-04-09] Render prompt as markdown on the transcript page too, with a copy button. Server-side markdown rendering via markdownToHtml, matching the prose styling used elsewhere.

[2026-04-09] Render conversation prompt as markdown in the ConversationResultView panel instead of plain text. Uses the existing render-md API endpoint with prose styling, falling back to plain text while loading.

[2026-04-09] Unified toolbar controls across all file types. Extracted Search, Terminal, AI Panel, and Theme Picker into a shared `HeaderActions` component. CSV, PDF, and Website/App viewers now include these global controls in their toolbars, matching the markdown editor experience.

[2026-04-09] Added "Open in Finder" option to each sidebar tree item's right-click context menu. Reveals the item in Finder (macOS) or Explorer (Windows) instead of only supporting the top-level knowledge base directory.

[2026-04-09] Fixed Claude CLI not being found in Electron DMG builds. The packaged app inherits macOS GUI PATH which lacks NVM paths. Added NVM bin detection (scans ~/.nvm/versions/node/) to RUNTIME_PATH in provider-cli.ts, enrichedPath in cabinet-daemon.ts, and commandCandidates in claude-code provider.

[2026-04-10] Added send icon to each agent card in the Team Org Chart. Clicking it opens the agent's workspace with the composer focused, letting users quickly send a task to any agent directly from the org chart. Also added to the CEO card.

[2026-04-10] Replaced send-icon navigation with a quick-send popup dialog on the Org Chart. Clicking the send icon on any agent card opens a blurred-backdrop modal with the full chat composer (textarea, @mentions, keyboard shortcuts). Submitting navigates to the conversation view.

[2026-04-10] Added in-app toast notifications for agent task completion/failure. When a conversation finishes, a slide-in toast appears in the bottom-right with agent emoji, status, and title. Clicking navigates to the conversation. Uses an in-memory notification queue drained by SSE. Documented in notifications.md.

[2026-04-10] Added notification sounds for task completion/failure toasts. Uses Web Audio API to synthesize tones — ascending chime for success, descending tone for failure. No audio files needed.

[2026-04-13] COO heartbeat: posted Week of April 13 operating review at example-text-your-mom/company/operations. Added "Marketing Activated?" and "Financial Risk" columns per CFO request. Sent messages to CFO (confirming column addition), CEO (overdue items + activation checklist), and Product Manager (OB-3 resizing + OB-6 schema priority). Created concrete 5-step activation checklist for paused marketing cabinets.

[2026-04-13] Separated chevron toggle from page navigation in sidebar tree nodes — clicking the chevron now only expands/collapses, clicking the label navigates.

[2026-04-13] Agents page: moved conversations panel to the right side; added heartbeat schedule and job pills to each agent card in the org chart.

[2026-04-13] Registry detail About section now renders markdown via dangerouslySetInnerHTML using server-side unified/remark HTML conversion. Added .registry-prose CSS class with parchment-palette styles (headings, lists, code blocks, blockquotes) to globals.css.

[2026-04-13] Registry About section: strip [[wiki links]] before rendering, fix list bullets (list-style-type: disc), increase vertical spacing for readability.

[2026-04-13] Replace native window.confirm() delete prompts in sidebar with styled Dialog — triangle-alert icon in destructive/10 background, context-aware title/description for cabinet vs page vs linked dir. Updated both tree-node and tree-view cabinet delete dialogs.

[2026-04-13] White and Black themes now explicitly set font to var(--font-sans) so they use Inter rather than the browser default when data-custom-theme is active. Also optimized registry template download to use a single recursive git tree API call instead of recursive per-directory listing.

[2026-04-13] Cabinet scheduler controls: replace alarming red/green split-button with neutral muted styling; add pulsing green "Live" indicator when agents are active; unify button sizing (same height, icon size, padding).

[2026-04-13] Fix split button separation: wrap main+chevron in a shared flex container so they render as one joined control.

[2026-04-13] New Cabinet dialog: replace tiny "import a pre-made team →" text link with a full-width card button featuring icon, title, description, and arrow — separated from the create form by an "or" divider.

[2026-04-13] New Cabinet dialog: move "Import from Registry" to header top-right as a compact button next to close; remove bottom card + or-divider that made dialog too tall.

[2026-04-13] Registry browser header: add "cabinets.sh" and "Star us" (→ github.com/hilash/cabinets) link buttons in top-right. Also committed calendar overflow fix from cabinet-view/schedule-calendar.

[2026-04-13] Registry header buttons: cabinets.sh uses accentBg/accent colors; Star us uses filled accent (#8B5E3C) with white text as primary CTA.

[2026-04-13] Registry header title changed from "Cabinet Registry" to "Cabinets | AI teams, off the shelf" with the tagline in muted weight.

[2026-04-13] Editor conversations now resolve their owning cabinet by walking up the directory tree to find the nearest .cabinet manifest. Added findOwningCabinetPathForPage utility. Conversations list shows "edited: {path}" for editor agent entries.

[2026-04-13] AI Editor panel now shows optimistic "starting" sessions immediately after submit and promotes one selected live session to a visible stream area, even when work is running on another page. Added page/agent context chips, "Open Page" jump action, and background-mounted hidden terminals for non-selected sessions so streaming stays alive while the UI feels responsive.

[2026-04-13] Moved editor file-type and Cabinet-structure knowledge out of `data/getting-started` and into the canonical editor library template at `src/lib/agents/library/editor/persona.md`. New cabinet creation and onboarding now resolve agent templates from the seeded library or source fallback, enforce mandatory `ceo` + `editor`, and create full agent scaffolds including `workspace/`.

[2026-04-13] AI editor runs now use the owning cabinet's `editor` persona when editing a page inside a cabinet, fall back to the shared editor template when needed, and default their working directory to the owning cabinet instead of the global data root. Electron packaging now seeds `.agents/.library` from `src/lib/agents/library` so fresh managed data directories can install agents correctly.

[2026-04-15] Updated stale runtime docs across `README.md`, `AI-claude-editor.md`, `CLAUDE.md`, `AI_PROVIDER_RUNTIME_PROGRESS.md`, and `data/getting-started/index.md` to reflect the current adapter-based execution model. Documented that tasks/jobs/heartbeats now default to structured transcript-driven runs, listed the remaining migration work, and clarified that `WebTerminal` is being kept intentionally for interactive and future tmux-like Cabinet features.

[2026-04-16] Cathie Wood heartbeat: applied disruptive innovation lens to AAPL and META. AAPL rated bullish conviction 3 — Apple Intelligence + health platform convergence is compelling but AAPL is a platform defender, not early-stage disruptor; 5-year bull case $350. META rated bullish conviction 5 — sits at exact AI × spatial computing × social convergence ARK targets; $115-135B capex is Wright's Law in action; 5-year bull case $1,400. Both signals appended to market-analysis/signals.csv.

[2026-04-16] Image Creator heartbeat: designed 2 TikTok carousels from Script Writer briefs. Carousel 01 ("Text your mom before she sends ?", 5 slides) uses lock-screen mockup + iMessage chat bubble + giant red "?" aesthetic. Carousel 02 ("The fake mental math of reply guilt", 6 slides) uses iOS Screen Time stats card + progress bar + timer CTA aesthetic. Both saved to data/example-text-your-mom/marketing/tiktok/carousels/ and content-ideas.csv updated to "Designed" status.

[2026-04-16] Built UI prototype for new task conversation experience at /tasks/[id] (full-page route). Multi-turn chat layout with inline per-turn artifacts (file edits, commands, tool calls, KB pages), Chat/Artifacts/Diff/Logs tabs, status badge state machine (idle/running/awaiting-input/done/failed/archived), token usage bar with 80%/95% thresholds, editable rolling summary, and composer with awaiting-input visual state. Self-contained with mocked data — no backend changes yet. Components in src/components/tasks/conversation/.
[2026-04-17] Data Analyst agent: initialized empty workspace readme as first-heartbeat baseline.
[2026-04-17] Operations Manager (real-estate) first heartbeat: created transaction-pipeline/index.md with stage gates, per-deal compliance checklists, deadline tracking, and bottleneck log.
[2026-04-17] Broker (real-estate) first heartbeat: created broker-playbook.md with pricing doctrine, weekly checks, and open questions; added listings/inventory-template.md for weekly listing-agent reporting.
[2026-04-17] Image Creator heartbeat: added 5 fresh TikTok content angles to marketing/tiktok/content-ideas.csv covering missed reply guilt, mom texting patterns (novel-length texts, typing-indicator anxiety), group chat avoidance, and wholesome streak accountability. Each idea includes hook, format, and suggested post date (Apr 21–30).

[2026-04-17] Polished task conversation prototype typography and artifact styling. Conversation body now uses serif (Tailwind font-serif) at 15.5px / 1.7 line-height for a friendlier reading feel. Artifact rows: file paths split into muted directory + bold filename (no full mono path), each row is a card with bg + ring, subtle hover, slightly larger icons. Same treatment applied to the Artifacts tab list.
[2026-04-17] Fundamentals Analyst heartbeat: wrote earnings-preview rows for META (Q1 ~Apr 30), AAPL (Q2 FY26 ~May 1), NVDA (Q1 FY27 ~late May) in ai-hedge-fund/market-analysis/signals.csv. Each row lists the specific line items investor agents should watch. Scorecards unchanged from Apr 14 cycle.
[2026-04-17] Reddit Researcher job: added analytics/signals-2026-04-17.md with five emotional/behavioral patterns (reply avoidance, mom-guilt, routine decay, pre-send anxiety, fake tripwire); updated subreddits.csv notes and added r/momforme, r/internetparents to Watch; linked digest from analytics index.
[2026-04-17] Portfolio Manager scan: appended April 17 intelligence entries to news.md for NeuralFlow, MintLayer, GreenPulse, DevForge (new file), DataStream (new file). Flagged GitHub Actions pricing hike as top signal in vc-os/index.md Intelligence Feed — directly relevant to DevForge Series A runway path.
[2026-04-17] Valuation Analyst heartbeat: wrote corrected DCF/multiples signals to ai-hedge-fund/market-analysis/signals.csv for AAPL (expensive/4, $260 vs base DCF $195 = 33% premium, PEG 3.8), NVDA (expensive/4, $850 vs base $680 = 25% premium, PEG fwd 1.6), META (fair/3, $630 vs base $565 = 12% premium, PEG 1.17). Incorporated Lynch price corrections, raised Reality Labs NPV drag to -$60/share per Munger/Burry governance argument.

[2026-04-17] Task conversation prototype: added context-aware "wrap-up" card after the latest agent turn. Appears only when status is `idle` and last turn is a settled agent reply (not pending, not awaiting-input). Offers "Mark done" + "Not yet" inline so the user doesn't have to reach back to the header. Header button now also wires to mark-done and disables once done. Send flow simulates a 1.8s settle so the wrap-up surfaces naturally after a reply.

[2026-04-17] Wrote Task Conversations PRD at data/TASK_CONVERSATIONS_PRD.md. Documents goals, status state machine, on-disk data model (tasks-as-directories with per-turn files), UI surfaces (quick-peek panel + full /tasks/[id] page), behavior specs (continuity, awaiting-input, auto-summary, token budgeting, wrap-up affordance), API surface, adapter changes for Claude Code/Codex/Gemini, phasing, success metrics, and open questions.

[2026-04-17] Phase 1 of Task Conversations: built new task-store with on-disk schema (data/{cabinet?}/.agents/.tasks/{id}/{task.md, turns/NNN-{user|agent}.md, session.json, artifacts.json, events.log}). New types in src/types/tasks.ts. Auto-status transitions: agent reply with awaitingInput → "awaiting-input"; non-zero exitCode → "failed"; clean reply → "idle"; user reply → "running". Token aggregation across turns, denormalized artifacts index rebuilt on each turn. 11 unit tests in task-store.test.ts cover create/append/update/list/delete/round-trip — all passing.

[2026-04-17] Phase 2 of Task Conversations: API endpoints under /api/tasks. GET (list, filter by status/trigger/agent/cabinet), POST (create with first user turn), GET /[id], PATCH /[id] (title/summary/status with completedAt handling), DELETE /[id], POST /[id]/turns (append user or agent turn), GET /[id]/events (SSE with 15s heartbeat). Added in-memory event bus (task-events.ts) wired into store writes for live updates. Smoke-tested end-to-end on :5354 — disk format matches PRD spec.

[2026-04-17] COO heartbeat in garden: normalized tag `poetry` → `poem` on hagrid/index.md for consistency with other garden poems.
[2026-04-17] Legal: drafted Privacy Policy at data/legal/privacy-policy/index.md covering self-hosted model, third-party AI provider data flows (Anthropic, OpenAI), GDPR/CCPA posture, and security responsibilities. Updated legal index with policy link and backlog (NDA, license, DPA, cookies).
[2026-04-17] Listing Agent heartbeat: created active-inventory.md as the live counterpart to inventory-template.md, with review queue and price-adjustment log scaffolds.
[2026-04-17] Marketing Specialist heartbeat: scaffolded marketing infrastructure — campaigns/, social-calendar/, and leads/ pages under real-estate/marketing/ with active campaign tracker, weekly posting template, and lead intake log + qualification checklist.

[2026-04-17] App Development PM heartbeat: ratified OB-7 A/B approach (device-ID bucketing, server-side persistence), resized OB-3 from M to L and deferred it, scoped Apr 21 sprint with named ownership gates, and wrote pm-updates/index.md. Updated backlog to reflect decisions.
[2026-04-17] Ops Manager daily lead follow-up run: created `real-estate/client-relations/leads.md` (funnel source of truth) and `real-estate/client-relations/daily-status/2026-04-17/index.md` (daily status note). Updated Client Relations index with sub-page map. Pipeline is empty — today's priority is seeding intake, not chasing follow-ups.

[2026-04-17] Phase 3 of Task Conversations: wired the /tasks/[id] page to real data. Page now takes taskId param, fetches /api/tasks/[id] on mount, subscribes to /api/tasks/[id]/events SSE, refreshes state on every event. Composer POSTs real turns; Mark done + summary edit PATCH the API. Demo route (/tasks/demo) keeps the in-memory mock + simulated agent reply for visual reference. Mock-data refactored to produce the real Task shape so artifact + turn components share types from src/types/tasks.ts. Added /tasks/new page with title + first-message form that creates a real task and redirects.

[2026-04-17] Cathie Wood heartbeat: filed CORRECTION signals for AAPL (downgraded bullish/3 → neutral/2 — price corrected from $175 to $260.48 breaks 5-year CAGR hurdle) and META (bullish/5 → bullish/4 — price corrected from $510 to $630 reduces entry asymmetry) and reaffirmed NVDA bullish/5 at ~$850. Updated signals.csv with three rows.
[2026-04-17] Warren Buffett agent issued corrections to AAPL (bullish/5 → neutral/3) and META (bullish/4 → neutral/3) signals after stale-price corrections reset valuations to .48 and .86 respectively. No margin of safety at current prices; governance concerns on META dual-class structure sharpened by Munger/Burry analyses.
[2026-04-17] Michael Burry heartbeat — appended three signals to ai-hedge-fund/market-analysis/signals.csv. Upgraded META bear conviction 3→4 into 13-day earnings catalyst (FCF <$5B = thesis confirmation). Refreshed NVDA bear at conviction 4 with new Microsoft Maia GA + Blackwell pricing pushback data. Passed on AAPL (expensive, but strong fundamentals — not a Burry setup).

[2026-04-17] Editor heartbeat: added new Luna Lovegood page to garden/ poetry collection (three poem sections: Luna herself, The Thestrals, After the War). Updated garden index to list Luna. Added cross-link from Neville to Luna.

[2026-04-17] Cathie Wood heartbeat: refined NVDA signal from bullish-5 to bullish-4 (convergence thesis intact but Wright's Law cuts both ways as Maia/TPU/Trainium scale); refreshed META bullish-5 at $630 — 5yr bull $1,400 still delivers 17.3% CAGR hurdle. Reaffirmed: drawdowns in intact-thesis platforms are buys, not sells.
[2026-04-17] DevOps reviewed bug-triage.csv: added Priority Rank, Trust Vector, and Monitoring Required columns; tightened DevOps Risk guidance with explicit ship recommendations. Created triage-decisions.md with severity rubric, ranked fix order, mermaid risk map, and open questions for CTO/PM.

[2026-04-17] Editor heartbeat (garden): added new Dobby poem page (four-stanza arc from pillowcase servitude to Shell Cottage grave), updated garden/index.md to list Luna + Dobby, and cross-linked Luna → Dobby. Garden now has 6 poem pages.

[2026-04-17] Image Creator heartbeat (example-text-your-mom): no carousel-format scripts in Script Ready queue. Updated carousels/index.md pattern library from 5 bullet-list entries to 8-pattern reference table (added Labeled Inventory, Decision Tree, Timeline) so future briefs can reference patterns by name. Flagged mockup findings from group-chat-shame/slide-2 for Script Writer to unblock research validation.

[2026-04-17] Copywriter heartbeat (example-text-your-mom): promoted the "reply guilt math" carousel from Designed to In Production by adding it to week-of-april-14 shooting brief as Script 3. Refined the hook from "You are not busy. You are emotionally buffering." to the caption-native "Not busy. Buffering." (original long form preserved as the Slide 3 payoff). Updated content-ideas.csv and linked the standalone brief to the shooting brief so activation week now has three pieces, not two.

[2026-04-17] Image Creator heartbeat (example-text-your-mom/marketing/tiktok): drafted the horror-movie-group-chat brief (5-slide carousel, escalating notification badge as the monster) and promoted Day 4 guilt + typing-dots briefs from Research to Script Ready after reconciling duplicate drafts from a parallel agent. Bumped typing-dots priority from Medium to High (most visually native concept in backlog), updated briefs/index.md with a Script Ready table of five carousels and a scheduled-post column, and flipped the three CSV rows in content-ideas.csv accordingly.

[2026-04-17] Copywriter heartbeat (example-text-your-mom): wrote nudge-copy-pool.md inside the smarter-reminder-timing PRD directory — 88 push-notification copy variants (22 each for parent/friend/sibling/partner) unblocking US-3 Contextual Nudge Copy. Every variant follows the banned-words list (no task/schedule/productivity/alert) and threads the "reply before the guilt spiral" engine. Added rotation rules for engineering, including the day-based suppression of {week_phrase} tokens and late-night tone preferences. Detected a parallel Day 4 carousel brief from another agent and removed my duplicate 4-slide version rather than fork the spec.

[2026-04-17] Phase 4 of Task Conversations: adapter session continuity. Patched claude-local adapter to honor ctx.sessionId — passes `--resume <id>` instead of `--no-session-persistence` when resuming. Flipped supportsSessionResume to true. Built task-runner.ts: loads task, picks adapter by providerId/adapterType, appends pending agent turn, runs adapter with resume session id (if alive) or replay prompt, updates turn with final content + tokens + exit state, persists new session handle. Wired POST /api/tasks + POST /api/tasks/[id]/turns to kick off runTaskTurn in the background after a user turn. Added 4 unit tests covering fresh run, live-session resume (only sends latest user message), replay-only adapter (full history concat), and failure handling. All 16 tests pass.

[2026-04-17] Phase 5 of Task Conversations: awaiting-input heuristic + auto-summary + token-window defaults. New task-heuristics.ts: looksLikeAwaitingInput() flags an agent reply as a clarifying question when the last non-fenced line ends with "?" and the content isn't mostly code; deriveSummary() builds a rolling 1-sentence summary from the latest settled agent turn (or first user turn as fallback), truncating to 180 chars. Wired into task-runner: after each agent turn settles we mark awaitingInput on the turn and flip task status; we also regenerate the summary unless the user edited it in the last 5 minutes. createTask now defaults runtime.contextWindow to 200k so the token bar always renders with real thresholds. 9 new heuristic tests + 3 new runner integration tests (awaiting-input detection, auto-summary, user-edit skips auto-summary). 28 tests total, all green.

[2026-04-17] Tasks index at /tasks — grouped list with status badges, summary line, runtime label, token count, relative last-activity. Filter pills (All / Active / Awaiting / Done). Empty state with CTA. POST /api/tasks now defaults to providerId=claude-code, adapterType=claude_local, model=claude-opus-4-7, effort=medium when caller omits them, so the task detail header reads a real runtime instead of "default runtime" from turn one.

[2026-04-19] Terminal mode fullscreen polish: (T16) fullscreen layout with 40px dark top strip + WebTerminal filling viewport (no tabs / token bar / summary / separate prompt card); (T17) status chip now always leads with terminal icon + pulsing emerald ring while running, absorbing the old separate PTY badge; (T18) fixed a silent functional bug — `continueConversationRun` bailed for legacy adapters because they have no `adapter.execute`; new branch reopens the PTY via `createDaemonSession` so terminal-mode Continue actually works; (T23) Stop button (SIGTERM via `stopConversation()`) appears in the top strip while running or awaiting input. (#10b) Model + effort passthrough for the remaining 6 providers — Gemini `-m`, Cursor/Grok/Copilot `--model`, OpenCode `--model` + `--variant`, Pi `--model` + `--thinking`. POST `/api/agents/headless { providerId, prompt, model?, effort? }` works for all 8 providers now.

[2026-04-19] Provider runtime clean-up round: shipped items #3, #7, #10 from PROVIDER-CLI §12. (3) Dynamic model discovery — `AgentProvider.listModels()` optional hook; OpenCode uses `opencode models`, Pi uses `pi --list-models`, both fall back to hardcoded lists. New `GET /api/agents/providers/[id]/models` route serves the list with a 60s TTL memory cache. (7) Adapter-lifecycle tests — `registry.test.ts` snapshot updated to include all 16 adapter types (8 structured + 8 legacy) and asserts every CLI provider has a matching `legacy_pty_cli` adapter; new `legacy-ids.test.ts` proves the client-safe `LEGACY_ADAPTER_TYPES` stays in sync with the server `LEGACY_ADAPTER_BY_PROVIDER_ID`. (10) Model + effort passthrough on `/api/agents/headless` — endpoint accepts optional `{ model, effort }`; `provider.buildOneShotInvocation` gained an `OneShotInvocationOptions` parameter. Claude (`--model`) and Codex (`--model` + `-c model_reasoning_effort=…`) wired today; `/providers-demo` now forwards the selected model + effort. Skipped #1 (already implemented via `writeSession(conversationId, { codecBlob })`) and #6 (superseded by first-class terminal mode). Deferred #2 (skills catalog decision), #4 (directory refactor), #11 (glyph artwork), and the remaining 6 providers' model-override wiring.

[2026-04-19] Terminal-mode runtime (round 1). Registered `<provider>_legacy` PTY adapters for all 8 providers (previously only Claude + Codex). `RuntimeMatrixValue` / `ConversationRuntimeOverride` gain `runtimeMode: "native" | "terminal"`. Runtime picker has a new Native/Terminal toggle above the provider tabs (`showRuntimeModeToggle` prop, enabled in the composer dropdown); Terminal mode hides model + effort controls, swaps the chrome to dark zinc, and tags the selection banner with a PTY pill and terminal glyph. POST `/api/agents/conversations` translates `runtimeMode === "terminal"` into the provider's legacy adapter via `LEGACY_ADAPTER_BY_PROVIDER_ID`. Fix: `normalizeSelection` + `sameSelection` now preserve `runtimeMode` so the toggle latches instead of reverting on the next render.

[2026-04-19] Terminal-mode runtime (round 2). Task viewer now renders a real xterm-backed `WebTerminal` for `isLegacyAdapterType(meta.adapterType)` tasks (previously the PTY's raw TUI was being rendered as scrambled markdown in the TurnBlock chat). Above the terminal sits a new `TerminalPromptHeader` with the original prompt, copy-to-clipboard, provider chip, PTY pill, and status-derived live indicator. When the task is idle, the existing `TaskComposerPanel` renders below in a dark-themed override with `runtimeMode: "terminal"` pinned in `initialRuntime` so Continue routes back through the legacy adapter via `continueConversationRun`. Icon markers added: task board cards get a left emerald rail + `PTY` chip in the top-right; task detail header gets a `PTY` chip next to the title; sidebar recent tasks gets a small terminal glyph at the trailing edge. All three surfaces key off `isLegacyAdapterType`. Fix: moved the legacy-type check into a client-safe `src/lib/agents/adapters/legacy-ids.ts` module (the full `adapters` barrel transitively imports `child_process` via `utils.ts` and can't be bundled into the client).

[2026-04-18] Onboarding provider step redesigned. Replaced the stacked full-width list with a 4-col responsive grid of compact cards (2-col on xs → 3-col sm → 4-col md+). Cards are sorted ready → installed-but-not-auth → not-installed and auto-select the first ready provider. Clicking a ready card selects it; clicking an unready card's chip (Log in / Install) or the Guide arrow opens a single install/verify drawer below the whole grid instead of expanding inline per row. Added RuntimeSelectionBanner above the model chip grid for visual continuity with the task composer. Refreshed the "Coming soon" list (removed stale Gemini CLI entry; replaced with Plugin SDK).

[2026-04-18] Welcome home step gets an animated blueprint background. New `HomeBlueprintBackground` component renders an SVG floor plan with 5 rooms (Office / Study / Lab / Family Room / Blank). On mount the animation sequence runs: corner brackets and measurement ticks first, then the outer walls draw clockwise, interior walls segment-by-segment leaving doorways, door arcs swing open, room labels fade in with an underline flourish, and cabinets / shelves / sofas / plants pop into each room with a staggered spring. Faint graph-paper grid with a radial vignette, plus small decorative dots that pulse continuously after the build. Respects `prefers-reduced-motion`. The name form sits on a frosted white panel so the blueprint reads as ambient background without fighting the input.

[2026-04-18] New `/providers-demo` page. Exercises every provider server API in one view: GET /api/agents/providers, GET /api/agents/providers/status, POST /api/agents/providers/[id]/verify, POST /api/agents/headless. Per-provider card shows glyph + id + status + version + adapter; has Verify and Send-prompt buttons with inline result panes (exit code, duration, stdout/stderr, hint). Shared prompt textarea with `{{provider}}` templating. Model/effort selectors are shown for reference (the headless endpoint currently uses provider defaults). Scrolling API call log at the bottom with method/url/status/ms and expandable request/response bodies.

[2026-04-17] Scheduler: propagate scheduledAt through jobs and heartbeats. cabinet-daemon's cron task now stamps each tick with a minute-rounded ISO timestamp and forwards it to the job/heartbeat run endpoints. conversation-runner, startJobConversation, executeJob, runHeartbeat, startManualHeartbeat all accept scheduledAt and thread it into ConversationMeta (new optional scheduledAt field in src/types/conversations.ts + conversation-store). Lets the calendar group runs by their intended fire time rather than actual run time.

[2026-04-17] Notion-grade editor upgrade — full parity pass. Added text color + background highlight (Tiptap Color/Highlight with custom palette), underline, subscript/superscript, 4-way text alignment, all wired into toolbar AND a new selection-bubble menu with swatch pickers. New Image node view has inline drag-to-resize handles with live pixel readout. New /Embed slash command + URL-paste auto-embed recognizes YouTube, Vimeo, Loom, X/Twitter, TikTok, Facebook, Instagram, Spotify, raw video files, and falls back to iframe for any URL; src/lib/embeds/detect.ts is the single detection helper. /Image, /Video, /File slash commands now open a rich popover with Upload + From URL tabs plus a tip showing paste/drop also works. Added emoji picker (emoji-mart) and KaTeX math via @aarkue/tiptap-math-extension with $…$/$$…$$ input rules. Drag handle plugin adds a hover ⋮⋮ gutter for block reordering. Slash menu grouped into Basic / Media / Advanced sections for discoverability. Turndown now preserves inline <span style="color:…">, <mark>, <u>, <sub>, <sup>, <video>, iframe/embed blocks, sized <img>, Twitter blockquotes, aligned blocks, and inline math — so every new mark/node roundtrips cleanly through markdown-on-disk. to-html rewrites relative data-src too so local videos inside embed blocks render. 14 new files under src/components/editor/extensions + /editor/*popover + /editor/bubble-menu.tsx + /editor/color-palette.tsx + /editor/emoji-picker.tsx + src/lib/embeds/detect.ts. Upload API response already carried url/filename/markdown; no route changes needed.

[2026-04-17] v2 task conversations integration — 7-phase build on top of the existing conversation system. (1) Extended ConversationMeta with multi-turn fields (turnCount, lastActivityAt, tokens, runtime, doneAt, archivedAt, awaitingInput, titlePinned, summaryEditedAt) + turns/ subdirectory schema. (2) continueConversationRun in conversation-runner with Claude --resume support and full-replay fallback; reuses buildAgentContextHeader / buildKnowledgeBaseScopeInstructions / buildDiagramOutputInstructions / buildCabinetEpilogueInstructions / buildMentionContext verbatim. (3) POST /api/agents/conversations/[id]/continue + GET /events + extended PATCH on the existing route. (4) UI rewired: task-client.ts hits conversation endpoints, view-model adapter in conversation-to-task-view.ts. (5) Artifact rows rewritten as KB-page cards (page-type-icons util + /api/kb/pages/meta resolver). (6) Retired v1 parallel plumbing (task-store, task-runner, task-events, /api/tasks/**). (7) Agents workspace convergence: TaskConversationPage gained variant="compact" + readOnly props; TaskDetailPanel embeds the compact viewer; transcript page demoted with "Open in task viewer" button.

[2026-04-17] Task follow-ups: runtime picker on /tasks/new (TaskRuntimePicker wired through createTaskRequest with ConversationRuntimeOverride), /compact header button wired to new POST /api/agents/conversations/[id]/compact (collapses prior turns into a digest via the adapter, kills session handle), global GET /api/agents/conversations/events SSE stream feeding sidebar recent-tasks + /tasks list for live auto-refresh, and strip trailing cabinet block from agent turn display content so it no longer shows in chat bubbles.

[2026-04-17] Office + Google Workspace document support. New inline viewers for .docx (docx-preview), .xlsx/.xlsm (SheetJS grid with sheet tabs), .pptx (pptx-preview slide list) — all loaded via next/dynamic so the parser libraries don't ship in the initial bundle. Office files drop into /data anywhere and render in place of the old "Open in Finder" fallback. Tree-builder adds DOCX/XLSX/PPTX classification and keeps legacy .doc/.xls/.ppt on the fallback path. Sidebar gets FileText/FileSpreadsheet/Presentation Lucide glyphs for the new types. Assets API now honours HTTP Range requests so large office files stream. New GoogleDocViewer + src/lib/google/detect.ts recognize Sheets/Slides/Docs/Forms/Drive URLs and compute embed + open URLs; a markdown page becomes a Google viewer by adding a google.url frontmatter key. OfficeChrome shared header (Reveal, Download, Open external) keeps visual parity with FileFallbackViewer. Read-only for this iteration; OAuth + offline snapshots are designed-for but deferred.

[2026-04-17] Daemon-backed continues + session-expired fallback. Fixed "No conversation found with session ID" error on continuing done conversations: runner now detects session expiry from adapter errorMessage and retries in replay mode with null session id, clearing alive flag. appendUserTurn clears doneAt / archivedAt so continuation reopens a done task. Routed continueConversationRun through cabinet-daemon (with in-process fallback for tests via CABINET_TASK_RUNNER=inprocess) — survives Next.js HMR. Daemon extended with adapterSessionId + adapterUsage fields on StructuredSession, exposed via /session/:id/output. Daemon persists session.json to conversation dir when adapter returns a resume session id. claude-local adapter dropped --no-session-persistence so fresh runs create resumable sessions. End-to-end verified: turn 2 correctly recalls turn 1 memory via claude --resume.

[2026-04-17] Task viewer follow-ups: Logs tab (events.log reader + raw transcript, color-coded event types), Diff tab (per-artifact git history within task window via getPageHistory + getDiff, unified diff rendering with +/- line tinting), TasksBoard kanban switched from 5s polling to global SSE + routes card clicks through setSection({type:"task"}) for in-shell viewer, structured <ask_user>...</ask_user> convention added to cabinet epilogue so awaiting-input is deterministic (question-mark heuristic kept as legacy fallback) with markers stripped on display.

[2026-04-18] Jobs & heartbeats calendar gets a Compact ↔ Roomy density slider. Day/week views now measure their scroll container with a ResizeObserver and compute HOUR_HEIGHT dynamically: at density=0 the visible TOTAL_HOURS exactly fits the page (no scroll), each density unit adds 1px per hour row so the grid grows past the viewport and scrolls. MIN_HOUR_HEIGHT=22 keeps the grid readable on tiny screens, and the slider is hidden in month mode where density doesn't apply. Slider lives next to the day/week/month picker in tasks-board.tsx.

[2026-04-18] Refresh perf: stop blanking the app on reload. Tree and last-opened page are now cached in localStorage and painted instantly on mount, then revalidated in the background (SWR). Onboarding config check and useCabinetUpdate deferred to requestIdleCallback so they don't sit in the critical path — and the "wizard done" flag is cached so returning users skip the blocking `showWizard === null` render that was the main blank-screen culprit. Tree cache key is bucketed by showHidden.

[2026-04-18] Fix Compact slider in Jobs & heartbeats. Wrapped the base-ui Slider in an explicit `w-24` container — the Slider Root's default `data-horizontal:w-full` had higher selector specificity than our `w-24` className override, so inside a flex row the slider was collapsing/expanding unpredictably and the thumb wasn't grabbable. Wrapping gives the Root a real 96px parent to fill.

[2026-04-18] Off-window event indicators + editable hour range for Jobs & heartbeats calendar. Visible hour window (default 5 AM – 11 PM) is now state, not constants — persisted globally in localStorage under `cabinet-calendar-hours` so user prefs survive reloads and apply across cabinets. New `src/lib/storage/calendar-prefs.ts` handles load/save with clamping. When events fall outside the visible range on visible days, a chevron strip renders above/below the time grid: "▲ N earlier · show 4 AM" / "▼ N later · show 11 PM". Click expands the range by one hour and saves the new bounds. Header gets a "5 AM – 11 PM" Clock3 button that opens a small dropdown with two hour-pickers (From/To, dedup-disabled) and a Reset button. Refactored TimeGridView to take `visibleStartHour`/`visibleEndHour` props instead of module constants; `TOTAL_HOURS` now derived per-render. Default behavior unchanged when no prefs are stored.

[2026-04-18] Schedule view header controls now actually filter the calendar/list. The agent dropdown ("CEO · Cabi", "All visible agents", etc.) was wired only to the kanban conversation list — picking a single agent did nothing on the calendar/list views. Added `scheduleAgents`/`scheduleJobs` memos that filter `overview.agents` by `scopedId` and `overview.jobs` by `ownerScopedId` whenever a single agent is selected, and pass those filtered lists to `<ScheduleCalendar>` and `<ScheduleList>`. Refresh button now also fires a `cabinet:toast` success event so users get visible feedback even when no data changed.

[2026-04-18] Fix "Encountered a script tag while rendering React component" error in `src/app/layout.tsx`. Replaced the raw `<script>` in `<head>` (which React refuses to execute on client renders under Next 16/Turbopack) with Next.js `<Script id="cabinet-electron-detect" strategy="beforeInteractive">`. Dropped the empty `<head>` wrapper since App Router manages head via metadata.

[2026-04-18] Added `have-fun/voldemort/index.md` — a six-part Voldemort poem sequence (Tom at eleven → diary → horcruxes → the name → King's Cross → epitaph).

[2026-04-18] Added `have-fun/typing-tests-haiku.md` — 30-word haiku sequence about typing tests (fingers, clock, mistakes, speed).

[2026-04-18] Fix Next 16 "Encountered a script tag while rendering React component" warning by dropping the `next/script` electron-detect tag from `src/app/layout.tsx` and moving the `CabinetDesktop` → `electron-desktop` class check into `ThemeInitializer`'s `useEffect`.
[2026-04-18] Brainstormed 10 Cabinet color theme directions in data/ideas/cabinet-themes.md — each with OKLCH values ready to drop into globals.css, covering paperback, nocturne, garden, graphite, sunset, cobalt, high-contrast, terminal amber, matcha, and monokai+ palettes.

[2026-04-19] Copywriter agent drafted Mother's Day email campaign (3 segmented versions + push + social) at `data/.agents/copywriter/workspace/mothers-day-email.md`. Send date May 8, Mother's Day is May 10. Aligned with "reply before the guilt spiral" voice and Q2 activation/re-engagement goals.

[2026-04-19] Fix two console errors at app boot. (1) `next-themes` 0.4.6 renders an inline `<script>` inside its provider for FOUC prevention; React 19/Next 16 logs "Encountered a script tag while rendering React component". Switched `src/components/theme-provider.tsx` to load `NextThemesProvider` via `next/dynamic({ ssr: false })` — Cabinet's own `ThemeInitializer` already applies theme classes from localStorage, so the next-themes script is redundant for our custom theme system. (2) `app-shell.tsx` initialized `showWizard` from `localStorage` inside `useState(() => …)`, so the server rendered the loading branch (`null`) while the client immediately rendered the main shell (`false`) for cached users — that mismatch was the source of the `<aside>`/`text-foreground` hydration warning. Initialized `showWizard` to `null` on both server and first client render, then read localStorage in an isomorphic `useLayoutEffect` so cached users still skip the blank-screen flash without diverging from SSR HTML.

[2026-04-19] Replace `next-themes` 0.4.6 with a tiny in-tree `<ThemeProvider>` + `useTheme()` in `src/components/theme-provider.tsx`. The upstream provider always renders an inline `<script>` for FOUC prevention, and React 19 + Next 16 logs that as a `Console Error` on every render — `dynamic({ ssr: false })` did not help because the script renders on client mount too. The replacement keeps the same API surface Cabinet actually uses (`theme` / `setTheme` / `resolvedTheme`, `attribute="class"`, `defaultTheme`, `enableSystem`, `disableTransitionOnChange`, localStorage `storageKey`), reads localStorage in `useEffect` so server and first client render match, and applies `light`/`dark` to `<html class>` post-mount. Updated four call sites to import from `@/components/theme-provider` instead of `next-themes`: `editor/emoji-picker.tsx`, `layout/theme-initializer.tsx`, `layout/theme-picker.tsx`, `settings/settings-page.tsx`. `next-themes` no longer imported anywhere in `src/`.

[2026-04-19] Added `data/have-fun/pink-fractals/` — embedded HTML app (with `.app` full-screen marker) that renders 7 recursive fractals (Fractal Tree, Sierpinski Triangle, Sierpinski Carpet, Koch Snowflake, Koch Line, Dragon Curve, H-Tree) depth-by-depth so users watch each iteration form. All-pink UI with 5 palette shades (Rose Garden, Hot Pink, Bubblegum, Pink Sunset, Cherry Blossom), Glow toggle, Build-up toggle (defaults flip per fractal: tree/carpet/H-tree stack, others replace), depth slider (1–9), step delay (80–2000ms), Pause/Resume/Reset, and Save PNG (composites the canvas onto its CSS gradient backdrop so saved files look like what's on screen). Sibling to existing `have-fun/fractal/` (which is the row-painting Mandelbrot/Julia escape-time app — left untouched).

[2026-04-19] Scanner job run for career-ops cabinet: scanned 20 portals, 0 new qualifying roles, flagged stale signals on ID 6 (Supabase), ID 10 (ElevenLabs), ID 19 (Cohere); verified ID 18 (Linear EU Remote) still live at 88 days old. Wrote `data/career-ops/pipeline/scan-digest-2026-04-19.md` and updated `data/career-ops/pipeline/tracker.csv` Last Update + Notes on rows 6/10/18/19. Discarded a prompt-injection attempt in the Stripe WebFetch response.

[2026-04-20] Pattern Analyst heartbeat for career-ops: confirmed pipeline has moved zero stages in 7 days since Apr 13 v2 report. New pattern surfaced — listing decay (3/5 recent portal discoveries went stale within 10 days: ElevenLabs, Supabase, Cohere). Vercel (14d) and Datadog (16d) past ghost threshold. Three critical outcomes unresolved in tracker (HF offer deadline passed Apr 18, Anthropic panel Apr 16, n8n follow-up +10 days overdue). Existing report at `data/career-ops/analytics/pattern-analysis-2026-04-20.md` covers full analysis — this heartbeat focused on dispatching tasks to Conductor/Evaluator rather than re-generating analysis.
[2026-04-20] CEO heartbeat: created daily standup note in .agents/ceo/workspace/ noting 0 active missions, team idle, and flagging need for strategic direction from human.

[2026-04-20] Content Marketer heartbeat: scaffolded /marketing/ directory (blog/, social/, newsletters/, drafts/). Wrote first blog draft "Why Your Knowledge Base Shouldn't Live in Someone Else's Cloud" arguing Cabinet's self-hosted thesis, and a bundle of 4 social posts adapting the blog angles for X/LinkedIn. Goal progress: +1 blog draft (1/10), +4 social posts (4/20).

[2026-04-20] Interview Strategist heartbeat for career-ops: inbox messages from Outreach Specialist were stale (both Anthropic Apr 16 panel and HF Apr 18 deadline have passed; prep packets already existed and were comprehensive). Pivoted work to outcome capture. Created `interview-prep/anthropic-panel-debrief.md` (structured template covering theme hit-rate, story performance, safety-answer landing, surprise questions, next-step intel). Added post-deadline status block to `hugging-face-negotiation.md` with three-scenario fallback matrix (accepted / declined / extended). Updated `interview-prep/index.md` to surface the three critical unknowns (Anthropic outcome, HF outcome, n8n debrief) flagged by Pattern Analyst's Apr 20 report.

[2026-04-20] Career Advisor heartbeat for career-ops: training hold (set 2026-04-12) expired yesterday. Converted to conditional hold pending Anthropic/HF/n8n signal resolution. Pre-staged full rubric evaluations for Reforge AI PM (draft: DO WITH TIMEBOX 30h) and dogfood-cabinet portfolio project (draft: BUILD, score 4.45). Updated `profile/career-advisor-notes.md` with 2026-04-20 directive, scenario-C action ordering, and revised opportunity-cost note. Pinged Pipeline Conductor for signal clarity on HF offer + Anthropic feedback.

[2026-04-20] Researcher heartbeat for career-ops: produced `evaluations/research-weights-and-biases.md` — full 6-axis deep research for W&B (Pipeline ID #13, Screen status, onsite pending). Key strategic framing: CoreWeave acquisition (2025) as required onsite talking point; LLM observability (Weave) vs. classic ML-ops tension as core roadmap conversation; DataPlatform proof points (SDK v3, TTFAC 45→8min, self-serve MRR growth) map bullseye to W&B PLG motion. Verified Mistral research already exists (written by prior agent).

[2026-04-20] Added Death Eaters essay to have-fun/harry-potter-poems/, linked from the collection index under a new Essays section.

[2026-04-20] Terminal-mode task viewer: stream-extract cabinet blocks from live PTY output (daemon now debounce-polls accumulated stdout every 1s; writes meta + fires task.updated + sends /exit on detection). Replaced "No summary captured" / "No artifacts" empty states with an emerald "Waiting for the CLI to emit a cabinet-block summary" hint while running. Bigger status-colored action pill (emerald Done / rose Retry / sky Running / amber Waiting / emerald outlined Mark done) replaces the small ghost button. The dead "..." overflow now opens a real DropdownMenu with Copy link / Open transcript / Restart / Delete. For claude-code tasks only: new Transcript sub-tab reads Claude's native session JSONL from ~/.claude/projects/<slug>/<session>.jsonl and renders clean user/assistant turns + collapsible tool calls via new /api/agents/conversations/[id]/claude-transcript endpoint.

[2026-04-20] Terminal-mode UX refinement: removed auto-exit on cabinet-block detection. Daemon still streams meta.summary / meta.artifactPaths live so the Details tab fills in as the CLI emits the epilogue block, but the PTY is left running so the user gets the full terminal experience and can continue the conversation. Task only finalizes when the user manually exits the CLI (Ctrl-D / /exit). Also deleted the redundant "Continue the conversation" composer card that used to appear under the terminal at idle — the CLI itself is the composer.

[2026-04-20] Codex terminal-mode fix: buildSessionInvocation was wrapping the prompt with `codex exec --dangerously-bypass… <prompt>`, which is the HEADLESS one-shot subcommand — so picking "terminal mode" for Codex actually ran the non-interactive flow piped through a PTY (the "regular Codex" UX the user saw). Now launches the interactive TUI (`codex`, no subcommand) with initialPrompt pasted after ready timer, and resumes via `codex resume <session-id>`.

[2026-04-20] Added have-fun/bye_bye_bye.py — pure-Python MIDI generator that writes bye_bye_bye.mid (format-1, 3 tracks: tempo, sawtooth-lead melody, synth-bass) for the NSYNC "Bye Bye Bye" chorus hook in C minor, and tries timidity/fluidsynth for playback.

[2026-04-20] Fixed agent avatar/icon/color changes reverting on the Agent detail page for sub-cabinet agents. Root cause: PUT helpers in agent-detail-v2.tsx omitted cabinetPath, so the API wrote to the root .agents directory while the UI re-read from the sub-cabinet. Server-side fix in persona-manager.ts: added findPersonaCabinetPath + resolveCabinetForSlug so readPersona/writePersona/deletePersona and memory/inbox/heartbeat helpers auto-discover the owning cabinet from the (globally unique) slug when the client omits it. Client-side cleanup in agent-detail-v2.tsx and agent-live-panel.tsx threads effectiveCabinetPath through every PUT/DELETE.

[2026-04-20] Terminal-mode tasks now render the terminal layout in compact/side-panel mounts too. `task-conversation-page.tsx` previously gated the fullscreen terminal branch on `!isCompact`, so clicking a PTY task from the board/kanban opened it in the detail panel showing the regular Chat/Artifacts/Diff/Logs tabs — even though the selected runtime was Terminal. Dropped the `!isCompact` guard so any mount (fullscreen, side panel, board-v2 detail panel) with a legacy adapterType gets the Terminal/Details(/Transcript) tabs + WebTerminal layout. Storage + adapter swap were already correct: `POST /api/agents/conversations` resolves `runtimeMode: "terminal"` to `LEGACY_ADAPTER_BY_PROVIDER_ID[providerId]` and persists e.g. `claude_code_legacy` in meta.json; only the UI conditional was hiding it.

[2026-04-20] Avatar/icon picker consistency: avatar preset thumbnails now match the icon cells (8-col grid of 32x32 tiles) instead of the old 6-col 40x40 layout. Icon catalog expanded from 40 to ~140 Lucide glyphs (Anchor through Wine), with the picker constrained to a scrollable 56-tall region so the popover stays compact.

[2026-04-20] Consolidated runtime-override handling across all task composers. New src/lib/agents/runtime-overrides.ts → normalizeRuntimeOverride(requested, fallback) is the single source of truth for provider/adapter/model/effort/terminal-mode resolution. Both POST /api/agents/conversations (new task) and POST /api/agents/conversations/[id]/continue now call it, fixing a drift where the new-task path never stripped model/effort in terminal mode (they leaked into meta.json). Added TaskRuntimePicker to the AI panel composer and the status-bar AI pill — both previously sent createConversation with no runtime fields at all, so users who picked terminal as their default never got terminal from those surfaces.

[2026-04-20] Added satirical CV for Sybill Trelawney to have-fun/ (cv-sybill-trelawney.md, order 15), matching the voice and structure of the existing Hogwarts-staff CV series (Umbridge, Lockhart, Slughorn, Hagrid).

[2026-04-20] Added 100 famous-figure avatar presets in /public/agent-avatars (avatar-13.svg…avatar-112.svg). Reproducible via scripts/generate-famous-figures.mjs, which composes a base silhouette (bg + shoulders + neck + face circle, same template as avatar-01…12) with hair/hat/glasses/beard/scar/mask primitives. The catalog labels are cryptic ("Lightning Scholar" = Harry Potter, "Plumber in Red" = Mario, "Relativity Rebel" = Einstein) so picking feels like a guessing game. Avatar picker grids in agent-detail-v2 and edit-agent-identity-dialog now scroll vertically so the expanded set stays inside the popover.

[2026-04-20] AgentIdentity avatar branch now uses `flex` + `rounded-md` to match the icon branch. Previously the span rendered as `display: inline` which made `overflow-hidden` a no-op — the SVG avatar's square corners bled past the rounded-2xl override on the hero, so picking an avatar looked visibly different from picking an icon. With `flex` the rounded corners actually clip, and both branches now pick up the caller's shape/size override identically.

[2026-04-20] Added satirical CV for Mundungus Fletcher to have-fun/ (cv-mundungus-fletcher.md, order 17), extending the Harry Potter CV series with the Order of the Phoenix's favourite unregistered goods dealer.

[2026-04-21] Added satirical CV for Minerva McGonagall to have-fun/ (cv-minerva-mcgonagall.md, order 22), extending the Harry Potter CV series with the Deputy/Headmistress's clipped, unyielding, and — at interview — distinctly unimpressed voice.

[2026-04-21] Added satirical CV for Percy Ignatius Weasley to have-fun/ (cv-percy-weasley.md), extending the Harry Potter CV series with the Ministry-bound elder brother whose formal, procedurally-precise voice includes a frank note on his 1995–1998 family estrangement.

[2026-04-21] Added satirical CV for Aberforth Dumbledore to have-fun/ (cv-aberforth-dumbledore.md, order 26), extending the Harry Potter CV series with the Hog's Head publican's gruff, reluctant, quietly-heroic voice.

[2026-04-21] Added satirical CV for Filius Flitwick to have-fun/ (cv-filius-flitwick.md, order 28), extending the Harry Potter CV series with the Charms Master's precise, courteous, quietly-proud voice — including the duelling championship he will not, by preference, discuss.

[2026-04-21] Added satirical CV for Augusta Longbottom to have-fun/ (cv-augusta-longbottom.md, order 33), extending the Harry Potter CV series with the formidable grandmother of Neville — vulture-hatted, red-handbag-wielding, decades-long steward of the Longbottom estate and of the Janus Thickey Ward visiting rota.

[2026-04-21] Added satirical CV for Bathilda Bagshot to have-fun/ (cv-bathilda-bagshot.md, order 35), extending the Harry Potter CV series with the elderly magical historian of Godric's Hollow — author of A History of Magic, correspondent of Dumbledore, and aunt to a nephew she declines, with characteristic brevity, to discuss.

[2026-04-21] Added satirical CV for Silvanus Kettleburn to have-fun/ (cv-silvanus-kettleburn.md, order 36), extending the Harry Potter CV series with the former Care of Magical Creatures professor — retired with limb count stabilised, registration still current, paddock still open for a reasonable afternoon's work.

[2026-04-21] Added satirical CV for Irma Pince to have-fun/ (cv-irma-pince.md, order 37), extending the Harry Potter CV series with the Hogwarts librarian — custodian of the Restricted Section, trained binder from Flourish and Blotts, keeper of the classical cataloguing schema, and long-standing enforcer of silence behind the Library gate.

[2026-04-21] Added `have-fun/cv-rufus-scrimgeour.md` — in-character CV for Rufus Scrimgeour (Head of the Auror Office; Minister for Magic, July 1996 onwards), framed as a private in-post accountancy document. Indexed in `have-fun/index.md`. Continues the Harry Potter CV library in the established understated voice; order: 39.

[2026-04-21] Added satirical CV for Luna Lovegood to have-fun/ (cv-luna-lovegood.md, order 47), extending the Harry Potter CV series with the Quibbler contributing editor and independent naturalist — still looking for the Crumple-Horned Snorkack, Patronus is a hare, prepared to be wrong in public. Indexed in have-fun/index.md.

[2026-04-21] Added satirical CV for Igor Karkaroff to have-fun/ (cv-igor-karkaroff.md, order 27) — former Durmstrang Headmaster and Wizengamot cooperative witness, written as an in-hiding job application: euphemistic about Azkaban, frank (on his own terms) about the Mark, and careful with his current address. Indexed in have-fun/index.md.

[2026-04-21] Added satirical CV for Regulus Arcturus Black to have-fun/ (cv-regulus-black.md, order 49) — framed as an in-character final document written in late 1979 on the eve of the cave expedition with Kreacher, pairing thematically with the existing Sirius Black CV and extending the Harry Potter CV library. Indexed in have-fun/index.md.

[2026-04-21] Added satirical CV for Madame Rosmerta to have-fun/ (cv-madame-rosmerta.md, order 50) — a Hogsmeade publican's CV framed as a family-record document she never got around to writing: thirty-eight years at the Three Broomsticks, Hogsmeade Traders' Guild member, past Chair of the Wizarding Publicans' Association, with a frank, on-her-own-terms note on the 1996–1997 Imperius affair. Indexed in have-fun/index.md.

[2026-04-21] Added satirical CV for George Weasley to have-fun/ (cv-george-weasley.md, order 53) — in-character application circa 2010: co-founder of Weasley's Wizard Wheezes, Order of the Phoenix veteran, Potterwatch's "Rapier", with a candid personal note on the lost ear and on Fred. Indexed in have-fun/index.md.

[2026-04-21] Added have-fun/steampunk-pirates/hermione.md — short steampunk-airship-pirate reimagining of Hermione Granger.

[2026-04-21] Added have-fun/steampunk-pirates/ron.md — steampunk airship pirate reimagining of Ron Weasley.

[2026-04-21] Added have-fun/steampunk-pirates/harry.md — steampunk airship-pirate reimagining of Harry Potter.

[2026-04-22] Redesigned the workspace Routines and Heartbeats lists as single-line rows with agent avatars, hover-reveal Run/Disable/Delete actions (reserved slot so layout never shifts), inline red delete-confirm, and a disabled/vague row state. Heartbeat rows expose only Run + Disable since heartbeats are a per-agent slot, not a standalone object. Added `src/components/agents/schedule-row.tsx` and list-row action handlers in agents-workspace that update `agentJobsMap` / `agents` locally for snappy feedback.

[2026-04-22] Fixed agents workspace dropping avatar/identity fields (displayName, iconKey, color, avatar, avatarExt) in the cabinet-path branch of `refreshAgents` and in the `cabinetAgentsView` useMemo. Real avatar images now render across "Meet the team", the Add-routine/job/heartbeat dropdowns, the Routines and Heartbeats rows, the Org chart modal, and the Team schedule. Also swapped Routine row hierarchy so the job name is the bold primary label and the agent name is secondary (Heartbeat rows keep agent-name-primary since they have no unique name).

[2026-04-22] Fixed the Routine/Heartbeat row action icons where Run and Enable looked identical (both Play triangles) once a row was disabled. Enable/Disable now uses the universal `Power` icon and tints emerald when the row is disabled (hint: "turn it back on"), muted-to-emerald on hover when enabled. Run keeps the familiar `Play` triangle; Delete keeps `Trash2`. Tooltips/aria still read "Enable" or "Disable" based on state.

[2026-04-22] Aligned the Edit Routine and Edit Heartbeat dialogs with the row's action vocabulary. Both dialogs now expose Run / Enable-Disable (`Power`) / (Delete, routines only) as header buttons with full tooltips explaining each action. The heartbeat dialog's standalone "Active" checkbox was removed — the header Enable/Disable button is the single source of truth and persists immediately. Swapped the heartbeat Run icon from `Zap` to `Play` to match the row. Added `onToggled`/`onToggledActive` optional callbacks so the parent list can update `agentJobsMap`/`agents` state while the dialog stays open.

[2026-04-22] Added `have-fun/cv-lavender-brown.md` — satirical first-person CV for Lavender Brown (Finnigan), framed as Senior Healer of the Creature-Induced Injuries ward at St Mungo's, Pack Nights founder, and Witch Weekly horoscope columnist. Keeps continuity with cv-seamus-finnigan.md (marriage, daughter Aoife) and linked from the Character CV Library in have-fun/index.md.
[2026-04-22] Added a folk-anthem "Song of Moses" to have-fun/ with four verses, chorus, and bridge; linked from the Creative Writing section of have-fun/index.md.

[2026-04-22] Added `have-fun/cv-hermione-granger.md` — satirical first-person CV for Hermione Granger, framed as current Head of the Department of Magical Law Enforcement, former Deputy Head of Regulation and Control of Magical Creatures, S.P.E.W. founder, Muggle-born. Includes a direct note on the 1997 memory charm on her parents. Linked from the Character CV Library in `have-fun/index.md` (alphabetised between Hannah Abbott and Horace Slughorn).

[2026-04-22] Added `have-fun/cv-padma-patil.md` — satirical first-person CV for Padma Patil, framed as the current Headmistress of Hogwarts (appointed 2023), former Professor of Arithmancy and Deputy Head (Curriculum), ex-Unspeakable, Ravenclaw prefect, DA veteran. Keeps continuity with cv-teddy-lupin.md (which already references "Headmistress Patil"). Linked from the Character CV Library in have-fun/index.md between Oliver Wood and Peeves.

[2026-04-22] Added `have-fun/cv-parvati-patil.md` — satirical first-person CV for Parvati Patil-Thomas, framed as founder/creative director of Sari & Spell on Diagon Alley, former Madam Malkin apprentice, ex-Witch Weekly fashion correspondent, Gryffindor DA veteran, practising tarot reader. Honours continuity with cv-padma-patil.md (Patil-Thomas married to Dean Thomas, daughter Anisha 13 in Gryffindor, co-owns shop, twin three minutes younger) and cv-lavender-brown.md (Wednesday-evening tarot tradition, "their two" children — second child Aravind, 8). Linked from the Character CV Library in have-fun/index.md between Padma Patil and Peeves.

[2026-04-22] Added a CV for Phineas Nigellus Black to the Have Fun character library (`data/have-fun/cv-phineas-nigellus-black.md`) and linked it from `data/have-fun/index.md`. Written from the portrait's posthumous point of view; matches the voice and section shape of the existing Parvati / Regulus CVs (Core Competencies, Employment History covering his Headmastership 1901–1913 and his portrait career 1925–present, "A Note on..." sections including the handbag, the most-unpopular-Headmaster distinction, and Mr Potter).

[2026-04-22] Added `have-fun/cv-helena-ravenclaw.md` — satirical first-person CV for Helena Ravenclaw (The Grey Lady), written c. 1000 years after her death as a submission to the Headmistress's parallel cataloguing exercise for ghosts. Voice is quieter and more archaic than the recent Phineas Nigellus Black CV to match nine centuries of silence: sections include the Diadem, the Baron, the girl in the second-floor bathroom, and Mr Potter (1998). Linked from the Character CV Library in `have-fun/index.md` between Hannah Abbott and Hermione Granger.

[2026-04-22] Added statistics notebook embedded app at `data/statistics-notebook/` — interactive single-page HTML tool with editable markdown+LaTeX text cells (KaTeX), standalone equation cells, editable data tables (paste-CSV import), Plotly charts (scatter/line/bar/histogram/box) that read from named tables, and automatic summary-statistics cells (n, mean, median, std, variance, min/max, quartiles). Persists to localStorage and exports/imports JSON. Marked as `.app` for full-screen mode.

[2026-04-22] Added interactive statistics course at `data/statistics-course/` — 8-lesson curriculum (Data & Variables, Measures of Center, Spread, Normal Distribution, Sampling & CLT, Confidence Intervals, Hypothesis Testing, Correlation & Regression) with live Plotly demos, KaTeX math, numeric exercises and MCQs with instant feedback, sidebar progress tracking, and localStorage persistence. Marked as `.app` for full-screen mode.

[2026-04-23] Added a prominent "New Task" split button to the top-right of the Tasks board header (`src/components/tasks/board-v2/tasks-board-v2.tsx`). The primary action opens `StartWorkDialog` in "now" mode; an attached chevron dropdown offers "New Task" and "New Routine" (dialog opens in "recurring" mode). Threads a `newTaskMode` state through to `StartWorkDialog` via `initialMode`.

[2026-04-23] Added three new themes — Windows 95 (teal desktop + silver panels + navy chrome, Pixelify Sans), Matrix (phosphor #00FF41 on near-black, Share Tech Mono body + Major Mono Display headings), and Apple (pure-white canvas, #F5F5F7 secondary gray, #1D1D1F text, #0071E3 system blue CTA, Inter) — to `src/lib/themes.ts`. Registered the three new Google Font families (Pixelify Sans, Share Tech Mono, Major Mono Display) in `src/components/layout/theme-initializer.tsx`. Authored `data/themes-spec.md` — a full design spec covering palette tokens, typography, design intent, accessibility contrast checks, and instructions for extending the theme set.

[2026-04-23] Refined the Windows 95 theme and added a Windows XP theme in `src/lib/themes.ts`. Win95: iconic #008080 teal moved to the sidebar (with white text and deeper-teal hover/border), silver #C0C0C0 now the page canvas, white content cards over it (Explorer-list style), and the font swapped from Pixelify Sans to Jersey 15 — proportional pixel, much closer to MS Sans Serif. WinXP: Luna theme — white canvas, Luna-blue (#245EDC) sidebar with a green (~#4FBE5E) sidebar-primary echoing the Start button, pale-blue secondary surfaces, Open Sans for the body/headings (Tahoma analogue). Registered Jersey 15 and Open Sans in `src/components/layout/theme-initializer.tsx` (removed the now-unused Pixelify Sans), and expanded `data/themes-spec.md` with the corrected Win95 section, a full Windows XP section, and updated accessibility/contrast notes.

[2026-04-23] Tuned the Windows 95 and Windows XP themes after the first render in Cabinet. Win95: swapped the body/heading font from Jersey 15 → `Arimo` (Arial / MS Sans Serif metric-compatible — reads cleanly in long-form content, where the pixel font turned paragraphs into a puzzle) and bumped the sidebar teal chromas from 0.06–0.08 → 0.09–0.11 so #008080 actually pops instead of reading as muted sage. WinXP: lifted the Luna blue from `oklch(0.42 0.22 263)` to `oklch(0.48 0.22 264)` on primary, ring, and sidebar (and cascaded the accent/border tones one step deeper), closer to the gradient-top highlight of real Luna chrome; updated the picker swatch from #245EDC → ~#2F6FE5. Registered `Arimo` in `src/components/layout/theme-initializer.tsx` and removed the now-unused `Jersey 15`. Spec doc `data/themes-spec.md` updated with the new palette rows, typography section, and font list.

[2026-04-23] Drafted `data/missions/red-rose-supply/index.md` — a strategy doc for the family flower business in Israel covering scoping questions, four supply options (domestic Israeli growers, Netherlands auction, Kenya/Ethiopia direct, vertical integration) ranked by lead time and cost, a peak-season multi-sourcing playbook, and a 30-day action plan with risks to watch.
[2026-04-23] Added missions/red-rose-supply/suppliers-south-israel.md — a research dossier on rose suppliers in the south of Israel (Sde Nitzan, Arava, plus B2B aggregators). Contact-first, no outreach sent. Includes clarifying questions for the human on product type (stems vs bushes) and volume.

[2026-04-23] Archived the previous `data/` as `data-old6/` and started a fresh onboarding. To avoid losing the prior KB content, copied the user-facing directories (`getting-started`, `saas-startup`, `statistics-course`, `statistics-notebook`, `themes-spec.md`) into a new `data/archive/` subtree with a descriptive `index.md`. Root-level runtime state (`.agents`, `.cabinet.db*`, `.git`, `.chat`, `.jobs`) stays only in `data-old6/` since nesting it would conflict with the active cabinet's own state.

[2026-04-23] Consolidated every previous data snapshot into `data/archive/` as one subfolder per backup (`data-old`, `data-old-2`, `data-old-3`, `data-old-5`, `data-old6`, `old-data`) so they're all browsable from the sidebar. Copied only user-facing content; dotfiles (`.agents`, `.cabinet.db`, `.git`, `.chat`, `.jobs`, `.DS_Store`) stayed in the original top-level backups since nested runtime state would conflict with the active cabinet. Added `index.md` to each subfolder that lacked one (data-old-2, data-old6) and rewrote `data/archive/index.md` as a map of the snapshots.
[2026-04-23] Extracted the high-level Cabinet architecture flowchart into a standalone architecture.mermaid file alongside architecture.md.

[2026-04-24] Fixed Tiptap "Duplicate extension names found: ['link', 'underline']" warning by passing `link: false, underline: false` to `StarterKit.configure` — `@tiptap/starter-kit` v3 now bundles both, colliding with our customized Link and the Underline added via `colorAndStyleExtensions`. Cut editor navigation work in half by skipping the transient empty-content render that `editor-store.loadPage` emits mid-fetch (and deduping identical `(path, content)` renders) in `src/components/editor/editor.tsx`, and froze the `unified()` markdown pipeline at module scope in `src/lib/markdown/to-html.ts` so every page switch reuses one processor instead of rebuilding it. Result: no console warnings on `/#/cabinet/./data/getting-started/delegating-between-agents`, warm in-app page switches measured at ~70ms.

[2026-04-24] Added drag-and-drop, paste, and file-picker attachments to the unified task composer (all 5 kickoff surfaces: cabinet, task detail, home, agent detail, agents workspace). Attachments upload per-conversation under `{cabinet?}/.agents/.conversations/{id}/attachments/` — kickoff turns stage in `_pending/{clientUuid}/` and move on submit once the real conversation ID exists; continuation turns upload directly. A new `use-composer-attachments` hook owns upload + abort + optimistic chip state; `AttachmentChips` render inline with `MentionChips` (thumbnails for images, icons + filenames for the rest). `/api/upload/[...path]` gained `?commit=0` (skip auto-commit), a 25MB cap, executable-extension blocklist, and a DELETE handler scoped to `.agents/.conversations/` so chip removal deletes the file. Attachments flow into the adapter as a new "Attached files" block in the prompt with cwd-relative paths so Claude Code can `Read` them without knowing Cabinet's virtual-path layout. A daily 03:17 cron in `cabinet-daemon.ts` (`cleanupStaleStagingAttachments`) sweeps `_pending/*` dirs older than 24h.

[2026-04-24] Rendered composer attachments inline in the conversation panel instead of leaking the adapter-facing "Attached files" block into the user's message bubble. Persisted `attachmentPaths` on `ConversationMeta` (kickoff) and each stored user turn (continuation), threaded through `conversationTurnToTaskTurn` → `Turn`. New `TurnAttachments` component in `src/components/tasks/conversation/turn-attachments.tsx` shows image thumbnails linking to `/api/assets/...` and icon chips for non-image files. Added `stripAttachmentTrailer` to `extractConversationRequest` so existing conversations with the block baked into `prompt.md` still display cleanly.

[2026-04-24] Restaged the onboarding tour's Data slide ("Your single source of truth") so it tells a beat-by-beat story: the sidebar file list fades in first, then the title + two copy lines write in alongside it (with a new second line — "One place for everything — so you and your AI team read, edit, and ship from the same files, not copies of copies."), then an animated mouse cursor glides from the bottom of the sidebar to the featured file, clicks with a ripple pulse, and only then does the file viewer fade in on the right. Added `cabinet-tour-cursor-pointer` and `cabinet-tour-click-ripple` keyframes in `src/app/globals.css`, drove cursor position via per-scene `--cursor-target-x/y` CSS variables keyed off `scene.featuredIdx`, and stretched `SCENE_DURATION_MS` to 6300ms so each scene gets ~3s of viewer time after the click. `SlideData` now imports `MousePointer2` + `CSSProperties` and the copy-column animation delays shifted from 60/180/320ms to 900/1100/1500/2000ms so text arrives after the files.

[2026-04-24] Merged the onboarding tour's two carousels (main back/next + Data slide's internal auto-rotating scene carousel) into one linear thread. `SlideData` now takes a `sceneIdx` prop instead of owning its own `useState` + `setInterval`, and exports `DATA_SCENE_COUNT` derived from `SCENES.length`. `tour-modal.tsx` expands its `SLIDES` array from 4 entries to 1 intro + N data + agents + tasks (currently 9 steps) using `Array.from({ length: DATA_SCENE_COUNT })`, and switched the stage div's `key` from `current.id` to `current.stageKey` — all data slides share `stageKey: "data"` so `SlideData` stays mounted while stepping between scenes (copy column doesn't re-animate on every click; only the scene-keyed sidebar body and viewer replay). The redundant per-scene dot row beneath the sidebar was removed since the footer progress dots now cover it.
[2026-04-24] Changed Agents tour slide closing line from 'Give them a role — they'll show up for it.' to 'Your AI agents work for you, 24/7.'
[2026-04-24] Refined Agents slide tagline to 'Your AI agents get the work done — 24/7. Go rest.'
[2026-04-24] Finalized Agents slide tagline to 'Go be human. They'll handle the rest.'
[2026-04-24] Extended Agents slide tagline to 'Your AI agents get the work done, 24/7. Go be human — they'll handle the rest.'
[2026-04-24] Rewrote the first-load breaking-changes warning dialog (`src/components/layout/breaking-changes-warning.tsx`) to cover data-safety and liability: removed the "come back in a few hours" line and added language about elevated agent permissions (`--dangerously-skip-permissions`), third-party AI provider data handling, a back-up-your-data advisory, and an "as is / no warranty" disclaimer. Bumped ack key to `v2` so existing users re-acknowledge, widened the dialog, and changed the CTA to "I understand — continue".
[2026-04-24] Removed all em dashes from `src/components/layout/breaking-changes-warning.tsx` (title, body copy, CTA, and SSR comment) per style preference; replaced with colons, commas, or periods as appropriate.
[2026-04-24] Tasks list view: moved the agent chip to the right of the title (immediately left of the trigger badge) and bumped row horizontal padding from px-4 to px-6 so rows breathe. File: `src/components/tasks/board/list-view.tsx`.
[2026-04-24] Tasks list view: shifted hover-action cluster 100px further left (right-28 → right-[212px]) so Stop/Restart/Reassign/Delete no longer overlap the trigger badge. File: `src/components/tasks/board/list-view.tsx`.
[2026-04-24] Tasks list view: widened the gap between hover-action cluster and the trigger/time column (right-[212px] → right-[260px]) so the icons breathe. File: `src/components/tasks/board/list-view.tsx`.
[2026-04-24] Tasks list view: pushed hover-action cluster to right-[300px] for a wider gap from the trigger/time column. File: `src/components/tasks/board/list-view.tsx`.
[2026-04-24] Tasks list view: added responsive horizontal padding to the list scroll wrapper (md:px-[4%], lg:px-[6%], xl:px-[8%], 2xl:px-[10%]) so rows inset from the viewport edges on wider screens, ramping to ~10% each side. File: `src/components/tasks/board/list-view.tsx`.
[2026-04-24] Added Songs/11-three-am-diner.md — original song (verses, chorus, bridge, outro) set in a 3am diner.
[2026-04-24] Added Songs/12-ghosts-on-the-dial.md — original song about losing radio signal on a long drive, with verses, chorus, and bridge.
[2026-04-24] Added Songs/14-quarters-for-a-ghost.md — americana/folk-rock song about the last working payphone in town.
[2026-04-24] Added Songs/16-iron-lace-at-dusk.md — original indie folk song about dancing on a fire escape at dusk with verses, chorus, and bridge.
[2026-04-24] Added new song 'Tape Still Rolling' (Songs/18-tape-still-rolling.md) — folk/americana piece built around messages left on an old answering machine.
[2026-04-24] Added Songs/17-greyhound-to-nowhere-in-particular.md — original folk/americana song set on an overnight bus with full verse/chorus/bridge/outro structure.
[2026-04-24] Added Songs/15-fluorescent-summer.md — original country-rock song about working retail during a heatwave.
[2026-04-24] Added Songs/19-parking-lot-apostles.md — original alt-country song about teenagers hanging out in a church parking lot during youth group.

[2026-04-24] Added Songs/20-the-coat-still-smells-like-you.md — original folk ballad about a dog grieving after losing its person, with verses, pre-chorus, chorus, bridge, and outro.
[2026-04-24] Tasks list view: reordered row to place the agent chip immediately left of the trigger badge (status · title · agent · trigger · time), bumped row horizontal padding px-4 → px-6, shifted the hover-action cluster from right-24 to right-[300px] so it no longer overlaps the trigger/time column, and added responsive page-edge gaps on the list scroll wrapper (md:px-[4%], lg:px-[6%], xl:px-[8%], 2xl:px-[10%]). File: `src/components/tasks/board/list-view.tsx`.
[2026-04-24] Added Terms of Service (https://runcabinet.com/terms) and Privacy Policy (https://runcabinet.com/privacy) links to the first-load warning dialog in `src/components/layout/breaking-changes-warning.tsx`, with `target="_blank"` + `rel="noopener noreferrer"` and an explicit "By continuing you agree to..." acceptance clause.
[2026-04-24] Added Songs/21-lana-del-rey.md — a poem about Lana Del Rey.
[2026-04-25] Added song 'Payphone on Fillmore' (Songs/22) — original lyrics with verses, choruses, and bridge about a forgotten payphone as a metaphor for connections that outlast their infrastructure.

[2026-04-25] Added Song 2 "The Long Way Home" as a melancholy folk ballad counterweight to the Song 1 dance-pop track, saved under data/marketing/drafts/.

[2026-04-25] Copywriter wrote 10 new songs (23–32) to data/Songs/, expanding the americana/folk catalog with character vignettes and slow-burn ballads.

[2026-04-25] Added 10 haiku songs (33–42) to Songs/ as a multi-task creative writing run.

[2026-04-25] Added a shared `AgentPicker` chip (`src/components/composer/agent-picker.tsx`) and wired it into the composers that lacked one: AI Panel (functional — explicit alternative to `@`-mention routing), Agent Detail (locked with tooltip "Locked to {agent} — this is their workspace"), and Task continuation composer (locked with tooltip "Continuing with {agent} — agent can't change mid-conversation"). Cabinet task composer and StartWorkDialog already had functional pickers.

[2026-04-25] Replaced the lame home-screen quick-action chips ("Brainstorm ideas", "Map user journey", etc.) with six punchier prompts that show off Cabinet — parallel agent fanout, scheduled daily reviews, weekly reflection from recent files, a Thailand trip planner, an interactive physics study app, and an article-connection graph. Chip labels stay short; the full prompt is what gets submitted (and shown via `title` on hover).
[2026-04-25] Added 10 Harry Potter character songs (43-52) to Songs/ — Harry, Hermione, Ron, Dumbledore, Snape, Hagrid, Draco, Luna, Sirius, Voldemort. Generated in parallel by 10 editor subagents.
[2026-04-25] Added getting-started/thailand-trip-planner with 14-day itinerary, lodging picks, food spots, and tiered budget.

[2026-04-25] Added `data/weekly-review/` with `index.md` (wins, blockers, themes for Apr 19–25 reconstructed from this week's file changes) and `tasks-next-week.md` (decide/do/fix/defer plan pulling from rose-mission ask_user gaps, marketing positioning blockers, and the architecture audit's Immediate items).
[2026-04-25] Added physics-for-beginners interactive study app under data/ — full-screen embedded webapp with 5 lessons (motion, forces, gravity, energy, waves), animated canvas sandboxes, and a final quiz with progress tracking.
[2026-04-25] Added `data/Songs/graph/` — full-screen embedded webapp (D3 force-directed graph) that visualises connections between the 52 Songs articles via shared characters, tags, genres, and 17 curated concepts (home, highway, memory, loss, religion, magic, etc.). Graph is pre-computed at build time and inlined; left panel filters by node type, right panel shows neighbour breakdown on click.

[2026-04-25] Reworked home-screen quick-action chips to actually showcase delegation per `data/getting-started/delegating-between-agents`. Five of seven chips now route to the `ceo` agent (which has `canDispatch` on by default for leads) and embed explicit LAUNCH_TASK / SCHEDULE_JOB / SCHEDULE_TASK verbs so the dispatch pending-actions panel is the immediate response: a 10-way song fan-out, a daily 9am cron job, a one-shot SCHEDULE_TASK for next Monday, a multi-agent Thailand trip split (librarian + editor with mixed effort), a librarian→editor pipeline for article connections, and a 6-module physics curriculum fan-out. The "Build me a physics study app" chip stays solo (no `agent`) so we still demo a single interactive-webapp task. Added per-chip `agent?` field plus a `runQuickAction` helper that bypasses the composer when a chip pins an agent — necessary because `composer.submit(text)` does not re-parse `@mentions` from raw text.
[2026-04-25] Added Songs/59-vacancy-light.md — noir-tinged original song using a flickering motel VACANCY sign as metaphor for fading love, in verse/chorus/bridge form.
[2026-04-25] Added Songs/58-haiku-woodsmoke-and-early-dark.md — three-haiku autumn song (leaves, woodsmoke, early dark) matching the spare style of the 33-42 haiku entries.
[2026-04-25] Added Songs/53-am-band-revelation.md — original late-night highway song with verse/chorus/bridge structure.
[2026-04-25] Added Songs/56-porch-light-weather.md — original americana song about watching a thunderstorm from a porch and weighing whether to stay or leave a relationship.
[2026-04-25] Added Songs/55-the-letter-i-never-sent.md — country-folk song about a father's unsent letter to his son.
[2026-04-25] Added Songs/62-the-morning-of-the-first-frost.md — original song about an old farmer's last season at first frost (verse/chorus/bridge).
[2026-04-25] Added Songs/60-the-recipe-she-never-wrote.md — original americana song about a grandmother's kitchen, unwritten recipes, and what endures after loss.
[2026-04-25] Added Songs/61-counting-cars.md — original americana song about being stopped at a train crossing, counting cars, and thinking about the life not chosen.
[2026-04-25] Added Songs/57-the-day-the-whistle-died.md — heartland-rock song about a one-industry town the day the factory closes; verse/chorus/bridge with working-class anger and dignity.
[2026-04-25] Added Songs/54-six-am-at-marlenes.md — original small-town diner song with verse/chorus/bridge structure.

[2026-04-25] Made home-screen delegation chips robust to cabinets without a CEO. Replaced the hardcoded `agent: "ceo"` with `preferredAgents: ["ceo", "cto", "pm"]` and a `dispatcherFor()` resolver that picks the first installed lead. Chips with `preferredAgents` are filtered out of `visibleActions` when none of their preferred dispatchers exist in `agents` — so trimmed installs never see a delegation chip that would silently route to a non-dispatcher. The solo physics-study-app chip always renders so the chip row is never empty during the brief agent-overview load.
[2026-04-25] Added Songs/draco-malfoy-song.md — short 3-verse + chorus song from Draco Malfoy's perspective.
[2026-04-25] Added Songs/snape-song.md — short 3-verse Snape song with chorus.
[2026-04-25] Added Songs/hagrid-song.md — short 3-verse song from Hagrid's perspective.
[2026-04-25] Added Songs/dumbledore-song.md — short 3-verse + chorus song from Dumbledore's perspective.
[2026-04-25] Added Songs/luna-lovegood-song.md — short 3-verse + chorus song from Luna Lovegood's perspective.
[2026-04-25] Added Songs/neville-longbottom-song.md — 3-verse Harry Potter song from Neville's perspective.
[2026-04-25] Added Songs/ron-weasley-song.md — short 3-verse song with chorus from Ron Weasley's perspective.
[2026-04-25] Added Songs/63-harry-potter-song.md — new 3-verse + chorus song from Harry Potter's perspective.
[2026-04-25] Added Songs/63-mcgonagall-tartan-and-tower.md — McGonagall-perspective song (3 verses + chorus).
[2026-04-25] Added Songs/hermione-granger-song.md — short 3-verse + chorus song from Hermione's perspective.

[2026-04-25] Added Songs/hp-luna-song.md — Luna Lovegood POV song (3 verses + chorus + bonus verse).
[2026-04-25] Added Songs/hp-dumbledore-song.md — Dumbledore POV song (greater good, regret, lemon drops, long games).
[2026-04-25] Added Songs/hp-snape-song.md — Snape POV song (always, doe patronus, double life).
[2026-04-25] Added Songs/hp-hermione-song.md — Hermione POV song (3 verses + bridge + chorus) on books, loyalty, being underestimated.
[2026-04-25] Added Songs/hp-ron-song.md — Ron Weasley POV song (Second-Hand Hero) with 4 verses + chorus.
[2026-04-25] Added Songs/hp-draco-song.md — Draco Malfoy POV song (4 verses + chorus): Lucius, the Dark Mark, the tower scene, and a quieter epilogue.
[2026-04-25] Added Songs/hp-harry-song.md — Harry Potter POV song on survivor's weight, scar, and chosen-one fatigue.
[2026-04-25] Added Songs/hp-hagrid-song.md — Hagrid POV song (3 verses + chorus + outro).
[2026-04-25] Added Songs/hp-neville-song.md — Neville Longbottom POV song (herbology, late bloomer, sword/snake).
[2026-04-25] Added Songs/hp-mcgonagall-song.md — short song from McGonagall's POV (tartan/tower/transfiguration/stern love).
[2026-04-25] Created physics-101 curriculum index and dispatched 6 editor LAUNCH_TASKs (one per module: motion, forces, energy, waves, electricity, light) at effort=high to build interactive lesson pages.
[2026-04-25] Built physics-101/module-1-motion/index.md — algebra-only kinematics lesson with hook, core concepts (position/velocity/acceleration/graphs), velocity-time slider thought experiment + PhET sim link, stoplight-sprint worked example, 5 practice questions with answers, and bridge to Module 2 (Forces).
[2026-04-25] Built physics-101/module-5-electricity/index.md — algebra-only electricity lesson with 9V-battery hook, core concepts (charge/current/voltage/resistance/Ohm's law/series-parallel), 4-scenario circuit reasoning exercise with reveal answers, series-circuit worked example, 5 practice questions, and bridge to Module 6 (Light) via accelerating charges → EM waves.
[2026-05-10] Isolated Windows Electron packaging and CLI runtime support onto a clean branch from origin/main. Added Windows package/make scripts, portable Electron runtime handling, Windows CLI command resolution, and process-tree cleanup helpers.
[2026-05-10] Kept the Windows Electron PR on Electron Forge and added retry handling around Windows recursive cleanup during Electron prep so transient EBUSY/ENOTEMPTY/EPERM locks do not fail packaging unnecessarily.
[2026-05-14] Added concise 5-step "To add Spanish" quick recipe to docs/CONTRIBUTING_I18N.md as a TL;DR above the existing detailed adding-a-locale section.
[2026-05-14] Moved Language section in Settings → Appearance to below Sidebar (was at the top). New order: Theme → Sidebar → Language. Separator line above for visual grouping.
[2026-05-14] Language picker now shows a full board: 2 active (English/Hebrew) + 20 'coming soon' locales rendered as dashed-border disabled buttons. Clicking a disabled one POSTs to cabinet-backend /language-requests (new endpoint backed by migration 0010_language_requests.sql + handler src/language-requests.ts) and shows a success toast. Already-requested locales persist in localStorage so the user can see their past clicks across sessions.
[2026-05-14] Deployed cabinet-backend: applied 0010_language_requests migration to remote D1 (telemetry-db via wrangler d1 execute --file), deployed worker version 18782c15-4587-4e62-892e-2d6d15e5ffcf. Smoketested POST https://reports.runcabinet.com/language-requests with {es, Español} → 200 ok, row verified in D1, then deleted.
[2026-05-14] Registered 0010_language_requests.sql in d1_migrations table on remote telemetry-db so future 'wrangler d1 migrations apply' runs treat it as already-applied (the migration itself was executed via 'wrangler d1 execute --file' due to an OAuth scope hiccup on the migrations endpoint).
[2026-05-14] Expanded 'More languages' picker from 20 → 80 languages (all major world locales by population + UI usage). Sorted A-Z by englishName field (predictable scan order regardless of script). Smaller cards: 3/4/5/6 col responsive grid (was 2/3), tighter padding (px-1.5 py-1 vs p-2), smaller text (11px vs 12px), truncate overflow. Hover tooltip shows englishName for users who can't read the native script.
[2026-05-14] Wired the Languages tab into reports.runcabinet.com dashboard. Five new endpoints under /reports/language-requests/{insights,by-locale,by-country,by-current-locale,daily} plus /reports/language-requests (rows). Tab UI: 4 KPI tiles (total / distinct locales / distinct voters / last request), ranked-locale table (click row to filter), daily-volume bar chart, current-UI-locale split, country breakdown (click to filter), recent-requests table. Deployed worker version d8f7ebce-277a-49fc-961f-c7e783a5b1bc. Seeded 7 sample rows to make the dashboard non-empty on first open.
[2026-05-14] Fixed dashboard navbar tabs disappearing on narrow viewports. Root cause: header used display:flex with no flex-wrap, and .tabs container/its buttons had no flex-shrink protection — when h1 + 5 tabs + 'Last updated' text exceeded viewport width, flex compressed the tab buttons until their text and borders got clipped off the right edge. Fix: header { flex-wrap: wrap; gap: 12px 16px; }, h1 + .last get flex-shrink: 0 + white-space: nowrap, .tabs { flex-wrap: wrap; min-width: 0; }, .tab { flex-shrink: 0; white-space: nowrap; }. Now tabs wrap to a second row on narrow viewports instead of disappearing. Deployed worker version 6ac5c07c-93ad-4a68-8d4d-db7ce45e810f.
[2026-05-14] Fixed sidebar tree indentation collapsing in RTL: tree-node row used inline paddingLeft and left: positions which always anchor to physical left regardless of dir, so RTL items all sat flush at the right edge. Switched to logical properties (paddingInlineStart, insetInlineStart, end-, start-, text-start, rounded-e-, ms-auto) so depth-16px indent now flips to the right edge in RTL.
[2026-05-16] Wired OpenCode dynamic model discovery end-to-end (fixes recurring Discord reports: users with Minimax/GLM/Kimi/Zen saw only the hardcoded OpenAI/Anthropic/Google/XAI fallback). Root cause was two coupled bugs: the listModels() endpoint shipped earlier had zero frontend consumers, and resolveProviderModel resolved only against the static list so saved opencode/* selections were silently snapped to models[0]. Providers API now advertises dynamicModels; new app-store ensureProviderModels() lazily hydrates the real entitlement-gated list (opencode models = authed providers + Zen free subset, verified live = 97 ids); GET .../models?refresh=1 busts the 60s cache; resolveProviderModel preserves an unknown saved id while un-hydrated; a searchable sub-provider-grouped combobox replaces the matrix for dynamic providers (Pi fixed for free). Extracted pure parseOpenCodeModels; added 10 unit tests. PROVIDER-CLI.md §12.1 #3 corrected (was a phantom) + migration entry #22.
[2026-05-16] Made the OpenCode dynamic-discovery fix generic across providers. Audited all 8 — only opencode + pi implement listModels(); the capability-driven wiring (provider.dynamicModels) already covers both. Pi had the identical latent bug (pi --list-models output that is non-empty but all-#-banner returned [] → blank picker), so applied the same hardening: extracted pure parsePiModels with the empty/banner-only → offline-fallback guard + 15s timeout, mirroring parseOpenCodeModels. Added test/pi-models-parse.test.ts (16/16 model-discovery tests pass). PROVIDER-CLI.md updated: §11 #22 + §12.1 #3 corrected (Pi was not "fixed for free"), §9 lists the new tests, §13 gained a "Dynamic model discovery (capability-driven)" contract so future listModels providers get the combobox/hydration/guard with zero component edits.
[2026-05-16] OpenCode seamless-integration follow-up (A+C+B). A: auth-aware healthCheck — parses `opencode auth list` (pure parseOpenCodeAuth, ANSI-stripped) so the readiness text is honest ("· 2 providers configured" vs "· no provider keys — Zen free models only") while keeping authenticated:true (Zen -free needs no key; flipping it would hide OpenCode from the composer). C: listModels() now throws on genuine CLI failure so the models route's `dynamic` flag is honest; dynamic returned on cache hits; app-store gates modelsHydrated on dynamic; combobox shows an amber "offline defaults — configure + Refresh" hint when dynamic:false. B: new optional AgentProvider.buildVerifyCommand(defaultModel) — verify route injects the Cabinet default model only when that provider is the configured default, so "verify passed" tests the real path. OpenCode+Pi only (6 others untouched). Tests 26/26, tsc/lint clean. PROVIDER-CLI.md §6 + migration #23.
[2026-05-17] Fixed "Failed to fetch" runtime error overlay crashing the tasks board and Skill Library. Both had an async refresh useCallback with try/finally but no catch, invoked from useEffect (and void refresh()/SSE/focus) without a catch — so a transient fetch rejection (offline, dev HMR dropping an in-flight request, daemon restart) bubbled to the Next error overlay instead of degrading. Wrapped refreshOverview + refreshConversations (use-board-data.ts) and refresh (skill-library.tsx) in try/catch that keeps last-known state and console.warns. Endpoints verified reachable (not a broken route); this is resilience for background refreshes and helps real users on network blips too.
[2026-05-17] Localized the entire app into 11 new languages (15 total). Added scripts/i18n-translate.mjs — a resumable, placeholder-safe Gemini JSON-mode batch translator (gemini-2.5-flash, GOOGLE_AI_API_KEY from .cabinet.env) that flattens en.json's 1120 leaf strings, translates in concurrent batches of 48, verifies {{interpolation}}/<n> tag/brand integrity per batch with retry+backoff, and rebuilds each locale in en.json's exact key order; exposed as `npm run i18n:translate`. Generated hi/es/fr/ar/bn/pt/ru/ur/id/de/ja.json (1120/1120 each, 0 missing, 0 placeholder loss). Wired all 11 into src/i18n/index.ts (SUPPORTED_LOCALES, LOCALE_LABELS native names, resources), formatters.ts (LOCALE_TO_BCP47), and removed them from REQUESTABLE_LOCALES. settings-page LanguageSection now derives the picker from SUPPORTED_LOCALES+LOCALE_LABELS so future locales surface automatically. ar/ur inherit existing RTL_LOCALE_PREFIXES. All four registries aligned (15==15==15==15), tsc/eslint clean for touched files.

[2026-05-22] Drafted docs/ROOMS_WORKSPACES_PRD.md — rooms (workspaces) feature: home dir holds fully-isolated room cabinets, Notion-style home-button switcher, migrate data/ to data/studio (default room "Studio"), nothing shared between rooms. No code changes yet (PRD only).

[2026-05-22] Resolved all open questions in docs/ROOMS_WORKSPACES_PRD.md: active room travels as per-request context (header + AsyncLocalStorage), not a global pointer, to allow two windows in different rooms; multi-window architected now / second-window UX deferred to Phase 5; per-room .cabinet.db with scoped search; .global-agents retired into Studio on migration; instant+crossfade switch; minimal onboarding now. Still PRD-only, no code.

[2026-05-22] Pivoted docs/ROOMS_WORKSPACES_PRD.md to a lighter direction (v2) after finding Cabinet already isolates every cabinet by path (agents/tasks/jobs/skills under data/<cabinetPath>/). Rooms now = top-level cabinets surfaced in a home switcher with per-cabinet icon+theme + path-scoped search; root stays the default room (no migration); window scope = cabinetPath (multi-window-ready). Still PRD-only, no code.

[2026-05-22] Implemented Rooms (workspaces) feature per docs/ROOMS_WORKSPACES_PRD.md (lighter direction). New: GET/PATCH /api/rooms + src/lib/cabinets/rooms.ts (top-level cabinets as rooms; icon/theme in .cabinet, lazy-promote); home-button RoomSwitcher dropdown (switch/customize/add/open-in-new-window) next to the logo; per-room theme via RoomThemeSync + rooms-store; multi-window — window scope = cabinetPath hash, web uses window.open, Electron adds cabinet:open-window IPC -> new BrowserWindow at baseAppUrl+hash. Hardened electron/main.cjs lifecycle (liveMainWindow/anyLiveWindow guards) to fix "Object has been destroyed" in the auto-updater dialog + second-instance handlers. Verified end-to-end in Chrome (switch, theme persist+fallback, multi-window) and live in Electron via CDP (openWindow spawns a scoped, themed BrowserWindow). tsc + eslint clean.

[2026-05-22] Fixed rooms sidebar scoping (user feedback: home showed all rooms nested under it). tree-view.tsx now hides the top-level room dirs from the root/home room tree (via rooms-store), so the home room shows only its own loose pages + symlinks; sub-rooms already scoped to their own subtree. Each room shows only its own subdirectories now; rooms are reached via the switcher.

[2026-05-22] Rooms: dropped the "HOME" badge so the root reads as a plain "Marketing" room (user kept it as a normal room, no migration). Dropdown is now a flat list of rooms; sidebar scopes each room to its own subtree.

[2026-05-22] Rooms switcher UX polish (user feedback): moved the switcher to its own full-width row under the logo (room name no longer cramped); replaced the default folder icon with a colored RoomAvatar (rounded square showing the chosen icon, or the room initial by default); auto-assigned a stable per-room color hashed from the room path (AGENT_PALETTE), shown in the trigger + dropdown. tsc + eslint clean.

[2026-05-22] Rooms switcher feedback round 2: trigger is now icon-only next to the logo (single row; room/cabinet name already shows in the drawer + main view, so not duplicated up top). Added a per-room Color picker (Auto + 8 swatches) to the Customize dialog; color persists in .cabinet (room.color), renders on the colored RoomAvatar in the trigger + dropdown, and falls back to an auto color hashed from the path. Default icon = room initial in its color (no folder). tsc + eslint clean; verified in Chrome (icon-only header, color pick→save→avatar updates, i18n labels).

[2026-05-23] Added a "Rooms" page to the getting-started seed (resources/getting-started/rooms/index.md, order 4) explaining the rooms/workspaces concept: home vs rooms, the room switcher (icon by the logo), per-room files/team/tasks/skills/theme isolation, Customize (name/icon/color/theme), open-in-new-window, the default home room, and adding rooms. Linked from resources/getting-started/index.md sub-pages. Ships to new cabinets via the getting-started seed.

[2026-05-23] Reframed the Rooms getting-started page around the furniture metaphor per user: "each room has one big cabinet" (ties to the product name) instead of the abstract "a room is a cabinet". Updated intro, Home-and-rooms section, the Tip, and the index sub-pages link.

[2026-05-23] Onboarding: no pre-made agents. Confirmed room-setup copy ("Pick a room" / "What's this cabinet for? Each room comes with its own AI team."). Reworked the team step into a "Create your first agent" form (name + role + instructions); removed the auto-seeded mandatory/suggested room team. Setup route no longer forces the room mandatory agents and now creates the one user-defined agent by writing persona.md directly (no provider-resolution dependency, since onboarding can skip the provider). Verified end-to-end in Chrome (wizard) + via the setup API (only the custom agent is created, with a valid persona.md).

[2026-05-23] Onboarding: restored the multi-agent launch-step chat animation (AgentChatPreview). After the "no pre-made agents" change it had collapsed to a single greeting; added a demoAgents cast (the user's own agent as lead + a few of the room's suggested teammates, illustrative only) so the lively delegation chat plays again. Left "created" card still shows only the user's actual agent. AgentChatPreview lead is now agents[0]; launch no longer requires an agent (optional). tsc + eslint clean; verified in Chrome (Scout leads, Assistant/Librarian reply).

[2026-05-23] Onboarding launch step: restored the Knowledge Base animation that the V2 rework dropped. Added a LaunchKbTree component (animated, terminal-style file tree of the cabinet: index.md + Getting Started + Rooms/Skills/Apps) shown under a "Knowledge base" label in the launch card, above the "AI team" agent list — so the launch screen shows KB + team together (matching the header). English-only strings (launch.knowledgeBaseLabel/aiTeamLabel); NOT translating other locales until onboarding flow is finalized (per user). tsc + eslint clean.

[2026-05-23] Onboarding Flow A: added 3 placeholder screens per the new concept flow. (1) "What is a Cabinet?" concept screen (new step 2, before Pick a room) with the workspace explanation + example chips. (2) "What can {agent} access?" placeholder (step 5) — interactive Files/GitHub/Slack/MCP/Plugins/Skills toggles, NOT yet wired to agent config. (3) "Should it run on a heartbeat?" placeholder (step 6) — Off/Daily/Weekly + token-cost note, NOT yet wired. Renumbered step machinery (STEP_COUNT 9->12; new STEP_WHAT_IS_CABINET/STEP_ACCESS/STEP_HEARTBEAT; COMMUNITY_START/END shifted to 8/10; STEP_NAMES; cloud-view hardcode 7->COMMUNITY_END_STEP), rewired Back/Next chain, added a StepNav helper. English-only strings (no translation until flow final). tsc + eslint clean; verified statically (devtools browser disconnected mid-session, so no live walkthrough this round).

[2026-05-23] Onboarding reorder: Connect your AI (provider) now comes BEFORE configuring the first agent (the agent needs a provider). New order: 0 intro, 1 welcome-home, 2 what-is-cabinet, 3 pick-a-room, 4 provider, 5 configure agent, 6 access, 7 heartbeat (off default), 8-10 community, 11 launch. Swapped step constants, rewired Back/Next chain (room-setup->provider->team->access->heartbeat->community), renamed goToTeamSuggestion->goToProvider. Also fixed a stray bug from the prior replace_all (agent step Back had pointed to what-is-cabinet). tsc clean.

[2026-05-23] Onboarding: added the live "cabinet" LEFT rail (reuses the tour MockupSidebar) on the config steps (what-is-cabinet -> heartbeat). Title updates live to the cabinet name; Data tab lists the project files; Team tab fills with the user agent once configured. Container widened to max-w-5xl + flex row [rail | content] on those steps; room-card grid 5->3 cols to fit beside the rail; rail hidden below lg. New OnboardingCabinetRail component + onboarding:rail.* strings (en only). Onboarding-only (real app sidebar untouched, per request). tsc + eslint clean.

[2026-05-23] Onboarding rail feedback round (4 items): (1) Pushed the rail further left, widening the config-step container max-w-5xl->6xl and the gap to gap-10. (2) The rail's tab now switches DATA->TEAM on the "Connect your AI" (provider) screen instead of "Create your first agent" (activeTab = tasks if step>=TASK, agents if step>=PROVIDER, else data). (3) Added a Tasks step (STEP_TASK=8, after heartbeat): new FirstTaskStep component explains what tasks are and lets the user file their first task for the agent they configured (textarea + example chips, optional); the rail gained a Tasks tab showing that task assigned to the agent. Renumbered step machinery (STEP_COUNT 12->13; COMMUNITY_START/END 8/10->9/11; STEP_NAMES first-task), wired heartbeat->first-task->community, extended showRail to <=STEP_TASK. (4) Added a user-profile footer row to the rail (avatar initial + the user's name from onboarding). New onboarding:firstTask.* + rail.tasksEmpty/tasksCaption/assignedTo/you strings (en only; not translating until flow final). tsc clean; eslint 0 errors (9 pre-existing dead-code warnings).

[2026-05-23] Onboarding "What is a Cabinet?" step: replaced the static example chips with interactive real-world examples sourced from the cabinet-website "How people actually use Cabinet" section. Removed the Sparkles "AI icon" at the top of the step. Added a CABINET_EXAMPLES dataset (6 personas: indie app founder, small business owner, solo creator, freelance consultant, OSS maintainer, solo founder), each with a project name, a nested file tree (with new/updated badges), and an AI team (emoji + cadence + running dot). The step now renders these as a 2-col grid of selectable buttons (emoji + persona + tagline); clicking one previews that cabinet live in the left rail. OnboardingCabinetRail gained an `example` mode (only on the What-is-a-Cabinet step): hides the tabs and shows a finished-cabinet snapshot (Knowledge base file tree + AI team list), no user footer. Real-build mode (steps 3-8) unchanged. New state exampleIndex; en-only strings whatIsCabinet.pickHint + examplesLabel reworded to "How people use Cabinet" + rail.filesLabel/teamLabel; example dataset kept inline English (sample content, like the help demos) pending flow finalization. tsc clean; eslint 0 errors (9 pre-existing dead-code warnings).

[2026-05-23] Onboarding feedback round (examples + rooms): (1) Restored the full sidebar chrome in the rail's example preview that the prior pass had hidden: the Data/Team/Tasks tabs and the user-name account footer are back. The example tabs are now INTERACTIVE (added an optional onTabChange to the shared MockupSidebar; tour usage unchanged/decorative) so you can click through a sample cabinet's pages (Data), agents (Team), and a sample task (Tasks). Default tab = Data so the example's pages show first, "just like the help demo". Added a `task` field to each CABINET_EXAMPLES entry. (2) Fixed the raw i18n strings (rail.filesLabel/teamLabel/whatIsCabinet.pickHint): the tabs now label the content so filesLabel/teamLabel are gone; pickHint + the examples header are inlined English (in-flux onboarding copy, English-only per request), so they render immediately without an app reload. Removed the 3 now-unused keys from en.json. Root cause of the raw keys: i18next inits once (if !isInitialized), so a running app keeps the startup en.json until a full reload; new t() keys (firstTask.*, rail.tasks*) still need one reload to appear. (3) Added 4 business rooms to the onboarding room picker: Sales, People & HR, Product, R&D (src/lib/onboarding/rooms.ts ROOM_TYPES + ROOMS, new RoomType union members, icons TrendingUp/Users/Package/Lightbulb, each with mandatory/suggested agents, keyword maps, and a greeting template). DEPARTMENT_ORDERS extended (reuse OFFICE_ORDER). Picker now shows 9 rooms; labels render via the existing roomText() fallback to rooms.ts (no i18n needed yet). tsc clean; eslint 0 errors (pre-existing dead-code warnings only).

[2026-05-23] Onboarding "What is a Cabinet?" redesign (per user, with screenshot): replaced the small left-rail example preview with the cabinet-website "How people actually use Cabinet" experience, rendered IN the step. Layout top->bottom: concept heading+body, a horizontal scrolling rail of showcase buttons (B2C App, Content Engine, B2B Sales, Newsletter, TikTok Factory, Product Team, Second Brain, Consulting, Open Source, Startup OS), then a full macOS-style window mockup below. New src/components/onboarding/cabinet-showcases.tsx: CABINET_SHOWCASES data (10 use cases, ported from the website incl. file trees + agents + page previews; added Content Engine/TikTok/Product/Second Brain) + ShowcaseWindow component (faithful port: window chrome with traffic lights + "Cabinet / project" + search; sidebar with cabinet header, AGENTS section w/ pulse dots, FILES tree w/ type icons + new/updated badges + active highlight; content pane renders the page preview — h1/meta/h2/p-skeleton/tags/table). Clicking a showcase swaps the window; selected button auto-scrolls into view. Wizard: rail no longer shows on the What-is-a-Cabinet step (showRail now STEP_ROOM_SETUP..STEP_TASK); step container widened to max-w-4xl; removed the rail's example mode + the inline CABINET_EXAMPLES dataset (deleted) — the rail is build-mode only again (Data/Team/Tasks + user footer for steps 3-8). Em-dashes avoided in sample copy (rule 17). Showcase copy is inline English (illustrative sample content). tsc clean; eslint 0 errors (pre-existing dead-code warnings only).

[2026-05-23] Onboarding "What is a Cabinet?" showcases: added live "webapp" pages (pages that render their own UI) and put them FIRST, per user. New ShowcaseApp kind on CabinetShowcase (preview is now optional; app?: "okr"|"review"|"sales"); ShowcaseWindow's content pane renders the app component when set, else the markdown-style doc. Three new app showcases at the start of CABINET_SHOWCASES: (1) Execs · OKRs (🎯 leadership-os) -> OkrApp: overall progress + objective cards with key-result progress bars colored by % (green/amber/red) and On track/At risk badges; (2) HR · Reviews (👥 people-ops) -> ReviewApp: completed/calibration/avg-score stat tiles + a people list with avatar, role, score bar, and Done/In review/Pending status badges; (3) Sales · Pipeline (💹 sales-os) -> SalesApp: pipeline/closed/win-rate tiles + an 8-week bar chart + top deals with stage badges. Each new showcase's active FILES node is an "app" type (AppWindow icon). Default selection is index 0 (OKR dashboard shows first). Built with the parchment palette; em-dash-free. tsc clean; eslint 0 errors (cabinet-showcases clean; pre-existing wizard warnings only).

[2026-05-23] Onboarding showcases: added a Thailand Trip webapp page (🏝️ thailand-2026, app:"trip", 4th showcase after the 3 business apps). TripApp renders a stylized travel map (soft sea gradient + organic land blobs + a dashed SVG route polyline) with pinned, photo-style emoji bubbles for Chiang Mai/Bangkok/Phuket/Krabi/Koh Samui, plus a 2-col grid of suggestion cards with emoji thumbnails (Wat Phra Singh, elephant sanctuary, khao soi, Phi Phi). Self-contained (emoji as imagery, no external map tiles/photos). ShowcaseApp type gained "trip"; switch + entry wired. 14 showcases total. tsc clean; eslint 0 errors.

[2026-05-23] Onboarding room-setup simplified per user: dropped the room-type picker grid (Office/Sales/HR/Product/R&D/Study/Lab/Family/Blank cards). The step now just asks two things — "Room name" and "What's it for?" — under a new heading "Create your first room" / "A room is one big cabinet for an area of your work or life... You can add agents, files, and skills later, right inside the app." The room starts BLANK: default answers.roomType is now "blank" (was "study"), so no pre-made team; capabilities are added later in the app. Removed the now-dead grid helpers (roomText, roomExampleAgent, activeRoom) and the ROOM_TYPES import. Copy is inline English (heading/subtitle/labels) so it shows without an i18n reload; the onboarding:roomSetup.* keys are now orphaned (harmless). The Sales/HR/Product/R&D room types remain in rooms.ts for in-app room creation. tsc clean; eslint 0 errors (pre-existing warnings only).

[2026-05-23] Onboarding room-setup copy reframed around use case + outcome (per user). Subtitle rewritten: "A room is your workspace: one big cabinet for a single part of your work or life, where your AI team helps you get it done." The two fields are now framed as questions: (1) "What is this cabinet for?" with use-case example chips that fill the name (My startup / My GitHub repo / My marketing work / My personal knowledge base / My client project); (2) "What do you want to accomplish?" using outcome language (placeholder "Ship my MVP and get my first 100 users") with outcome chips (Ship faster / Grow my audience / Stay on top of everything / Spend less time on busywork). Goal: frame onboarding around the user's real pain, not technical setup. Inline English (shows without reload). tsc clean; eslint 0 errors.

[2026-05-23] Onboarding room-setup: removed the card container (rounded-2xl border + bgCard + padding) around the two questions; they now sit directly on the page (space-y-5). Inputs keep their own styling.

[2026-05-23] Onboarding room-setup tags: replaced the static example chips with a small, auto-scrolling marquee (RotatingTags component + cabinet-marquee keyframe in globals.css; track holds the list twice for a seamless loop, pauses on hover, edge fade mask, tabIndex=-1). Tags are tiny/low-contrast and use short punchy labels — Q1 (use case): work, startup, second brain, family os, gtm, sales, marketing, client project, repo, research, content, studies; Q2 (outcome): ship faster, grow my audience, get organized, save time, close deals, launch, write more, automate busywork, stay focused. Clicking a tag fills the field. Overall feel is more minimalistic (combined with the earlier card-container removal).

[2026-05-23] Onboarding: removed the "What can <agent> access?" placeholder screen (STEP_ACCESS / AgentAccessStep) per user. Renumbered steps: heartbeat 7->6, first-task 8->7, community 9-11->8-10, launch 12->11, STEP_COUNT 13->12; removed the STEP_ACCESS constant, its STEP_NAMES entry, the agentAccess state, and the now-unused Github/MessageSquare/Plug/Puzzle imports. Flow is now: provider -> configure agent -> heartbeat -> first task -> community -> launch. tsc clean; eslint 0 errors (9 pre-existing warnings).

[2026-05-23] Onboarding RotatingTags fix: the marquee was not animating because the Tailwind arbitrary [animation:...] class was not being generated. Switched to an inline animation style (animation: cabinet-marquee 32s linear infinite; play-state toggled by hover via React state) referencing the globals.css keyframe. Now scrolls reliably; flex-nowrap + w-max keeps it a single row.

[2026-05-23] Onboarding agent step copy: dropped "No pre-made team here." Heading is now "Meet your AI team" and subtitle "Start by hiring your first teammate. They work for you 24/7, getting your work done while you drink your coffee. You can hire more anytime from inside the room." Inline English (onboarding:team.heading/subtitle keys now orphaned).

[2026-05-23] Onboarding agent step overhaul ("Meet your AI team"): (1) trimmed the subtitle (dropped the weak "You can hire more anytime..." tail). (2) Removed the form container around the questions; they sit directly on the page. (3) Added a RotatingTags marquee of fun agent-name suggestions under the name field (Cabi, CEO, CTO, Harry, Diana, Salesman, Jarvis, Alfred, Friday, Scout, Ghostwriter, Sherlock, Nova, Maestro, Closer) that fill the name. (4) Removed the "Role" field. (5) Instructions label is now "What should this agent do?". (6) Built an in-step heartbeat: a green iOS-style Toggle (new Toggle component, off by default) + explanation + a 3-card schedule picker (Hourly/Daily/Weekly) shown when on, with the token-cost note; the card border turns green when enabled. (7) Removed the standalone "Should it run on a heartbeat?" step (HeartbeatStep + STEP_HEARTBEAT); renumbered (first-task 7->6, community 8-10->7-9, launch 11->10, STEP_COUNT 12->11). WIRED FOR REAL: heartbeat schedule -> cron (scheduleToCron: hourly 0 * * * *, daily 0 9 * * *, weekly 0 9 * * 1) sent in the launch payload; setup route now writes persona heartbeat (cron) + heartbeatEnabled from firstAgent (was hardcoded "" / false). Flow: provider -> configure agent + heartbeat -> first task -> community -> launch. Inline English copy. tsc clean; eslint 0 errors (9 pre-existing warnings).

[2026-05-23] Onboarding agent step polish: heading "Meet your AI team" -> "Hire your AI team"; instructions textarea rows 4 -> 2; replaced the custom Toggle with the shared base-ui Switch (src/components/ui/switch.tsx) for consistency with the agents view; heartbeat icon Zap -> HeartPulse (pulses via cabinet-task-heartbeat when on); softened the enabled card outline (harsh green line -> rgba(34,197,94,0.4) border + a blurred green glow box-shadow); schedule cards now show a green human-readable line when selected ("every hour" / "every day at 9AM" / "Mondays at 9AM"); and the left rail's agent status dot now does a heartbeat pulse (cabinet-task-heartbeat) when the heartbeat is enabled (new heartbeatOn prop on OnboardingCabinetRail). tsc clean; eslint 0 errors (9 pre-existing warnings).

[2026-05-23] Onboarding rail progress checks: the build rail tabs (DATA/TEAM/TASKS) now show a green CheckCircle2 (pop-in animation) as the user completes each part — DATA when the cabinet is named, TEAM when an agent is hired, TASKS when the first task is filed. Added an optional completedTabs prop to MockupSidebar (tour usage unaffected); OnboardingCabinetRail derives it from cabinetName/agentName/firstTask. tsc clean; eslint 0 errors.

[2026-05-23] Onboarding rail check badge restyle: moved to the bottom-right corner, dropped the ring/border, light-green (#dcfce7) rounded background with a green (#16a34a) check, and a pop-in (cabinet-tour-pop-in overshoot) on appear.

[2026-05-23] Onboarding rail check badge: fixed placement to the tab cell bottom-right via inline insets (bottom/insetInlineEnd) instead of Tailwind bottom-0.5/end-0.5 (which were not generating, so the badge sat on the icon). Light-green bg + green check + pop-in retained, RTL-aware.

[2026-05-23] Onboarding rail check badge: now always-rendered and toggled via a bouncy scale/opacity transition (back-out cubic-bezier), so it pops IN on completion and pops OUT when un-completed (replaces the mount-only animation that had no exit).

[2026-05-23] Onboarding welcome step: added an email field under the name field (label "What's your email?", type=email, no "optional" wording; Next still gated by name only). Stored in answers.email + sent in the launch payload (not yet persisted server-side). Also softened the heartbeat "Recommended" label to small lowercase muted text (was a filled accent pill).

[2026-05-23] Tour "data" slide (slide-data.tsx): the first scene now features Phuket sunset.jpg with a CSS-rendered sunset ImageViewer (replaces the Night market.mp4 VideoViewer + its Play button), shown immediately (removed the viewerRevealed reveal gate here and the data-0 two-click gate in tour-modal.tsx). Rewrote the copy (inline, em-dash-free): heading "Your Cabinet is one place for all your files and dashboards"; para 1 "One place for everything, so you and your AI team read, edit, and ship from the same files, not copies of copies."; para 2 "Markdown, PDFs, spreadsheets, slides, images, video, audio, linked repos, embedded web apps, Google Docs. Mention any of it with @." Dropped now-unused Play/Trans imports + viewerRevealed prop/state.

[2026-05-23] Post-tour first task: the starter prompt is now a friendly templated message shown as the composer PLACEHOLDER (not a pre-filled value): "Hi, I'm <name>! I just created this Cabinet and I want it to help me get my work done. Ask me about my goals, then let's set it up together: create useful pages, beautiful dashboards, and web apps for me." Name comes from useUserProfile. Added placeholderOverride to StartWorkDialog; app-shell passes the templated text there instead of initialPrompt; tour onLaunchTask is now () => void (tour:starterTask i18n now unused).

[2026-05-23] Onboarding feel/stability pass: (1) Provider step shows 4 shimmer skeleton rows while providers load (was a lone spinner), so the list does not pop in. (2) Layout stability: config steps top-anchor (items-start + consistent pt-16) instead of vertically re-centering, so the progress bar + title hold a steady, higher position; welcome/intro stay centered; the left rail is now vertically centered (items-center on its column). (3) Page transitions: step content wrapped in a per-step keyed container that fades + slides up (cabinet-tour-fade-up) on each navigation, while the progress bar stays put (outside the animated wrapper). Also fixed launch useCallback deps (was missing heartbeatEnabled/heartbeatSchedule -> could send stale heartbeat). tsc clean; eslint 0 errors (9 pre-existing warnings).

[2026-05-23] Rooms isolation gap analysis + v3 design. Investigated why rooms are not isolated: root data/ is simultaneously the root cabinet AND the parent of every other room, so isolation is faked with a UI tree-filter (tree-view.tsx:190-196) + default "own" visibility; listRooms() also lists plain folders (songs, getting-started) as rooms. Decision: true sibling rooms WITH migration (data/ becomes a neutral container, each room a self-contained data/<room>/ with its own .cabinet.db). Wrote docs/ROOMS_V3_TRUE_ISOLATION_PLAN.md (model, where-everything-lives move-list, code/DATA_DIR refactor surface, idempotent git-checkpointed migration, phased plan); supersedes the v2 lighter direction in ROOMS_WORKSPACES_PRD.md. No code changed yet (design-first per request).

[2026-05-23] Rooms v3 implemented + verified: rooms are now isolated sibling cabinets. Migration (scripts/migrate-rooms-v3.mjs) makes data/ a neutral home container and moves the old root cabinet into data/sales/; existing nested cabinets (salesons/dauther/fff) become sibling rooms with distinct icons/colors. Code: listRooms() now requires a .cabinet + excludes the home (no more folders shown as rooms); /api/rooms returns defaultRoom from data/.home/home.json; app-shell lands you in the default room; bottom "New Cabinet" nests inside the current room while top "Add room" makes an isolated top-level room (kind:room + auto icon/color); search is scoped to the active room (server/search filters pages/agents/tasks by room prefix, cabinet param threaded through daemon + /api/search + palette); overview roll-up never crosses a room boundary (home returns no descendants); room switch clears the stale selected page. Verified via Chrome DevTools: switcher lists only the 4 real rooms with distinct avatars, switching shows fully different agents/activity/heartbeats/tree, creating a "personal" room yields an isolated top-level sibling, and search?cabinet=salesons does not return sales pages. tsc + eslint clean. Follow-up: onboarding still scaffolds the first cabinet at the data root (new installs would need the same migration); design in docs/ROOMS_V3_TRUE_ISOLATION_PLAN.md.

[2026-05-23] Rooms v3 onboarding fix + from-scratch verification. Onboarding (api/onboarding/setup) now scaffolds the first cabinet as an isolated ROOM at data/<slug>/ (kind:room + icon/color from room type) instead of the data-dir root: global app config (workspace/company/onboarding-complete/user/providers) stays at the container data/.agents/.config, the room gets its own .agents/.chat/.jobs/getting-started, and the route writes the home marker (data/.cabinet kind:home + data/.home/home.json defaultRoom). app-shell handleWizardComplete now force-reloads the rooms store so the post-onboarding landing redirect drops you inside the new room (rooms store had been cached empty during the wizard). Verified from scratch via Chrome DevTools: wiped data/, ran the full onboarding wizard (named cabinet "Work", agent "Jarvis"), launched -> data/work/ created as an isolated room, app lands inside it, tree/agents scoped to the room, no stray top-level cabinets. tsc clean, no console errors. (Stray empty data/sales+salesons dirs seen mid-test were auto-mkdir from stale browser localStorage routes, not onboarding; removed.)

[2026-05-23] Rooms v3 — verified the last two follow-ups (both were already implemented; this was verification, no code change). (1) Per-room theme on switch: RoomThemeSync (mounted in layout.tsx) applies room.theme on switch/load with global fallback. Verified via Chrome DevTools by setting work=matrix and personal=sakura and watching the CSS swap live on switch (and on reload). (2) Multi-window "open in new window": room-window.ts opens a window scoped to the room (Electron cabinet:open-window IPC -> native BrowserWindow reusing backend, preload exposes CabinetDesktop.openWindow; web uses window.open). Verified in-browser: clicking Open in new window -> Work spawned a 2nd window at #/cabinet/work that rendered Work matrix theme independently of window 1 (personal/sakura), proving per-window scope + theme. Note: navigate to a URL differing only in hash is a hash-change not a reload, so the rooms store can look stale in tests — use a real reload. Restored clean state afterward (reset work theme to default, removed the test "personal" room). tsc clean.

[2026-05-23] Fix: onboarding starter-task placeholder (and home greeting) showed the OS username ("Hi, I'm <os-username>!") instead of the name typed in onboarding. Root cause: app-shell reads the user profile during the wizard, before onboarding writes workspace.json, so profile-io seedProfileFromOnboarding() fell back to os.userInfo().username AND persisted it to user.json; onboarding never wrote the entered name, so the OS value stuck. Fix: (1) /api/onboarding/setup now writes user.json with answers.name; (2) readUserProfile only persists a seeded profile when the name came from onboarding (workspace home name) - a pure OS-username fallback stays transient so it can be corrected. tsc clean.

[2026-05-23] feat(onboarding): save + register the user's email. user.json now stores the welcome-step email (UserProfile.email); /api/onboarding/setup persists it next to the name. New recordOnboardingEmail() (waitlist-client) posts { email, source:"onboarding-welcome", visitId } to reports.runcabinet.com/waitlist on launch (best-effort). Routed through the waitlist PII channel, NOT the anonymous telemetry pipeline, so catalog.ts allowlist + the "anonymous telemetry" promise stay intact. Note: the waitlist POST is direct-to-backend so it is not gated by the telemetry opt-out (isTelemetryEnabled is enforced in /api/telemetry); consent copy + opt-out gating flagged as follow-ups.

[2026-05-23] Onboarding email consent + Cloud prefill. The welcome-step email is no longer sent on launch unconditionally: added an OPTIONAL "Keep me in touch" consent checkbox on the "Before you launch" screen (separate from the required disclaimer accept; not required to launch). recordOnboardingEmail() now only fires when that box is checked AND a valid email was entered. New i18n key onboarding:launch.keepInTouch (en.json; other locales fall back to en). Also: the Cabinet Cloud waitlist field is pre-filled with the welcome-step email when one was entered (effect seeds cloudEmail on reaching the cloud step while empty). user.json still stores the email locally regardless. tsc clean.

[2026-05-23] Onboarding consent checkbox polish: shortened the keepInTouch copy to "Keep me in touch. Cabinet is open source and built in the open, and we'd love to learn what to build next. No spam, we promise."; default the checkbox to CHECKED (opt-out) instead of unchecked; moved it ABOVE the "I understand and want to continue" disclaimer checkbox on the launch screen.

[2026-05-23] Complete the onboarding-welcome waitlist funnel. The welcome step now fires anonymous recordWaitlistView("onboarding-welcome") on reaching it and recordWaitlistStart("onboarding-welcome") on the first keystroke in the email field (mirrors the Cloud step). submit still only fires on launch, gated on the keep-in-touch consent. This gives the backend Visits/Drop-offs panels a full view->start->submit picture for the source instead of submits-only. No PII in view/start (visitId + source only). Backend needs no change: source is free-form (regex-gated, not allowlisted) and the dashboard renders sources dynamically; documented onboarding-welcome in cabinet-backend/WAITLIST.md. No DB schema/data changes.

[2026-05-23] Name-free, teammate-voiced first-task placeholder. The post-tour composer placeholder was "Hi, I'm {profileName}! I just created this Cabinet..." which surfaced a stale/odd profile name (e.g. the OS username "<os-account>" persisted in a pre-fix user.json). Replaced with: "Hi, I'm your teammate. I've just joined your Cabinet and I'm ready to get to work. What would you like to accomplish? I can create useful pages, beautiful dashboards, and web apps for you." userFirstName still used by the file-handoff chat greeting, so it stays. Verified new copy in the live dev bundle; old template gone.

[2026-05-23] Fix: sidebar "New Page" / "New Cabinet" / registry-import created items at the data-dir (home) root as siblings of the rooms, not inside the current cabinet. (1) NewPageDialog hardcoded parentPath="" -> now takes parentPath and creates inside the current cabinet; navigation uses the real created path (was loadPage(bare slug)). (2) sidebar computes currentCabinetParent = section.cabinetPath (or defaultRoom), treating "." (home) as "use default room" so New Cabinet never lands at the home root. (3) registry import now sends targetPath = the current cabinet (carried via section.cabinetPath when the registry is opened from the New Cabinet dialog); the import route already supported targetPath. Verified in-app: New Page in the gtm room created data/gtm/fix-test-page (inside the room), not data/fix-test-page. tsc clean.

[2026-05-23] Fix: running a task failed + task page failed to load. Multiple rooms-v3 regressions found via a real run ("convert this onboarding video to webm" mentioning a 74MB onboarding.mov): (1) ROOT: buildMentionContext (conversation-runner) inlined the mentioned file's raw bytes into the prompt — a 74MB .mov became a 134MB prompt.md, which failed the run and made the conversation API return ~134MB so the task page choked. Now binary/large mentions (video/audio/image/pdf/office/archive by extension) are referenced by path ("open with the Read tool") instead of inlined, and inlined text is capped at 200KB. (2) Task dispatched to "editor" which didn't exist — onboarding only created the user's first agent (ceo). onboarding/setup now always instantiates the canonical "editor" agent; also added editor to the existing sales room. (3) TaskConversationPage fetched the task with no cabinetPath (chicken-and-egg) so it couldn't find the room's conversation -> "fails to load"; now app-shell passes section.cabinetPath and the initial fetchTask uses it. (4) The recent-tasks rail fetched conversations + agent roster unscoped (empty home after rooms-v3); now scoped to the active room. Deleted the broken 134MB conversation. tsc clean.

[2026-05-23] Fix: top-level rooms showed a "Back" button that navigated up into the neutral home container ("Jane's Home" / data-dir root) — you should always be inside a room, never the dir above. tree-view: the Back button (and openParentCabinet) is now hidden/guarded when the parent cabinet is the home root (parentIsHome = parentCabinet.path === "."), so it only appears for genuinely nested sub-cabinets within a room. Defensive: app-shell's landing redirect now also snaps ANY content section scoped to "." (home container) into the default room, not just the bare home screen — so the app can never sit on the dir above a room. tsc clean.

[2026-05-23] Auto-refresh the sidebar tree when a conversation creates artifacts. app-shell now subscribes to the global conversation event stream (/api/agents/conversations/events) and, on a task.updated whose payload.artifactPaths is non-empty, reloads the tree (debounced 400ms) so agent-created/modified files appear without a manual refresh. App-wide (works from any section); harmless no-op when the daemon isn't running. Note: not runtime-verified this pass (data/ + servers were torn down per the prior request); tsc clean.

[2026-06-07] Docs: added docs/SCHEDULE_CALENDAR_PRD.md — PRD to (A) consolidate the three drifting "Schedule" surfaces (tasks→Schedule, agents→Schedule, agent-editor→Schedule, all already wrapping the shared ScheduleCalendar) into one full-bleed ScheduleView, and (B) evolve that custom grid into a Google-Calendar-grade scheduler: drag-to-move, mark-to-create, a one-off scheduled-task primitive (leans on existing JobConfig.oneShot/runAfter the daemon already honors; gaps are CabinetJobSummary plumbing + getScheduleEvents one-off rendering + an exceptions[] EXDATE mechanism), and "this occurrence vs all events" editing. PRD only; no source changes.

[2026-06-07] Implemented the Schedule Calendar PRD (Phases 0-4). Consolidation: the three drifting "Schedule" surfaces (tasks→Schedule tab, agents→Schedule tab, agent-editor→Schedule button) now all mount one canonical full-bleed component, src/components/cabinets/schedule-view.tsx (deleted the duplicate tasks/board/schedule-view.tsx, agents/v2/schedule-tab.tsx, and the local ScheduleView in agent-detail-v2.tsx; all three gained identical prev/today/next + range label + day/week/month nav — the agents tab previously had no navigation). Google-Calendar interactivity on the existing custom grid (schedule-calendar.tsx): drag a future job pill to reschedule (15-min snap, live ghost, new onEventMove callback) and click an empty slot to create (new onCreateAt callback + inline "new task" popover). One-off task primitive: added exceptions[] to JobConfig + exceptions/oneShot/runAfter to CabinetJobSummary + overview normalizeJob mapping; getScheduleEvents now renders one-offs as a single event at runAfter and filters per-occurrence exceptions; promoted isoToCronExpression into a shared src/lib/agents/one-off.ts. Recurring drag prompts "this occurrence / all events": all-events rewrites the cron; this-occurrence adds an EXDATE to the series + creates a separate one-off (each created one-off POSTs an explicit unique id so it can't overwrite the series). Server-side enforcement: the jobs run handler skips a scheduledAt that matches an exception. Daemon hardening: one-shot jobs now auto-disable on both success and failure paths. Optimistic updates in schedule-view (patch + delayed forced refresh, rollback on failure). Verified end-to-end in-browser via Chrome DevTools (all three routes, create-by-marking, one-off drag, all-events cron rewrite, this-occurrence split, server-side exception suppression). tsc + eslint clean. PRD status updated to Implemented.

[2026-06-07] Schedule calendar UX pass (expert-designer round, on top of the PRD impl). schedule-calendar.tsx: (1) hover-to-create ghost — a faint primary-tinted block at the snapped slot ("+ New task · 11:30 AM") follows the cursor over empty grid and hides over pills; (2) drag is now allowed only for FUTURE job pills — past runs and heartbeats show a not-allowed cursor and, on a drag attempt, fire onBlockedDrag with an explain-why message ("Past runs can't be moved…" / "Heartbeats are set per agent…"); (3) right-click context menu hooks (onEventContextMenu / onEmptyContextMenu) on both the time grid and month cells; (4) onCreateAt now passes the click anchor so the create popover can anchor to the slot. schedule-view.tsx: (a) the create popover is now anchored to the clicked slot (AnchoredPopover) instead of a center modal, and gained a Repeat control (Once / Daily / Weekly / Custom-cron) — "Once" creates a one-off, the others create a recurring routine with the matching cron; (b) full right-click context menu (CalendarContextMenu): event → Run now / Edit / Duplicate / Skip this occurrence (recurring only) / Enable-Disable / Delete; heartbeat → Run now / Edit heartbeat / Enable-Disable; past run → Open log; empty slot → New task here / New recurring routine; (c) new optimistic actions: runNow, toggleEnable, duplicateJob (unique id), skipOccurrence (adds an EXDATE), deleteJob (optimistic deletedIds set with rollback); (d) blocked-drag toasts via showInfo. Verified in-browser via Chrome DevTools: hover ghost, empty + event context menus (per-type item sets), anchored popover + Repeat ("Every Wednesday at 11:30 AM" → "Create routine"), and blocked-drag not-allowed cursor + toast. tsc + eslint clean.

[2026-06-08] Implemented Schedule Calendar PRD Phase 5 item "this and following" (recurrence series split). Dragging a recurring occurrence now offers a third choice between "This occurrence" and "All events": cap the original series at the split instant and fork a new recurring series from there with the dropped cadence. Mechanism is a symmetric series window — added `since`/`until` (iCalendar DTSTART/UNTIL) to JobConfig + CabinetJobSummary, mapped in normalizeJob/normalizeJobConfig (conditional-spread like exceptions); a recurring job emits no occurrences before `since` (inclusive) or at/after `until` (exclusive). BOTH bounds are required for correctness: capping the original with `until` alone would let the forked daily cadence "leak" backward onto days before the split and double-book them, so the fork carries `since`. getScheduleEvents filters the window (early `until` break since occurrences are monotonic; `since` skip); the shared `withinSeriesWindow(job, when)` helper in one-off.ts is the single source of truth, reused server-side. node-cron has no end-date, so the jobs run handler enforces the window server-side (like EXDATE): out-of-window scheduler fires return `skipped: series-window`, and a fully-ended capped series (its `until` now past) is retired inline (enabled:false + reload) so the daemon stops firing the zombie cron daily. schedule-view performMove gained a "following" scope (optimistic `until` patch on the original + POST a `since`-bounded fork with explicit unique id, rollback on failure); reconcile + jobConfigToSummary now carry since/until; MoveConfirmDialog shows the third option. New test/cron-compute-series-window.test.ts (6 cases: until exclusive, since inclusive, open bounds, capped-before-split, no-backward-leak vs unbounded control, clean week partition). tsc + eslint clean; new + existing cron-compute tests pass. In-browser drag verification still pending. PRD updated.

[2026-06-10] Telegram Remote Control PRD (docs/TELEGRAM_REMOTE_CONTROL_PRD.md): specced the inbound gateway that drives Cabinet from Telegram (run agents + query KB), opposite of the existing outbound MCP. Locked decisions via two Q&A rounds: default orchestrator routing with @slug per-message override, reuse the connected bot (gateway owns getUpdates), allowlist-only/always-on/DM-only access, concise+/verbose streaming, MarkdownV2 with plain fallback, queue-one-deep concurrency, file-attaches-to-next-message, and /room switching in v1.

[2026-06-10] Telegram Remote Control PRD: locked 4 final opens (session state = ephemeral in-memory, no snapshot; run timeout = none, rely on /stop; default room = always home, no TELEGRAM_DEFAULT_ROOM env; rate limit = 1 in-flight + 10 msgs/60s). Updated docs/TELEGRAM_REMOTE_CONTROL_PRD.md §2/§3/§5/§11/§12/§15/§18 and memory.

[2026-06-10] Telegram Remote Control PRD: source-verification review fixed 4 spec-vs-code contradictions (gateway-owned poll loop vs hidden 15-min deadlines in waitForConversationCompletion/pollDaemonSessionUntilDone; daemon does not watch .cabinet.env so gateway brings its own; conversation continuity needs continueConversationRun; runSearch positional signature + SearchSources) and added trigger:"telegram" union value, boot fast-forward past update backlog, daemon-URL port self-check, typing keep-alive, entity-safe 4096 chunking. Updated §2/§5/§6/§7/§8/§9/§12/§15/§17/§18 + memory.

[2026-06-10] Telegram Remote Control implemented (PRD Phases 0-5 + catalog UI): new server/telegram/ gateway inside the daemon (long-poll getUpdates, allowlist fail-closed, DM-only, boot fast-forward, .cabinet.env watch, owner.json marker, port self-check), router with /start /help /new /status /stop /search /agents /room /verbose + @slug one-shot + queue-one-deep + 10/60s rate limit + file staging, MarkdownV2 rendering with plain fallback and fence-safe 4096 chunking. Added "telegram" trigger to ConversationTrigger/TaskTrigger (+3 UI maps), CABINET_DAEMON_SELF=1 so daemon-side continues route through stoppable daemon sessions, and TELEGRAM_ALLOWED_USERS/TELEGRAM_DEFAULT_AGENT catalog fields + setup step. 15 unit tests (test/telegram-gateway.test.ts), tsc/lint clean, daemon smoke-boot verified. Phase 6 (read_recent notice) pending cabinet-mcp-telegram release.

[2026-06-11] Telegram gateway dogfood fixes: (1) chats now default to a REAL room instead of the neutral Rooms-v3 home (home-config defaultRoom -> lastActiveRoom -> first listRooms() entry; home only when no rooms exist) so agent writes land in a cabinet, not the data root; (2) personalized welcome message (first name, active room, run/switch/@slug/search/file how-to) on /start, new /welcome command, and /new; (3) removed em-dashes from all user-facing bot replies per the no-em-dash copy rule. PRD sections 2/6/13 updated; tsc/eslint/tests clean.

[2026-06-11] Integrations connect API: POST /api/agents/config/mcp-catalog/connect now reconciles when the caller passes an explicit provider list (the connect panel and settings dialog both do): environments that were connected but are no longer checked get removeEntry() instead of staying connected forever. Fixes "unchecking an agent environment + Update snaps back to checked" on integration detail pages. Fallback path (no explicit list) stays additive.

[2026-06-11] Telegram gateway: /model command for per-chat runtime override. No args lists current runtime + all registered providers with model ids (providerRegistry); /model <provider> [<model>] validates and sets a sticky per-chat override (provider switch uses defaultAdapterTypeForProvider; model rides adapterConfig on conversation starts and the per-turn override on continues, which upstream handles as a runtime switch with replay); /model reset returns to persona defaults. Shown in /status and /help. PRD section 6 updated.

[2026-06-11] Telegram /model: effort-level support. /model <provider> [<model>] [<effort>] with registry validation; when the chosen model has effortLevels and none is given, the HIGHEST level (e.g. max) is applied automatically; bare effort token (/model claude-code max) applies to the persona model; effort rides adapterConfig on starts and the per-turn override on continues; shown in /status; /model reset clears it.

[2026-06-11] Telegram-launched tasks now carry a paper-plane prefix in the task title ("\u2708\ufe0f <title>"), set at conversation creation in the gateway router, so they are recognizable at a glance in the board/lists/toasts on top of the "Telegram" trigger badge.

[2026-06-11] Telegram /model: provider-only overrides now also default to the highest effort level, sourced from the provider-level effortLevels list (e.g. /model claude-code applies max); model-specific picks still use the model's own levels, and effort-less models like haiku stay unforced.

[2026-06-11] Fixed images not rendering in standalone .md pages (found via a Telegram-launched task that wrote songs/hermione-granger.md + .jpg as siblings): (1) markdown pipeline now rewrites BARE relative src/data-src refs (![x](img.jpg) without ./) to /api/assets, not just ./-prefixed ones; href intentionally excluded to keep page-to-page links working; (2) new PageData.assetBase from readPage (page path for index.md directory pages, PARENT dir for standalone .md files) threaded through editor-store into both markdownToHtml call sites in editor.tsx, with assetBase in the render dedupe key so cached paints re-render once the real base arrives. Verified end-to-end: pipeline unit cases + GET /api/pages returns assetBase + the jpg serves 200.

[2026-06-11] Telegram-launched tasks now show the Telegram brand mark instead of a paper-plane emoji: new TelegramMark component (inline SVG, same glyph/blue as the Integrations Hub logo) rendered in the list-view trigger badge, as a chip on kanban cards, next to titles in the sidebar Tasks drawer, and in the agent-detail + cabinet-utils trigger icon helpers; the gateway no longer prefixes titles with the plane emoji. Verified in the running app (list + kanban); the one legacy local task title with a baked-in plane was cleaned in data/.

[2026-06-11] Integrations Hub launch gate: only Telegram and Discord are live; every other connector tile is grayed out, unclickable (disabled button, no hover glow/giggle), and carries a "Soon" pill. Gate lives in preview-catalog (LAUNCHED set ANDed with the MCP-catalog derivation, easy to widen per launch); "Soon" now also wins over a stale "Connected" badge on gated tiles so they never advertise a state you cannot open. Deep-linked detail pages of gated connectors already render the "Coming soon" panel. Verified in the running app.

[2026-06-12] Wrote docs/LOGGING_AND_FILE_HISTORY_PRD.md: architecture for unified diagnostic logging (per-process rotated JSONL under .cabinet-state/logs with a 40 MB hard cap, console capture without migration, crash handlers, Settings "Export diagnostics" bundle with secret redaction) and attributed file edit history (git at the cabinet root as ground truth with per-commit actor authorship and Cabinet-Agent/Cabinet-Run trailers, run-end agent commits, regenerable file-history.jsonl index, two-actor model that is multi-user/org ready, symlink-mounted cabinets get their own managed repos, and a 200 GB scale story: binaries excluded from git, fsmonitor/untrackedCache, graceful degradation to journal-only).

[2026-06-12] LOGGING_AND_FILE_HISTORY_PRD review decisions applied (now Draft v2): logging pillar ships before the history engine; binary versioning is a per-cabinet threshold defaulting to text-only (off / 2MB / 5MB); the file-history journal moved from a global DATA_DIR file to per-room <cabinetRoot>/.cabinet-meta/file-history.jsonl to match Rooms v3 isolation and travel with symlink mounts; Activity view is file-events-only in v1.

[2026-06-12] LOGGING_AND_FILE_HISTORY_PRD finalized (status: Final, all 8 review decisions recorded in §7): git history kept forever (gc only, no trim tool); commit identity comes from the existing onboarding-captured user profile (.agents/.config/user.json via profile-io.ts), no new profile UI; the per-file history extends the existing Version History panel (actor chips + journal events) instead of adding a second surface; four minimal telemetry events added to the plan (crash.detected, diagnostics.exported, history.restored, history.tier).

[2026-06-12] LOGGING_AND_FILE_HISTORY_PRD: added §3.5 error feedback. Every user-facing error surface (toasts, composer send-failure banner, task-failed states, crash-on-last-launch prompt) gets a Feedback button pre-filled with error context, plus an "Attach recent logs" checkbox (default on, with preview): redacted ~500-line tails per process, gzipped, 1 MB hard cap, riding the existing feedback transport (local feedback.jsonl first, best-effort backend POST). Nothing is ever sent without the explicit button press; checkbox off means zero log bytes leave the machine.

[2026-06-12] Implemented docs/LOGGING_AND_FILE_HISTORY_PRD.md. Pillar A (diagnostics): new src/lib/log/logger.ts (JSONL rotating logger, 5MB x 2 generations per process under .cabinet-state/logs/, console capture so all existing console.* call sites persist, crash markers via uncaughtExceptionMonitor) wired into the daemon, Next instrumentation.ts, and electron/main.cjs (CJS twin electron/logger.cjs); renderer capture (src/lib/log/client.ts batched to POST /api/system/client-log); Settings Diagnostics section (export zip via /api/system/diagnostics/export with secret redaction src/lib/log/redact.ts, verbose toggle, storage usage, compact); crash-on-last-launch prompt (DiagnosticsBoot); error Feedback button on the composer send-error banner with "Attach recent logs" checkbox + preview (redacted 500-line tails, 1MB cap) riding the existing feedback transport. Pillar B (file history): new src/lib/history/engine.ts (per-actor debounced commit buckets staging EXPLICIT paths only, --author from the onboarding user profile, Cabinet-Agent/Cabinet-Run trailers, per-room file-history.jsonl journal 5MB cap, repoForCabinetRoot with realpath + cabinet.managed guard so foreign symlink-mounted repos are journal-only, binary threshold per cabinet default text-only, .git/info/exclude maintenance, index.lock retry, untrackedCache); agent run-end capture (src/lib/history/agent-commit.ts) hooked into finalizeConversation (git status over the cabinet, commit as the agent, journal events union ARTIFACT paths); rename/move now journaled with from-paths (they were never committed before); Version History panel shows actor chips (You / agent + room, task links) and journaled-not-versioned entries; new Activity feed in the status bar (per-room, filter by actor); /api/history/{activity,compact,config}. Telemetry: crash.detected, diagnostics.exported, history.restored, history.tier allowlisted in catalog.ts + TELEMETRY.md. Verified live: user page create/delete produce scoped commits authored "Hila <hila@local>" with journal events.

[2026-06-12] Logging & file-history verification pass + fixes. Fixed: journal/history-config moved from <room>/.cabinet-meta/ to <room>/.cabinet-state/ (".cabinet-meta" is the link-metadata FILE in linked cabinets - directory name collided; PRD updated with implementation note); internal-path filter now also excludes .agents/.runtime and .agents/.config (an agent run-end commit had swept telegram/owner.json). Verified live end-to-end: daemon.log/next.log/renderer.log writing with console capture and scope parsing; 5MB rotation (renderer.log -> renderer.1.log); client-log ingestion; secret redaction in log-tail ("token=[redacted]"); diagnostics export zip (logs + system.json + integrations + conversations-index + audit tail); error-feedback row with logsAttached flag; user page create/delete -> scoped commits authored from the user profile ("Hila <hila@local>") + per-room journal events; simulated agent run-end -> commit authored "CTO (home) <agent@cabinet.local>" with Cabinet-Agent/Cabinet-Run trailers + journal actor (displayName/runtime/trigger); symlink-mounted cabinet-data (foreign repo) -> journal-only, zero commits into the foreign repo; Activity feed in status bar showing mixed Hila/CTO chips (screenshot-verified via CDP); Version History panel actor chips; Settings Diagnostics section (export, verbose toggle round-trip info->debug->info, storage usage, binary threshold + journal-only controls); crash-on-last-launch prompt shown from a planted marker and cleared on dismiss. Lint/tsc clean on all new files (2 pre-existing impure-function errors untouched).

[2026-06-12] File-history UX rework (4 user-reported issues). (1) The File History button now lives inside ViewerToolbar, so EVERY viewer gets it (PDF, CSV, office, image, media, source, mermaid, notebook, website, fallback - verified on the image viewer), not just the markdown editor; VersionHistory takes an optional path prop, cabinet-view passes its cabinetPath. (2) History is never empty: new /api/history/file returns git commits + journal events + OS stat, and the shared timeline renders "Created on disk" / "Modified on disk" anchors (verified on a jpg with only a legacy sweep commit). (3) Activity dialog reworked: 980px x 85vh two-pane master-detail - clicking a file opens its full timeline on the right with vimdiff-style colored diffs (new /api/history/diff, file-scoped patch with first-commit and rename fallbacks). (4) One shared UI: new src/components/history/file-timeline.tsx + diff-view.tsx power both the Version History panel and the Activity detail pane (timeline spine, actor chips linking agent entries to their task, inline expandable diff + Restore; /api/git/restore now also accepts exact non-md paths so any viewer can restore). Production-verified organically mid-session: a real "editor" agent run committed as "Editor (hilas-home) <agent@cabinet.local>" with Cabinet-Agent/Cabinet-Run trailers one minute after a user commit authored "Hila <hila@local>" - the two-actor attribution working live, screenshot-verified diff rendering via CDP.

[2026-06-12] Readable diffs for non-developers: DiffView now defaults to a "Changes" mode that parses the unified diff and renders the actual document lines - additions on a green wash with a green left border, deletions on a red wash with strikethrough, unchanged context muted, hunk boundaries as thin dividers - hiding all git machinery (no +/- prefixes, no diff --git/index/@@ lines). Binary-only changes get a plain-language note. A "Raw diff" toggle keeps the developer view one click away. Verified in the Activity dialog via CDP: the ice-and-fire.md agent commit renders as clean lyric/frontmatter lines on green, zero git noise.

[2026-06-13] Docs only (branch feat/rooms-hardening): folded a combined two-phase plan into docs/ROOMS_WORKSPACES_PRD.md. Incorporated an SWE review that reframes rooms as a product/UX boundary (not a security sandbox) and turns §10 into a shipping contract (fail-closed search - a verified cross-room leak in search-service.ts when the cabinet param is omitted -, per-room chat, soft-delete with scoped commits, reopen-to-last-path, required tests). Added §11 Clean-path routing: migrate off hash routing to clean /room/... URLs that mirror the file tree (drop the phantom /data/, the doubled prefix, index/.md; rename cabinet->room; /-/ marker for agents/tasks views), fix the nested-cabinet reload bug, and free # for in-page section anchors. Sequencing: Phase 1 (rooms hardening) first so its lastActivePath reopen layer is reused by Phase 2. No implementation code yet.

[2026-06-13] Phase 1 §10.1 — search fails closed to the active room. runSearch now takes an explicit includeOtherRooms flag; with no resolved room and no opt-in it returns nothing instead of every room (closing a verified cross-room leak where a missing cabinet param searched all rooms). /api/search resolves a valid room (resolveDefaultRoom) when none is passed and it isn't an explicit cross-room request; the daemon /search forwards includeOtherRooms; sidebar-search now threads the active room (the palette already did). New test/search-room-scope.test.ts (5 cases). E2E verified via the running app: q=index with no cabinet returned 1 room (was 3); includeOtherRooms=1 returned 3.

[2026-06-13] Phase 1 §10.5 — reopen-to-last-path. home.json gains lastActivePath; getHomeConfig/patchHomeConfig handle it; new setLastActive(path) persists lastActiveRoom+lastActivePath (ignoring the home container and unknown rooms) and resolveReopen() resolves valid lastActivePath -> lastActiveRoom -> defaultRoom -> first room. /api/rooms GET now returns a reopen target; new POST /api/rooms/active persists it; rooms-store exposes reopen; app-shell lands on the last active room and persists the current path (debounced). E2E verified: POST a nested path -> GET reopen returns it; unknown room and "." are ignored.

[2026-06-13] Phase 2 (partial) — fixed the nested-cabinet reload bug in the hash router (the original user complaint). parseHash's cabinet branch now marker-scans: it finds the first structural segment (data/agents/tasks) and treats everything before it as the cabinet path, so deep paths like #/cabinet/a/b/c/d/data/... round-trip instead of collapsing to #/cabinet/a. No-marker forms (#/cabinet/a/b/c) are cabinet roots; cabinets named exactly data/agents/tasks are reserved. The legacy audit-#021 no-/data/ page form now resolves to a cabinet root (accepted back-compat change; the canonical builder always emits /data/). buildHash exported for round-trip tests; test/hash-route.test.ts grew to 14 cases (deep root/page/agents/sub-tab/agent/tasks, page paths containing 'data', and buildHash∘parseHash identity). The full /room rename + clean-path migration + section anchors (§11) remain.

[2026-06-13] Phase 2 §11 cutover, steps 1-2 (incremental, checkpointed). Step 1: added src/app/[...slug]/page.tsx catch-all that renders <AppShell/> so clean /room/<path> URLs serve the shell instead of 404 (verified: /room/a/b/c -> 200+shell; /api/* and / unaffected; app still hash-routed, additive). Step 2: added src/lib/navigation/route-scheme.ts (buildPath/parsePath for the /room/<path> scheme with the /-/ view marker; bare content returns CleanRoute 'content' and defers cabinet-vs-page to the apply layer) + test/route-scheme.test.ts (8 cases: globals, deep content, /-/ splitting, round-trip, marker-in-content). Step 3 (the live useHashRoute->useRoute cutover onto the History API + applyCleanRoute + legacy-hash redirect) is intentionally NOT started — it's the core-router rewrite and carries an open product decision (nested /room/<cab>: overview dashboard vs index page) plus back/forward/Electron/persistence interactions that need a focused, fully browser-verified pass. App remains on hash routing and fully working.

[2026-06-13] Phase 2 §11 cutover, step 3 — WEB CUTOVER LIVE. useHashRoute -> useRoute: the app now routes off window.location.pathname via the History API (replaceState on store-driven nav, popstate for back/forward) instead of the hash; applyCleanRoute maps route-scheme CleanRoute -> section, resolving bare /room/<path> to a cabinet overview vs a page via a new isCabinetPath check (tree-node when loaded, else GET /api/cabinets/classify on cold load — single-segment paths are always rooms). On load, any legacy #/cabinet|#/p|#/tasks|... link is translated to the clean path and replaceState'd (drops /data/ and the doubled prefix). app-store goBack/goForward now replaceState + dispatch popstate; navHistory holds clean paths. Secondary nav sites moved off the hash: task-route gains buildTaskPath; conversation-result-view + task-conversation-page (copy-link, delete->home) + file-timeline links + agent-detail-v2 KB-open all use clean paths/pushState+popstate. Browser-verified via Chrome DevTools: / -> /room/hilas-home (overview, clean URL, no #, no console errors); legacy #/cabinet/cabinet-examples/data/... -> /room/cabinet-examples/cabinets/audits/_template/progress (redirect drops /data/+doubling); cold deep-link resolves the page (no collapse); nested cabinet /room/.../dev shows its overview dashboard. tsc + lint clean; suite 145/147 (2 pre-existing data-fixture failures). Remaining: Electron clean-path window URLs (step 4) and section anchors (step 5).

[2026-06-13] Phase 2 §11 cutover, steps 4-5 + anchor bugfix — §11 COMPLETE. Step 4 (Electron): room-window.ts buildRoomHash -> buildRoomPath (clean /room/<path> via route-scheme); openRoomWindow passes a clean path to window.open / desktop.openWindow; electron/main.cjs openRoomWindow concatenates baseAppUrl + the clean-path suffix (logic unchanged, renamed hash->suffix). Step 5 (section anchors): the editor click handler now treats a bare #heading as an in-page scroll target (was swallowed by internal-link handling); a new effect scrolls to location.hash on load + hashchange, retrying frames until the HeadingAnchors decoration has applied the id. No new dep (reused the editor's existing heading slugs; rehype-slug for the agent-message preview renderer deferred). Bugfix caught in browser testing: the legacy-hash redirect was treating ANY hash as a legacy route, so a section anchor like #field-mapping redirected to home — now it only translates route-shaped #/... hashes, leaving bare #anchors for the scroll handler. Browser-verified via Chrome DevTools: /room/.../feedback-tracker/ingestion#field-mapping loads the page and scrolls to the Field mapping heading (id from decoration), no console errors; nested-cabinet overview + legacy redirect + cold deep-link still good. tsc + lint clean; suite 145/147 (2 pre-existing fixture failures). §11 web + Electron + anchors all done; the # is now free for in-page sections.

[2026-06-13] §11 verification + minor PRD items. Browser-verified the remaining §11 interactions: in-app Go-back/Go-forward (app-store goBack/goForward -> replaceState + dispatched popstate -> applyCleanRoute) correctly move between /room/hilas-home and a nested cabinet; open-in-new-window uses the verified buildRoomPath + /room serving (window.open popup not captured by the automation browser). Minor items done: (1) section anchors in agent-message/preview rendering — new src/lib/markdown/heading-slug.ts (shared slugifyHeading + addHeadingIds), wired into to-html.ts and reused by the editor's HeadingAnchors extension so a #section resolves identically in both surfaces; test/heading-slug.test.ts (4 cases). (2) §10.3 delete refinement — deleteRoom now also repoints home.json lastActivePath and makes a best-effort SCOPED git checkpoint (stages only the room + trash + home.json, never git add .; non-fatal on failure). (3) §10.6 guardrail comment at GLOBAL_AGENTS_DIR. §10.2 per-room chat deliberately deferred (documented gap: it's the live agent-messaging system; threading cabinetPath through chat-io/slack-manager/heartbeat is its own verified pass). tsc + lint clean; suite 149/151 (2 pre-existing data-fixture failures). Everything committed on feat/rooms-hardening; nothing pushed.

[2026-06-13] Made test/cabinet-v2's 2 long-failing tests pass (they depended on a removed example-text-your-mom fixture). Rewrote them to build a self-contained company-cabinet fixture under a unique temp cabinet (root + app-development + marketing/tiktok + marketing/reddit, agents + a job via saveAgentJob) in before/after hooks, asserting against it. Adjusted two now-stale assumptions vs current overview behavior: own-agent check is a subset (global .global-agents/* agents intentionally appear in every cabinet), and the descendant agent uses a distinct slug (same-slug agents merge across cabinets when visibility expands). Full suite now 151/151 green (was 149/151).

[2026-06-15] Scheduler daemon now authenticates its server-to-server triggers (jobs, one-shot disable, heartbeats) against the KB_PASSWORD gate via authCookieHeader() in the shared PBKDF2 kb-auth module. Fixes silent 401s that disabled all scheduled runs whenever KB_PASSWORD was set (supersedes #137, builds on #140's PBKDF2 hardening; merged as #142). Follow-up: generalized the daemon's .env backfill to all three auth inputs (KB_PASSWORD + CABINET_AUTH_SALT + CABINET_LOGIN_PBKDF2_ITERS) so an override set only in .env can't desync the prod start:daemon path; refreshed docs/AUTH.md (daemon S2S is done, not "separate"); added authCookieHeader unit test + an end-to-end proxy.test.ts guard that the daemon's cookie passes the real proxy() gate. Suite 327/327; tsc + lint clean.

[2026-06-16] Launched the Microsoft 365 integration with a no-Azure personal-account path. Added `microsoft-365` to the `LAUNCHED` gate in `preview-catalog.ts` (was grayed out as "Soon"); made the Entra credentials optional in `mcp-catalog.ts` so a blank connect = personal account on the server's built-in app, and added `resolveServerEnv()` in `mcp-config-writer.ts` to drop unset `${ENVKEY}` placeholders so empty creds never override that built-in default. The connect panel (`connect-panel.tsx`) gained a Personal / Work chooser: personal mode runs an in-panel Microsoft device-code sign-in driven by a new backend (`src/lib/agents/microsoft-login.ts` + `/api/agents/config/mcp-catalog/microsoft/login`) that spawns `@softeria/ms-365-mcp-server --login`, parses the device URL + code (verified against real output: `https://login.microsoft.com/device` + 9-char code), polls to completion, then auto-registers the MCP server; work mode keeps the paste-your-own-Entra-app flow. `integration-detail-page.tsx` is now mode-aware: the setup guide collapses to a "no setup needed" note for personal, the capabilities list is trimmed to Outlook mail/calendar + OneDrive (Teams/SharePoint are work/school-only), and the TrustNote text is variant-accurate (personal stores nothing in Cabinet; token cached by the connector in the OS keychain). Also collapsed the "other environments" list unless more than one env is connected so the primary action button isn't pushed below the fold, and removed em-dashes from the new user-facing strings (CLAUDE.md rule 17). Added `docs/microsoft-integration-status.md`. tsc + eslint clean. Open: full sign-in completion detection + token-cache sharing not yet confirmed end-to-end on a live account.

[2026-06-16] PR review fixes for the Microsoft 365 sign-in. (1) `microsoft-login.ts` no longer leaks `LoginSession` entries: added a `markTerminal()` helper that records a `finishedAt` and schedules removal from the Map after a 5-min grace window (unref'd timer), applied at every terminal transition (success/error/expired/exit/fail); `cancelLogin` clears the timer; and a defensive `sweepSessions()` runs on each new login to reap finished sessions (covers HMR-lost timers) and kill+drop pending sessions orphaned past the device-code lifetime. (2) Reworded the device-code step in `docs/microsoft-integration-status.md` to make the external flow explicit: Cabinet shows a verification URL + user code, the user enters the code on Microsoft's sign-in page (not back in Cabinet), and Cabinet polls to completion. tsc + eslint clean.

[2026-06-19] Landed the Google Drive + Gmail integration (#150) onto current main and grafted the illustrated native Drive tile. Brought gmail_integration up to date with main (resolved the #148 overlap in preview-catalog LAUNCHED + integration-detail-page ConnectPanel/msMode), then grafted local 3132e08e: google-drive is now a `native` catalog entry (always available, in-app GoogleDriveSection config, no MCP/OAuth gate) whose detail page renders the Telegram/Discord-style illustrated setup guide (GoogleDriveStepArt via stepArtFor). Detail page reconciles all three: Drive->GoogleDriveSection, Gmail->GmailSection, M365->ConnectPanel(msMode); setupSteps = entry?.setupSteps ?? item.setupSteps so native tiles get their own guide. Synced package-lock for the new Gmail deps (imapflow, nodemailer, @types/nodemailer) and suppressed main`\s stricter react-hooks/set-state-in-effect false-positive on the async fetchDriveTree effect. tsc clean, lint 0 errors, tests green (the lone action-dispatcher failure is a known cross-test flake, passes in isolation and on suite re-run).

[2026-06-19] Landed docs/CONNECT_KNOWLEDGE_PRD.md on main (per-room knowledge-source model; spec for the Connect Knowledge / KB build).

[2026-06-19] Connect Knowledge P0 (Slice 1): made Google Drive truly per-room, replacing the global `google_drive_mounts` SQLite table with per-room `<room>/.agents/.config/knowledge-sources.json` (docs/CONNECT_KNOWLEDGE_PRD.md §5). New `src/lib/knowledge-sources/store.ts` owns the per-room source registry (read/add/remove, atomic writes, dedupe) plus DriveMount helpers (`listDriveMounts` per room, `listAllDriveMounts`/`resolveAuthorizedMountPaths` union for serve guards). Rewired all Drive-touching routes off the table: tree/status/mounts(GET/POST)/mounts[id](DELETE) are now strictly per-room via `?cabinet=`; serve, reveal, and the assets gdrive branch authorize against the room (`?cabinet=`/body) or the cross-room union. Migration 004 drops the now-dead table (existing global rows intentionally not migrated, §9). Sidebar `GoogleDriveTreeSection` is keyed on the active room (`dataRootPath`), fetches with `?cabinet=`, caches per room, and passes cabinet on reveal; settings/Hub `GoogleDriveSection` resolves the active room (section scope -> defaultRoom, mirroring the sidebar) so a Hub connect lands in — and shows up in — that room. tsc clean, lint 0 errors, tests 327/327, production build compiles + type-checks (the only build failure is a pre-existing /login useSearchParams-without-Suspense prerender error, unrelated to this change). Next: Slice 2 — Connect Knowledge provider menu (Local + Drive picker -> inline symlink + per-connection policy), provider registry, iCloud/OneDrive disabled entries.

[2026-06-19] Connect Knowledge P0 (Slice 2): provider menu + Drive connect with policy. New `src/lib/knowledge-sources/providers.ts` registry (PRD §10: local + google-drive enabled; icloud + sharepoint present-but-disabled). The sidebar "Connect Knowledge" item is now a provider submenu (ContextMenuSub): Local folder (existing LinkRepoDialog) + Google Drive (new ConnectDriveDialog) + the disabled providers rendered from the registry with a "Soon" tag. New `connect-drive-dialog.tsx` browses the per-room Drive-for-Desktop mount (status/browse APIs from Slice 1), offers a per-connection View-only/Read & write policy toggle (default view-only, PRD §6.1), and on connect POSTs to the per-room mounts endpoint (now policy-aware) + fires GDRIVE_MOUNTS_CHANGED_EVENT so the room's Drive browser refreshes. New en.json keys (connectLocalFolder/connectGoogleDrive/comingSoon; 38 locales propagate later via i18n script). Deferred to fast-follow: the inline-symlink-at-clicked-node surface (PRD F2) + tree-node policy gating. tsc clean, lint 0 errors, build completes, tests 327/327. Runtime/visual smoke (submenu + picker) still pending.

[2026-06-19] Connect Knowledge fixes from live smoke test: (1) Drive mount name no longer floats to the middle — the sidebar inherits text-align:center and the google-drive-tree name spans (mount header + file rows) did not override it, so a short name like "My Drive" centered in the flex-1 span; added text-start. (2) The per-room Drive browser mount now shows the Google Drive brand logo (/logos/google-drive.svg) instead of the generic Lucide cloud. Added a `logo` field + `providerLogo()` to the provider registry (google-drive/onedrive/dropbox brand marks) so other providers map to their icons when they gain a browser surface. Verified live in the danas-home room (text-align start, img renders). tsc clean, lint 0 errors.

[2026-06-19] Connect Knowledge is now a tile-grid picker (per user request, styled like the Integrations Hub "Files & Storage" row) instead of a context submenu. New `connect-knowledge-dialog.tsx` renders a 3-col grid of provider tiles (brand logos in rounded cards + "Soon" pills for disabled): Local folder + Google Drive enabled; iCloud, OneDrive, SharePoint, Dropbox, Box, Notion, Confluence shown as disabled roadmap tiles. Driven by CONNECT_KNOWLEDGE_TILES in the provider registry. BOTH entry points now open it: the data-area menu (tree-view, replacing the Slice-2 submenu) and the per-node menu (tree-node, replacing the direct LinkRepo open). Picking a tile hands off via onPick: Local -> LinkRepoDialog (symlink at the clicked node / room root), Google Drive -> ConnectDriveDialog (per-room browser, cabinet-scoped). Pruned now-unused submenu imports. Verified live in danas-home: menu item -> tile grid (9 tiles, logos + Soon) -> Google Drive opens the picker. tsc clean, lint 0 errors.

[2026-06-20] Connect Knowledge: Notion + Confluence tiles now route to the Integrations Hub (they are MCP connectors, not file/folder sources). New tile kind "hub" in CONNECT_KNOWLEDGE_TILES; clicking opens setSection({type:"integrations", slug}) deep-linked to the connector and closes the picker. Tiles render enabled with an "in Hub" caption instead of "Soon".

[2026-06-20] Connect Knowledge F2: inline Drive mounts + read-only policy gating (docs/CONNECT_KNOWLEDGE_PRD.md §6/§6.1). From a folder node, Connect Knowledge -> Google Drive now mounts the chosen Drive folder INLINE at that node via a symlink (new /api/knowledge-sources/connect-inline), recording an inline source (provider/policy/treePath) in the room`\s knowledge-sources.json. No .cabinet-meta is written into the Drive folder, so a read-only folder is never mutated. The tree-builder cross-references getInlineSourceMap() (data-root-relative treePath -> provider/policy, since the builder has no per-room context) and marks the mount node (knowledgeProvider) + propagates knowledgePolicy to descendants. tree-node shows the provider brand logo on the mount + a "view" badge, and disables the mutating items (Add group, Rename, Move, Delete, + F2/Delete keyboard) for read-only nodes — while keeping Edit Symlink + Unlink (disconnect) on the mount. Server safety net: assertWritablePath() gates pages PUT/POST/PATCH(+move dest)/DELETE, assets PUT, upload POST -> 403 ReadOnlySourceError for any write STRICTLY under a read-only inline treePath (the mount node itself stays disconnectable); deleting the mount symlink also clears its registry record (removeInlineSourceByTreePath). ConnectDriveDialog gained an inline mode (mountAt) that reloads the tree on success. Verified: tsc clean, lint 0 errors, production build, 327/327 tests, and a 6-assertion store-level gating/marking test (block-under / allow-node / allow-unrelated / map-marks / disconnect-removes / gate-lifts) all PASS. Browser smoke deferred (devtools MCP disconnected; Drive root had no subfolders to pick).

[2026-06-20] Connect Knowledge P1+P2: multi-provider cloud sources. detect-desktop.ts gained detectProvider(provider) for iCloud (~/Library/Mobile Documents/com~apple~CloudDocs), OneDrive/SharePoint (~/Library/CloudStorage/OneDrive-*), and Dropbox (~/Library/CloudStorage/Dropbox or ~/Dropbox), plus scanCloudStorage() that auto-detects all installed desktop-sync accounts at once (new /api/knowledge-sources/scan, PRD §12 P2). The connect flow is now provider-parameterized: the tile picker uses a "cloud" tile kind + provider field (onLocal/onCloud callbacks); ConnectDriveDialog takes a provider prop (provider-aware status detection, labels, connect body); status/mounts/connect-inline accept and validate against the selected provider`\s mount. iCloud/OneDrive/SharePoint/Dropbox flipped from "Soon" to live tiles; Box stays "Soon". Non-Google providers mount INLINE (symlink) at the node or room root — the dedicated browser section stays Google-Drive-only. tree-node falls back to a Cloud glyph for providers without a brand asset (iCloud). Added "onedrive" to KnowledgeProviderId + TreeNode.knowledgeProvider. Verified: tsc clean, lint 0 errors, build, 327/327 tests, and a logic test confirming live detection (Google Drive + iCloud both found here), auto-scan (2 accounts), and a non-Google inline source flowing through the store/read-only gate. Browser smoke deferred (devtools MCP disconnected). Follow-up: a "connect a detected account" picker UI on top of /scan, and a multi-provider browser surface.

[2026-06-20] Fix: connecting iCloud/OneDrive/Dropbox failed with "Path is outside the Google Drive mount". The /api/google-drive/browse route was hardcoded to detectDriveDesktop() (Google Drive), so the picker for any non-Google provider browsed Google Drive`\s mount and the chosen folder failed the containment check. Made browse provider-aware (?provider= -> detectProvider); ConnectDriveDialog now passes provider on browse; and the containment error messages in connect-inline + mounts use providerLabel(provider) instead of a hardcoded "Google Drive". Verified iCloud detection + containment against the real FS (this machine`\s iCloud root is empty, but detection/containment now resolve against iCloud, not Drive).

[2026-06-20] Connect Knowledge follow-ups: (1) Read-only editor mode — the editor now reads the current node`\s knowledgePolicy (propagated by the tree-builder) and calls editor.setEditable(false) for pages under a read-only inline mount, with a "Read-only — connected for viewing" banner, so editing is blocked up front instead of failing at save with a 403. (2) iCloud brand logo — added public/logos/icloud.svg (blue cloud) and wired it into the provider registry + tile so iCloud mounts/tiles show a brand mark instead of the generic cloud glyph. Housekeeping: deleted the p0-before-squash safety tag (squash verified). Deferred (each a larger, parallel effort): a "connect a detected account" picker on /scan (the per-provider tiles already cover connecting; auto-scan API exists), a multi-provider browser surface (non-Google providers already mount inline, which is the primary surface), and inline native Google Docs viewing (the normal page pipeline doesn`	 parse .gdoc pointers the way the Drive browser does). tsc clean, lint 0 errors, build, tests green.

[2026-06-20] Connect Knowledge follow-ups (round 2): (1) Inline native Google Docs — the normal tree-builder previously SKIPPED .gdoc/.gsheet/.gslide(s)/.gform pointer files, so they were invisible in inline-mounted Drive folders. New shared src/lib/google-drive/native-docs.ts (googleNativeKind + parseGoogleNative) is used by the tree-builder (push a node with frontmatter.google + the icon) and by readPage (a .gdoc resolves to Google frontmatter so GoogleDocViewer renders it, not raw JSON). (2) Detected-account picker — ConnectKnowledgeDialog fetches /api/knowledge-sources/scan on open and shows a "Detected on this Mac" row of the actually-installed desktop-sync accounts (provider logo + account label); clicking one jumps to that provider`\s folder picker. Verified: tsc clean, lint 0 errors, build, tests, + a native-doc parse test (PASS).

[2026-06-20] Fix: Edit Symlink dialog fields overflowed the dialog box (reported on a Google Drive-linked symlink with a long absolute path). The form is a grid item of DialogContent and the field rows truncate, but neither the form nor the rows had min-w-0, so the long path propagated its intrinsic width up the chain and pushed the inputs/paths past the white background. Added min-w-0 to the form + the "Links to" and "Re-point" rows so truncation actually constrains, and widened the dialog sm:max-w-lg -> sm:max-w-2xl for comfortable room with absolute paths. tsc + lint clean. (Visual confirm pending — chrome MCP dropped mid-session; the app hot-reloads so it shows immediately.)

[2026-06-20] Rebuilt the agent "Channels" (internal team-chat) viewer orphaned by the v2 agents migration. Found the whole src/components/mission-control/ dir is unmounted dead code (no route, nothing imports MissionControl/SlackPanel) — but the backend is live: agents still post to data/.agents/.slack/ every heartbeat (280KB, 25 channels, 91 msgs in #general) with no in-app viewer. Reused the existing live SlackPanel instead of rewriting: added a `fill` prop (h-full, hides the resize-dock chrome) and mounted it as a new "Channels" tab in the v2 Team workspace (tabs-layout.tsx, full-bleed like Schedule). Wired the tab through AgentsTabKey/ALL_TABS (agents-workspace-v2), AgentsTab/isAgentsTab (route-scheme), AGENTS_SUB_TABS (use-hash-route), and app-store agentsTab. Renamed the visible "Agent Slack" label -> "Channels" so it stops colliding with the real Slack MCP connector in the Integrations Hub. Left slack-manager/notification-service/heartbeat writes intact (user chose keep-and-rebuild over rip-out). Verified in-browser at /room/<home>/-/agents/channels: tab renders + fills, shows all 25 channel pills + 91 #general messages + composer. tsc 0 errors.

[2026-07-04] Paste UX: a bare URL pasted on its own line no longer force-embeds a generic web page (many sites refuse framing → dead "refused to connect" grey box). Now it drops a plain link plus an inline Link/Embed chooser (new paste-link-menu.tsx); default = Link (Esc / keep typing keeps it), and Embed is gated on /api/browser/frame-check so framing-hostile sites (e.g. gumloop.com) show Embed disabled with a "can't be framed" tooltip. Media providers (YouTube/Vimeo/Loom/Spotify/tweets) still auto-embed anywhere; video files still embed on their own line. editor.tsx handlePaste branch + menu wiring; tsc 0 errors, lint clean, frame-check verified live (gumloop blocked:true, example.com blocked:false).

[2026-07-04] Embed → link: added a hover "Link" button (top-right) on embed blocks that replaces the embed with a plain link back to its original URL (originalUrl ?? src), via insertContentAt over the node range. Works for all providers; hidden on read-only mounts (editor.isEditable guard). Completes the round-trip with the paste chooser (link⇄embed both directions). embed-extension.tsx; tsc + lint clean.

[2026-07-04] Release prep for v0.5.0. Confirmed the earlier package-lock.json churn was pure npm-version drift: a clean `npm install` under npm 10.8.2 reproduced the committed lock exactly (zero diff), so no dependency refresh was needed. Bumped package.json + cli + cabinetai 0.4.4 -> 0.5.0, refreshed lock version fields, regenerated cabinet-release.json for v0.5.0 (asset names Cabinet-0.5.0-arm64.dmg etc.). Wrote the v0.5.0 CHANGELOG.md section (238 commits: Connect Knowledge, Integrations Hub Slack/Google/Microsoft/Snowflake, Channels, Manila Arc, in-app browser, editor Mermaid/LaTeX/paste-chooser, Fable 5 + Opus 4.8) and prepended the matching 0.5.0 entry to the in-app WhatsNewCard RELEASE_HIGHLIGHTS (card is gated by cabinet.last-seen-version vs package.json). test:unit 5/5 pass; no em-dashes in new user copy (rule 17). Not yet tagged/pushed — tag v0.5.0 triggers npm publish + Electron build.

[2026-07-04] Corrected docs/deployment-packaging-versioning.md to match the actual release.yml: npm publishing is OIDC trusted publishing (no NPM_TOKEN); the tag workflow runs 3 chained jobs (draft release + cabinetai + create-cabinet publishes) and does NOT build Electron (separate manual electron-release.yml); noted npx create-cabinet stays broken until the draft release is published.

[2026-07-04] Fixed the v0.5.0 Electron release build (both jobs failed, both pre-existing, neither from 0.5.0 code). Windows: MakerSquirrel/electron-winstaller failed "Description is required" -> added description to forge.config MakerSquirrel + package.json. macOS: electron-packager ENOENT on .next/standalone/.next/node_modules/node-pty-<hash> -> that's a Next.js output-tracing dedup symlink to node_modules/node-pty, which prepare-electron-package.mjs deletes (line 160, so the daemon resolves node-pty only from staged .native/), leaving the traced symlink dangling; packager crashes stat-ing it. Added removeDanglingTracedSymlinks() sweep after the node-pty removal. Reproduced both locally on the Mac (identical hash) and validated the fix: electron:make now packages + signs Cabinet.app cleanly (local DMG cp to /Volumes fails only due to session sandbox, not a code issue; CI notarizes normally). Fixes on main; v0.5.0 tag still points before them.

[2026-07-04] v0.5.0 desktop build: Windows now SUCCEEDS (nuspec description fix) and its installer/zip/nupkg/RELEASES are attached to the draft release. macOS build now packages + signs cleanly (node-pty symlink fix confirmed on CI: Copying files + Preparing native dependencies both pass), but fails at notarization with Apple notarytool HTTP 403 "a required agreement is missing or has expired" — an Apple Developer Program account-level legal-agreement block, NOT a code bug. Account Holder must accept the pending agreement at developer.apple.com, then re-dispatch electron-release.yml (tag=v0.5.0). Draft release intentionally NOT published until the macOS DMG/ZIP attach. Saved memory project_apple_notarization_agreement. Also noted: manifest's Windows setupExeAssetName is "Cabinet-0.5.0 Setup.exe" (space) but GitHub stored it as "Cabinet-0.5.0.Setup.exe" (dot) — latent name mismatch to fix in generate-release-manifest.mjs later.

[2026-07-04] v0.5.0 RELEASED. After the Apple Developer agreement was accepted, re-dispatched electron-release.yml — both jobs green, macOS notarization passed. Release now carries the notarized macOS DMG (Cabinet-0.5.0-arm64.dmg) + ZIP + Windows installer/zip/nupkg/RELEASES + cabinet-release.json. Published the draft (--draft=false --latest); verified releases/latest/download/cabinet-release.json resolves to 0.5.0. Full 0.5.0 is live: npm (cabinetai + create-cabinet), source tarball, and signed/notarized desktop apps for macOS + Windows.

[2026-07-04] Post-0.5.0 follow-ups. (1) Fixed the Windows installer asset-name mismatch: generate-release-manifest.mjs advertised "Cabinet-<v> Setup.exe" (space) but GitHub stores it as "Cabinet-<v>.Setup.exe" (dot) -> generator now emits the dot form; also patched the committed cabinet-release.json and re-uploaded the corrected manifest to the published v0.5.0 release (verified via uncached API; latest/download was just CDN-stale). (2) Bumped CI actions off the deprecated Node 20 runtime to current majors in both workflows: checkout@v7, setup-node@v6, upload-artifact@v7, action-gh-release@v3. (3) Added a "Release Troubleshooting: Desktop Packaging Failures" runbook to docs/deployment-packaging-versioning.md documenting the three v0.5.0 build failures (Apple agreement 403, node-pty dangling symlink, Squirrel nuspec description), the Windows space->dot asset name, and the tag-move procedure for rebuilding desktop artifacts on an already-tagged release.

[2026-07-04] Post-release cleanup after v0.5.0 shipped. Set the (previously empty) GitHub Release v0.5.0 body from the CHANGELOG v0.5.0 section so the release page shows full notes. Updated stale memories: project_release_v0_4_0 (OIDC npm publish now works end to end — no more manual fallback; added the v0.5.0 pipeline + packaging-fix notes) and project_windows_support_pr130 (Windows Squirrel installer now BUILDS on CI and ships attached; needed the nuspec description fix). Repo working tree confirmed clean + in sync with origin/main; nothing else outstanding to commit.

[2026-07-05] Migrated all GitHub org URLs from hilash to cabinetai (PR #185): release-manifest/auto-update endpoints, npx CLI paths, registry REPO_OWNER constants, forge publisher owner, badges, docs. hilash/cabinet-cloud references intentionally left (repo not transferred). Local origin remote repointed to cabinetai/cabinet.

[2026-07-06] Cloud-mode hardening (branch feat/cloud-gate; OSS cabinet repo, defense-in-depth for the hosted edition). (1) src/proxy.ts (the Next 16 request interceptor, formerly middleware.ts) gained a CABINET_CLOUD=1 branch that runs BEFORE the existing KB_PASSWORD gate and is fully independent of it: it verifies the `cabinet_jwt` cookie as a Supabase ES256 JWT against CABINET_JWT_JWKS_URL via jose (createRemoteJWKSet + jwtVerify, algorithms pinned to ["ES256"] so alg-confusion/`none` is rejected; exp/nbf enforced by jose). Valid token -> NextResponse.next() forwarding the verified `sub` as an `x-cabinet-user` request header (Headers.set overwrites any client-supplied value, so it can't be spoofed). Missing/invalid -> 401 for /api/*, else redirect to CABINET_CLOUD_LOGIN_URL (resolved against the request; falls closed to 401 when unset); /api/health always passes so liveness probes work pre-auth. The non-cloud/desktop path is byte-for-byte unchanged; the JWKS set is memoized per URL so jose's key cache is reused. (2) Added `jose` as a direct dependency (^6.2.2; it was already present transitively at 6.2.2, so no reinstall is required to run, but `npm install` should be run to record it in package-lock). (3) Connect-Claude affordance: new GET /api/cloud/status returns {cloud, claudeConnected, panelUrl} where claudeConnected = existence of .oauth-token or .credentials.json under CLAUDE_CONFIG_DIR || /data/.claude-config, and a new CloudConnectClaudeBanner (mounted next to DaemonHealthBanner in app-shell, same Manila-Arc card styling, accent-toned) shows only in cloud mode until Claude is connected, linking to {CABINET_CLOUD_PANEL_URL}/connect (re-checks on window focus so it self-clears; falls back to a `claude setup-token` hint when no panel URL is set). Verified: tsc 0 errors, eslint clean, the existing 6 proxy tests still green, and a throwaway end-to-end test (local ES256 JWKS server) confirmed valid-token 200 + forwarded sub, spoofed inbound header stripped, tampered/missing -> 401, page redirect to the panel, fail-closed 401 with no login URL, and health-open. i18n: banner copy is inline English (cloud is an English-first beta); extract when the hosted UI is localized. Follow-ups: promote the cloud-gate test into test/proxy.test.ts; consider pinning issuer/audience (aud=authenticated) on the JWT verify; one-click OAuth connect remains panel-side polish.

[2026-07-07] Cloud Connect-Claude on /settings/providers: added server/claude-login.ts (drive `claude setup-token` via node-pty in-container, capture OAuth URL + sk-ant-oat token, persist + set CLAUDE_CODE_OAUTH_TOKEN so agents auth without restart), daemon /auth/claude/{start,code,status,clear} routes (CABINET_CLOUD-gated), /api/agents/claude-login/[action] proxy, and an in-page ConnectClaudeCard in the providers tab. Made the in-app cloud JWT gate opt-in via CABINET_JWT_JWKS_URL so CABINET_CLOUD=1 without it can't fail closed. On feat/cloud-gate.

[2026-07-07] Connect-Claude card polish: copy-to-clipboard button next to the login link, removed the "no API key, no terminal" line, swapped the Sparkles icon for the real Claude mark (simple-icons siClaude).

[2026-07-11] Reviewed anh-chu's open PRs and merged the good ones to cabinetai/cabinet main. #114 closed (superseded — the slowInterval type fix was already on main). #115 merged (Electron startup try/catch). #65 merged after reworking it for ~199 commits of drift: applied the hilash→cabinetai org move to the manifest/bundle URLs (cabinetai/src/lib/release-manifest.ts + generate-release-manifest.mjs, GITHUB_REPOSITORY-derived), kept the legacy source + npm-install path as a fallback in app-manager.ts/run.ts for platforms with no prebuilt bundle (Windows), preserved main's Windows tar.exe extraction fix, extended prepare-electron-package node-pty staging to linux, dropped a duplicate standalone-runtime script. Validated on macOS: build + electron:prep + test:bundle boots the real bundle (app+daemon health 200) and Chrome DevTools confirmed onboarding renders/advances with no console errors. Opened draft PR #192 for the win32-x64 bundle (needs Windows validation). Docs: updated docs/CABINETAI.md (ensureApp bundle-vs-source flow) and docs/deployment-packaging-versioning.md (publish-app-bundles job, appBundles manifest field).
[2026-07-11] Walked the Google Workspace integration setup end to end in a browser against a real Google account. Found the current guide is unfollowable (Google blocks OAuth client creation until the consent screen exists) and that the integration ships broken (sign-in requests Calendar-only scopes while the card advertises Docs/Sheets/Slides/Tasks/Forms/Chat). Verified two shortcuts end to end: a Desktop-app OAuth client accepts any loopback port (deleting the redirect-URI step), and one enableapi deep link enables all APIs. Spec: docs/superpowers/specs/2026-07-11-google-workspace-setup-ux-design.md

[2026-07-11] Fixed final pre-merge review findings on feat/slack-setup-ux: the mcp-catalog `verify` route no longer spawns Claude Code with `bypassPermissions` over untrusted Slack content — now `--strict-mcp-config` + a single-server `--mcp-config` + `--allowedTools mcp__<server>` (verified live: target tool still works, Bash/WebFetch/other MCP servers are blocked, no `bypassPermissions` needed since non-interactive mode auto-denies anything off the allow-list). Also: stopped echoing raw stderr to the client, made the Slack "failed to connect" hint per-integration via a new optional `CatalogEntry.connectFailureHint` instead of hardcoded in `claude-mcp-login.ts`, fixed a literal `<workspace>` in setup copy, removed dead `scopes` plumbing from `generic-setup-art.tsx`, guarded `verify`'s JSON body parse, added `aria-live` to the connect panel's async result regions, cleared stale test/auth state on disconnect, cleaned up a task-log-style comment, and corrected spec doc drift about the `/test` route. Full report: `.superpowers/sdd/final-fix-report.md`.

[2026-07-11] Slack setup UX: replaced the 5-step manual app-creation guide with a 3-step flow built on a prefilled Slack app-manifest deep link (creates the app with MCP already enabled, redirect URL registered, and exactly 6 user scopes instead of the ~26 the manual toggle auto-adds). Extended CatalogSetupStep with optional action/callout/image/fallback fields and raised SetupGuide body contrast to WCAG AA for all integrations. Added a per-integration connect-failure hint and an on-demand "Test connection" probe (tool-pinned, no bypassPermissions).

[2026-07-13] CI smoke-test Task 2: extracted scripts/smoke-checks.mjs, a journey-check module hung off the boot scripts/test-bundle.mjs already pays for, so each new assertion costs seconds of CI rather than another 90-second boot (runChecks iterates a CHECKS array for later tasks to extend). Three checks: checkDaemonBridge (GET /api/health/daemon -> 200 {status:"ok"}) catches a stale runtime-ports.json; checkDaemonTokenGate (unauthenticated GET daemon /search -> 401) proves the auth gate is actually on; checkAuthenticatedProxy (GET /api/search -> 200 SearchResponse) proves the app holds a token the daemon accepts, since that route forwards Authorization: Bearer via getOrCreateDaemonToken() and answers 503 "Daemon returned 401" on a mismatch. The bridge check alone cannot see the token at all -- the daemon exempts /health from its gate -- so the gate check is the control that stops the proxy check passing vacuously. Fault-injected all three (flipped expectations, and pointed the proxy at a nonexistent route) to confirm they go red before trusting them green. Confirmed footgun: src/app/[...slug]/page.tsx's catch-all SPA route has no /api exclusion, so an unmatched /api/* path 200s with the app-shell HTML instead of 404ing -- a status-only assertion passes against a route that does not exist, which is why every check asserts on the body too. Filed as an issue.

[2026-08-16] Added lilbee to the MCP integrations catalog: stdio entry via `npx -y lilbee-mcp` with optional library-location and remote URL + session-token credentials (the shim bridges stdio to a remote lilbee server; Cabinet http entries carry no auth headers). Verified: tsc clean, 410/410 root tests pass, live connect writes cabinet-lilbee into ~/.claude.json, Cabinet probe lists lilbee tools, and a headless Claude Code task answered from an indexed PDF with a vault-relative citation.

[2026-08-16] lilbee integration: added the browse-gallery card (knowledge category) and launch-list entry so the catalog entry is reachable from the Integrations hub; detail page and connect panel render from the MCP catalog entry with no further wiring.

[2026-08-17] Serialized per-session transcript appends in the daemon: emitSessionOutput fire-and-forgot syncConversationChunk, whose meta-read-then-append raced under rapid small chunks and scrambled transcripts pairwise (local-model providers stream per-token deltas; hosted APIs send big throttled chunks and never hit it). Appends now chain on a per-session tail promise and finalizeSessionConversation drains the chain before reading the transcript.

[2026-08-17] lilbee entry now invokes the general npm launcher: npx -y lilbee@0.1.0 mcp (package renamed from lilbee-mcp; same shim behavior, plus full CLI passthrough for npm users).

[2026-08-17] Task chrome now names the wire-reported model: adapters already captured the model id the endpoint actually served (stream-json message.model), but it was dropped at every persistence hop. The daemon and runner now thread it into meta.runtime.servedModel, and buildRuntimeLabel prefers it over the requested alias (short path-basename display, full ref as the header tooltip). Matters when a provider CLI resolves an alias against a non-default endpoint, e.g. local models behind an Anthropic-compatible URL: the header shows the model that really answered instead of the alias label.

[2026-08-17] Review pass on the lilbee-integration branch: extracted per-key serial task chains into server/serial-chains.ts (SerialChains) with ordering/error/isolation tests, the daemon transcript serialization now uses it; added tests pinning that buildRuntimeLabel prefers the wire-reported model and formatServedModel shortens GGUF path refs.

[2026-08-17] Model picker labels now follow the runtime env: when ANTHROPIC_DEFAULT_*_MODEL remaps a Claude Code alias (local models behind an Anthropic-compatible URL), the providers API relabels that alias with the model that actually runs. Same merge as adapter spawns, so label and behavior cannot diverge.

[2026-08-17] Live .cabinet.env edits no longer need a restart: loadCabinetEnv records which process.env keys it injected (marker var, so it survives Next module duplication and child-process inheritance), and withAdapterRuntimeEnv lets the live file win for those keys while genuine shell overrides still take precedence. Before this, any key present at boot shadowed every later file edit.

[2026-08-17] The model picker now lists the lilbee server's installed chat models (GET /api/models on the configured LILBEE_URL/ANTHROPIC_BASE_URL origin), active model first. Picking a non-active model makes the claude adapter set it as lilbee's chat model (PUT /api/models/chat) before spawn; the engine queues requests during the reload. Without lilbee the static alias catalog stands. Raw model ids in the activity feed and the composer runtime hint now render through formatServedModel and prefer the wire-reported served model.

[2026-08-17] The running-task header now formats the requested model id through formatServedModel: when the picker selects a repo-path model ref (or the default names one), the header shows the short model name from the first frame instead of the raw path, and alias ids pass through unchanged.

[2026-08-17] Pre-hydration synthetic model entries in the runtime picker now render repo-path refs as their short name (formatServedModel) instead of the raw path; alias ids pass through unchanged.

[2026-08-17] Wiki-links to non-markdown files now resolve: [[cv-manual]] finds cv-manual.pdf. findPageBySlug also matches the extensionless basename (raw and slugified); markdown targets keep their existing resolution order.

[2026-08-17] The lilbee integration page now documents how to turn model reasoning off. A local thinking model reasons on every turn; setting MAX_THINKING_TOKENS=0 in .cabinet.env makes Claude Code send thinking:{"type":"disabled"} to the lilbee server. The value reaches the adapter through withAdapterRuntimeEnv, so the next task picks it up without a restart. Docs and settings surface only, matching how the integration handles URL and token; a catalog invariant test pins the variable name.

[2026-08-17] The reasoning-off setup step now leads with the server-side switch: lilbee's messages_reasoning = "off" suppresses thinking on /v1/messages regardless of what the client sends (a thinking model reasons by template default, so a client that merely omits the thinking parameter does not stop it). MAX_THINKING_TOKENS=0 stays documented as the per-turn client request.

[2026-08-18] Review fixes on the lilbee branch. Restored the localBuild field and the telegram/discord/stackadapt entries an earlier edit deleted, so a source checkout still runs those local builds. listModels throws when lilbee is configured but unreachable, keeping the models route's `dynamic` flag honest: a fallback list marked live made the picker overwrite a saved GGUF id with models[0]. The PTY spawn path now shares mergeAdapterEnv with the adapter path, so a .cabinet.env edit reaches terminal sessions too. A failed engine swap fails the task instead of running it on the wrong model. isLilbeeModelRef keys on the .gguf suffix, so another provider's namespaced id no longer costs a blocking round trip per spawn, and lilbeeRestConfig no longer treats ANTHROPIC_BASE_URL as lilbee, which sent the auth token to any Anthropic-compatible host. Wiki-links resolve markdown first and strip only known binary extensions, so notes.csv cannot steal [[notes]] from notes.md and a page named Q3.2026 keeps its name.

[2026-08-18] The lilbee icon is drawn in the project's own vocabulary: a block "b" built from the square modules of its ASCII wordmark, in its Rose Pine palette (base #191724, gold #f6c177, iris #c4a7e7), replacing a drawn bee that matched nothing on the site.

[2026-08-18] MCP server env placeholders are now substituted. resolveServerEnv checked that ${VAR} had a value and then wrote the literal ${VAR} into the CLI config, so every catalog entry using a placeholder handed its server the raw text. For lilbee that silently downgraded a remote server to a local one: the agent's status tool reported the laptop's own hardware and an unloaded model. The resolver takes an injectable values map so the substitution is unit-tested.
